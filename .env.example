# Per-worktree isolation. Copy to .env and set unique values.
# Use your shell function to auto-assign a free port.
DUNGEONMASTER_PORT=4737
DUNGEONMASTER_HOME=/path/to/this/worktree/.dungeonmaster-home
