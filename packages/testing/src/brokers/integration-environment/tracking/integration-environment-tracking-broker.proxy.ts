/**
 * PURPOSE: Proxy for integration-environment-tracking-broker testing
 *
 * USAGE:
 * const proxy = integrationEnvironmentTrackingBrokerProxy();
 * // Tracking broker is pure - no mocks needed
 */

export const integrationEnvironmentTrackingBrokerProxy = (): Record<PropertyKey, never> => ({});
