{
  "name": "@dungeonmaster/hooks",
  "version": "0.1.0",
  "description": "Claude hooks for dungeonmaster - sanitization and process escape handling",
  "bin": {
    "dungeonmaster-session-start-hook": "./dist/startup/start-session-start-hook.js",
    "dungeonmaster-pre-edit-lint": "./dist/startup/start-pre-edit-hook.js"
  },
  "scripts": {
    "build": "tsc",
    "postbuild": "chmod +x dist/startup/*.js 2>/dev/null || true",
    "build:clean": "rm -rf dist && npm run build",
    "test": "jest",
    "test:unit": "jest --testPathIgnorePatterns='\\.integration\\.'",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "ward": "bash -c 'FILES=$(fd --glob \"$1\" src); FILE_PATTERN=$(echo \"$FILES\" | tr \" \" \"|\" | sed \"s/|$//\"); echo \"=== LINT: $1 ===\"  && (npx eslint $FILES --fix 2>&1 | grep -A 999999 -E \"$FILE_PATTERN\" || echo \"No lint issues\"); echo \"\" && echo \"=== TYPECHECK: $1 ===\" && npx tsc --noEmit 2>&1 | grep -E \"$FILE_PATTERN\" || echo \"No type errors\"' --"
  },
  "files": [
    "dist/",
    "src/",
    "README.md"
  ],
  "keywords": [
    "claude",
    "hooks",
    "sanitization",
    "process-escape"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@dungeonmaster/testing": "*"
  },
  "peerDependencies": {
    "eslint": "^9.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint-plugin-eslint-comments": "^3.2.0"
  },
  "dependencies": {
    "@dungeonmaster/shared": "*",
    "debug": "^4.4.1",
    "zod": "^3.25.76"
  }
}
