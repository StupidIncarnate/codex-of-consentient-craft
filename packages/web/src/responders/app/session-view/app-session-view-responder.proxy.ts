import { SessionViewWidgetProxy } from '../../../widgets/session-view/session-view-widget.proxy';

export const AppSessionViewResponderProxy = (): Record<PropertyKey, never> => {
  SessionViewWidgetProxy();

  return {};
};
