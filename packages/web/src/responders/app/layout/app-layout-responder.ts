/**
 * PURPOSE: Provides the application layout shell as a route element
 *
 * USAGE:
 * <Route element={<AppLayoutResponder />} />
 * // Renders the AppWidget layout with spacers, logo, and map frame
 */

import { AppWidget } from '../../../widgets/app/app-widget';

export const AppLayoutResponder = AppWidget;
