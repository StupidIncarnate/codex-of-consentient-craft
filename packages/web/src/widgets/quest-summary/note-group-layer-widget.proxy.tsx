import { NoteRowLayerWidgetProxy } from './note-row-layer-widget.proxy';

export const NoteGroupLayerWidgetProxy = (): Record<PropertyKey, never> => {
  NoteRowLayerWidgetProxy();
  return {} as Record<PropertyKey, never>;
};
