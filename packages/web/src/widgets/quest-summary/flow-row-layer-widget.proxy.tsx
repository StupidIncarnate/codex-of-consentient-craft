import { TrackRowLayerWidgetProxy } from './track-row-layer-widget.proxy';

export const FlowRowLayerWidgetProxy = (): Record<PropertyKey, never> => {
  TrackRowLayerWidgetProxy();
  return {} as Record<PropertyKey, never>;
};
