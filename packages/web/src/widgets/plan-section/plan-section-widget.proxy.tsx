import { SectionHeaderWidgetProxy } from '../section-header/section-header-widget.proxy';

export const PlanSectionWidgetProxy = (): Record<PropertyKey, never> => {
  SectionHeaderWidgetProxy();

  return {};
};
