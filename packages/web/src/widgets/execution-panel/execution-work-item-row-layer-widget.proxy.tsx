import { ExecutionRowLayerWidgetProxy } from './execution-row-layer-widget.proxy';

export const ExecutionWorkItemRowLayerWidgetProxy = (): Record<PropertyKey, never> => {
  ExecutionRowLayerWidgetProxy();
  return {};
};
