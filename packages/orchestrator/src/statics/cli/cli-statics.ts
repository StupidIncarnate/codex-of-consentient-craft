/**
 * PURPOSE: Defines immutable CLI configuration values and command names for orchestrator
 *
 * USAGE:
 * cliStatics.commands.claude;
 * // Returns 'claude' command name
 */

export const cliStatics = {
  commands: {
    claude: 'claude',
  },
} as const;
