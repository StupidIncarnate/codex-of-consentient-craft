/**
 * PURPOSE: Placeholder statics file whose sole purpose is to host the Phase-2 consistency test that compares shared metadata against legacy orchestrator statics (deleted in Phase 8 with the legacy statics)
 *
 * USAGE:
 * questStatusMetadataConsistencyStatics.note;
 * // Returns a note describing the temporary consistency check
 */

export const questStatusMetadataConsistencyStatics = {
  note: 'Phase-2 double-entry bookkeeping between shared metadata and legacy orchestrator quest-status statics. Deleted in Phase 8.',
} as const;
