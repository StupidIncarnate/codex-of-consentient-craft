/**
 * PURPOSE: Empty proxy for orchestratorSessionIdExtractorAdapter - runs real code to validate logic
 *
 * USAGE:
 * orchestratorSessionIdExtractorAdapterProxy();
 * // Empty proxy - transformer runs real in tests
 */
export const orchestratorSessionIdExtractorAdapterProxy = (): Record<PropertyKey, never> => ({});
