/**
 * PURPOSE: Empty proxy for orchestratorSignalFromStreamAdapter - runs real code to validate logic
 *
 * USAGE:
 * orchestratorSignalFromStreamAdapterProxy();
 * // Empty proxy - transformer runs real in tests
 */
export const orchestratorSignalFromStreamAdapterProxy = (): Record<PropertyKey, never> => ({});
