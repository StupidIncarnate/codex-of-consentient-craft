/**
 * PURPOSE: Empty proxy for orchestratorStreamJsonToTextAdapter - runs real code to validate logic
 *
 * USAGE:
 * orchestratorStreamJsonToTextAdapterProxy();
 * // Empty proxy - transformer runs real in tests
 */
export const orchestratorStreamJsonToTextAdapterProxy = (): Record<PropertyKey, never> => ({});
