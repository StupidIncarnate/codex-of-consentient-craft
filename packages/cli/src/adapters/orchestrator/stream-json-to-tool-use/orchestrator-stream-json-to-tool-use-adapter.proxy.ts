/**
 * PURPOSE: Empty proxy for orchestratorStreamJsonToToolUseAdapter - runs real code to validate logic
 *
 * USAGE:
 * orchestratorStreamJsonToToolUseAdapterProxy();
 * // Empty proxy - transformer runs real in tests
 */
export const orchestratorStreamJsonToToolUseAdapterProxy = (): Record<PropertyKey, never> => ({});
