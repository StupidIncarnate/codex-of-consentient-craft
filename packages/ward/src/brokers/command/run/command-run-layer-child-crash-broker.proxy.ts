// commandRunLayerChildCrashBroker is a pure builder — everything it needs is passed in by the
// caller. No proxy methods needed; the empty proxy satisfies enforce-implementation-colocation.
export const commandRunLayerChildCrashBrokerProxy = (): Record<PropertyKey, never> => ({});
