/**
 * PURPOSE: Empty proxy — likelyCauseLayerBroker is a pure function over its own arguments, with no
 * adapter or global to mock.
 *
 * USAGE:
 * likelyCauseLayerBrokerProxy();
 */

export const likelyCauseLayerBrokerProxy = (): Record<PropertyKey, never> => ({});
