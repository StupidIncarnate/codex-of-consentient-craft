// PURPOSE: Empty. This layer builds source STRINGS and translates one page reading; the parent
// adapter owns `addInitScript` and `evaluate`, so there is no npm boundary here to mock.
// USAGE: refRegistryLayerAdapterProxy();

export const refRegistryLayerAdapterProxy = (): Record<PropertyKey, never> => ({});
