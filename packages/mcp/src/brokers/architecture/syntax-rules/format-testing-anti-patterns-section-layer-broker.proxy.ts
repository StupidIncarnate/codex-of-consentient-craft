/**
 * PURPOSE: Proxy for formatTestingAntiPatternsSectionLayerBroker - no dependencies to mock
 *
 * USAGE:
 * formatTestingAntiPatternsSectionLayerBrokerProxy();
 * // Returns empty object as broker has no external dependencies
 */
export const formatTestingAntiPatternsSectionLayerBrokerProxy = (): Record<
  PropertyKey,
  never
> => ({});
