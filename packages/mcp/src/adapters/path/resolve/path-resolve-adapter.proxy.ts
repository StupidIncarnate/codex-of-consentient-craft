/**
 * PURPOSE: Test setup helper for path resolve adapter
 *
 * USAGE:
 * const proxy = pathResolveAdapterProxy();
 * // No mocking needed - adapter is deterministic
 */
export const pathResolveAdapterProxy = (): object => ({});
