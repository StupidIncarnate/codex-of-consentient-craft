import { orchestratorGetSmoketestStateAdapterProxy } from '../../../adapters/orchestrator/get-smoketest-state/orchestrator-get-smoketest-state-adapter.proxy';

export const ToolingSmoketestStateResponderProxy = (): Record<PropertyKey, never> => {
  orchestratorGetSmoketestStateAdapterProxy();
  return {};
};
