{
  "name": "@dungeonmaster/eslint-plugin",
  "version": "0.1.0",
  "description": "ESLint plugin enforcing Dungeonmaster architecture standards with custom rules for Zod contracts, folder structure, and type safety",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "configs/**/*"
  ],
  "exports": {
    ".": "./dist/index.js",
    "./tsconfig": "./configs/tsconfig.json",
    "./config": "./configs/eslint.dungeonmaster.js"
  },
  "scripts": {
    "build": "tsc",
    "build:clean": "rm -rf dist && npm run build",
    "test": "jest",
    "test:unit": "jest --testPathIgnorePatterns='\\.integration\\.'",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "ward": "bash -c 'FILES=$(fd --glob \"$1\" src); FILE_PATTERN=$(echo \"$FILES\" | tr \" \" \"|\" | sed \"s/|$//\"); echo \"=== LINT: $1 ===\"  && (npx eslint $FILES --fix 2>&1 | grep -A 999999 -E \"$FILE_PATTERN\" || echo \"No lint issues\"); echo \"\" && echo \"=== TYPECHECK: $1 ===\" && npx tsc --noEmit 2>&1 | grep -E \"$FILE_PATTERN\" || echo \"No type errors\"' --"
  },
  "keywords": [
    "eslint",
    "eslint-plugin",
    "typescript",
    "zod",
    "architecture",
    "dungeonmaster",
    "type-safety",
    "folder-structure"
  ],
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-eslint-comments": "^3.2.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@dungeonmaster/shared": "*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@dungeonmaster/testing": "*",
    "@typescript-eslint/parser": "^8.45.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
