/**
 * Proxy for test-file-path-to-colocated-proxy-path transformer.
 * Provides mock setup for testing the transformer.
 */
export const testFilePathToColocatedProxyPathTransformerProxy = (): Record<
  PropertyKey,
  never
> => ({});
