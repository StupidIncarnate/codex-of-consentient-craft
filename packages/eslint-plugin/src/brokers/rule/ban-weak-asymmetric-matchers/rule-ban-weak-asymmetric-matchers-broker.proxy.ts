/**
 * PURPOSE: Proxy for ban-weak-asymmetric-matchers rule broker
 *
 * USAGE:
 * ruleBanWeakAsymmetricMatchersBrokerProxy();
 *
 * WHEN-TO-USE: Empty proxy - ESLint rules run with real parsing to validate DSL logic.
 */
export const ruleBanWeakAsymmetricMatchersBrokerProxy = (): Record<PropertyKey, never> => ({});
