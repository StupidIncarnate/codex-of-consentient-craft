/**
 * PURPOSE: Empty proxy for validate-harness-constructor-side-effects-layer-broker
 *
 * USAGE:
 * const proxy = validateHarnessConstructorSideEffectsLayerBrokerProxy();
 * // Empty proxy — pure layer function with no dependencies to mock
 */

export const validateHarnessConstructorSideEffectsLayerBrokerProxy = (): Record<
  PropertyKey,
  never
> => ({});
