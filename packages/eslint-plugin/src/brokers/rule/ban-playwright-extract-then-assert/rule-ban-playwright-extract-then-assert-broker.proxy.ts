/**
 * PURPOSE: Proxy for ban-playwright-extract-then-assert rule broker
 *
 * USAGE:
 * ruleBanPlaywrightExtractThenAssertBrokerProxy();
 *
 * WHEN-TO-USE: Empty proxy - ESLint rules run with real parsing to validate DSL logic.
 */
export const ruleBanPlaywrightExtractThenAssertBrokerProxy = (): Record<PropertyKey, never> => ({});
