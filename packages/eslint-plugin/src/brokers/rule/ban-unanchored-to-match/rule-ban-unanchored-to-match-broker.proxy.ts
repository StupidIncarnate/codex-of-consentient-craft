/**
 * PURPOSE: Proxy for ban-unanchored-to-match rule broker
 *
 * USAGE:
 * ruleBanUnanchoredToMatchBrokerProxy();
 *
 * WHEN-TO-USE: Empty proxy - ESLint rules run with real parsing to validate DSL logic.
 */
export const ruleBanUnanchoredToMatchBrokerProxy = (): Record<PropertyKey, never> => ({});
