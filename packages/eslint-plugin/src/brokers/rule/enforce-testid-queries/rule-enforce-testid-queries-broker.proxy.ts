/**
 * PURPOSE: Proxy for enforce-testid-queries rule broker
 *
 * USAGE:
 * ruleEnforceTestidQueriesBrokerProxy();
 *
 * WHEN-TO-USE: Empty proxy - ESLint rules run with real parsing to validate DSL logic.
 */
export const ruleEnforceTestidQueriesBrokerProxy = (): Record<PropertyKey, never> => ({});
