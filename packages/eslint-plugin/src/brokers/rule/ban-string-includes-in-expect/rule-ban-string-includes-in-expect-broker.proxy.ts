/**
 * PURPOSE: Proxy for ban-string-includes-in-expect rule broker
 *
 * USAGE:
 * ruleBanStringIncludesInExpectBrokerProxy();
 *
 * WHEN-TO-USE: Empty proxy - ESLint rules run with real parsing to validate DSL logic.
 */
export const ruleBanStringIncludesInExpectBrokerProxy = (): Record<PropertyKey, never> => ({});
