/**
 * Proxy for ban-adhoc-types rule broker.
 * Empty proxy - ESLint rules run with real parsing to validate DSL logic.
 */
export const ruleBanAdhocTypesBrokerProxy = (): Record<PropertyKey, never> => ({});
