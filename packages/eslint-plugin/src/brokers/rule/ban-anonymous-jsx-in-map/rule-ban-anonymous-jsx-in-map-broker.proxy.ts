/**
 * PURPOSE: Proxy for ban-anonymous-jsx-in-map rule broker
 *
 * USAGE:
 * ruleBanAnonymousJsxInMapBrokerProxy();
 *
 * WHEN-TO-USE: Empty proxy - ESLint rules run with real parsing to validate DSL logic.
 */
export const ruleBanAnonymousJsxInMapBrokerProxy = (): Record<PropertyKey, never> => ({});
