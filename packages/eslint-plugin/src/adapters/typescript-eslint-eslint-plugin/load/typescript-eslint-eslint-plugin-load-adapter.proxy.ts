// Proxy for typescript-eslint-eslint-plugin adapter
// This adapter is a simple re-export, no mocking needed
export const typescriptEslintEslintPluginLoadAdapterProxy = (): Record<PropertyKey, never> => ({});
