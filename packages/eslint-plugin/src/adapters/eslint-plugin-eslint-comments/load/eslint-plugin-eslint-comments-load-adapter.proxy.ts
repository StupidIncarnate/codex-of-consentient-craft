// Proxy for eslint-plugin-eslint-comments adapter
// This adapter is a simple re-export, no mocking needed

export const eslintPluginEslintCommentsLoadAdapterProxy = (): Record<PropertyKey, never> => ({});
