{
  "name": "@questmaestro/config",
  "version": "0.1.0",
  "description": "Configuration parser and resolver for QuestMaestro architecture lint rules",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "ward": "bash -c 'FILES=$(fd --glob \"$1\" src); FILE_PATTERN=$(echo \"$FILES\" | tr \" \" \"|\" | sed \"s/|$//\"); echo \"=== LINT: $1 ===\"  && (npx eslint $FILES --fix 2>&1 | grep -A 999999 -E \"$FILE_PATTERN\" || echo \"No lint issues\"); echo \"\" && echo \"=== TYPECHECK: $1 ===\" && npx tsc --noEmit 2>&1 | grep -E \"$FILE_PATTERN\" || echo \"No type errors\"' --"
  },
  "keywords": [
    "questmaestro",
    "config",
    "lint",
    "architecture"
  ],
  "dependencies": {
    "@questmaestro/shared": "*"
  },
  "devDependencies": {
    "@questmaestro/testing": "*"
  },
  "peerDependencies": {},
  "publishConfig": {
    "access": "public"
  }
}