{
  "name": "@dungeonmaster/config",
  "version": "0.1.0",
  "description": "Configuration parser and resolver for Dungeonmaster architecture lint rules",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "postbuild": "chmod +x dist/startup/*.js 2>/dev/null || true",
    "build:clean": "rm -rf dist && npm run build",
    "test": "dungeonmaster-ward run --only test",
    "test:unit": "jest --testPathIgnorePatterns='\\.integration\\.'",
    "lint": "dungeonmaster-ward run --only lint",
    "typecheck": "dungeonmaster-ward run --only typecheck",
    "ward": "dungeonmaster-ward run"
  },
  "keywords": [
    "dungeonmaster",
    "config",
    "lint",
    "architecture"
  ],
  "dependencies": {
    "@dungeonmaster/shared": "*"
  },
  "devDependencies": {
    "@dungeonmaster/testing": "*"
  },
  "publishConfig": {
    "access": "public"
  }
}
