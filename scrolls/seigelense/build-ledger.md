# Siegelense build ledger

Running coverage of `siegelense-tooling.md` (3,100 lines, after this round's markers) against what has been planned and
delivered.
**One row per spec section**, keyed by heading text and line number. Every round updates this file.

Status vocabulary:

| Status | Means |
|---|---|
| `NOT STARTED` | nothing plans it yet |
| `PLANNED chunk N` | a chunk plan under `plans/` covers it, wholly |
| `PLANNED chunk N (part)` | a chunk plan covers some of it; the notes column says which part, and the rest stays open |
| `DELIVERED chunk N` | chunk N built it, wholly, and the code was read against this section's own words |
| `DELIVERED chunk N (part)` | chunk N built some of it; the notes column says exactly which part landed and which stays open |
| `DELIVERED` | built outside the numbered chunks, in the six-call pass; the plan is `plans/call-<name>.md` |
| `BLOCKED chunk N` | chunk N wrote the code (often a whole test suite) and believes it sound, but could not RUN it to confirm — the blocker is named in Notes. Never read as tested |
| `N/A — spec-side, not tooling` | the document itself says this is not tooling work |

Chunk plans live in `scrolls/seigelense/plans/`. Chunk 1 is `plans/chunk-01-registry-spine.md`; chunk 2 is
`plans/chunk-02-driver-and-batch.md`; chunk 3 is `plans/chunk-03-read-path-and-perception.md`; chunk 4 is
`plans/chunk-04-cli-surface.md`.

**The last six calls were built one agent per call rather than as a numbered chunk**, so their plans are
`plans/call-capacity.md`, `call-docs.md`, `call-profile.md`, `call-prune.md`, `call-recipes.md` and
`call-snapshots.md`. Each opens with a numbered requirements table citing spec lines and grades itself
against that table at the end. Read the grade, not the summary: three of the six are complete CALLS
sitting on an incomplete substrate, and the grade is where that is stated.

**The spec carries the same verdicts inline.** Every section this ledger marks as delivered or partial has a
`> **Status: DELIVERED (chunk N)**` or `> **Status: PARTIAL (chunk N)**` line under its heading in
`siegelense-tooling.md`. Those marker lines shifted every line below them, so the line numbers in this file are the
POST-marker ones — re-derive them from heading text, never from an older copy of this table.

---

## Part 1 — The architecture (line 15)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Decision: an INSTANCE service, reached over the CLI | 17 | Thirteen `dungeonmaster siegelense <call>` subcommands over a per-instance driver; steps are DATA inside `run`, never their own call | **DELIVERED chunk 4 (part)** | Seven of the thirteen calls built and working end-to-end — `start`, `run`, `results`, `kill`, `status`, `cleanup`, `compare` — and every one of them is now reached ONLY as `dungeonmaster siegelense <call>`, on the CLI the spec's own words require. Verified by reading `packages/siegelense/src/flows/siegelense/siegelense-flow.ts`'s `CALL_ROUTES` map (seven entries) and `packages/cli/bin/cli-entry.integration.test.ts`'s spawned-binary seam suite, which proves the gate above `SiegelenseFlow` forwards rather than refuses. **The MCP layer is gone**: `packages/mcp/src/flows/siegelense/`, its two responders and its seven `.strict()` input contracts are deleted, confirmed by `mcp-server-flow.integration.test.ts:100-114`'s own absence assertion (`no tool name starts with "siegelense-"`). Six stay pinned and unclaimed: `capacity`, `profile`, `prune`, `snapshots`, `recipes`, `docs` — chunk 4 makes each of them refuse BY NAME ("`capacity` is a siegelense call but is not built yet") rather than as an unknown subcommand, which is a reachability improvement, not delivery of the six |
| The shape | 52 | `start` → id; `run` → status-index; `results` → narrow query; `kill` → teardown | **DELIVERED chunk 3** | All four calls now read exactly as this row words them. `results-read-broker.ts` queries by run id, narrowly, off disk, needing no live instance — verified by reading the broker and the real stdio `siegelense-results` call in `mcp-server-flow.integration.test.ts` |
| An instance is a TIMELINE of runs, and every run is addressable | 77 | Run ids minted per run, step numbering restarting at 1, run-namespaced shots, continuous buffers with per-run windows, refs surviving a run boundary | **DELIVERED chunk 3 (part)** | Chunk 2 delivered the write half (run ids, per-run numbering, namespaced shots, recorded windows). Chunk 3 delivers the read half: every run stays queryable and addressable by run id plus step, dead or alive, through `results` — verified by reading `results-read-broker.ts`'s `RunIdRequiredError` refusal against a finished instance with no run named, and its `since: 'boot'` whole-timeline path. NOT YET: the retention window, and refs surviving a run boundary — `look` does not exist yet |
| Many sessions, one machine: a disk REGISTRY, not a master | 134 | `registry.json`, `boot.lock`, `profiles/<hash>/`, `guilds/<guildId>/instances/<id>/`, `unowned/instances/<id>/`, the `<repoRoot>/.siegelense` symlink, guild partitioning by the QUEST's guild | **DELIVERED chunk 1** | The whole tree and every resolver: `locations-root-path-find`, `-registry-path-find`, `-boot-lock-path-find`, `-registry-lock-path-find`, `-profiles-path-find`, `-instance-evidence-path-find` (the guild/`unowned` branch pair) and `-repo-link-path-find`. The symlink is written by `install-link-create-responder` and resolves through the same `dungeonmasterHomeFindBroker` at install time and at runtime |
| What the registry has to make safe | 199 | Port claim before bind, `boot.lock` across processes, reservation before boot, append-only profile samples, assets under the minted id | **DELIVERED chunk 1 (part)** | Four of the five rows: `instance-reserve-broker` claims the pair in the registry before anything binds and re-rolls on collision; `boot.lock` is taken by an OS-atomic exclusive create; a reservation is a row with `bootedAtMs: null`; assets live under the minted id. `profile writes` (line 190) needs the sampler and moves to the capacity chunk. **The registry-lock absence-classification defect this row used to flag is CLOSED and committed** (`packages/siegelense/src/brokers/registry/lock-acquire/registry-lock-acquire-broker.ts`, `git log` shows it landed at `b27b0ebf2`): the read-failure branch classifies `readError.cause.code !== 'ENOENT'` and rethrows anything else, `mkdir -p`s the siegelense root before the exclusive create, and swallows an `ENOENT` from the stale-lock unlink when two contenders race the same cleanup — confirmed by reading the current file, not the commit message |
| Retention: assets outlive their instance, and something must prune them | 219 | Age-out windows, `prune` refusals, video first, citation resolution through `.quest-plans/`, never prune on `start` | NOT STARTED | Part 7 item 2c. Chunk 1 delivered the tombstone fields the answers hang off (`prunedAtMs`, `prunedByRule`) and the recorded `questId`; chunk 3's `cleanup` (chunk-03 §3.E) deliberately declines the ageing half of this row rather than shipping it ahead of a citation resolver that also covers a `WALKED` quest-note line (Part 7 item 11g) — ageing without that resolver would age out a clean happy walk's own baseline shots. No ageing, no refusal and no citation resolver exists |
| Reading evidence starts nothing, and that is a SEPARATE PATH through the tool | 260 | Every evidence read resolves off disk; only `start`/`run`/`kill` need a driver; tombstones; no browsing | **DELIVERED chunk 3 (part)** | Part 7 item 2a. `results`, `status`, `compare` and `cleanup` all resolve off disk or the registry, starting nothing — verified by reading `results-read-broker.ts`, `status-read-broker.ts`, `compare-read-broker.ts` and `cleanup-run-broker.ts`, none of which touch the driver socket. `results`/`compare` read the asset tree; `status`/`cleanup` read the registry and the machine, matching this section's own table. NOT YET: `prune`'s refusals, `snapshots`, `capacity`, `profile`, `recipes`, `docs` |
| What a batch buys beyond call count | 317 | Batched steps at millisecond gaps so a race is reachable; stop-on-first-failure; mid-walk branching is the cost | **DELIVERED chunk 2** | `run-execute-broker.ts` runs a batch's steps in strict sequence and stops at the first non-`ok` reading unless `stopOn: 'never'`; `step-dispatch-broker.ts` inverts `ok` around `expect: 'error'` once, centrally, rather than in each verb broker |

## Part 2 — The capabilities, and the problem each one solves (line 330)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Addressing: a listing, not a selector | 332 | The key: four columns, element-bound refs, the whole flag set, `within` scoping, document order, own text nodes, the naming ladder | NOT STARTED | Part 7 item 7 |
| The `attrs` column — what the element DECLARES, in the app's own words | 434 | `href` as `→ /path`, `data-*`, input constraints, `type`, `title`; a budget; runtime-id values dropped; `className` excluded | NOT STARTED | Part 7 item 7 |
| "Does this element have a click handler?" — four routes, and none of them answers it here | 475 | `not-tabbable` stays a labelled PROXY; listeners only on `dom` over CDP; nothing infers a dead control | NOT STARTED | |
| Two key-level readings, which are not row flags at all | 505 | The duplicate-testId line under the key; `role` beside the tag in the element column | NOT STARTED | |
| What is deliberately NOT computed, and why | 522 | No sibling rect intersection, no per-row listeners, no z-index analysis, nothing about motion; a flag earns its place by being ABSENT on most rows | NOT STARTED | |
| `dom` is the ESCAPE HATCH, and the ladder above it has four rungs | 589 | `look` → `look { within }` → `box` → `dom`; own text by default, `fields:` projection, a cap that reports the true count | NOT STARTED | Part 7 item 10 |
| Perception: three artifacts, and they are not interchangeable | 649 | The shot (evidence), the map (optional, later), the key; different filters; always capture, open on signal | **DELIVERED chunk 3 (part)** | Part 7 item 6. "Report how much the picture CHANGED" now lands: `pixelChange` on every `ShotListing` and `StepReading`, verified by reading `step-dispatch-broker.ts` and `shot-change-read-broker.ts`. "Always capture" and "the RUN lists every shot… and flags which to open" were chunk 2's (`shot-open-decide-transformer.ts`). NOT YET: the map |
| `pixelChange` — an attention router, not a measurement | 696 | One number per acting step; `null` on the first capture; `blank` checked FIRST with its colour; paired with the element delta | **DELIVERED chunk 3 (part)** | Part 7 item 6. The whole mechanism: one number per acting step (`shot-change-read-broker.ts`), `null` on the first capture in an instance rather than `0%` (`driverSessionState.lastShotPath()` starts null), `blank` checked FIRST and unconditionally (`shot-blank-read-broker.ts`, called alongside `pixelChange` in `step-dispatch-broker.ts`), full-blank only, and every comparison capture frozen (chunk 2's `animations: 'disabled'`/`caret: 'hide'`) — verified by reading all three files and their tests. NOT YET: the element-delta pairing table — `elements` does not exist until `look` (chunk 4) |
| Settling: a step ends when the page is DONE, not when a clock says so | 768 | Network + paint + DOM quiet together, repeating patterns discounted, a CEILING not a timeout, the settle state reported | NOT STARTED | Deliberately deferred to chunk 4 with `until` and `hold` — chunk-03 §1 |
| Instrumentation: run a script before the page's | 803 | A `before` step over `addInitScript` | NOT STARTED | Part 7 item 5 |
| Computed findings: on the key's rows, not in a command of their own | 811 | The geometry checks ride the key row, never a separate command | NOT STARTED | Part 7 item 7 |
| Time: what is decidable, and what is not | 826 | `hold` detects non-settlement; `video` is for a human; nothing grades motion quality | NOT STARTED | Part 7 item 11 |
| What no track can settle goes to a PERSON, with its evidence attached | 862 | `verifyByHuman: true`, dropped from every automated denominator, collected into a list with its evidence | `N/A — spec-side, not tooling` | Part 7 item 11b: "otherwise the only options are an unsignable unit nobody can close, or a real expectation nobody ever checks. Spec-side work, not tooling" |
| The test lives in ONE shared place, and ChaosWhisperer reads it too | 915 | One statics, three readers — the author, the denominators, the walker | `N/A — spec-side, not tooling` | Same item 11b. The walk-side evidence collection it describes is the `video` step, tracked under Part 7 item 11 |
| Survival: what a stress tester needs and the verifier does not | 961 | `health` — one reading, one verdict line, including the server logs | NOT STARTED | Part 7 item 8 |
| Resetting: three layers, and a reset must say which one it touched | 1004 | `page` / `state` / `instance`; named snapshots with an explicit `to`; the diff reported; the snapshot covers STATE only | NOT STARTED | Part 7 item 14 |
| Baselines: promoting a walk's shots | 1063 | Per-shot promotion off sign-offs, the `node:` label, baselines fetched by `results` against the happy walk's instance | **DELIVERED chunk 3 (part)** | The mechanism: `results { instance, run, kind: 'screenshots' }` returns each shot with the `node` it was taken at, against the happy walk's own instance, starting nothing — verified by reading `results-read-broker.ts`'s `screenshots` branch, which reads `storedReturn.shots` off disk. NOT YET: the promotion RULE itself (per-shot sign-off gating), which is orchestrator/spec-side work, not this call |
| Teardown: the failure that is silent, costs three processes, and is never noticed by the session that caused it | 1109 | Process-group kill, SIGTERM → grace → SIGKILL, skip the signal for an exited child, home removed, evidence kept | **DELIVERED chunk 2** | `lane-teardown-broker.ts`: SIGTERM every live pgid, wait `driverStatics.teardown.graceMs`, SIGKILL the survivors; a group `processIsAliveAdapter` already reports dead gets NEITHER signal; `fsRmAdapter` removes only `session.homePath`, and `session.evidencePath` is never passed to it. The suite that walks this end to end against real processes is GREEN — see the "Testing teardown" row below. Its run also forced a fix here: the SIGKILL loop now rechecks each pgid's liveness rather than reusing the pre-SIGTERM list |
| When it dies without warning: OOM, SIGKILL, a full disk | 1145 | The heartbeat file (pid, instance id, every child's pgid, a timestamp), reaping on next contact, per-step transcript flush, `status` as post-mortem, disk checks | **DELIVERED chunk 3 (part)** | Chunk 1: the heartbeat writer and the staleness guard. Chunk 2: the heartbeat ticker, reaping on next contact, `kill`'s orphan-reap path, the per-step transcript flush. Chunk 3 adds `status`, `likelyCause` and the RSS/OOM readings — verified by reading `machine-read-broker.ts` (`freeMemMB`/`totalMemMB`/`freeDiskMB`/`cores`/`loadAvg`/`oomKillsSinceBoot` off `os`/`statfs`/`/proc/vmstat`), `likely-cause-layer-broker.ts` (renders the evidence sentence, `null` for a live instance) and `instance-entry-layer-broker.ts` (`rssAtLastBeat` off the heartbeat file's own `rssMB`). NOT YET: the disk pre-flight check on `start`, and `lastOomAt` (stays `null` — the journal line needs privileges this process may not have) |
| When a session's instance dies under it: bubble up, never self-heal | 1238 | A dead instance is `rework`, never `wall`; no self-healing; only the operator sees the pool | `N/A — spec-side, not tooling` | Prompt text for the walker, the antagonist and the planner — not a change in this package. Ships with the `docs { for: … }` scopes |
| The crash a walker must NOT mistake for a defect | 1262 | `status` separates a driver death from a child death; a walk checks `status` before writing anything down | **DELIVERED chunk 3 (part)** | The tooling half: `status` separates a driver death from a child death (`instance-entry-layer-broker.ts`'s `likelyCauseLayerBroker`), and `blank` rides every capture regardless of whether anyone asked (`step-dispatch-broker.ts`) — verified by reading both files. The ordering rule — a walk checks `status` BEFORE writing anything down — is prompt text, not tooling, and stays open |
| Handing a defect to a fixer: what a walker must write down | 1283 | Instance id, run id, failing step, the prelude, the evidence paths; `results` answering for a killed instance; a required run id against a finished one | **DELIVERED chunk 3 (part)** | Part 7 items 2a and 4b. The tooling half: `results` keeps answering for a killed instance (`instanceState` rides every answer), and the run id is REQUIRED against a finished instance rather than defaulted (`RunIdRequiredError`, naming the state and the run count, never a run id) — verified by reading `results-read-broker.ts` and `run-id-required-error.ts`. The recording requirement itself (what a walker writes into the quest record) is spec-side/prompt work and stays open |
| The fixer writes the e2e, and the PRELUDE is what makes that possible | 1324 | A recipe is a plain function an e2e calls directly, so the walk and its regression test share one seeding vocabulary | NOT STARTED | Part 7 items 3 / 3a |
| What the operator owns after a crash | 1345 | `status` → `cleanup` → re-read `capacity` → re-dispatch; no phase advances with an instance in an unknown state | **DELIVERED chunk 3 (part)** | Steps 1 and 2 of the four — `status` (`dungeonmaster siegelense status`, `siegelense-status`) and `cleanup` (`dungeonmaster siegelense cleanup`, `siegelense-cleanup`), both real and callable — verified by reading `siegelense-status-responder.ts`, `siegelense-cleanup-responder.ts` and `siegelense-flow.ts`'s route table. NOT YET: step 3 (`capacity`) and step 4 (re-dispatch, prompt-side) |
| `cleanup` — the operator's bookend | 1362 | `cleanup {}` reaping by staleness, releasing ports and the lock, ageing assets, and reporting `leftAlone` | **DELIVERED chunk 3 (part)** | Reaping by staleness, port release, `boot.lock`/`registry.lock` release only past each TTL, and `leftAlone` with both real reasons this chunk can produce (`live — last beat Ns ago`, `reserved — booting, no beat yet`) — verified by reading `cleanup-run-broker.ts`, `stale-reap-layer-broker.ts` and `lock-release-layer-broker.ts`. **Not** `assetsAged` — chunk-03 §3.E: ageing needs a citation resolver that also covers a `WALKED` quest-note line (Part 7 item 11g), which does not exist yet, so this chunk reaps and releases only and the field is ABSENT from `cleanupAnswerContract` (`.strict()`) rather than reported as zero |
| Testing teardown — ONCE, as the tool's own suite. Never per quest. | 1399 | Seven teardown assertions, each shown FAILING against broken teardown first, including the three-instance parallel case | **DELIVERED chunk 3 — RUN GREEN** | All sixteen tests in `packages/siegelense/src/flows/driver/driver-flow.integration.test.ts` pass against REAL spawned drivers via `driver-fleet.harness.ts`, up from fifteen at chunk 2. The driver-spawn env-inheritance bug that blocked this suite for three sessions is fixed and documented in `HANDOFF.md` ("The source-condition bug: SOLVED") — `instance-start-broker.ts` now builds an explicit `process.env` snapshot for the spawn instead of relying on the default inherit. The sixteenth test is a regression test this round added for a further defect found while verifying: the driver's own process stayed alive after a successful `kill` because its listening socket was never closed (`net-unix-serve-adapter.ts`) and an un-cancelled `setTimeout` inside `driver-idle-wait-layer-responder.ts` kept the event loop referenced for up to 900s — both now fixed, see "Chunk 3 open follow-ups" below. The snapshot row is still named NOT APPLICABLE YET in its own `describe`, honestly, rather than a vacuous pass |

## Part 3 — Restructuring the pass (line 1436)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Profiling: measure what an instance costs, then divide | 1438 | Sample RSS per process group, record STEADY and PEAK, key by the spec's content hash, divide by `peak` | NOT STARTED | Chunk 1 delivered the `SpecHash` contract and `locations-profiles-path-find-broker` (`<root>/profiles/<hash>/`); chunk 3 delivered the RSS READING primitive (`machine-rss-by-pgid-broker.ts`) that a future sampler will call, but nothing samples and nothing writes a profile yet |
| Phase zero IS the profiling run, and that has two consequences | 1467 | The planner profiles solo; solo profiling is optimistic; an OOM death corrects the profile; memory travels between machines and timing does not | NOT STARTED | |
| The TOOL staggers, because nobody else can | 1506 | `start` queues, admits one boot at a time, refuses past the pool, blocks and reports `queuedMs` / `aheadOfMe`; `capacity` counts foreign load; one hard floor | **DELIVERED chunk 2 (part)** | `instance-start-broker.ts` blocks on `bootLockAcquireBroker`, times the wait into `queuedMs`, and counts unbooted reservations into `aheadOfMe` — both fields verified present in the returned manifest by reading the broker. NOT YET: the pool-size refusal, which needs a measured profile from `capacity` |

## Part 4 — Decisions already taken (line 1582) · 4A — What the tool implements (line 1596)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Readings, and what a step may never do | 1598 | A step returns a READING; ambiguity THROWS with candidates; the no-pick rule held by LINT; `querySelector` banned in eval source by the package `CLAUDE.md` | **DELIVERED chunk 2 (part)** | Chunk 1: the rule as prose in `packages/siegelense/CLAUDE.md`. Chunk 2 makes it real code: every step broker returns a reading, never a verdict; `step-target-resolve-broker.ts` throws `StepAmbiguousError`/`StepNoMatchError` rather than picking a match; `playwright-session-adapter.ts`'s `describeMatches`/`nearestNames` use `querySelectorAll`, never `querySelector`. NOT YET: the LINT rule that would hold this mechanically over code nobody has written yet — being built by a separate agent in `packages/local-eslint`, not this chunk's to grade |
| Addressing: the key, refs, the map | 1612 | Every key/ref/map decision — four columns, the flag set, the `dom` ladder and its guards, ephemeral refs, the map shipped last | NOT STARTED | Part 7 item 7 |
| Perception: shots, pixelChange, animation | 1643 | Only the shot is evidence; always capture with `open:` flags; the `node:` label; frozen comparison captures; nothing grades animation | **DELIVERED chunk 3 (part)** | Chunk 2: capture-always, the shot list, frozen comparison capture, the `node:` label. Chunk 3 adds `pixelChange`, `blank` and `blankColour` on every capture — verified by reading `step-dispatch-broker.ts`, `shot-blank-read-broker.ts` and `shot-change-read-broker.ts` and their tests. NOT YET: `video`, and grading animation quality, which is never built by design (spec line 1843: "no capture mode gives a model that") |
| The service: instances, runs, batches | 1666 | Thirteen calls; `docs` as a call; thin clients over a socket; the disk registry; port claim; reservation; `boot.lock`; append-only samples; staleness reaping; minted ids; run timelines; disk-resolved reads; `compare`; `stopOn`; settle | **DELIVERED chunk 4 (part)** | Chunk 2: the driver process, `start`/`run`/`kill`, three calls, browserless-spec errors, run timelines, `stopOn`, opportunistic staleness reaping. Chunk 3 adds four more calls built (`results`, `status`, `compare`, `cleanup` — seven of thirteen total) and disk-resolved reads for all four. Chunk 4 moves all seven onto `dungeonmaster siegelense <name>` and deletes the MCP layer outright — verified by reading `siegelense-flow.ts`'s `CALL_ROUTES` map and `mcp-server-flow.integration.test.ts:100-114`'s absence assertion. NOT YET: `docs`, `capacity`, `profile`, append-only PROFILE samples, and settle-based stepping — a per-step timeout ceiling still stands in for it |
| Snapshots, reset, and the state/evidence line | 1710 | Named snapshots, the automatic `run_N:start`/`run_N:end` pair, the state-subtree boundary, three reset levels, the diff reported | NOT STARTED | Part 7 item 14 |
| Teardown and crash recovery | 1720 | `kill` mandatory; teardown its own concern; the heartbeat file and its pgids; staleness reaping; tombstones; guild partitioning; the `.siegelense` symlink; per-step flush; disk checks; no auto-restart | **DELIVERED chunk 3 (part)** | Chunk 2: `kill` itself, teardown as its own broker, the per-step transcript flush, opportunistic staleness reaping on `start`. Chunk 3 adds `status` (the post-mortem this row names) and `cleanup` as its own call — verified by reading `status-read-broker.ts` and `cleanup-run-broker.ts`. NOT YET: retention, the disk checks; no-auto-restart is unexercised. **The suite that proves teardown end to end against real processes is GREEN at sixteen of sixteen** — see the "Testing teardown" row in Part 2 |
| Capacity and profiling | 1755 | A measured pool size; RSS sampling; the content hash key; `suggested: 2` with no profile; staggered starts; `queuedMs`; the policy ceiling; foreign load; the `why` sentence; the hard floor | NOT STARTED | Chunk 3 delivered the RSS-reading primitive (`machine-rss-by-pgid-broker.ts`) `status` uses now and a future `capacity`/`profile` will reuse, but no sampler, no steady/peak computation and no ceiling arithmetic exist |
| Recipes: what one is and what holds it | 1775 | The package names as conventions; `init` scaffolds the recipes package; an empty one is a real answer; recipes are a graded PACKAGE; listable without running; explicit dependencies; `fidelity` / `mirrors:`; determinism; `seed` as a step; the oddities file | **DELIVERED chunk 1 (part)** | Delivered: both names are conventions at their exact paths, `init` scaffolds `packages/siegelense-recipes/`, an empty one answers as empty, and a real graded workspace package made with `dungeonmaster create-package`. The recipe CONTENT, `produces:`, `fidelity`, `mirrors:`, the listing, the colocated recipe tests and the DOM-handle lint rule are Part 7 items 3 / 3a / 16 |

## Part 5 — The determinism this system depends on (line 1805)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| What the TOOLING must guarantee | 1815 | Nine invariants: ref stability, key row order, element identity, port claiming, retention, append-only evidence, per-run step numbering, settle-based steps, one boot at a time, pool size on every sample | **DELIVERED chunk 3 (part)** | Chunk 2 enforced append-only evidence and per-run step numbering as write-side guarantees. Chunk 3 makes the append-only evidence row READABLE per step as well as written: `results` resolves `console.jsonl`/`network.jsonl`/`ws.jsonl`, the transcript, and the server-log window, all flushed per step by chunk 2's writer — verified by reading `results-read-broker.ts` and its layer brokers. Ref stability, key row order, element identity, the retention window, settle-based steps and the per-sample pool size all stay open |
| What the CONTENT must guarantee | 1836 | A recipe's output bytes, its timestamps, ids that paint, ordering; no `Date.now()` / `Math.random()` / `randomUUID()` reaching a screen | NOT STARTED | Part 7 items 3 / 16 |
| Animation is the one that conflicts with the product | 1849 | `animations: 'disabled'` and `caret: 'hide'` on every comparison capture; `before` freezes JS-driven motion; `video` and `hold` stay live | **DELIVERED chunk 3 (part)** | The frozen-capture precondition (chunk 2's `animations: 'disabled'`, `caret: 'hide'`) is now load-bearing, since `pixelChange` is the number it exists to de-noise (`shot-change-read-broker.ts` reads the same frozen captures) — verified by reading both files. NOT YET: the `before` lever for JavaScript-driven motion — `before` does not exist yet (chunk 4) |
| The honest limit | 1881 | `pixelChange: 0%` is a strong signal, never a verdict; `blank` is the one exact exception | **DELIVERED chunk 3** | Both rows: `pixelChange` is a percent string a caller interprets, never a verdict (`pixel-change-contract.ts`), and `blank` is computed unconditionally on every capture, independent of and before `pixelChange` — the one exact exception (`step-dispatch-broker.ts` calls `shotBlankReadBroker` alongside `shotChangeReadBroker` on every shot) — verified by reading both files |

## Part 6 — The recipe book (line 1897)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| The tool is `siegelense`, and its recipes live beside it | 1899 | `packages/siegelense/`, `dungeonmaster siegelense`, `dungeonmaster siegelense <name>` subcommands, `packages/siegelense-recipes/`; `init` scaffolds the recipes package; an empty one is a real answer; both real workspace packages; subpath-importable barrels that do not pull msw; the monorepo limit stated | **DELIVERED chunk 4 (part)** | Chunk 2 adds: `dungeonmaster siegelense` in both forms — `driver --instance <id>` and the bare fleet listing — plus three of the thirteen calls (`start`/`run`/`kill`), built and callable. Chunk 3 adds `status` and `cleanup` as real `dungeonmaster siegelense` subcommands and four more calls total. **Chunk 4 finishes the subcommand half for every built call**: `start`, `run`, `results`, `kill`, `status`, `cleanup` and `compare` are each a real `dungeonmaster siegelense <name>` subcommand routed through `SiegelenseFlow`'s `CALL_ROUTES` map, and `CliSiegelenseResponder` validates nothing above it — verified by reading `cli-siegelense-responder.ts` and `siegelense-flow.ts`. No MCP tool remains for any of the seven. NOT YET: the recipe listing, and the six unbuilt call names, which now refuse by name rather than lacking a route |

## Part 7 — Where to go, in order (line 1992)

| # | Line | Requires | Status | Notes |
|---|---|---|---|---|
| 1 | 2000 | Gate `POST /api/tooling/smoketest/run` at registration, with an absence test | **DELIVERED chunk 1** | Built after the chunk 1 plan wrote it out of scope. `packages/server/src/flows/tooling/tooling-flow.ts:28` — `if (process.env.TOOLING_SMOKETEST_HTTP === '1') {` wraps the `app.post`, so an unset flag leaves no route and an unauthorized caller gets the framework's own 404. The absence test is `tooling-flow.integration.test.ts:18`, `'VALID: {TOOLING_SMOKETEST_HTTP unset} => 404, route not registered…'`, shown failing against the ungated route first. Same shape as `E2E_SIGNAL_BACK_HTTP` in `quest-flow.ts` |
| 2 | 2001 | The instance service — start / run / results / kill / capacity / profile / status / cleanup / docs, status-as-index with shot list and `open:` flags, refs invalidated on navigation/reset/restart, RSS per process group | **DELIVERED chunk 4 (part)** | Chunk 2: the driver process, `start`/`run`/`kill`, three calls. Chunk 3 adds `results`, `status` and `cleanup` as real calls, and the RSS-per-process-group READING (`machine-rss-by-pgid-broker.ts`), though not yet a sampler. **Chunk 4 puts all seven built calls on `dungeonmaster siegelense <name>` and deletes the MCP layer** — verified by reading `siegelense-flow.ts` and `mcp-server-flow.integration.test.ts:100-114`'s absence assertion. NOT: `capacity`, `profile`, `docs`, refs, and append-only PROFILE sampling |
| 2a | 2002 | The evidence read path — `results`/`status` off the asset tree, guild partitioning, the `.siegelense` symlink `init` creates and ignores, every returned path absolute and repo-local | **DELIVERED chunk 3** | `results` and `status` both resolve off the asset tree/registry, never the driver socket — verified by reading `results-read-broker.ts` and `status-read-broker.ts`. The symlink, guild/`unowned` partitioning and the repo-local resolver were chunk 1's; every path this round's brokers return is built through `locationsInstanceEvidencePathFindBroker`, the same resolver |
| 2b | 2003 | Teardown and crash recovery, tests written red-first — process groups, port release, home removal, evidence retention, idle-timeout reaping, the three-instance parallel case, the heartbeat file and stale-instance reaping | **DELIVERED chunk 3 — VERIFIED, sixteen of sixteen** | The implementation and the suite that proves it are both delivered, and the suite runs green at sixteen tests, up from fifteen at chunk 2: process-group SIGTERM→grace→SIGKILL with a per-pgid liveness recheck, port release and home removal, the idle timeout as a real backstop, and orphan reaping of a SIGKILLed driver's own recorded pgids. **The one leak chunk 2 left open — the driver's own top-level process not exiting after a successful `kill` — is now CLOSED**: `net-unix-serve-adapter.ts` closes the listening socket and every reply now uses `socket.end()` rather than `socket.write()`, and `driver-idle-wait-layer-responder.ts` clears its sleep timer on both branches of the race rather than leaving an un-cancelled, referenced `setTimeout` alive for up to 900s. See "Chunk 3 open follow-ups" below for how it was found |
| 2c | 2004 | Retention and tombstones — the quest id recorded at `start`, `prune`/`cleanup` resolving citations through `.quest-plans/` and naming the citing file, a reaped entry surviving as a tombstone, a pruned query answering `pruned` | **DELIVERED chunk 1 (part)** | Unchanged by chunk 3 — deliberately deferred per chunk-03 §3.E, the same reasoning chunk 2's plan gave. The `questId` field, the tombstone fields, and `instance-release-broker` never deleting a row remain chunk 1's. NOT: the ageing, the refusal rules and the citation resolver, still blocked on item 11g |
| 3 | 2005 | The recipe book — free the HTTP-only harnesses, expose the free ones by name, add `produces:` and `fidelity`, make them listable | NOT STARTED | The foundation half (the package exists, scaffolded in every repo) is chunk 1; the content is not |
| 3a | 2006 | Recipe integration tests — each asserts its own `produces:`; `direct` ones assert against `mirrors:`; production ones share one instance | NOT STARTED | |
| 3b | 2007 | The PLANNER role — maps paths to recipes, proves every prelude by running it, dispatches for research and diagnosis | NOT STARTED | Prompt work in `packages/orchestrator`, not this package |
| 4 | 2008 | A transcript of every step and reading, written by the instance | **DELIVERED chunk 3** | Chunk 2 wrote it (`run-transcript-append-broker.ts`, flushed per step). Chunk 3 makes it READABLE by anyone: `results { kind: 'steps' }` and `results { step: N }` parse `runs/run_N.jsonl` back — verified by reading `transcript-read-layer-broker.ts` |
| 4b | 2009 | The record's `WALKED` field — instance id and run id against every path walked and every attack run, clean ones included | `N/A — spec-side, not tooling` | The document's own words: "Spec-side work: a quest-contract change, not tooling. See `siege-verification-remainder.md` Part 4". **It still gates tooling:** the `WALKED` line is one of the citations `prune` and `cleanup` must refuse to delete over, so item 2c's refusal rule cannot be finished until this and item 11g land |
| 5 | 2010 | `before` — run a script ahead of the page's own | NOT STARTED | |
| 6 | 2011 | Capture on every acting step, frozen for the comparison path, with a change-amount number; open start and end | **DELIVERED chunk 3** | Chunk 2 delivered capture-always, the frozen comparison capture and open-start/open-end. Chunk 3 completes the row: the change-amount number, `pixelChange`, is real, measured against `pixelmatch`/`pngjs` — verified by reading `pixelmatch-compare-adapter.ts` and `shot-change-read-broker.ts` |
| 7 | 2012 | The key as a tree — element-bound refs, `within`, four columns, the duplicate-testId line; ship without the map | NOT STARTED | |
| 8 | 2013 | `health`, one reading with one verdict line, including the server logs | NOT STARTED | |
| 9 | 2014 | `until` — wait on a response, a file or a predicate | NOT STARTED | |
| 10 | 2015 | Selectable readings — `network` by method and path with field projection; the same plus a self-reporting cap on `dom` | **DELIVERED chunk 3 (part)** | The `network` half: `results { kind: 'network', where: { path, method }, fields: [...] }` filters and projects, and `matched`/`returned`/`truncated` self-report the `resultsStatics.limits.maxRows` cap — verified by reading `results-read-broker.ts` and `resultsAnswerContract`. The `dom` half stays with the ladder it caps (Part 7 item 7) |
| 11 | 2016 | `hold` (non-settlement, live) plus `video` (for a human, never graded) | NOT STARTED | |
| 11b | 2017 | The human-check route — a flag on the observable, dropped from automated denominators, collected into a list handed to the user | `N/A — spec-side, not tooling` | The document's own words: "Spec-side work, not tooling". Blocks no tooling requirement; the walk-side evidence it wants is the `video` step under item 11 |
| 11c | 2018 | The declared-value block and its third reader — extract the duplicated enumeration into one interpolated statics, add the siege consequence, give `siegemaster-prompt-statics.ts` a rule for an UNFLAGGED one | `N/A — spec-side, not tooling` | The document's own words: "Spec-side work, not tooling". Blocks no tooling requirement |
| 11d | 2019 | `siegemaster-reader` — a minion that opens the files a walk must not, returning values with `file:line` | NOT STARTED | Not this package: `agentPromptNameContract`, `agentPromptClassificationStatics.minionNames` and `agentNameToPromptTransformer` all live in `packages/orchestrator` |
| 11e | 2020 | `siegemaster-operational` — a per-operational-flow minion, plus `process-state`, `environment`, a wider log tail and a named elapsed figure | NOT STARTED | Split ownership: the surfaces are steps in this package, the minion is an orchestrator prompt |
| 11f | 2021 | The `(human-check)` PANEL on the quest, in the web UI | NOT STARTED | `packages/web`, not this package |
| 11g | 2022 | A `walked` kind on `questNotes`, with typed `instanceId` and `runId` | **DELIVERED (part) — not this chunk's** | The contract half landed, outside `packages/siegelense`, in the same commit as this chunk's own work: `questNoteKindContract` (`packages/shared/src/contracts/quest-note-kind/`) now includes `'walked'` as its fifth member, and `questNoteContract` carries `instanceId`/`runId` as `.nullish()` fields typed with the shared-side `siegeInstanceIdContract`/`siegeRunIdContract` — confirmed by reading both files. **Still gates tooling**: nothing in `packages/siegelense` reads a `walked` note yet — `cleanup-run-broker.ts` and `stale-reap-layer-broker.ts` were read this round and neither references `questNotes` — so item 2c's citation resolver and `assetsAged` stay blocked on the CONSUMING half, not the typed-field half |
| 11h | 2023 | Print the owning NODE id in `get-qa-checklist` | NOT STARTED | `packages/orchestrator` / `packages/mcp`, not this package |
| 12 | 2024 | Server-side failure injection | NOT STARTED | |
| 13b | 2025 | `compare { runA, runB }` — the index delta between two runs | **DELIVERED chunk 3 (part)** | `compare-read-broker.ts` computes signed deltas for `console`/`server`/`network`, the messages new to run B, and a `pixels` percent — verified by reading the broker against the worked examples at spec lines 2354-2358 and 2856-2861. **Not** `elements` — chunk-03 §3.F, absent from `compareAnswerContract` (`.strict()`) rather than shipped null |
| 14 | 2026 | The three reset levels with named snapshots and an explicit `to`, each reporting the diff it undid | NOT STARTED | |
| 15 | 2027 | `resize`, and a direct `request` step for the curl surface | NOT STARTED | |
| 16 | 2028 | The two local lint rules — no `.first()`/`.last()` in a command, no DOM handle in a recipe — plus the package `CLAUDE.md` | **DELIVERED (part) — not this chunk's** | The `.first()`/`.last()` half is real: `@dungeonmaster-local/ban-locator-pick`, built by the separate agent the chunk 3 plan named, confirmed registered in `eslint.config.js` and implemented in `packages/local-eslint/src/brokers/rule/ban-locator-pick/`. It landed in the same commit as this chunk's own work but is not this chunk's to claim. The DOM-handle-in-a-recipe half stays open — it ships with the recipe book, which holds no recipes yet |
| 17 | 2029 | The lane spec and N ports; move it where consumers get it | **DELIVERED chunk 2 (part)** | Unchanged by chunk 3. The FIRST half: the lane spec is DATA — `lane-spec-contract.ts` takes N processes and a declared `browser` flag, `lane-spec-hash-broker.ts` keys it by content hash. NOT YET: the SECOND half — `lane-spec-statics.ts` still names `@dungeonmaster/server` and `@dungeonmaster/web` directly, so a consumer repo installing this package gets a tool that cannot boot its own app |

## Part 8 — The surface, consolidated (line 2046)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| The rule that governs every targeting step | 2051 | Three outcomes per targeting step; AMBIGUOUS carries the candidates with their refs; NO MATCH names near misses; a ref is never ambiguous and answers `stale` | **DELIVERED chunk 2 (part)** | All three outcomes hold, verified in `step-target-resolve-broker.ts`: one match proceeds; more than one throws `StepAmbiguousError` carrying every candidate (with the `within` that would disambiguate it, in place of a ref); zero throws `StepNoMatchError` naming the near-miss testIds. NOT YET: refs — the candidate rows carry `within`, not a `ref`, until `look` exists |
| Holding the no-pick rule mechanically | 2090 | A local lint rule banning `.first()`/`.last()` on a locator in a command; `.nth()` only with caller input; the rule states its own scope; `querySelector` in eval source held by the package `CLAUDE.md` | **DELIVERED — not this chunk's** | The rule is real, not only prose: `@dungeonmaster-local/ban-locator-pick` is registered in `eslint.config.js` and implemented at `packages/local-eslint/src/brokers/rule/ban-locator-pick/rule-ban-locator-pick-broker.ts` — it flags `.first()`/`.last()` always, and a literal `.nth(N)` (never a variable index), scoped to `packages/siegelense/src/brokers/step/**` via `isLocatorPickScopeFileGuard`. Built by the separate agent the chunk 3 plan named, landed in the same commit as this chunk's own work (`ed7b2036c`) but not this chunk's to claim. `querySelector` in eval source stays prose in the package `CLAUDE.md`, deliberately — the plan's own caution that a rule inspecting string contents is a liability |
| Refs are for DRIVING. Selectors are for RECORDING. | 2114 | A ref is ephemeral, scoped to one instance and one page state; four boundaries it cannot cross; never stored; binds to an element not a row number | NOT STARTED | The package `CLAUDE.md` carries the rule as prose; nothing mints or resolves a ref |
| The package needs a `../../CLAUDE.md`, and these are the entries | 2164 | Twelve named invariants, each with the measurement behind it | **DELIVERED chunk 1** | All twelve written at `packages/siegelense/CLAUDE.md`, in this table's own order, each carrying its measurement. Read entry by entry against the table |
| The thirteen calls | 2190 | Every call is `dungeonmaster siegelense <name>`; steps are not calls; only `start`/`run`/`kill` need a live instance | **DELIVERED chunk 4 (part)** | Seven of thirteen — `start`, `run`, `results`, `kill`, `status`, `cleanup`, `compare` — built and callable end to end, and **every one is now `dungeonmaster siegelense <name>`, with no MCP tool behind any of them** — verified by reading `siegelense-flow.ts`'s `CALL_ROUTES` map and the spawned-binary seam suite in `packages/cli/bin/cli-entry.integration.test.ts`. `look` correctly stays absent. The remaining six (`capacity`, `profile`, `prune`, `snapshots`, `recipes`, `docs`) now refuse BY NAME — "`capacity` is a siegelense call but is not built yet. Built calls: …" — rather than "unknown subcommand", which is a reachability improvement and not delivery of the six |
| Steps that exist today and are kept | 2467 | `goto`, `waitFor`, `look`, `click`, `type`, `key`, `paste`, `box`, `screenshot`, `dom`, `storage`, `eval`, `file`; ambiguity now throws; `end` becomes `kill` | **DELIVERED chunk 4 (part)** | Six of the thirteen ship: `goto`, `waitFor`, `click`, `type`, `screenshot`, `eval` — confirmed exhaustively in `step-statics.ts`'s `verbs.all`. Ambiguity throws; `end` is gone, replaced by the instance-level `kill`. **`screenshot` could not succeed in any invocation until chunk 4**: `locationsShotPathFindBroker` computed a shot path only for `goto`/`click`/`type`, so a `screenshot` step always got a `null` path and threw, and its own `name` field was parsed and dropped rather than used — see the "Defects found and fixed alongside chunk 4" section below. `locations-shot-path-find-broker.ts` now takes an optional `name` and `run-execute-broker.ts:188-191` computes a shot path for `screenshot` too. NOT YET: `look`, `key`, `paste`, `box`, `dom`, `storage`, `file` |
| Steps that are new | 2501 | `look`, `before`, `health`, `reset`, `snapshot`, `seed`, `until`, `hold`, `video`, `request`, `resize` | NOT STARTED | Confirmed absent — none of these appear in `step-statics.ts`'s six-verb `verbs.all` |
| Results queries | 2627 | `console` · `network` · `ws` · `server` · `screenshots` · `steps`; per-step attribution on every entry; projection and filtering | **DELIVERED chunk 3** | All six kinds, with per-step attribution on every console/network/ws/server entry, `where` filtering and `fields` projection — verified by reading `results-read-broker.ts`'s per-kind dispatch and its layer brokers (`buffer-read-layer-broker.ts`, `server-window-read-layer-broker.ts`, `transcript-read-layer-broker.ts`) |
| What every acting step returns, on top of its own reading | 2670 | `shot`, `pixelChange`, `elements` on every acting step | **DELIVERED chunk 3 (part)** | `shot` was chunk 2's. `pixelChange` joins it this round — verified by reading `step-reading-contract.ts` and `step-dispatch-broker.ts`. NOT YET: `elements`, which needs `look` (chunk 4) |
| A worked batch | 2687 | `as` naming a step's output and `{name.field}` reading it back; `stopOn`; `expect: 'error'`; a step expecting failure that succeeds is itself a finding | **DELIVERED chunk 2 (part)** | Unchanged by chunk 3. `stopOn` and `expect: 'error'` both hold, confirmed in `run-execute-broker.ts` and `step-dispatch-broker.ts` — a step declaring `expect: 'error'` that succeeds returns `ok: false` and stops the batch. NOT YET: `as` naming a step's output and `{name.field}` reading it back, which need `seed` |
| A FIXER reading a finished instance | 2727 | Steps 1–4 start nothing; repo-local absolute shot paths; server-log window; per-step network; reproduction on a FRESH instance; the same recipes in the e2e | **DELIVERED chunk 3 (part)** | Steps 1 to 4, verbatim, all four starting nothing: `status { instance }` (step 1), `results { instance, run, step }` (step 2), `results { kind: 'server', where: { steps, level } }` (step 3), `results { step, kind: 'network', fields }` (step 4) — verified by reading `status-read-broker.ts` and `results-read-broker.ts` against this section's own worked example. Steps 5–7 already work (chunk 2's `start`/`run`/`kill`), except step 6's recipe half, which needs recipes that do not exist yet |
| Interleaving recipes and steps | 2812 | `seed` placeable anywhere; recipes composed by explicit parameters; seeding with a page open; a reload where no socket drives the surface | NOT STARTED | |
| Cycles — run, snapshot, collect, repeat | 2861 | The two cycle shapes; `compare`; the clean status return; the failing return carrying the key; a timeout naming its step | **DELIVERED chunk 3 (part)** | The `RunResult` shape (chunk 2) holds. Chunk 3 adds `compare` — verified by reading `compare-read-broker.ts` against the worked example at spec lines 2856-2861, minus `elements` — and the shot list's `pixelChange`/`blank` columns, now measured rather than placeholder. NOT YET: the two cycle shapes, which need `snapshot`/`reset`, and a genuine settle-based timeout |

### The thirteen calls, individually (Part 8, line 2244)

| Call | Line | Requires | Status | Notes |
|---|---|---|---|---|
| `start` | 2290 | Stands up an instance; returns id, baseUrl, home, evidence dir, log paths, seeded ids; optional `quest` decides the partition and the citation; browserless specs are first-class; queues and reports `queuedMs` | **DELIVERED chunk 4 (part)** | `instance-start-broker.ts` returns the full `InstanceManifest` — id, `baseUrl` (`null` for a browserless spec), home, evidence dir, both log paths, `queuedMs`, `aheadOfMe`, `bootMs` — confirmed by reading the broker end to end. **The bare `command: 'dungeonmaster'` spawn defect a real manual run found is fixed AND committed** (`cliPackageBinResolveAdapter`, spawning `process.execPath` against a resolved bin path — `git log` shows `b27b0ebf2`), but has still not been re-verified end to end by a person since the fix landed. **Reached now as `dungeonmaster siegelense start --spec <specName> [--quest <questId>] [--guild <guildId>]`** — verified by reading `siegelense-start-responder.ts` and `start-args-parse-transformer.ts`; no MCP tool remains. Has no integration coverage in the flow suite — like `run` and `kill`, it needs a live driver the suite deliberately has none of — so its coverage is `siegelense-start-responder.test.ts` plus the manual drive. NOT YET: `seeded` ids — recipes are declared but `--seed` is unbuilt. **The pool-size refusal IS now built**: `instanceStartBroker` calls `capacityReadBroker` and throws `CapacityRefusedError` when it answers 0, before any reservation is minted |
| `run` | 2337 | Submit a batch; blocks; returns a STATUS, never a payload | **DELIVERED chunk 4** | `instance-run-broker.ts` sends one `run` request over the socket and returns the parsed `RunResult` — an index and a shot list, never the steps' own payloads. **Reached now as `dungeonmaster siegelense run --instance <id> (--steps <json> \| --steps-file <path>) [--stop-on error\|never]`** — verified by reading `siegelense-run-responder.ts` and `run-args-parse-transformer.ts`; no MCP tool remains. Has no integration coverage in the flow suite — it needs a live driver the suite deliberately has none of — so its coverage is `siegelense-run-responder.test.ts` plus the manual drive |
| `results` | 2339 | Query narrowly by run/step/kind; starts nothing; answers for a dead instance; run id required against a finished one; every answer carries `instanceState` | **DELIVERED chunk 4** | `results-read-broker.ts` implements the whole call: queries by run/step/kind, `where`, `fields`, `since: 'boot'`, `instanceState` on every answer, `pruned`/`unknown` as real answers, a required run id against a finished instance. **Reached now as `dungeonmaster siegelense results --instance <id> [--run <runId> \| --since boot] […]`**, its flag vocabulary the spec's own field names kebab-cased — verified by reading `results-args-parse-transformer.ts` and the argv-driven read-path suite in `siegelense-flow.integration.test.ts`'s `'the seven calls, through argv'` block; no MCP tool remains |
| `kill` | 2542 | Tear down; `{ stopped, ports: 'released' }`; removes the throwaway state and never the evidence | **DELIVERED chunk 4** | `instance-kill-broker.ts` sends `kill` over the socket and, on `DriverUnreachableError` (a SIGKILLed driver), falls back to reading the heartbeat file's own recorded pgids and reaping them directly — both paths confirmed by reading the broker. `lane-teardown-broker.ts` removes only the throwaway home; the evidence directory is never passed to `fsRmAdapter`. **Reached now as `dungeonmaster siegelense kill --instance <id>`**, and `siegelense-kill-responder.ts` checks the registry first so a typo'd id refuses by name rather than surfacing `DriverUnreachableError`. No MCP tool remains. Has no integration coverage in the flow suite for the same live-driver reason as `run`; its coverage is `siegelense-kill-responder.test.ts` plus the manual drive |
| `capacity` | 2514 | `suggested`, `ceiling`, a `why` sentence, `measured` and `profile`; counts foreign load; advisory with one hard floor | **DELIVERED** | `capacity-read-broker.ts` divides a MEASURED profile group rather than a typed number, counts every registry row including reservations (debiting their peak), and returns `suggested`, `ceiling`, `why`, `measured` and the one `profile` group it used. **`start` now refuses when this answers 0**, after the opportunistic stale reap and BEFORE `instanceReserveBroker`, so a refusal leaves no registry row and no claimed port pair — that closes the pool-size refusal chunk 2's marker listed as NOT YET. Two knowing departures from the spec's illustrative example, both argued in `plans/call-capacity.md`: the `why` sentence reports MB rather than GB (the spec's own 5320MB→5.2GB and 2600MB→2.6GB cannot both hold — one divides by 1024, the other by 1000 — and the sentence's job is that the arithmetic be recomputable against `measured`), and `profile.fromRuns` is the CHOSEN GROUP's run count rather than the total across groups (the spec's block pairs one group's memory figures with a 14-run total, which is the blend the never-average rule exists to prevent). `--spec` and `--pool` are additions: without `--pool`, "the group matching the pool it is about to open" is circular at a terminal. Reached as `dungeonmaster siegelense capacity [--spec <name>] [--pool <n>]` |
| `profile` | 2529 | Processes, hash, measuredAt, fromRuns, bootMs, samples grouped by pool size and never averaged across them | **DELIVERED** | `profile-read-broker.ts` reads what was measured and never measures on demand. **The write path is new too**: `driverHeartbeatTickBroker` records RSS per beat and `instanceStartBroker` records `bootMs`, both in a caught branch, so a profile write can never void a heartbeat (the only defence a SIGKILLed driver leaves) or tear down an instance that booted fine. The never-average rule is owned by one transformer and proven by an integration test feeding two pool-size groups; the agent mutated the expected value to confirm the assertion bites, then reverted. Known limit: the ticker starts after boot, so `peakMB` is the highest RSS observed POST-boot — a peak covering the boot spike needs a sampler inside `laneBootBroker`. `specName` is an extra field beyond the spec's six, kept because a JSON document on stdout should say what it profiled. Reached as `dungeonmaster siegelense profile --spec <specName>` |
| `status` | 2493 | The post-mortem — `monitored`, `machine`, one entry per instance; a dead one carrying last beat, last step, RSS, orphan pgids, evidence paths and `likelyCause`; never lists runs or evidence for an instance you did not name | **DELIVERED chunk 4** | `status-read-broker.ts` implements the whole call: `monitored` derived from `machineStatics.monitored`, a real `machine` block, one entry per instance, a dead one carrying last beat, last step, RSS, orphans, evidence paths and `likelyCause`, and evidence/`lastStep` staying `null` for every row in a `status {}` fleet listing even a dead one — verified by reading `status-read-broker.ts` and `instance-entry-layer-broker.ts` against the no-browsing rule (chunk-03 §3.D). Chunk 4 adds `queriedInstanceState` so a named unknown-id query is no longer byte-identical to an empty fleet (`status-read-broker.ts:66-69`), and fixes a crash on a busy machine where a process exiting mid-read threw instead of being skipped (`machine-rss-by-pgid-broker.ts:73-75`, `orphan-read-broker.ts`). **Reached now ONLY as `dungeonmaster siegelense status [--instance <id>] [--human]`** — no MCP tool remains |
| `cleanup` | 2456 | Reap stale instances, release ports and the lock, age assets; report `reaped`, `portsReleased`, `lockReleased`, `assetsAged` and `leftAlone`; never touch a live instance | **DELIVERED chunk 4 (part)** | `cleanup-run-broker.ts` implements reaping by staleness, port release, conditional lock release and `leftAlone` — verified by reading the broker and `stale-reap-layer-broker.ts`. **`assetsAged` now lands too**, through the same reclaim path `prune` uses: a `2d` video pass then a `7d` everything pass, refusing exactly what `prune` refuses and naming the citing file in `leftAlone`. **Reached now ONLY as `dungeonmaster siegelense cleanup [--human]`** — no MCP tool remains |
| `prune` | 2432 | Reclaim by `olderThan` / `instance` / `kind`; refuse anything a `VERIFIED` prelude, an open issue or an open quest's `WALKED` line cites, naming the citing file | **DELIVERED minus one citation kind** | `prune-run-broker.ts` reclaims by `--older-than` (defaulting to `7d`, never to everything, because this call deletes), `--instance` and `--kind`, and returns `freedMB`, `freedBytes`, `removed[]`, `refused[]` and `unresolved[]`. **Two of the three citation kinds resolve**: a `walked` quest note (re-parsed through siegelense's own `instanceIdContract`, and only while the quest is non-terminal) and a `VERIFIED` prelude under `<worktreePath>/.quest-plans/**.md`. **The third cannot be built.** No issue record exists anywhere carrying a typed instance or run id — `signoffContract` has neither, `questNoteKindContract` has no `issue` member, and no issue contract folder exists in any package. It is declared as a permanent `CitationGap` and rides `unresolved[]` on every answer, so an empty `refused` never reads as "nothing cites any of this". The `VERIFIED` match is a heuristic rather than a parse: a prelude line names a RUN, the quest scopes it, so a quest holding two instances that both hold a `run_7` refuses both — broader than the spec's wording, in the safe direction. Reached as `dungeonmaster siegelense prune [--instance] [--kind] [--older-than] [--human]` |
| `compare` | 2470 | The index delta between two runs of one instance; a READING, never a verdict; no cross-instance form | **DELIVERED chunk 4 (part)** | `compare-read-broker.ts` implements the call, minus `elements` — see Part 7 item 13b for the full grade. The no-cross-instance-form rule is enforced both by `compareQueryContract`'s `.strict()` and, at the argv layer, by `compare-args-parse-transformer.ts` naming `--instance-a`/`--instance-b` and the no-cross-instance rule directly rather than a generic unknown-flag refusal. **Reached now as `dungeonmaster siegelense compare --instance <id> --run-a <runId> --run-b <runId>`** — no MCP tool remains |
| `snapshots` | 2481 | List what `reset level: 'state'` can return to; manual names plus the automatic `run_N:start`/`run_N:end` pair; gone with the instance | **DELIVERED, on a substrate that is not** | `snapshot-list-broker.ts` returns `instanceId`, `instanceState` and one row per restore point, oldest first. The automatic pair is minted by `runExecuteBroker` and is a REAL recursive copy of the throwaway home's state subtree, not a marker — `fsCpAdapter` is `readdir` plus one `fs.cp` per child, because `fs.cp` refuses a destination inside its own source. `snapshotResolveBroker` errors on a name that does not exist rather than falling back to the nearest, and a manual name in the `:start`/`:end` namespace is refused at capture. **Every row reports `manual: false` today**, because the `snapshot` STEP VERB is not built: registering it cascades through `stepStatics.verbs.all`, `stepVerbContract`, the step discriminated union and `stepDispatchBroker` (whose first act throws for a lane with no browser, which a disk verb must branch out before). Everything beneath the verb is built and tested on both values of `manual`. Deviations: `atMs` rather than the spec's rendered `at: '20:03:11'`, matching every other JSON answer here; the store lives at `<throwawayHome>/.siegelense-snapshots/`, the only directory `laneTeardownBroker` already removes. Retention is declared and unenforced. Reached as `dungeonmaster siegelense snapshots --instance <id>` |
| `recipes` | 2326 | Every recipe name with its `produces:` and `fidelity`; listable without being run; no instance needed | **DELIVERED as a DECLARATION, not as a runner** | `recipe-book-read-broker.ts` lists both recipes the spec names — `guild-with-three-quests` (fidelity `production`) and `session-with-nested-subagent` (fidelity `direct`) — each with its `produces:` text verbatim from the spec, its `fidelity`, its `mirrors:` pointer, and the parameters and ids it declares. An EMPTY list is a real answer; an absent `packages/siegelense-recipes/` is a `RecipePackageMissingError` instead, so the two never read alike. A contract refinement enforces `mirrors:` in both directions against `direct` fidelity. **Nothing RUNS a recipe**: `seed` is an unbuilt step verb and `start` takes no `--seed`, so what ships is a typed declaration those files will code against — and the runner will resolve through `recipeBookReadBroker`, the same call the listing makes, so the two cannot drift. `recipeFidelityContract` is deliberately unbranded (a three-member enum is already closed, and a brand makes it unusable as a key into the statics describing it — precedent: `workItemRoleContract`, `frameworkContract`). Known limit: the book is compiled data, so a consumer repo's own recipes are not enumerated. Reached as `dungeonmaster siegelense recipes [--human]` |
| `docs` | 2373 | The tool's own instructions, served by a call; seven scopes — `operating`, `planning`, `walking`, `attacking`, `fixing`, `driving`, `operational`; `operating` carries no step verbs | **DELIVERED** | `docs-statics.ts` plus a compose and a render transformer serve all seven scopes; `docs {}` serves the whole surface. **`operating` holds no step verb at all**, asserted by flattening the scope to JSON, lowercasing it and filtering `stepStatics.verbs.all` against it to `toStrictEqual([])` — which bans the substrings `type`, `click` and `eval` outright, so the prose deliberately avoids "typed", "clicking" and "retrieval". That assertion caught a coordinator edit during wiring. `walking` carries the ladder in order (key first, `dom` last and narrow); all five rows of the spec's `driving` table appear verbatim — the spec has FIVE, not four. An unknown scope is refused BY NAME and lists the seven. **The rule for unavailable capabilities**: every line naming one ends in the literal token `NOT BUILT YET.` and, where one exists, names the callable thing that does the job today; a line without the marker is callable now, and the `about` preamble states that rule once. Omission was rejected because four of the ladder's five rungs are unbuilt, so an omitting `walking` would teach `eval` as the only reading — the founding-rule breach the spec warns of. `--human` renders the same document as indented text; JSON stays the default and is structured rather than prose-in-a-string, which is what lets a session cite one section instead of pasting the page. Reached as `dungeonmaster siegelense docs [--for <scope>] [--human]` |

## Part 9 — What exists as scratch (line 3087)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| What exists as scratch | 3087 | Nothing in Parts 1–3 is built; the `tmp/siege*` prototypes are throwaway | `N/A — spec-side, not tooling` | An inventory, not a requirement. `packages/web/test/siege-driver/` is superseded rather than renamed and is retired by Part 7 item 17 |

---

## Defects found by an adversarial read of `status` (W13) — all three CLOSED

| # | Defect | Where | State |
|---|---|---|---|
| 1 | A failed RSS measurement voids the whole heartbeat | `brokers/heartbeat/write/heartbeat-write-broker.ts:57` | **CLOSED.** `machineRssByPgidBroker` throws on EACCES **by design** — correct for a `status` read a session can retry, wrong inside a periodic beat. `rssMB` is `.nullable()` so an unmeasurable value degrades rather than voiding the beat |
| 2 | `status` and `results` counted runs from different files and disagreed for a crashed run | `status/read/instance-entry-layer-broker.ts`, `results/read/run-list-layer-broker.ts` | **CLOSED.** Both now call the SAME `runEvidenceComputeTransformer` — verified by reading `instance-entry-layer-broker.ts` (destructures `runCount`/`latestRunId`/`evidenceComplete` off it) and `run-list-layer-broker.ts` (returns its result directly). The transformer counts runs off `.jsonl` transcripts, never the `.json` stored return, so a crashed run still counts — `run-evidence-compute-transformer.ts`'s own header states the reasoning |
| 3 | `evidenceComplete` was quoted in acceptance criteria and did not exist | `contracts/instance-status/instance-status-contract.ts` | **CLOSED.** The contract now carries `evidenceComplete: z.boolean()`, populated in every row (fleet or named) by `instanceEntryLayerBroker` from the same `runEvidenceComputeTransformer` — verified by reading both files. It answers "did the last run's `.json` stored return land", true when there are no runs at all or the latest run's `.json` sits beside its `.jsonl`, false only when the driver died before that closing write ran |

**A MILDER version of defect 1 survives the fix, and it was found while fixing it.** `heartbeatWriteBroker`
calls `registryUpdateBroker` AFTER it writes `heartbeat.json`, and `registryLockAcquireBroker` throws when
`registry.lock` is held fresh by another process past `instanceLifecycleStatics.registryLock.waitCeilingMs`. So
under sustained contention the heartbeat FILE lands with fresh pgids while the registry row's `lastBeatMs` does
not get stamped. **`isStaleRegistryEntryGuard` reads `lastBeatMs` off the registry row, not the heartbeat
file**, so repeated missed stamps reproduce the same "healthy instance reads as stale" cascade in milder form.
Each tick retries the lock fresh, so it is self-healing rather than stuck. The shape worth considering: the
heartbeat FILE is the more reliable of the two signals here, and nothing reads it for staleness.

**One residual risk the audit named but could not settle**: in `instanceEntryLayerBroker`'s `named: true` plus
`state: 'alive'` path, `orphanReadBroker` and `machineRssByPgidBroker` run CONCURRENTLY through one `Promise.all`
and both independently `path.join` the identical `/proc/<pid>/stat` for a matching pid. No test exercises that
interleaving, so whether the shared FIFO join queue is staged correctly there is **untested rather than
confirmed**.

## Chunk 3 open follow-ups, found while building

| Item | Where | What is needed |
|---|---|---|
| ~~`compare`'s `console.new` / `server.new` / `network.new` are always `[]`~~ **CLOSED** | `brokers/compare/read/compare-read-broker.ts` | Now calls `resultsReadBroker` — the `results` domain's ENTRY file — once per run per kind, and feeds each pair into the existing `newLinesLayerBroker`. **It could not call `bufferReadLayerBroker` directly**: that is a LAYER file, and `enforce-project-structure` refuses a cross-domain import of one, so the entry file is the only door in. The agent that first hit this reverted its wiring attempt rather than leave unreachable code, and the gap stayed open until `results-read-broker.ts` existed. Proven by a test that returns `Array []` against the old code and the exact line against the new |
| ~~The driver's own process does not exit after a successful `kill`~~ **CLOSED** | `adapters/net/unix-serve/`, `responders/siegelense/driver/` | Reproduced first: the driver's pid was still alive 60 seconds after a fully acknowledged kill, confirmed by `ps` as `State: S (sleeping)`, not a zombie. **Two bugs, not one.** (1) The listening socket was never closed anywhere, and replies used `socket.write()` where the protocol is one-request-per-connection — now `socket.end()`, and `netUnixServeAdapter` returns a `close` the serve responder awaits LAST, after every teardown branch, so an in-flight reply is never dropped. (2) `DriverIdleWaitLayerResponder` races a sleep against the kill signal, and **`Promise.race` does not cancel the loser** — a referenced timer stayed scheduled for up to `driverStatics.idle.timeoutMs` (900s) after every successful kill. Found by sending the hung process `SIGUSR1`, attaching to its inspector over a raw WebSocket, and reading `process.report.getReport().libuv`, which showed a `timer` handle with `is_referenced: true` — **invisible to `_getActiveHandles()` and `_getActiveRequests()`**, which is why it survived every earlier look. A regression test now asserts the driver's own pid exits within 10s of kill; all 16 teardown tests pass |
| ~~`status`/`results` run-count disagreement, and the missing `evidenceComplete` field~~ **CLOSED** | `results/read/run-list-layer-broker.ts`, `status/read/instance-entry-layer-broker.ts`, `transformers/run-evidence-compute/run-evidence-compute-transformer.ts` | Both entry brokers now read one shared transformer rather than counting independently — see "Defects found by an adversarial read of `status` (W13)" above, #2 and #3 |
| ~~`fsReadFileAdapter` read PNG bytes as utf8, corrupting every screenshot it touched~~ **CLOSED** | `adapters/fs/read-file/fs-read-file-adapter.ts` | The adapter's only prior caller was text (`registry.json`, lock files, `runs/run_N.json`), so it defaulted to and only ever used `'utf8'`. This chunk is the first to read a PNG's own bytes back off disk, for `shotBlankReadBroker`/`shotChangeReadBroker`'s decode step — and `'utf8'` collapses invalid byte sequences into lossy replacement characters, corrupting the binary before `pngjsDecodeAdapter` ever sees it. The adapter now takes an optional `encoding` (default `'utf8'`, unchanged for every existing text caller), and both shot brokers pass `encoding: 'latin1'` — the one encoding that maps each byte to a single code unit and round-trips binary data losslessly — confirmed by reading `shot-blank-read-broker.ts:35` |
| The MCP search tools' worktree blindness is fixed in code, not live | `packages/mcp/src/brokers/caller-repo-root/resolve/caller-repo-root-resolve-broker.ts` | Off-spec work landed alongside chunk 3 (same commit, `ed7b2036c`), not required by `siegelense-tooling.md`. `discover`/`get-project-map`/`get-project-inventory` resolved the dungeonmaster project root from the MCP SERVER's own `process.cwd()`, which sits on `master` and has never held `packages/siegelense` — the exact blindness `HANDOFF.md` and every chunk plan's own "Rules every work item respects" section name. `callerRepoRootResolveBroker` now resolves the CALLER's own cwd instead, via a JSONL scan keyed on `_meta['claudecode/toolUseId']`, falling back to the server's cwd and reporting which source it used. **Not live in this session**: the fix ships in `packages/mcp/src/**` source, and this session's own MCP child is a stdio process already running the OLD `dist/`; it takes a rebuild of `@dungeonmaster/mcp` and a reconnect before any session's `discover` call reaches worktree-aware code. Until that happens, the workaround this file and every chunk plan already document (`Read`, `ls -R`, a `python3 -c` walk) stays necessary |
| The read-path integration suite the chunk 3 plan names (W18) has not landed | `packages/siegelense/test/harnesses/` | The plan's own §1 calls this suite — a real evidence tree built with `installTestbedCreateBroker`, real PNGs written by `pngjs`, driving `results`/`status`/`compare`/`cleanup` against it with no driver and no browser — "the reason this chunk is buildable today" and names it explicitly. As of this pass, `packages/siegelense/test/harnesses/` holds only `driver-fleet/` (chunk 2's teardown harness); no `evidence-tree/` harness and no `siegelense-flow.integration.test.ts` read-path suite exist. The `siegelense-flow.integration.test.ts` file that DOES exist tests only CLI argument routing (seven tests: fleet/driver/status/cleanup dispatch and rejection shapes), not the deep read-path assertions the plan's W18 lists. This round's grading instead rests on unit tests per broker (real math, staged fixtures) plus the real stdio MCP integration tests in `mcp-server-flow.integration.test.ts` — both independently verified by reading the code, but neither is the single real-evidence-tree suite the plan called for |
| Composing two proxies that mock the SAME raw builtin steals one-shots | `@dungeonmaster/testing` proxy chains, package-wide | **Three agents hit this in one session.** W12: computing paths through the real location-resolver brokers at test-SETUP time consumed the `pathJoinAdapter` one-shots staged for the code under test. W15: `lockReleaseLayerBrokerProxy` and `instanceKillBrokerProxy` both mock `os.homedir()`, and the semantic chain re-stamps that mock's sticky default at construction time, then stages one-shot overrides that are consumed by whichever real call runs next — regardless of which proxy staged them. `instance-start-broker.proxy.ts` already carries a comment warning about exactly this, so it is a known fragility that keeps being rediscovered rather than fixed. **The workaround both agents landed on: register the raw builtin mock directly and sticky, rather than going through the semantic one-shot chain.** A real fix would scope one-shots to the proxy that staged them |
| ~~A raw control byte in a source file is INVISIBLE to every text tool~~ **SWEPT CLEAN** | package-wide | A `0x00` renders as a space in `Read`, in `Edit`'s diff, and in jest's own printed diff. **It already made a careful reviewer report a bug in correct code this session** — and the blindness runs both ways, so a reviewer can equally read WRONG code and see nothing. A byte-level sweep of all files under `packages/siegelense/**` (every byte below 0x20 that is not tab, LF or CR) found **no hits**; the one known case is on visible escapes (`' '` in the broker, `String.fromCharCode(0)` in the fixture). **Re-run the sweep rather than trusting this row**: `os.walk` the package, open each file `'rb'`, flag any byte `< 0x20` outside `{0x09, 0x0A, 0x0D}`. Never use `Read` or `grep` for this — they are the tools that cannot see it |

## Defects found by an adversarial read of chunk 2 — two CLOSED, one PARTLY closed

Three defects in code marked `DELIVERED chunk 2`, each found by reading the code against the spec's own words
rather than by a test. **Every one had a passing test suite sitting on top of it**, and the reason each suite
missed it is recorded below, because that reason is the reusable part.

| # | State | The fix, and what proves it |
|---|---|---|
| 1 | **CLOSED** | The SIGKILL loop now re-checks `processIsAliveAdapter` per pgid, mirroring `instance-kill-broker.ts:115-117`. The new proxy method stages a pgid as alive-then-gone across two probes and **deliberately leaves SIGKILL unstaged**, so a regression throws `registerMock: nothing set up for the call mockConstructor(-5501, "SIGKILL")` rather than passing quietly. That exact error is what the test printed against the unfixed code |
| 2 | **CLOSED** | `step-dispatch-broker.ts` now captures before rethrowing a real failure, guarded by the same `shotPath !== null && step.step !== 'screenshot'` condition used elsewhere. The capture's own failure is logged and swallowed so it can never mask the original error, which still propagates. Against the unfixed code the new tests printed `Expected: [[{"filePath": ".../step1.png"}]]` / `Received: []` |
| 3 | **PARTLY closed — the lock leak is fixed, the caller's wait is not** | `siegelense-driver-responder.ts` releases `boot.lock` when `laneBootBroker` throws, then rethrows unchanged — confirmed by reading the current file, whose own header now states the reasoning directly: a dead boot no longer blocks a DIFFERENT queued `start` for three minutes, and the real cause lands in `driverLogPath`. **The original caller still waits out its own full poll deadline** — the file's own comment says so: releasing the lock here "frees a QUEUED SIBLING boot immediately… rather than making it wait out this one's dead poll window too", which is explicit that the caller of THIS boot is not who benefits. `instance-start-boot-poll-layer-broker.ts` is untouched by chunk 4 as well (confirmed absent from its diff) — the chunk 4 plan names this defect explicitly and declines it on purpose, since the fix widens `registryEntryContract` and is not a surface concern. **Stays open**, reassigned to the capacity chunk (chunk 8) — it needs a side channel the poller can also read, a failure marker the driver writes before dying, which touches the registry-entry contract the same way a future profile sampler will |

### The original finding, kept for the blind spots it names

The three rows below are the audit's own report, recorded because **the reason a suite missed a defect is more
reusable than the defect.** Each names a test-design blind spot this package can repeat anywhere.

| # | Defect | Where | Severity | Why its tests missed it |
|---|---|---|---|---|
| 1 | Teardown SIGKILLs every group that was alive BEFORE the SIGTERM, never rechecking who survived the grace period | `brokers/lane/teardown/lane-teardown-broker.ts` | A pgid that exits during the grace window can be recycled by the OS, so the SIGKILL lands on an unrelated process. The spec says "SIGKILL the **survivors**" | `lane-teardown-broker.proxy.ts` staged `processIsAliveAdapter` to answer a CONSTANT per pgid for the whole test. No test made a group alive at the first check and dead at the second, so the mock could not tell "checked once" from "checked twice" — the wrong value and the right value were indistinguishable. **The fix already existed one file over**: `instance-kill-broker.ts`'s own recheck before its SIGKILL |
| 2 | A step that failed for a REAL reason never captured its screenshot; a step declaring `expect: 'error'` did | `brokers/step/dispatch/step-dispatch-broker.ts` rethrew with no capture; `brokers/run/execute/run-execute-step-layer-broker.ts` hardcoded `shot: null` | Silent loss of the single most useful evidence in a failing run. Spec line 676: "always capture — the evidence trail is complete whether or not anyone looked" | Every test exercising an unexpected throw passed `shotPath: null`. No test combined a real throw with a non-null `shotPath`, which is the production shape for an actual defect |
| 3 | A driver-side boot failure was never caught, so `boot.lock` released only when the client's own poll exhausted its 180-second deadline | `responders/siegelense/driver/siegelense-driver-responder.ts`; `brokers/instance/start/instance-start-boot-poll-layer-broker.ts` | `laneBootBroker` can throw in milliseconds, but the poller could not tell "driver already exited" from "Chromium still launching" — both read as a refused connection | `siegelense-driver-responder.test.ts` had two scenarios: a clean boot, and a missing registry row. No test made `laneBootBroker` itself throw |

One possible defect the audit named but did NOT pursue to a repro, recorded so it is not lost: a DOM-state
race between `session.countMatches` and `session.describeMatches`/`nearestNames` in
`step-target-resolve-broker.ts`, which are two separate Playwright queries against a live page.

## Open defects in delivered code

Found while grading chunk 1 against the spec. Both are CLOSED and committed — confirmed by reading the current
files, not by trusting a commit message.

| Defect | Where | State | What was done |
|---|---|---|---|
| `registry.lock`'s acquire read every failed lock read as ABSENCE | `packages/siegelense/src/brokers/registry/lock-acquire/registry-lock-acquire-broker.ts` | CLOSED, committed at `b27b0ebf2` | The read-failure branch classifies `readError.cause.code !== 'ENOENT'` and rethrows anything else unchanged, mirroring `boot-lock-acquire-broker`'s own fix; a further pass on top of that (also committed at `b27b0ebf2`, not left open as an earlier ledger pass recorded) adds an `errorIsNativeErrorAdapter` check for a cross-realm `fs/promises` rejection, an `mkdir -p` of the siegelense root before the exclusive create, and swallows an `ENOENT` from the stale-lock unlink when two contenders race to clear the same expired lock — confirmed by reading the current file and `git log`, which shows no uncommitted changes against it |
| The check-glob insert missed a SINGLE-LINE array | `packages/siegelense/src/responders/install/ignore-write/array-entry-anchor-insert-layer-responder.ts` | CLOSED chunk 2 | Rewritten around `ArrayEntryLineParseLayerResponder`, a structural per-line parser that finds every quoted array entry on a line rather than assuming one entry per line, and decides per anchor whether to insert on its own new line or inline on a shared one — confirmed by reading the current file, which handles both `"exclude": ["node_modules", "worktrees"]` (inline) and a one-entry-per-line array |

Two smaller deviations from the chunk 1 plan, neither a defect in behaviour:

- `instance-reserve-broker` resolves the owner from `process.pid` internally, as the plan asked, but the proxy never
  mocks `process.pid` — the test reads the live pid of the test runner. The plan's own wording was "mocking
  `process.pid` through the proxy" (`plans/chunk-01-registry-spine.md:641`).
- The port re-roll test asserts the returned pair, not the WRITTEN row; only the sibling happy-path test asserts the
  row through the registry proxy. The plan asked for the written row on the collision case specifically.
- `port-claim-exhausted-error.ts`'s `WHEN-NOT-TO-USE` describes a sequential ask-check-reask retry. The broker
  fetches all `claimAttempts` candidates from the OS upfront in parallel and picks the first free one inside one
  atomic mutate — a deliberate, documented TOCTOU-avoiding design. The error's doc comment and the broker's header
  disagree about which one the code does.

## Off-spec work delivered alongside chunk 1

Not required by `siegelense-tooling.md`. All three were found while building chunk 1 and all three are fixed; they are
recorded so the work is not invisible.

| Item | Where | State | What it was |
|---|---|---|---|
| `dungeonmaster create-package` seeded a jest config that could not run an integration test | `packages/cli/src/statics/package-scaffold-config/package-scaffold-config-statics.ts` | DELIVERED, committed | A scaffolded package importing `@dungeonmaster/testing`'s root barrel hit `SyntaxError: Unexpected token 'export'` from msw's ESM. `jestConfigNodeIntegration` now carries `transformIgnorePatterns` AND a widened `transform`; three scratch jest runs proved each field is independently required. The transformer picks it over `jestConfigNode` for a seed whose `needsMswTransform` is true |
| `@dungeonmaster/hooks` install failed on a repo with no `.claude/` directory | `packages/hooks/src/adapters/fs/ensure-write/`, `.../responders/install/create-settings/install-create-settings-responder.ts`, `packages/hooks/test/harnesses/fresh-project/` | DELIVERED, committed (`ca60415be`) | Writing `.claude/settings.json` into a brand-new project threw `ENOENT` because nothing created the parent directory. `fsEnsureWriteAdapter` now `mkdir -p`s it first. `installTestbedCreateBroker` could never reproduce this — it pre-creates `.claude/` to satisfy other packages — so `freshProjectHarness` makes a project holding only a `package.json`, and `install-flow.integration.test.ts` drives the fresh case against it |
| `dungeonmaster init` printed the literal string `undefined` for a failed package install | `packages/cli/src/responders/cli/init/cli-init-responder.ts` | DELIVERED, committed (`ca60415be`) | `InstallResult` populates `message` on success and `error` on failure, never both, and the line printed `result.message` unconditionally. Every real failure therefore reported `[FAIL] @dungeonmaster/x: undefined` |

## Off-spec work delivered alongside chunk 2

Not required by `siegelense-tooling.md`. Found while building chunk 2's driver and its tests. Verified by reading the
current code, not by trusting a commit message.

| Item | Where | State | What it was |
|---|---|---|---|
| `ward --committed` refused any run whose scope named a deleted-but-uncommitted file | ward's git-diff scoping (per commit `86ef4a415`) | DELIVERED, committed (`86ef4a415`) | `--committed` diffs merge-base to HEAD with no knowledge of the working tree, so a file deleted but not yet committed put a path in scope that was not on disk, and ward refused the whole run rather than the one path. A git-derived scope now drops a gone path and names it; a path a human typed after `--` still refuses, because there it is a typo |
| Our own `locationsStatics` additions broke lint in several unrelated packages | `packages/siegelense/src/statics/evidence-file/evidence-file-statics.ts` (per commit `a1c10a7fd`) | DELIVERED, committed (`a1c10a7fd`) | Four bare-extension fragments (`.jsonl`, `.json`, `.png`, `step`) added to the shared, repo-wide `locationsStatics` tripped `no-bare-location-literals` in every package that happened to write one of those strings for an unrelated reason — commit message records lint failing across nine packages before the fix. The fragments moved to a package-local `evidenceFileStatics`, since a bare extension is domain vocabulary, not a repo-wide claim on a string every package uses |
| The shared mock-staging helper re-wrapped cross-realm errors | `packages/testing/src/transformers/mock-staging-create/mock-staging-create-transformer.ts`, `.../middleware/mock-staging-create/`, `.../adapters/error/is-native-error/` | DELIVERED, committed (`b27b0ebf2`) | `rejects()`/`throws()` tested a staged value with `val instanceof Error`, which reads `false` for a genuine error Node's own internals constructed outside a Jest test file's vm realm (an `fs/promises` rejection, a `process.kill` failure, `fetch`'s I/O) — so a real error got silently re-wrapped in a generic `new Error(String(val))`, LOSING its `.name`/`.code`/`.cause`. `isNativeError` (checking the V8-internal error slot via `util/types.isNativeError`) is now OR'd alongside the `instanceof` check, injected through a new middleware since a transformer cannot import a node builtin directly. `registry-lock-acquire-broker.ts`, `compare-read-broker.ts` and `results-read-broker.ts` all use the same `errorIsNativeErrorAdapter` check directly, for the identical reason |
| The proxy-mock transformer keys its `jest.mock()` dedup on the raw import specifier | `packages/testing/src/transformers/mock-calls-merge-by-module/mock-calls-merge-by-module-transformer.ts` | DELIVERED, committed (`a1c10a7fd`) | `'process'` and `'node:process'` resolve to the same module, so two proxies mocking one builtin under different specifiers became two competing partial `jest.mock()` factories — jest keeps one and the loser falls through to the real syscall with nothing naming the cause. The dedup key is `mock.moduleName.replace(/^node:/u, '')` in `mock-calls-merge-by-module-transformer.ts`, unioning the two records' `identifierNames`. The fix is generic and reaches every package, because `jest.config.base.js` wires the transformer into every workspace's jest run |

## Off-spec work delivered alongside chunk 3

Not required by `siegelense-tooling.md`. Found and fixed while building and verifying chunk 3's read path. Verified
by reading the current code, not by trusting a commit message.

| Item | Where | State | What it was |
|---|---|---|---|
| The MCP search tools (`discover`, `get-project-map`, `get-project-inventory`) were blind to every worktree | `packages/mcp/src/brokers/caller-repo-root/resolve/caller-repo-root-resolve-broker.ts` and its supporting brokers under `claude-code-caller-cwd/` | DELIVERED, committed (`ed7b2036c`) — **NOT LIVE for any already-connected MCP session** | The MCP server resolves the project root from its OWN `process.cwd()`, which is the main checkout on `master` — the exact blindness this chunk's own plan and `HANDOFF.md` document as the reason every agent had to fall back to `Read`/`ls -R`/`python3`. `callerRepoRootResolveBroker` instead resolves the CALLER's cwd from a JSONL scan keyed on `_meta['claudecode/toolUseId']` (the mechanism `packages/mcp/CLAUDE.md`'s sub-agent-identity section documents), falling back to the server's own cwd and reporting which source answered in `configFound`/`source`. Needs a `@dungeonmaster/mcp` rebuild and an MCP reconnect before any session's `discover` call benefits — this session's own tool calls this round still went through the workaround, unchanged |
| The `status`/`results` run-count split-brain and the missing `evidenceComplete` field | `transformers/run-evidence-compute/run-evidence-compute-transformer.ts` | DELIVERED, committed (`ed7b2036c`) | See "Defects found by an adversarial read of `status` (W13)" above — two independently-counting brokers unified behind one transformer, and `evidenceComplete` added to `instanceStatusContract` and computed for real |

## Chunk 3 defect count found and fixed, for the record

Fifteen-plus defects were found and fixed across chunk 3's own build and its adversarial re-reads, several in
code this ledger had already marked delivered from chunk 1 or chunk 2 (the registry-lock hardening, the two
teardown leaks, the driver-exit leak, the `status`/`results` split-brain). The count is not tallied here because
a tally rots the moment the next pass finds one more; what is checkable is the shape above — each fix cites the
file it landed in and what proves it.

## Defects found and fixed alongside chunk 4

Found while moving the surface onto the CLI, in code this ledger had already marked delivered from chunk 2 or
chunk 3. None of them is on the chunk 4 plan. Verified by reading the current code, not by trusting the commit
message.

| # | Defect | Where | State |
|---|---|---|---|
| 1 | ~~The `screenshot` step verb could not succeed in any invocation~~ | `brokers/locations/shot-path-find/locations-shot-path-find-broker.ts`, `brokers/run/execute/run-execute-broker.ts:188-191` | **CLOSED.** `locationsShotPathFindBroker` computed a shot path only for `goto`/`click`/`type` (`stepStatics.verbs.acting`), so a `screenshot` step always got `shotPath: null` and threw; its own `name` field was parsed by `stepContract` and never read. The broker now takes an optional `name`, and `run-execute-broker.ts` resolves a `screenshot` step's path from `step.name` as a second branch. Invisible to the unit test, which hands the consuming broker a good path and never touches the broker that computes it — found by driving the real tool |
| 2 | ~~`status` crashed on a busy machine~~ | `brokers/machine/rss-by-pgid/machine-rss-by-pgid-broker.ts:62-81, 109-128`, `brokers/orphan/read/orphan-read-broker.ts:50-69, 104-123` | **CLOSED.** Both brokers read several hundred `/proc/<pid>/stat`/`/statm`/`cmdline` files inside one `Promise.all`, and a process that exits between the directory listing and the read makes its entry vanish — `ENOENT` when `/proc/<pid>` is already gone before the read opens it, `ESRCH` when it exits in the gap after that open succeeds. Both codes now mean "gone" and are skipped rather than thrown; any other read failure (EACCES, a bad handle) still propagates. Proven with a probe racing real child exits; the second broker running the identical walk was found only because the suite was run twice |
| 3 | ~~`status --instance <unknown>` returned JSON byte-identical to an empty fleet~~ | `brokers/status/read/status-read-broker.ts:37-92`, `contracts/status-answer/status-answer-contract.ts` | **CLOSED.** Flipping `status`'s default from a human table to JSON (§3.A of the chunk 4 plan) handed the ambiguity to a reader that cannot recover from it — only the human renderer could tell the two cases apart before. `queriedInstanceState` now carries the resolved state of the NAMED id (`'unknown'` included) for a named query, `null` for a fleet listing; a killed instance still reports `killed`, never `unknown`, so the tombstone rule holds |
| 4 | ~~`stepContract` required `node`, rejecting `{"step":"goto","path":"/"}` with no `node: null`~~ | `contracts/step/step-contract.ts` | **CLOSED.** `node`, `within` and `timeoutMs` are `.nullable()` but were not `.default(null)` on any of the six verbs that carry them — unusable once a human types a batch over argv rather than assembling MCP-era JSON by hand. All three are now `.nullable().default(null)`; an explicit `null` still parses to `null`, so no existing construction site breaks, and `.strict()` still rejects an unknown key |
| 5 | ~~Error messages told callers to type MCP-era JSON~~ | sixteen error files across `packages/siegelense/src/errors/**` | **CLOSED.** `run-id-required-error.ts` said `{ run: 'run_N' }` and pointed at what `siegelense-start` returned; help text already said `--run`. Messages now name the flag a caller actually types — `run-id-required-error.ts:34-36` reads "Name a run with `--run <runId>` … See `dungeonmaster siegelense status --instance <id>`", and `instance-unknown-error.ts:23` reads "Check the id `dungeonmaster siegelense start` returned." `within` correctly stays JSON-shaped in any message that quotes it, since step fields live inside `--steps`, not on argv directly |
| 6 | ~~Seven arg parsers, built by five agents, disagreed three ways on how to refuse a mistake, and two of seven rejected `--json`~~ | `transformers/flag-value-read/flag-value-read-transformer.ts` and the seven `*-args-parse-transformer.ts` files | **CLOSED.** One shared reader (`flagValueReadTransformer`) now owns the refusal sentence for a missing value, a value that itself starts with `--`, and a flag named twice — a value flag followed by another flag names the flag whose value is missing, never a token two positions later. Every parser accepts `--json` uniformly as the explicit affirmation of the default (chunk 4 plan §3.A) |

Six defects, named above; the count is not tallied further because a tally rots the moment the next pass finds
one more.

## Off-spec work delivered alongside chunk 4

Not required by `siegelense-tooling.md`. Found and fixed while moving the surface onto the CLI. Verified by
reading the current code, not by trusting a commit message.

| Item | Where | State | What it was |
|---|---|---|---|
| Nothing granted `Bash(dungeonmaster siegelense:*)` | `packages/shared/src/statics/agent-qa-permissions/agent-qa-permissions-statics.ts` | DELIVERED, committed (`8d4994c41`) | The seven MCP tools this chunk deletes each carried their own `mcp__dungeonmaster__siegelense-*` grant; no Bash grant covered the CLI subcommands. A dispatched agent has no interactive approver (`agent-qa-permissions-statics.ts:9-11`), so every siege call was DENIED outright to the exact reader the CLI exists to serve. `allow` now carries `'Bash(dungeonmaster siegelense:*)'`, confirmed by reading the statics and its test; the grant stops at `siegelense` deliberately, since `init`, `create-package` and `start` are not siege calls |
