# Siegelense recipes ledger

Running coverage of `siegelense-recipes.md` against what has been planned and delivered. **One row per
heading under Part 3A, Part 5 and Part 6**, keyed by heading text and its line number in the CURRENT file.
Every round updates this file.

**Line numbers shift whenever anyone edits the spec.** Re-derive them from heading text before trusting a
number in this table — never carry one forward from an older copy of this ledger.

Status vocabulary, carried over from `build-ledger.md` (the sibling ledger for `siegelense-tooling.md`),
plus one addition this ledger needed that that one did not:

| Status | Means |
|---|---|
| `NOT STARTED` | nothing plans it yet |
| `PLANNED chunk N` | a chunk plan under `plans/` covers it, wholly |
| `PLANNED chunk N (part)` | a chunk plan covers some of it; Notes says which part |
| `DELIVERED chunk N` | chunk N built it, wholly, and the code was read against this section's own words |
| `DELIVERED chunk N (part)` | chunk N built some of it; Notes says exactly which part landed and which stays open |
| `BLOCKED chunk N` | the code exists and is believed sound but could not be RUN to confirm; Notes names the blocker. Never read as tested |
| `DELIVERED (pre-existing)` | the code already exists and was read this round, but it was built before this ledger's own chunk numbering began — by the siegelense-tooling build, not a chunk plan under `scrolls/seigelense/plans/` for this document. Add `(part)` on the same terms as the chunk-numbered statuses |

**Six chunk plans exist under `plans/`:** `recipes-chunk-01-03-framework-types.md` (chunks 1–3),
`recipes-chunk-04-06-runner.md` (chunks 4, 5, 6 and 6b), `recipes-chunk-07-repo-ingredients.md` (this
repo's own ingredients), `recipes-chunk-08-siegelense-listing-and-seed.md` (the siegelense `recipes`
listing and the `seed` step), `recipes-chunk-09-10-migration.md` (the suite conversion), and
`recipes-chunk-12-combinatorial-rounds.md` (the combinatorial session). Each plan's own
amendments/findings section records where the plan was wrong and what shipped instead; a row below
reads against what shipped, not against the plan's original text, wherever the two disagree.

**This round is a grading pass, not a documentation-only pass.** It re-derives every line number from
the current spec by reading the file fresh, and reads the real code behind every status this round
changes — the master merge that removes the MCP layer, the Part 6 rewrite that states the CLI surface,
the seven chunk-8 findings folded into Part 5, the `copies:` external-producer contract, and this
repo's own ingredients and runner. **Every status this round did not itself re-read against code is
carried forward from the last grading pass that did** — the vocabulary's `DELIVERED chunk N` entry
requires only that SOME round read the code, not that this one did; where this round re-read a row, its
Notes says so and names the file.

**A row reading `DELIVERED (pre-existing)` is work the siegelense-tooling build already did**, unrelated
to any chunk plan, and stays as it was unless a Note says this round touched it.

**What this ledger does not cover.** Per the standing instruction: the front matter about the prompt that
builds this document, the suggested spine, and the branch-tracking instructions ("This branch tracks
master…" through "The rename is its own step…") are directions to the build, not requirements of it, and
carry no row. `docs { for: … }`'s seven scopes and the rest of the thirteen-call CLI surface belong to
`siegelense-tooling.md` and are tracked in `build-ledger.md`, not here — this document only adds the
`recipes` call and the `seed` step to that surface. **The interface for that whole surface is the CLI.**
`packages/mcp` carries no siegelense tool — `packages/mcp/src/flows/mcp-server/mcp-server-flow.integration.test.ts:100-114`
asserts directly that no `tools/list` entry starts with `"siegelense-"`, the same absence assertion
`build-ledger.md` reads this claim against — and `packages/siegelense` owns its own CLI flow
(`SiegelenseFlow`, entered through `startup/start-siegelense.ts`) that routes each call as a CLI
subcommand. dungeonmaster's OWN quest-orchestration surface (`get-quest`, `start-quest`, `run-ward`, …)
is a separate, still-live MCP tool set and is not what this paragraph is about.

---

## Part 3A — The decision index (line 230)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Recipes: the three packages, the three tiers, and what holds them | 232 | Serves as the condensed decision index for every rule Part 5 states in full — three packages with import/publish boundaries, three verb tiers, the handle/chain mechanics, one state-setting verb, `filter`'s zero-match throw, link-by-name typing, route/`copies:` rules, determinism rules, the plan-as-data rule, the ingredient-test/correctness-vs-fitness split, and the migration rules — with Part 5 winning wherever the two disagree | **DELIVERED chunk 3 (part)** | No edit landed inside Part 3A itself this round. See the Part 5 rows below for the checkable detail behind each condensed line. Two things this index states as settled are now CONFIRMED against real code rather than carried as design intent: "AN INGREDIENT declares the routes" (`packages/siegelense-recipes/src/brokers/*/ingredient/*-ingredient-broker.ts`, five files, each declaring its own `routes`) and "`siegelense` NEVER imports `hydration-recipes`" (`packages/siegelense/src/flows/siegelense/siegelense-flow.ts` has no import of `@dungeonmaster/siegelense-recipes` or `@dungeonmaster/hydration` anywhere in its CALL_ROUTES wiring, matching the three-package table's `neither of the others` cell) |

## Part 5 — The recipe book (line 281)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Four words this document uses, and two rules that outlive the split | 291 | Defines instance/run/recipe/prelude precisely, and states that a recipe returns a plan runnable in-process with no MCP boundary, and that one row must make "the right one" and "the first one" the same value (the two-of-anything rule via `defaults(index)`) | **DELIVERED chunk 4 (part)** | `run(plan, target)` is now built and exercised, not merely proven on values: `packages/hydration/src/brokers/hydration/create/hydration-create-broker.ts:93-98` exports `run` (and `listing`) off `planRunBroker`, and `packages/siegelense-recipes/CLAUDE.md`'s own "registry() and run() MUST come from the SAME call" section documents it driving five real ingredients. "No MCP boundary anywhere" is now doubly true: the framework never had one, and the tool that calls it has none either (see the header note above) |
| The problem | 314 | Names the four separate seeding vocabularies (e2e harnesses, hand-rolled `fs` in Jest integration tests, `questHydrateBroker` + blueprints for smoketests, a throwaway walk script) that one recipe book must replace with a single shared spelling | NOT STARTED | A narrative problem statement, not a build artifact. Unchanged this round |
| Three packages, and what may cross between them | 343 | Three packages — `@dungeonmaster/siegelense` (ships), `@dungeonmaster/hydration` (ships, repo-agnostic), `packages/hydration-recipes` (does not ship, absent from root `package.json` `dependencies` with a colocated test pinning that) — where `siegelense` discovers recipes at run time and never imports the recipes package | **DELIVERED chunk 3 (part)** | The absent-from-root-dependencies rule stays delivered: `packages/siegelense-recipes/src/statics/siegelense-recipes/siegelense-recipes-statics.test.ts` still pins it (per `packages/siegelense-recipes/CLAUDE.md`'s own section on the test). **The package is still named `siegelense-recipes` on disk, not `hydration-recipes`** — the rename Part 3A's "#### The rename is its own step" section describes has not happened; every citation in this ledger uses the on-disk name. `siegelense` never importing the recipes package (or `hydration`) is now confirmed by reading `siegelense-flow.ts` directly rather than inferred |
| Three tiers, and only the middle one is written per entity | 370 | Structural verbs (`add`, `filter`, `set`, `remove`, `saveRecordAs`) live once in the framework; capability verbs (like a `transitions`-driven `set`) are declared once and typed per ingredient, promoted only on the second ingredient that wants one; extras are per-ingredient verbs nothing else gets | **DELIVERED chunk 3 (part)** | Now also proven on a second capability user, not just described: `quest` and (per the industry-terms section) the framework's own worked examples both declare `transitions`; `session`'s `withNestedChain` and `quest`'s `advanceOneStep`/`corruptToLegacySchema`/`withWardResultDetail` are real `extras`, each its own broker under `packages/siegelense-recipes/src/brokers/` |
| Every property on an ingredient, and what goes in it | 389 | An `ingredient()` config accepts required `name`/`description`/`fields`/`record`/`routes` and optional `links`/`transitions`/`defaults`/`copies` (required with a `write` route)/`extras`; `links` gains an optional `from` for a parent whose own id field is not `id`; `reach` receives the walked `record`; a route cannot always call what `copies:` names, because a package's `exports` map decides what crosses a boundary; `extras` is one `{ args, apply }` object, never a bare contract; `copies:` names either a bare in-repo identifier or an `external:<name>` producer, and never a `/` | **DELIVERED chunk 7 (part)** | The `copies:` external-producer form is CONFIRMED built, not merely specified: `packages/hydration/src/contracts/copies-target/copies-target-contract.ts:25` refuses any value containing `/`, line 35 refuses `external:` with nothing after the prefix, and its colocated test proves a bare identifier, an `external:claude-cli` value, and a slash-containing value all resolve as the contract states. `session` and `subagent` (`packages/siegelense-recipes/src/brokers/session/ingredient/`, `.../subagent/ingredient/`) both declare `copies: 'external:claude-cli'` for real, closing the gap this row's Notes used to carry as open. `links.from` (the session's `{ of: 'guild', as: 'cwd', from: 'path' }`) and `reach` receiving `record` (`quest-reach-route-broker.ts`, walked through `questModifyBroker`) are both exercised by real ingredients, not only the type fixtures |
| What a recipe declares | 587 | `recipe({ name, description, inputs? }, builder)` takes a required name, a one-line present-tense description of the end state that `recipes {}` prints, an optional `inputs` zod schema for what the recipe needs from an earlier step, and a builder taking the recipe's typed inputs (if any) and returning ops | **DELIVERED chunk 7 (whole)** | Three real recipes exist and each is exercised by its own colocated `.integration.test.ts`: `guild-mid-execution`, `quest-advances-one-step`, `session-with-nested-chain` (`packages/siegelense-recipes/src/brokers/{guild-mid-execution,quest-advances-one-step,session-with-nested-chain}/recipe/`). `recipeManifestContract.parse([...])` in `packages/siegelense-recipes/index.ts` is the manifest chunk 8 reads |
| The listing `recipes {}` prints | 613 | `recipes {}` prints, per recipe, its name, description, `inputs` (KEY NAMES off the static input contract, never the branded type — zod stores no trace of a brand at run time), `runs` (`serverless` only when every ingredient in the plan has a write route, else `needs a server: <ingredient>`), and `makes` (each ingredient's NAME, not its description, counted off the plan, `varies` wherever a filter or transition decides the count); a plan for an input-taking recipe needs a LISTING PROBE before `runs`/`makes` can be computed at all | **DELIVERED chunk 8 (part)** | The COMPUTATION this line needs exists and is tested: `hydrationCreateBroker`'s `listing(plan)` (`packages/hydration/src/brokers/hydration/create/hydration-create-broker.ts:93-98`) folds `planRunsTransformer` and `planMakesTransformer` over the same `registeredIngredients` closure `run` reads, and `dmRegistryBroker.listing` (`packages/siegelense-recipes/src/brokers/dm/registry/dm-registry-broker.ts:44,54`) exposes it for this repo, with both paths asserted on real values (`hydration-create-broker.test.ts:143-214`, `dm-registry-broker.test.ts:58-79`). What does NOT yet exist: the CLI-facing render. `recipesListingBuildBroker` (the listing-probe broker chunk 8's plan calls B1) is not on disk anywhere under `packages/siegelense-recipes/src/brokers/`, `packages/siegelense/src/flows/siegelense/siegelense-flow.ts`'s `CALL_ROUTES` has no `recipes` entry, and `packages/siegelense-recipes/index.ts` still exports only `recipesManifest` — none of `recipesListingBuildBroker`/`recipesSeedRunBroker` chunk 8's own header says it will add. So `dungeonmaster siegelense recipes` prints nothing today; the framework-side half of this requirement is built, the tool-side half is not. `planMakesTransformer` (`packages/hydration/src/transformers/plan-makes/plan-makes-transformer.ts:42-48`) sets `'varies'` only on an ingredient a `filter` op actually names, confirming by direct read that a transition-only mint with no filter naming it stays absent from `makes` entirely — this is a structural consequence of the op tree carrying no `reach` node, not a special case the transformer had to add |
| Linking a child to its parent | 670 | A `links` entry names its parent by NAME; the registry inverts names into accessor keys; the runner walks the plan depth-first, writing each ancestor's id into the named field, but SKIPS a field the row's own `fields` already carries rather than overwriting it (which is what lets `under()`'s caller-supplied id outrank an ancestor's); a child accessor appears only on a host both named in that child's `links` and satisfying every one of them; a `fromSaved` naming a record declared later, or at top level, is refused by the PRE-FLIGHT; the mechanism covers one field on the child pointing at one field on the parent and nothing narrower — a foreign key shaped like a prefixed string inside an array cannot be expressed | **DELIVERED chunk 7 (part)** | The array-foreign-key limit is now CONFIRMED against a real entity, not left a hypothetical: this repo's five ingredients are `guild`, `quest`, `operation`, `session`, `subagent` (`packages/siegelense-recipes/src/brokers/*/ingredient/`) — no `workItem` ingredient exists, and `packages/siegelense-recipes/CLAUDE.md` states directly that its rows "stay a field on `quest`'s own `fields`", matching `quest`'s `relatedDataItems` shape exactly. The explicit-field-beats-ancestor-link skip and the pre-flight forward-reference refusal are unchanged from the last grading pass that read them (`link-values-transformer.ts:40-43`, the runner plan's own D5) |
| The chain: every call names what it affects | 752 | `add(n, build)` hands its builder a fixed-length tuple of handles plus `all` as a second argument, so every subsequent call is made on a handle rather than an implicit current level | **DELIVERED chunk 3 (whole)** | Unchanged this round |
| One verb sets state, and the INGREDIENT decides whether that means a walk | 786 | `set()` is the only verb that assigns a value; the ingredient routes a plain field to a plain write and a field named under `transitions` to a walk through `reach`; `setRaw()` writes a transition field with no walk at all; a broadcast `all.set()` on a transition field is one full gate walk PER ROW, not one shared write | **DELIVERED chunk 7 (part)** | Now proven against a real gate-walking ingredient, not only the framework's own transformer test: `quest-reach-route-broker.ts` walks `status` through `questModifyBroker` over a shortest path computed by `questStatusWalkPathTransformer`, and `quest-ingredient-broker.CLAUDE.md`'s own section documents the twelve reachable statuses versus the excluded ones (`created`, `pending`, `paused`, `blocked`, `merging`, `merged`). The broadcast-handle cost multiplication is unchanged from the last grading pass that read `collection-chain-transformer.ts:147-156` |
| Every verb but `add` takes an object | 825 | Every chainable except `add` (which keeps a bare count) takes exactly one object argument, so a growing verb adds a key rather than a new parameter | **DELIVERED chunk 1** | Unchanged this round |
| `filter` selects rows that only exist at RUN time | 839 | `filter({ where, expect })` returns a set with no index access and no `add`; `where` is a typed match object, never a closure; `expect` defaults to `'some'` and a zero match THROWS by default; a `filter` nested inside an `add` is scoped to its immediate host, never the whole instance | **DELIVERED chunk 4 (part)** | `packages/hydration/CLAUDE.md`'s own `filter` section restates this as delivered fact (scope via the op's own `scope` field, matched against the ancestor chain), and `quest-ingredient-broker.integration.test.ts`'s `g[0].quests.filter({...}).set({title})` case (named directly in `packages/siegelense-recipes/CLAUDE.md`) exercises it through a real, non-mocked plan |
| A recipe takes typed inputs, so it can stack on what an EARLIER STEP made | 878 | A recipe needing a value from an earlier seed step declares a typed input parameter as a zod schema under `inputs`, and a batch supplies it through a `params` object that never flattens onto the step's own keys | **DELIVERED chunk 7 (part)** | Confirmed on a real recipe: `session-with-nested-chain-recipe-broker.ts` declares `inputs: { guildPath }` and parses it at build time — `packages/siegelense-recipes/CLAUDE.md`'s "Two of the three recipes diverge" section documents this by name, including WHY it is `guildPath` and not the specification's own worked `guildId` example (the session ingredient links via `{ of: 'guild', as: 'cwd', from: 'path' }`). The `params`-object-never-flattened rule is still design-only for the `seed` step itself — see "Steps that are new" below |
| Every chainable, with an example | 923 | Every chainable — `add`, `set`, `setRaw`, `filter`, `remove`, `saveRecordAs`, `fromSaved`, `under`, and an ingredient's own extra — exists exactly as shown and compiles; `defaults` is spread AFTER `under()` and silently overwrites a shared field; `saveRecordAs` freezes a SNAPSHOT at create time, not a live view; a saved NAME is a key and a repeat save is last-wins, silently | **DELIVERED chunk 4 (part)** | `defaults`-after-`under()` (`collection-chain-transformer.ts:115-118`), the `saveRecordAs` snapshot (`op-create-apply-layer-broker.ts:112`, and `packages/siegelense-recipes/CLAUDE.md`'s own section citing `quest-advances-one-step-recipe-broker.integration.test.ts` reading the ledger off real disk for exactly this reason) and the last-wins saved name (`opSaveRecordApplyLayerBroker`'s plain `Map.set`) are unchanged from the last grading pass that read them. **A gap this round found, not previously on this ledger: no chainable reaches a row that already exists.** The spec's own text (lines 1058-1065) states it directly — `add` mints new rows, `filter` matches rows the target already holds, `fromSaved` and `under` both reach only a row this same plan is creating — so nothing in the chain can append a line to a session a dispatched agent is already driving, or rewind a quest's status by reading a file the real server wrote. The spec calls this "a hole in the chain, not something solved elsewhere in this document," and `packages/siegelense-recipes/CLAUDE.md`'s own "Two known gaps this chunk did not close" section names the concrete cost: no recipe or ingredient can modify a row an earlier seed step created, addressed only by its id. **A second gap this round found that the spec does not name anywhere, in this heading or in the Known Gaps table**: `.under()` does not carry ancestor names forward for child accessors. `dm.quests.under({ guildId }).add(1, (q) => [...])` type-checks, but `q[0].operations` inside that builder does NOT (`TS2339`) — `collectionChainTransformer`'s own `under` implementation passes the same (usually empty, top-level) `ancestorNames` the ORIGINAL collection carried into the child collection it returns, so a row made via `.under()` has no ancestors as far as the type system is concerned, even though the runtime link value is real. Documented only in `packages/siegelense-recipes/CLAUDE.md`'s "`.under()` does not carry ancestor names forward for child accessors" section, which also names the concrete cost: `quest-advances-one-step-recipe-broker.ts` writes its ledger through the quest's plain `operations` FIELD via `set()` rather than through the `operation` ingredient's own child accessor, as a documented workaround |
| A plan is data, and one plan runs three ways | 1103 | `recipe()` returns pure data at build time; the same plan runs via `run(plan, { home, baseUrl })` from a walk or e2e spec, and via `run(plan, { home })` (write routes only) from a Jest integration test, with a targetless caller restricted to write-route ingredients by the types | **DELIVERED chunk 7 (whole)** | `run` is exercised by real ingredients against a real target through `dmRegistryBroker.run`, gated by the same-instance rule `packages/siegelense-recipes/CLAUDE.md` documents ("registry() and run() MUST come from the SAME call"). `server-app.harness.ts` (`packages/server/test/harnesses/server-app/`) is a caller outside `siegelense-recipes` itself now doing exactly this — importing `dmRegistryBroker` and `recipesHydrationCreateBroker` off `@dungeonmaster/siegelense-recipes/brokers` |
| Determinism is structural, not a rule to remember | 1127 | The chain owns each row's index and hands it to `defaults(i)`, so no ingredient ever calls `Date.now()`, `Math.random()`, or `crypto.randomUUID()` for anything reaching a screen, and the same plan run twice produces identical bytes | **DELIVERED chunk 7 (part)** | The lint rule's LOGIC is now built, not only described: `ban-nondeterminism-in-ingredients` exists under `packages/eslint-plugin/src/brokers/rule/`, backed by a `nondeterministic-call-watchlist` static, with a colocated RuleTester test — confirmed passing this round by a scoped `npm run ward -- --only unit -- packages/eslint-plugin/src/brokers/rule/ban-nondeterminism-in-ingredients/rule-ban-nondeterminism-in-ingredients-broker.test.ts` (`PASS`). `packages/siegelense-recipes/CLAUDE.md`'s "An ingredient never calls a clock or a random source" section names the deliberate exceptions this repo's own routes carry (mint fields `guild-add-broker.ts` and `quest-write-route-broker.ts` produce, justified inline as fields a two-route comparison projects away). **Correcting a claim two earlier passes both made and got wrong: the rule IS wired to fire.** `packages/eslint-plugin/src/brokers/config/dungeonmaster/config-dungeonmaster-broker.ts:142` sets `'@dungeonmaster/ban-nondeterminism-in-ingredients': 'error'` (read directly, byte for byte — an earlier grep in this same round missed it), `packages/eslint-plugin/src/responders/eslint-plugin/create/eslint-plugin-create-responder.ts:69,136,208` imports the rule broker and assembles it into the published plugin's own `rules` object under that exact key, and `eslint.config.js:23,4299,4549,5190` calls `configDungeonmasterBroker()` and spreads its `.typescript`/`.fileOverrides` output straight into the exported config array this repo's own `npx eslint`/ward lint actually runs. `start-eslint-plugin.ts` itself never needed to name the rule — it is a one-line delegator to `EslintPluginFlow()`, which is what calls the create responder where the real assembly happens; the package's own `CLAUDE.md` naming that file as a required registration step is imprecise about which file in the chain, not wrong about the rule being live |
| Routes: how an ingredient makes its state | 1141 | An ingredient declares one or more of `api`, `write` (requiring `copies:`), or `recording`; the TARGET picks which one runs; `routes` also carries optional `query`, `update` and `remove` for a chain call that reaches a row the create route did not just make, refused in the pre-flight when the ingredient declares none | **DELIVERED chunk 7 (part)** | `query`/`update`/`remove` routes are now built for real ingredients, not only the runner's own type/pre-flight machinery: `guild-query-route-broker`, `guild-remove-route-broker`, `quest-query-route-broker`, `quest-update-route-broker`, `quest-remove-route-broker`, `operation-query-route-broker`, `operation-update-route-broker`, `operation-remove-route-broker`, `session-query-route-broker`, `session-remove-route-broker`, `subagent-query-route-broker`, `subagent-remove-route-broker` all exist under `packages/siegelense-recipes/src/brokers/`, each with its own test. `packages/siegelense-recipes/CLAUDE.md` documents a real divergence found while building `quest-update-route-broker.ts`: it returns the reloaded `Quest` record rather than `questModifyBroker`'s own `{success, error?}` envelope, reusing the same "return real data on success, throw on failure" pattern the `api`-route unwrap adapter uses rather than a second unwrap adapter |
| An ingredient for a FILE, and an ingredient for a DATABASE ROW | 1181 | The framework is generic over its own target type via `createHydration<T>()`, proven by the prototype's `ingredients.ts` (file) and `db.ts` (database) declaring identical chain mechanics over different targets | **DELIVERED chunk 4 (whole)** | **Correction: the spec's own text is stale here, and the code says so.** It reads "Nothing positively exercises every chainable over the database-backed repo the same way… This is open work, not a decision." `packages/hydration/test/type-fixtures/positive/every-chainable-db.ts` exists and is exactly that — its own header calls it "`every-chainable.ts`'s counterpart, proving the SAME chain composes over a foreign-key shape," with the one honest, documented exception being `extras` (`sql-target.ts`'s three ingredients declare none). Commit `583cde7ed`'s own message confirms this landed as delivered work: "The database-backed shape proven too, since one chain over two target kinds is the claim that makes this a framework." The gap is closed; the spec's sentence describing it as open has not caught up |
| An ingredient with only one route, and what it costs | 1276 | A plan runs serverless only when EVERY ingredient in it declares a write route (an ALL, not an ANY); the listing reports `runs serverless` or names the first ingredient lacking one; the runner refuses that mismatch before seeding anything | **DELIVERED chunk 7 (part)** | Now confirmed for this repo specifically, not only as a framework claim: every one of the five ingredients declares a `write` route (`packages/siegelense-recipes/CLAUDE.md`'s own ingredient table), so every recipe this repo ships runs serverless today. `guild`'s `write` route is `copies: 'guildAddBroker'`, mirrored the same way `quest`'s is `copies: 'questPersistBroker'` |
| Every ingredient carries a test, and a two-route ingredient tests its routes against each other | 1314 | Every ingredient has a colocated integration test asserting what it made; an ingredient declaring both `api` and `write` runs both and compares them rather than snapshotting either alone; every `api`-route test in a suite shares one booted instance | **DELIVERED chunk 7 (part)** | Reverses the last grading pass's finding that "chunk 7 needs a real ingredient to test, and none is wired" — all five ingredients now carry colocated tests: `guild-ingredient-broker.test.ts`, `quest-ingredient-broker.test.ts` (plus its own `.integration.test.ts`), `operation-ingredient-broker.test.ts`, `session-ingredient-broker.test.ts`, `subagent-ingredient-broker.test.ts`, and `guild`/`quest` each declare both `api` and `write` routes, giving both a real two-route PAIR to compare. **But no test actually runs the comparison**: `quest-ingredient-broker.integration.test.ts` exercises only the `write` route and the transition walk against real disk (confirmed by reading it directly — its own header says so: "Real disk, real `questModifyBroker`… `quest-reach-route-broker.test.ts` beside this one proves the routing logic against a STUBBED `questModifyBroker`"), and a grep of every `*.test.ts` under `packages/siegelense-recipes/src` for "two-route"/"compares" finds nothing. `plans/recipes-chunk-09-10-migration.md`'s own §G0-c names this precisely: "nothing has yet run an `api` route against a real server through the runner… that is now batch **10.1**'s job" — the two-route-comparison spec this heading requires is planned (§11.4) but not yet written or run |
| What the type suite proves, and what it changed | 1347 | The committed type-fixture tree (a `positive/` tree that compiles with zero diagnostics, `declaration/` and `call-site/` trees each carrying exactly one deliberate error) proves each design rule — link-by-name, `all` as a second builder argument, the immediate-parent-plus-satisfied-links accessor rule, a generic `Target` type — and a ten-case malformed-declaration probe found nine compile-time-catchable mistakes plus one needing a runtime check | **DELIVERED chunk 4 (whole)** | The heading itself changed from "prototype" to "suite" between gradings — the same `packages/hydration/test/type-fixtures/` tree, now read as a standing, mutation-proof suite rather than a one-time proof-of-concept. **Correction: the spec's own text is stale on the database-backed positive exercise, and this round catches it.** It still reads as if the database-backed shape is exercised only by the negative fixtures needing its particular shape. `positive/every-chainable-db.ts` closes that — see the row immediately above for the fixture and the commit-message evidence (`583cde7ed`) |
| The sad paths, which no type catches | 1436 | Every listed run-time failure (a refused `api` connection; a 4xx/5xx with the body carried verbatim; a bad-shape `record` on either `api` or `write`; a `write` failure naming the path; a missing parent directory being CREATED; a `reach` throw naming from/to/gate; a mid-plan `filter` failure named distinctly from a zero match; a serial file-repo runner; discovery distinguishing "not built" from empty; params validation before seeding) must fail loudly and name what it was doing; a mid-batch seed failure halts the batch and marks the instance unusable; diagnosis reads the thrown value's SHAPE, never `instanceof`, because each test file gets its own copy of the built-in constructors; nothing inside the framework throws a transaction rollback — the target's own wrapper does | **DELIVERED chunk 4 (whole)** | The one item the last grading pass carried as unresolved — "who throws the transaction rollback," missing from `packages/hydration/CLAUDE.md` — is now written there directly: `## HydrationTransactionRolledBackError has no thrower in this package, by design` states the framework names neither files nor SQL, the class stays exported for a database-backed repo's own wrapper to throw, and names what driving it for real would need (a real database engine, a real constraint, a consumer's own wrapper) — none of which has an honest home in this repo, because dungeonmaster's state is files. The shape-not-`instanceof` diagnosis rule and the write-route bad-record rule are unchanged from the last grading pass that read `routeFailureTransformer`/`writeFailureTransformer` |
| Two kinds of probe, and neither substitutes for the other | 1484 | States that the type suite (fast, catches a malformed declaration and a bad call site) and a sad-path run (slow, catches a refused connection and a failed write) are two separate axes of coverage, and a green type suite proves nothing about the sad-path table | **DELIVERED chunk 3 (part)** | Unchanged this round |
| The combinatorial planning session, and what it works from | 1494 | A dedicated planning session (not a build-chunk step) works from Table 1 and Table 2 to find composition bugs the doc could not anticipate, writing every finding back into this document next to the verb it concerns rather than routing around it in a recipe | **PLANNED chunk 12** | Unchanged this round. Chunk 8's own findings (see Table 2's Notes below) are the mechanism working in miniature — a real build finding something the spec did not anticipate and writing it back in as a rule — but they came out of ordinary chunk planning, not out of a chunk-12 combinatorial session, and chunk 12 itself has not run |
| Table 1 — what an ingredient MUST have, and what it MAY have | 1504 | For each ingredient property, states whether it is required or optional, what its absence does, and what a wrong value does — the reference the combinatorial rounds check each property against | **PLANNED chunk 12** | Unchanged this round |
| Table 2 — the rounds, and the question each one asks | 1519 | Four rounds — A (one property at a time), B (nesting depth and scope), C (composing pairs/triples of chainables), D (driving the sad-path table for real) — each producing findings written into this document, never a recipe worked around a finding | **PLANNED chunk 12** | **Correction to a claim made elsewhere: no combinatorial round has been run.** A prior handoff document states a round has been run; it has not. `scrolls/seigelense/plans/recipes-chunk-12-combinatorial-rounds.md` states 65 distinct cases (A1–A17, B1–B8, C1–C19, D1–D21) and carries zero run or verification markings against any of them, confirmed by reading the file directly. The ledger and the chunk-12 plan were last written together in commit `583cde7ed` (`git show --stat` on that SHA touches both), and neither file has a commit against it since. **A second correction, precise rather than approximate: commits since `583cde7ed` are NOT "two proxies and a handoff."** `git log --stat 583cde7ed..HEAD` (twenty commits, merge included) shows five of them touch `scrolls/seigelense/recipes-HANDOFF.md` (one, `de0f3247d`, ALSO touches two `*-api-route-broker.proxy.ts` files — that commit alone is the "two proxies and a handoff" shape), but the rest are substantive: `8d4994c41` alone is 140 files — "the whole siegelense surface on the CLI, and the end of the MCP layer," the commit this ledger's own CLI-surface rows below are read against; `701990a2f`, `ac3827f8f`, `bfe269f4c`, `8c3354c6c` and `70de1f214` are further real-code fix commits (ingredient contracts, the CLI's start/kill cycle, run-execute-broker, the web queue-dir resolver); `c4cfab9d9` and `05a0ae6a7` add the chunk-4 and chunk-5 CLI plan documents; `c399480d7` and `71629e126` update `build-ledger.md`/`siegelense-tooling.md`; `b4ad398c5` rewrites the spec's own Part 6 and the chunk-9/10 migration plan. Whatever landed, none of it is a chunk-12 run — that fact stands regardless of how the intervening work is characterized, which is the one claim this row needs to carry plainly |
| Known gaps, named rather than discovered later | 1568 | Names the open gaps — an api-only plan's pre-flight, an untyped `fromSaved` field, no recipe-composition `include()`, no unexercised `recording` route proof, no duplicate-name registry check, a top-level orphan-link that compiles clean, production code minting uuids/timestamps that reach the screen, the `ban-primitives` eslint carve-out, a composed recipe spanning two stores, a package the recipes depend on being out of reach for conversion — that block nothing but cost more the later they are found | **DELIVERED chunk 7 (part) — two rows of the spec's own table are STALE, not open** | The spec's own gaps table marks several rows **CLOSED** by strikethrough, and each is confirmed built rather than merely marked: **`copies:` for an external producer** — `copies-target-contract.ts`, confirmed above. **The two lint rules — CLOSED, not merely built.** `ban-dom-handles-in-ingredients` and `ban-nondeterminism-in-ingredients` both exist under `packages/eslint-plugin/src/brokers/rule/`, each with a colocated RuleTester test, and both are registered at `'error'` severity in `config-dungeonmaster-broker.ts:141-142` (read directly this round, correcting an earlier misread in this same pass that reported zero matches there) and assembled into the published plugin's own `rules` object by `eslint-plugin-create-responder.ts:69,136,208`; `eslint.config.js` spreads that broker's output into the config array this repo's own lint actually runs. So "UNBUILT, no chunk owns either" is stale, and so is any reading of "built but not enforced" — the rules are live. **The duplicate-registry-name check and the nested-`filter`-scope check** — unchanged, still confirmed via the runner. **Still open, unchanged from the last grading pass:** a typed plan output (the plan's output stays an untyped record the caller casts), `recording` unexercised, no `include()` for recipe composition, production uuid/timestamp minting reaching the screen, `ban-primitives`'s carve-out. **Still open, confirmed this round:** a composed recipe spanning two stores — `packages/siegelense-recipes/CLAUDE.md`'s own "A `DmTarget` alone does not isolate a `write` route" section names it directly (`guildWriteRouteBroker`/`operationWriteRouteBroker` resolve home off the global `DUNGEONMASTER_HOME`, `questWriteRouteBroker` off `target.home`) — and the package-dependency gap, see the dedicated row below. **The second stale row, corrected plainly: "A row added at TOP LEVEL whose `links` nothing supplies compiles clean … the RUNNER must refuse it before the first write" is no longer true — the runner already does.** `planPreflightBroker`'s LINKS check (`plan-preflight-broker.ts:133-163`) walks every `create` op's `ancestors` (empty for a top-level `add`) against its ingredient's `links` and throws `HydrationUnlinkedRowError` when none supplies a required link; `plan-preflight-broker.test.ts:154-188` tests exactly the top-level case by name, and the thrown message itself reads *"including a row added at the top level, which the type system allows freely"* — the identical phrase the spec's own gaps-table row uses to describe the problem. The TYPE-level half of the claim stays true (`dm.quests.add(1, …)` at top level still compiles); only the "the RUNNER must refuse it" half is stale — it already does, and this ledger's own "Mechanics the framework has to implement" row already carries this check as delivered, so the gaps table and the mechanics section now disagree with each other. **Two further gaps this round found that are absent from the spec's own table entirely** (not merely undated) — see "Every chainable, with an example" above for both: "no chainable reaches a row that already exists" is stated in the spec's OWN prose elsewhere but never carried into this table, and `.under()` failing to forward ancestor names for child accessors is named only in `packages/siegelense-recipes/CLAUDE.md`, nowhere in the spec |
| Mechanics the framework has to implement | 1593 | The runner walks a plan (a tree of `create`/`set`/`remove`/`saveRecord`/`filter`/`extra` ops) depth-first in declaration order, never crosses the MCP wire itself, implements no undo, splits refusals into pre-flight (an unservable route, an unresolvable/top-level `fromSaved`, an unfilled `links`, a chain call needing an undeclared route, a `set` asking for an undeclared transition value) versus mid-run (a `filter` under-matching, a route throwing); every error class carries the ingredient and the recipe name | **DELIVERED chunk 4 (whole)** | The runner is now built and run against real files, not only proven by the type/pre-flight machinery: the commit introducing this repo's own ingredients (`583cde7ed`, "the runner, this repo's ingredients, and what running them found") states directly that every apply layer is wired — create, set, setRaw, update, remove, saveRecord, extra and filter — a plan writes real files to a real directory, creates missing parent directories per the sad-path table, and is proven SERIAL by mutation rather than by reading. "Never crosses the MCP wire" now also holds trivially, since the tool calling it has no MCP wire at all (see the header note) |
| How siegelense finds recipes without importing them | 1657 | `siegelense` lists recipes by globbing `packages/hydration-recipes/dist/index.js`, dynamically importing it, and reading its exported manifest — reading compiled output only, and distinguishing an absent directory (installation problem) from an empty one (no recipes yet) | **DELIVERED chunk 8 (part)** | The shared CONVENTION both sides read is built and tested: `recipesConventionStatics` (`packages/shared/src/statics/recipes-convention/recipes-convention-statics.ts:17-30`) pins the package directory name, the dist-relative entry path and the three export names, `toStrictEqual`-tested against the whole object. The actual LOCATE/READ mechanism chunk 8's plan calls C2/C3 (`recipesLocateBroker`, `recipesReadBroker`) is not on disk — confirmed absent by glob against `packages/siegelense-recipes/src/brokers/**`, which lists five ingredients' worth of route brokers and no `recipes-listing` or `recipes-seed` folder. So the convention that would let this mechanism distinguish an absent package from an empty one from an unbuilt one exists; the code that walks it does not yet |
| Where each part lands in this repo's architecture | 1680 | Maps each piece to a folder type and package — `IngredientConfig`/`Plan`/`Op`/`Target`/the manifest as `hydration` contracts, the chain builder as `hydration` transformers, `createHydration`/`ingredient`/`registry`/`recipe`/the runner as `hydration` brokers, route helpers as `hydration` adapters, refusal errors as `hydration` errors, recipe discovery/listing and the `seed` step as `siegelense` brokers, the recipes-package scaffolder as `siegelense` (already built), each ingredient/recipe as its own folder in `hydration-recipes` | **DELIVERED chunk 8 (part)** | The `hydration`-side mapping is confirmed built exactly as stated (contracts, transformers, brokers, adapters, errors, each under its named folder type). The scaffolder (`InstallRecipesScaffoldResponder`) is confirmed pre-existing, unchanged. **Not yet built:** recipe discovery/listing and the `seed` step as `siegelense` brokers — see the two rows immediately above and "Steps that are new" below |
| The migration IS the validation, not cleanup afterwards | 1702 | Converting the real suites (Playwright e2e specs seeding domain state, convertible integration domain-seeders) to the recipe framework is in scope and is how the design gets proven, run via `python3 scrolls/tools/seed-census.py` for the target list and the running call-site count, in the order integration-then-e2e-then-one-manual-siegelense-round | **PLANNED chunk 9-10 (part)** | `scrolls/tools/seed-census.py` exists (confirmed on disk). Conversion has genuinely started, not merely planned: `packages/server/test/harnesses/server-app/server-app.harness.ts` imports `dmRegistryBroker` and `recipesHydrationCreateBroker` off `@dungeonmaster/siegelense-recipes/brokers`, and the commit that landed it (`583cde7ed`) states "the first conversion landed: 78 assertion statements and 33 test names byte-identical, verified by extraction and diff" — this is the one integration target outside `orchestrator` the dependency-cycle finding below leaves reachable. The e2e half (step 2, the larger half) has not started |
| The order, and why each step proves something the last one could not | 1772 | Step 1 (integration domain-seeders, files only) proves write routes and the no-`baseUrl` case; step 2 (e2e specs) proves api routes and one plan serving both callers; step 3 (one manual siegelense round) proves `seed`, `recipes {}` and params validation, which neither suite crosses | **PLANNED chunk 9-10 (part)** | Same evidence as the row above: step 1 has begun on its one reachable target (`server-app.harness.ts`). Step 3 cannot start before chunk 8's remaining work (`seed`, `recipes {}`) exists as a CLI surface — see "Steps that are new" below |
| The rule that makes the conversion a proof | 1790 | A converted test keeps its assertions exactly, changing only its setup; a test needing a different assertion means the ingredient is wrong; a test that cannot convert is a finding written into this document; the one integration test whose SUBJECT is a `copies:` target (`quest-hydrate-broker.integration.test.ts`) is excluded, while tests that merely use that broker as setup are ordinary conversion targets | **PLANNED chunk 9-10 (part)** | The exclusion rule's premise is confirmed structurally: `packages/siegelense-recipes/CLAUDE.md` states the quest ingredient's `write` route is `questPersistBroker`, not `questHydrateBroker` — the latter is what `reach` calls to seed the relay on a walk to `in_progress`, matching the spec's own reasoning for the exclusion exactly |
| A package the recipes depend on is out of reach for this conversion | 1832 | `siegelense-recipes` depends on `@dungeonmaster/orchestrator`, so `orchestrator` cannot depend back without a real dependency cycle; most integration conversion targets sit inside `orchestrator` and are descoped for exactly this reason; duplicating ingredients into `orchestrator` to dodge the cycle is rejected outright | **PLANNED chunk 9 (part)** | **The spec states this more confidently than the plan behind it can support, and that is worth flagging.** The spec's own text (lines 1836-1838) reads: "that would be a real dependency cycle, not a hypothetical one: under Jest's own `--conditions=source` resolution the cycle breaks at module-evaluation time, not merely at install time" — stated as settled fact. But `recipes-chunk-09-10-migration.md`'s own G0-b section reads: "The orchestrator half is settled by **descoping, not by spiking**… **Q9-4 is closed; do not re-open it**" — i.e. the module-evaluation-time claim was never actually run as a spike; the plan deliberately chose to decide the question by ruling rather than by measuring it. The descoping decision itself is sound and real (`packages/web/package.json:36` and `packages/server/package.json:33` both declare `@dungeonmaster/siegelense-recipes` as a devDependency; `@dungeonmaster/server` is out of `siegelense-recipes`'s own `dependencies`), but the specific "not a hypothetical" phrasing is an assertion, not a confirmed run |
| An ingredient touches STATE, never a screen | 1858 | An ingredient never holds a DOM handle; enforcement is layered — a runtime guard as the layer that actually protects, a durable step-shape type structurally omitting `ref`, and a lint rule in the published `@dungeonmaster/eslint-plugin` flagging any DOM mention in an ingredient file | **DELIVERED chunk 7 (part)** | `ruleBanDomHandlesInIngredientsBroker` (`packages/eslint-plugin/src/brokers/rule/ban-dom-handles-in-ingredients/`) exists with a colocated test, flags `page.locator(...)`, `getBoundingClientRect()`, `useRef()`, JSX and a `react`/`playwright` import inside an ingredient declaration file, backed by a `dom-handle-watchlist` static and an `is-ingredient-declaration-file`/`is-ingredient-declaration-call` guard pair — closing what the last grading pass carried as unbuilt. **Correcting the same misread the two rows above carried: this rule is also registered and live**, not merely written — `config-dungeonmaster-broker.ts:141` sets `'@dungeonmaster/ban-dom-handles-in-ingredients': 'error'`, and `eslint-plugin-create-responder.ts:68,135,207` assembles it into the plugin's exported rules under that key, reached the same way as `ban-nondeterminism-in-ingredients` above. Both of the two lint rules this heading and "Determinism is structural" depend on now enforce for real |
| Some claims can only be asserted in a BROWSER | 1901 | Ingredient tests come in three costs — files-only, a shared server instance, or a full server-plus-browser instance for a claim about what a URL renders — and a browser-asserted ingredient test is deliberately rare because the prelude's own `VERIFIED` run already proves rendering | NOT STARTED | Unchanged this round — this repo's five ingredients all assert at the files or server-response cost; none needs a browser |
| What this is, in industry terms | 1925 | Frames this as a test-data factory library, keeping sequence/trait/lint/explicit-dependencies from the factory family while refusing random data, fluent builders as the top-level form, static fixtures, and object-mother scenario naming; names the Cucumber failure mode it guards against | NOT STARTED | Unchanged this round — narrative framing, not a build artifact |
| Guidance for other repos | 1976 | For a consumer repo, only `@dungeonmaster/hydration`, the empty scaffolded `packages/hydration-recipes/`, and five stated conventions travel — not this repo's own recipes | **DELIVERED (pre-existing) (part)** | The mechanics this guidance describes are real and shipped — `@dungeonmaster/hydration` ships, `InstallRecipesScaffoldResponder` scaffolds the empty package in a consumer repo — but the guidance PROSE itself is not something a build delivers; kept as a narrative row, now cross-referenced against the code that makes it true rather than left `NOT STARTED` with no pointer |

## Part 6 — The surface, consolidated (line 1993)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Each package needs a `../../CLAUDE.md`, and these are the entries | 1999 | Splits a set of named invariants across `siegelense`'s, `hydration`'s and `hydration-recipes`'s own `CLAUDE.md` files, by which package breaks if the rule breaks | **DELIVERED chunk 7 (part)** | `packages/hydration/CLAUDE.md` carries essentially every entry this row names for that package: the handles-not-records rule, the object-argument rule with `all` as a second builder argument, the one-verb-sets-state rule, the route-lives-on-the-ingredient rule, `copies:`, determinism, plan-as-data — each as its own heading. `packages/siegelense-recipes/CLAUDE.md` carries the recipes-package-specific entries (which ingredients exist, what each `copies:`, the root-dependencies test). **One entry is still wrong, not yet fixed:** `packages/siegelense/CLAUDE.md` still reads "A recipe touches state, never a screen" — chunk 8's own §6 F-findings section (D4) already names this as backwards, since Part 6 assigns the rule to `hydration` and it is about an INGREDIENT, not a recipe; the wording has not been corrected |
| The calls this document uses | 2033 | `recipes {}` lists states with no instance needed, printing exactly the fields Part 5's "The listing `recipes {}` prints" specifies; `docs { for: … }` serves the tool's own instructions across seven scopes | **PLANNED chunk 8 (part)** | The spec's own text now states the call surface as thirteen names pinned by `siegelense-call-statics.ts` — `start, run, results, kill, capacity, profile, status, cleanup, prune, compare, snapshots, recipes, docs` — with no registration and no prefix, matching what `siegelense-flow.ts` actually does for the seven calls it HAS built (`start, run, results, kill, status, cleanup, compare`; `driver` sits outside the named surface by design). `recipes` is not among `CALL_ROUTES`' seven built entries and is not yet reachable — see "The listing `recipes {}` prints" above. `docs` is out of this ledger's scope per the header note |
| Steps that are new | 2080 | A `seed` step runs a named recipe's plan against the current instance and returns the ids it made under `as:`; its `params` are typed by the `recipe` value in-process and validated by a generated zod schema before seeding over the CLI wire, naming the bad input and what that recipe takes; the return is that row's WHOLE record (no invented `url` field); a `seed` step must set `DUNGEONMASTER_HOME` to the lane's home for its own duration, because the CLI's driver process inherits the OPERATOR's environment, not the lane's | **PLANNED chunk 8 (part)** | This is where most of chunk 8's seven findings land, and each is confirmed WRITTEN INTO THE SPEC by direct read, distinct from being built in code: **F1** ("makes prints ingredient NAMES, not descriptions") — spec line ~647, already reflected in "The listing recipes {} prints" above. **F2** ("inputs cannot print the branded type, zod stores no trace of a brand at run time") and **F3** (this repo's own `session-with-nested-chain` prints `guildPath`, not the spec's example `guildId`) — spec lines 633-639. **F4** (the listing-probe requirement) — spec lines 649-658. **F5** (params validation splits between what siegelense refuses off the listing alone and what the recipe's own schema refuses) — spec lines 2094-2111 of this section. **F6** (the `DUNGEONMASTER_HOME` requirement beside `seed`, because the driver process inherits the operator's env) — spec lines 2113-2122 of this section. **F8** (the return is the row's whole record, no `url` field) — spec lines 2082-2092 of this section. **None of these findings' CODE is built.** `step-statics.ts` (`packages/siegelense/src/statics/step/`) has no `seed` entry in its verb list — confirmed absent by grep. No `stepSeedBroker`, `recipesSeedRunBroker` or `recipesListingBuildBroker` exists anywhere under `packages/siegelense{,-recipes}/src/brokers/`. So the design decisions are settled and written into the spec; the CLI-reachable `seed` step itself does not exist |
| A worked batch | 2136 | A batch step's output is addressed as three segments, `{<step>.<row>.<field>}` (never the two-segment `{step.field}`); `stopOn: 'error'` is the default; a step declaring `expect: 'error'` that unexpectedly SUCCEEDS itself halts the batch as a finding; a plan's output is FLAT, carrying every `saveRecordAs`'d row under its own name | **PLANNED chunk 8 (part)** | Depends on `seed` existing as a step at all (see the row above) and on the reference grammar chunk 8's plan calls E1 (`stepRefContract` under `packages/siegelense/src/contracts/step-ref/`), confirmed absent by glob. `stopOn` and `expect: 'error'` are pre-existing `run` mechanics, unaffected by this gap |
| Interleaving recipes and steps | 2195 | `seed` is placeable anywhere in a batch, not only as a prologue; a later `seed` in the same batch can take `params` from an earlier step's saved output; seeding while a page is already open is how a socket-driven live-update path gets exercised | **PLANNED chunk 8 (part)** | Same dependency as the row above — `seed` and the reference grammar (`{g.guild.id}`-style substitution) are both design-only. The spec's own interleaving example already reads with the real recipe's actual input (`quest-advances-one-step` seeding a fresh quest rather than reaching into one an earlier step made), which chunk-8's own §6 note (`b4ad398c5`'s commit message) states was fixed in the same pass that rewrote Part 6 |

---

## Sections read and deliberately given no row

Per the standing instruction, these are directions to the build rather than requirements the build must
satisfy, so they carry no row above:

- `## The prompt that builds this` (line 10) and its subsections `### What this doc asks for, and what it
  does not` (88) and `### A suggested spine, for the first planner round` (108)
- `### This branch tracks master, and siegelense is already in it` (137) and its subsections `#### What
  that leaves to schedule` (156), `#### When to merge` (172), `#### The rename is its own step, and it is
  bigger than the folder` (205)

`docs { for: … }`'s scope content and the rest of the thirteen-call CLI surface (`start`, `run`, `results`,
`kill`, `capacity`, `profile`, `status`, `cleanup`, `prune`, `compare`, `snapshots`, `docs`) belong to
`siegelense-tooling.md` and are tracked in `build-ledger.md`. This ledger only adds the `recipes` call and
the `seed` step to that surface, both of which have rows above.
