# Siegelense recipes ledger

Running coverage of `siegelense-recipes.md` against what has been planned and delivered. **One row per
heading under Part 3A, Part 5 and Part 6**, keyed by heading text and its line number in the CURRENT file.
Every round updates this file.

**Line numbers shift whenever anyone edits the spec.** Re-derive them from heading text before trusting a
number in this table — never carry one forward from an older copy of this ledger.

Status vocabulary, carried over from `build-ledger.md` (the sibling ledger for `siegelense-tooling.md`),
plus one addition this ledger needed that that one did not:

| Status | Means |
|---|---|
| `NOT STARTED` | nothing plans it yet |
| `PLANNED chunk N` | a chunk plan under `plans/` covers it, wholly |
| `PLANNED chunk N (part)` | a chunk plan covers some of it; Notes says which part |
| `DELIVERED chunk N` | chunk N built it, wholly, and the code was read against this section's own words |
| `DELIVERED chunk N (part)` | chunk N built some of it; Notes says exactly which part landed and which stays open |
| `BLOCKED chunk N` | the code exists and is believed sound but could not be RUN to confirm; Notes names the blocker. Never read as tested |
| `DELIVERED (pre-existing)` | the code already exists and was read this round, but it was built before this ledger's own chunk numbering began — by the siegelense-tooling build, not a chunk plan under `scrolls/seigelense/plans/` for this document. Add `(part)` on the same terms as the chunk-numbered statuses |

**Two chunk plans exist for the framework's types and its declaration/chain surface** —
`plans/recipes-chunk-01-03-framework-types.md` (chunks 1–3) and `plans/recipes-chunk-04-06-runner.md`
(chunks 4, 5, 6 and 6b). A third — `plans/recipes-chunk-07-repo-ingredients.md` — plans this repo's own
ingredients. A fourth — `plans/recipes-chunk-12-combinatorial-rounds.md` — plans the combinatorial
session. A fifth — `plans/recipes-chunk-09-10-migration.md` — plans the suite conversion, chunks 9 and 10.
Each plan's own amendments section records where the plan was wrong and what shipped instead; a
row below reads against what shipped, not against the plan's original text, wherever the two disagree.

**This round is a documentation round, not a grading pass.** It reads four findings out of the chunk 4–6
and chunk 7 plans' own amendment sections and writes each one into `siegelense-recipes.md` as a rule,
next to the verb it concerns — no TypeScript changed, no test ran, no ward run happened. **Every Status
below is carried forward unchanged from the last grading pass**, or set to `NOT STARTED`/`PLANNED chunk N`
for a brand-new row this round's edits created, on the same conservative terms: a status of `DELIVERED`
is earned only by a round that reads the code, and this one did not. Every Line number below was
re-derived by reading the current file fresh, independent of whatever a prior copy of this ledger said —
several had already drifted by a few lines before this round touched anything, from edits between
gradings.

**A row reading `DELIVERED (pre-existing)` is work the siegelense-tooling build already did**, unrelated
to any chunk plan, and stays as it was unless a Note says this round touched it.

**What this ledger does not cover.** Per the standing instruction: the front matter about the prompt that
builds this document, the suggested spine, and the branch-tracking instructions ("This branch tracks
master…" through "The rename is its own step…") are directions to the build, not requirements of it, and
carry no row. `docs { for: … }`'s seven scopes and the rest of the thirteen-call surface belong to
`siegelense-tooling.md` and are tracked in `build-ledger.md`, not here — this document only adds the
`recipes` call and the `seed` step to that surface.

---

## Part 3A — The decision index (line 230)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Recipes: the three packages, the three tiers, and what holds them | 232 | Serves as the condensed decision index for every rule Part 5 states in full — three packages with import/publish boundaries, three verb tiers, the handle/chain mechanics, one state-setting verb, `filter`'s zero-match throw, link-by-name typing, route/`copies:` rules, determinism rules, the plan-as-data rule, the ingredient-test/correctness-vs-fitness split, and the migration rules — with Part 5 winning wherever the two disagree | **DELIVERED chunk 3 (part)** | Unchanged this round — no edit landed inside Part 3A itself. See the Part 5 rows below for the checkable detail behind each condensed line, including the four rows this round's findings touched: `copies:`'s external-tool gap, the foreign-key-in-an-array limit, the transaction-rollback thrower, and the pre-flight/mid-run split |

## Part 5 — The recipe book (line 281)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Four words this document uses, and two rules that outlive the split | 291 | Defines instance/run/recipe/prelude precisely, and states that a recipe returns a plan runnable in-process with no MCP boundary, and that one row must make "the right one" and "the first one" the same value (the two-of-anything rule via `defaults(index)`) | **DELIVERED chunk 3 (part)** | Unchanged this round. Last graded: the two-of-anything rule and `defaults(index)` proven on VALUES by `collectionChainTransformer`'s, `ingredientDeclareBroker`'s and `registryCreateBroker`'s own tests. `run(plan, target)` still not confirmed built as of that grading |
| The problem | 314 | Names the four separate seeding vocabularies (e2e harnesses, hand-rolled `fs` in Jest integration tests, `questHydrateBroker` + blueprints for smoketests, a throwaway walk script) that one recipe book must replace with a single shared spelling | NOT STARTED | A narrative problem statement, not a build artifact. Unchanged this round |
| Three packages, and what may cross between them | 343 | Three packages — `@dungeonmaster/siegelense` (ships), `@dungeonmaster/hydration` (ships, repo-agnostic), `packages/hydration-recipes` (does not ship, absent from root `package.json` `dependencies` with a colocated test pinning that) — where `siegelense` discovers recipes at run time and never imports the recipes package | **DELIVERED chunk 3 (part)** | Unchanged this round. Last graded: the absent-from-root-dependencies rule delivered; `siegelense` never importing `hydration-recipes` still unverifiable either way, no `packages/siegelense` file touched as of that grading |
| Three tiers, and only the middle one is written per entity | 370 | Structural verbs (`add`, `filter`, `set`, `remove`, `saveRecordAs`) live once in the framework; capability verbs (like a `transitions`-driven `set`) are declared once and typed per ingredient, promoted only on the second ingredient that wants one; extras are per-ingredient verbs nothing else gets | **DELIVERED chunk 3 (part)** | Unchanged this round |
| Every property on an ingredient, and what goes in it | 389 | An `ingredient()` config accepts required `name`/`description`/`fields`/`record`/`routes` and optional `links`/`transitions`/`defaults`/`copies` (required with a `write` route)/`extras`, exactly as the reference table and the worked `questIngredient` example show | **DELIVERED chunk 2 (whole)** | Unchanged this round |
| Every property on an ingredient, and what goes in it — a link's `from` | 449 | A `links` entry gains an optional `from`, naming the field on the PARENT record that carries its id, defaulting to `'id'` — needed wherever a parent's own id field is not called `id`, as the session record is | **DELIVERED chunk 3 (part)** | Unchanged this round |
| Every property on an ingredient, and what goes in it — `reach` receives the `record` | 468 | `reach` receives the `record` being walked, alongside `from`, `to` and `target`, because `add(3, …)` mints three rows and nothing else tells `reach` which one to walk | **DELIVERED chunk 3 (part)** | Unchanged this round. Last graded: the type-level requirement lands (`ReachFn` requires `record`); the runtime backstop and the runner passing the real record were both still open as of that grading |
| **[NEW]** Every property on an ingredient, and what goes in it — a route cannot always call what `copies:` names | 521 | This round's finding: a package's own `exports` map decides what crosses into another package, so a production broker with no export entry is invisible even to a route in the same repo. The route then imitates the code instead of calling it, which `copies:` names honestly either way | **PLANNED chunk 7 (part)** | New this round, written beside `copies:`. Chunk 7's own §11 Q7-5 already names one concrete instance of this tension — whether a `.harness.ts` in `siegelense-recipes` may import `@dungeonmaster/server`'s `flows` directly — and recommends "try the Hono sub-app first; fall back to the responders" without a ward run confirming which the architecture allows. This row generalises that into the rule; no round has confirmed it against a real `npm run ward -- --only lint` yet |
| Every property on an ingredient, and what goes in it — `extras` is `{ args, apply }` | 527 | An `extras` entry is one object, `{ args, apply }` — never a bare arg contract — so a verb cannot be declared with arguments and no body to run | **DELIVERED chunk 1** | Unchanged this round |
| What a recipe declares | 573 | `recipe({ name, description, inputs? }, builder)` takes a required name, a one-line present-tense description of the end state that `recipes {}` prints, an optional `inputs` zod schema for what the recipe needs from an earlier step, and a builder taking the recipe's typed inputs (if any) and returning ops | **DELIVERED chunk 3 (whole)** | Unchanged this round |
| The listing `recipes {}` prints | 599 | `recipes {}` prints, per recipe, its name, description, `inputs` (off the static input contract, now a zod schema), `runs` (`serverless` only when every ingredient in the plan has a write route, else `needs a server: <ingredient>`), and `makes` (each ingredient's description counted off the plan, `varies` wherever a filter or transition decides the count) | **DELIVERED chunk 3 (part)** | Unchanged this round |
| The listing `recipes {}` prints — a transition's minted INGREDIENT, not just its count | 627 | A plan whose only reference to an ingredient is a transition, with no `filter` naming it, cannot appear in `makes` at all — not even as `varies` — because `reach` is opaque and nothing in a plan ties a transition to the child ingredient it mints | NOT STARTED | Checked this round against the finding asking to "sharpen" this exact passage: the text already states the sharpened claim in full — "The INGREDIENT cannot be known either, wherever nothing else in the plan names it" — so no edit landed here. `planMakesTransformer` still does not distinguish the two cases as of the last code grading |
| Linking a child to its parent | 637 | A `links` entry names its parent by NAME (never a reference, to avoid the `TS7022` inference cycle); the registry inverts names into accessor keys; the runner walks the plan depth-first, writing each ancestor's id into the named field; a child accessor appears only on a host both named in that child's `links` and satisfying every one of them; and a `fromSaved` naming a record declared later is refused by the runner's PRE-FLIGHT, before the first write, not by the chain as it builds | **DELIVERED chunk 3 (part)** | Unchanged this round |
| **[NEW]** Linking a child to its parent — an explicit field beats an ancestor-derived link | 666 | A `links` entry that names a field already present on a row's own `fields` is skipped by the runner rather than overwritten — this is the mechanism that makes `under()`'s caller-supplied id outrank an ancestor's | **DELIVERED chunk 4** | New row, this round's combinatorial finding. `linkValuesTransformer` (`link-values-transformer.ts:40-43`) reads `if (link.as in ownFields) { continue; }` before ever resolving an ancestor, and its own header already states the rule in these terms; the runner plan names the same fact as its own D5. Read directly this round |
| Linking a child to its parent — a foreign key inside an array is not expressible | 707 | The mechanism covers one field on the child pointing at one field on the parent, and nothing narrower — a foreign key shaped like a work item's `relatedDataItems: ['operations/<id>']` (a prefixed string inside an array) cannot be written by a `links` entry or reached by `fromSaved`, so that entity cannot be an ingredient at all | NOT STARTED | **This round adds the confirmation**: building this repo's own ingredients confirmed the limit against a real entity rather than leaving it a hypothetical — `workItem` is exactly this shape and has no ingredient, its rows staying a field on `quest`'s own `fields`. Still documentation-only, matching chunk 7 plan's Q7-1, which keeps `workItems` a field on `quest` rather than promoting it to its own ingredient |
| The chain: every call names what it affects | 719 | `add(n, build)` hands its builder a fixed-length tuple of handles plus `all` as a second argument, so every subsequent call is made on a handle rather than an implicit current level | **DELIVERED chunk 3 (whole)** | Unchanged this round |
| One verb sets state, and the INGREDIENT decides whether that means a walk | 753 | `set()` is the only verb that assigns a value; the ingredient routes a plain field to a plain write and a field named under `transitions` to a walk through `reach`; `setRaw()` writes a transition field with no walk at all | **DELIVERED chunk 3 (part)** | Unchanged this round |
| **[NEW]** One verb sets state, and the INGREDIENT decides whether that means a walk — a broadcast handle multiplies a transition's cost | 785 | `all.set({ … })` on a transition field is not one write shared across a set of rows — it is one full gate walk per row, so the cost of a broadcast `set` on a transition scales with the row count exactly as a per-row `set` would, with nothing in the call's shape distinguishing it from the cheap, plain-field case | **DELIVERED chunk 3 (part)** | New row, this round's combinatorial finding. `collectionChainTransformer`'s `all.set` (`collection-chain-transformer.ts:147-156`) maps `opSetTransformer` over every ref in the `add`, each op carrying the ingredient's own `transitions` unchanged, so the runner (chunk 5) walks each one independently. Read directly this round; no existing test asserts the row-count-to-walk-count ratio by name |
| Every verb but `add` takes an object | 792 | Every chainable except `add` (which keeps a bare count) takes exactly one object argument, so a growing verb adds a key rather than a new parameter | **DELIVERED chunk 1** | Unchanged this round |
| `filter` selects rows that only exist at RUN time | 806 | `filter({ where, expect })` returns a set with no index access and no `add`; `where` is a typed match object, never a closure; `expect` defaults to `'some'` and a zero match THROWS by default, mirroring the tool's own no-pick rule; and a `filter` nested inside an `add` is scoped to its immediate host, never the whole instance | **DELIVERED chunk 3 (part)** | Unchanged this round |
| A recipe takes typed inputs, so it can stack on what an EARLIER STEP made | 845 | A recipe needing a value from an earlier seed step declares a typed input parameter as a zod schema under `inputs`, and a batch supplies it through a `params` object that never flattens onto the step's own keys | **DELIVERED chunk 3 (part)** | Unchanged this round |
| Every chainable, with an example | 890 | Every chainable — `add`, `set`, `setRaw`, `filter`, `remove`, `saveRecordAs`, `fromSaved`, `under`, and an ingredient's own extra — exists exactly as shown, and every snippet shown is copied out of `proto/usage.ts`, which the type check compiles | **DELIVERED chunk 3 (part)** | Unchanged this round |
| **[NEW]** Every chainable, with an example — `defaults` overwrites an id `under()` supplies | 985 | An ingredient's `defaults(index)` is spread AFTER `under()`'s supplied values onto a row's fields, so a `defaults` naming the same field as `under()` silently wins over what the caller passed in | **DELIVERED chunk 3** | New row, this round's combinatorial finding. `collectionChainTransformer`'s `create` op fields read `{ ...(underValues ?? {}), ...(ingredientConfig.defaults?.(index) ?? {}) }` (`collection-chain-transformer.ts:115-118`) — `under` first, `defaults` second, so `defaults` overwrites on a shared key. Read directly this round; the ordering is exercised by the transformer's own tests but not asserted BY NAME as a precedence rule |
| **[NEW]** Every chainable, with an example — `saveRecordAs` saves a snapshot, not a live view | 1003 | A row's saved record is captured once, at the moment the row is CREATED, and nothing later re-reads it — a sibling create appending to the same underlying row, or a later `set` on the same row, changes what is on disk without changing what the plan already saved. A value written after the row was saved must be read back from the target, never taken off the plan's output | **DELIVERED chunk 4** | New row, this round's ruling on when the record `saveRecordAs` captures is taken. `op-create-apply-layer-broker.ts:112`'s `state.records.set(op.ref, parsedRecord.data)` runs once, at create, and nothing in the runner updates it afterward — already documented directly in `packages/siegelense-recipes/CLAUDE.md` ("`saveRecordAs` freezes a row's record at CREATE time — it is not a live read"), and confirmed there against `quest-advances-one-step-recipe-broker.integration.test.ts`, which reads its quest's operation ledger off the real file on disk rather than off the saved record for exactly this reason. This round writes the general rule into the spec; it re-reads no code beyond that `CLAUDE.md` and the cited broker line |
| **[NEW]** Every chainable, with an example — a saved name is last-wins, silently | 1019 | A saved name is a KEY: two rows saved under one name — via `all.saveRecordAs` on a broadcast handle, or two calls naming the same string by hand — leave exactly one entry in the plan's output, and it is whichever row saved LAST, with nothing refusing either case | **DELIVERED chunk 4 (part)** | New row, this round's combinatorial finding. `opSaveRecordApplyLayerBroker`'s `state.saved.set(op.name, state.records.get(op.ref))` is a plain `Map.set`, so a repeat name always overwrites; `collectionChainTransformer`'s `all.saveRecordAs` (chunk 3) is what lets one call mint N ops sharing one name. Read directly this round; no test in either package pins the last-wins outcome by name |
| A plan is data, and one plan runs three ways | 1070 | `recipe()` returns pure data at build time; the same plan runs via `run(plan, { home, baseUrl })` from a walk or e2e spec, and via `run(plan, { home })` (write routes only) from a Jest integration test, with a targetless caller restricted to write-route ingredients by the types | **DELIVERED chunk 3 (part)** | Unchanged this round |
| Determinism is structural, not a rule to remember | 1094 | The chain owns each row's index and hands it to `defaults(i)`, so no ingredient ever calls `Date.now()`, `Math.random()`, or `crypto.randomUUID()` for anything reaching a screen, and the same plan run twice produces identical bytes | **DELIVERED chunk 3 (part)** | Unchanged this round |
| Routes: how an ingredient makes its state | 1108 | An ingredient declares one or more of `api` (the real code path), `write` (direct storage matching what the app would write, requiring `copies:`), or `recording` (replays a captured run); the TARGET, not the recipe, picks which route runs when more than one is declared; optionally `query`, `update` and `remove` for a chain call that reaches a row the create route did not just make, refused in the pre-flight when the ingredient declares none | **DELIVERED chunk 3 (part)** | Unchanged this round |
| Routes: how an ingredient makes its state — `query`, `update` and `remove` | 1131 | `routes` gains three optional entries — `query`, `update` and `remove` — so `remove()`, `filter()` and an unfoldable `set` each have a route to call; a plan calling a verb whose ingredient does not declare the matching route is refused in the pre-flight, naming the ingredient and the verb, via a dedicated error class | **DELIVERED chunk 3 (part)** | Unchanged this round |
| An ingredient for a FILE, and an ingredient for a DATABASE ROW | 1148 | The framework is generic over its own target type via `createHydration<T>()` (a file-backed home, or a database-backed transaction), proven by the prototype's `ingredients.ts` (file) and `db.ts` (database) declaring identical chain mechanics over different targets | **DELIVERED chunk 3 (whole)** | Unchanged this round |
| An ingredient with only one route, and what it costs | 1243 | A plan runs serverless only when EVERY ingredient in it declares a write route (an ALL, not an ANY); the listing reports `runs serverless` or names the first ingredient lacking one; the runner refuses that mismatch before seeding anything, naming the ingredient and the missing route | **DELIVERED chunk 3 (part)** | Unchanged this round |
| Every ingredient carries a test, and a two-route ingredient tests its routes against each other | 1281 | Every ingredient has a colocated integration test asserting what it made; an ingredient declaring both `api` and `write` runs both and compares them rather than snapshotting either alone; every `api`-route test in a suite shares one booted instance | NOT STARTED | Unchanged this round. Chunk 7 needs a real ingredient to test, and last grading found none wired to `ingredient()` |
| What the type prototype proved, and what it changed | 1314 | The committed prototype at `proto/` (eight TypeScript files under `strict`, compiled by `node proto/check.mjs`, with `@ts-expect-error` on every case that must fail) proves each design rule the doc states — link-by-name to avoid `TS7022`, `all` as a second builder argument, the immediate-parent-plus-satisfied-links accessor rule, a generic `Target` type — and its ten malformed-declaration probe found nine compile-time-catchable mistakes plus one (duplicate names in a registry) needing a runtime check | **DELIVERED chunk 3 (part)** | Unchanged this round |
| The sad paths, which no type catches | 1403 | Every listed run-time failure (a refused `api` connection; a 4xx/5xx with the body carried verbatim; a 2xx shape `record` rejects on `api` or `write`; a `write` failure naming the path; an absent parent directory being CREATED rather than failing; a `reach` throw naming from/to/gate; a mid-plan `filter` failure named distinctly from a zero match; a serial file-repo runner; discovery reporting "not built" rather than an empty list; and params validation before seeding) must fail loudly and name what it was doing, and a mid-batch seed failure halts the batch and marks the instance unusable rather than being retried | **DELIVERED chunk 1 (part)** | Unchanged this round |
| The sad paths, which no type catches — a `write` route's bad record | 1419 | A `write` route answering with a shape `record` rejects halts and names the field, exactly as a bad `api` response does, because a bad record poisons every saved record and every link the same way | **DELIVERED chunk 1 (part)** | Unchanged this round |
| The sad paths, which no type catches — who throws the transaction rollback | 1424 | Nothing inside the framework throws the transaction-rollback error; the repo's own wrapper around `run()` does, because the framework names neither files nor SQL and does not own the transaction — the target IS the transaction | **DELIVERED chunk 1 (part)** | Unchanged this round. `packages/hydration/CLAUDE.md` was, as of the last grading, still missing the line naming who throws it, against the chunk 4–6 plan's own Q6 ruling — still overdue, not touched by this round's markdown-only edits |
| **[NEW]** The sad paths, which no type catches — diagnosing a thrown value by shape, never by `instanceof` | 1429 | This round's finding: an error minted by the runtime's own machinery is not reliably recognised by `instanceof` across a test boundary, because each test file gets its own copy of the built-in constructors, so every classification reads shape (`code`, `path`, `status`) off the caught value instead | **DELIVERED chunk 4 (part)** | New row, but not new code: this documents a decision already graded delivered elsewhere in this ledger — `routeFailureTransformer` and `writeFailureTransformer` (chunk 4) already mine `{url,status,responseBody}`/`{path}` off an unknown throw rather than checking its class, per the "The sad paths, which no type catches" row above. This round only writes the general rule into the spec; it re-reads no code itself |
| Two kinds of probe, and neither substitutes for the other | 1451 | States that the type suite (fast, catches a malformed declaration and a bad call site) and a sad-path run (slow, catches a refused connection and a failed write) are two separate axes of coverage, and a green type suite proves nothing about the sad-path table | **DELIVERED chunk 3 (part)** | Unchanged this round |
| The combinatorial planning session, and what it works from | 1461 | A dedicated planning session (not a build-chunk step) works from Table 1 and Table 2 to find composition bugs the doc could not anticipate, writing every finding back into this document next to the verb it concerns rather than routing around it in a recipe | **PLANNED chunk 12** | Unchanged this round |
| Table 1 — what an ingredient MUST have, and what it MAY have | 1471 | For each ingredient property, states whether it is required or optional, what its absence does, and what a wrong value does — the reference the combinatorial rounds check each property against | **PLANNED chunk 12** | Unchanged this round |
| Table 2 — the rounds, and the question each one asks | 1486 | Four rounds — A (one property at a time), B (nesting depth and scope), C (composing pairs/triples of chainables, e.g. `filter` over rows a same-plan transition just minted), D (driving the sad-path table for real) — each producing findings written into this document, never a recipe worked around a finding | **PLANNED chunk 12** | This round's four findings are exactly this mechanism in action — Round D and Round C findings written back as rules rather than routed around in a recipe. No round has itself been run as of this ledger |
| Known gaps, named rather than discovered later | 1535 | Names the open gaps — an api-only plan's pre-flight, an untyped `fromSaved` field, no recipe-composition `include()`, the MCP wire's remaining wire-side validation, the unexercised `recording` route, no duplicate-name registry check, a top-level orphan-link that compiles clean, production code minting uuids/timestamps that reach the screen, and the `ban-primitives` eslint carve-out — that block nothing but cost more the later they are found | **DELIVERED chunk 3 (part)** | The "no sad path is implemented or tested" gap this row's Notes used to carry now has its own dedicated row below, since this round's finding narrowed it from a blanket gap to two named, structural exceptions. Every other item in this row is unchanged |
| Known gaps, named rather than discovered later — the new gap this round added | 1544 | A typed plan output is scheduled work, not an abandoned requirement: "A plan is data, and one plan runs three ways" and "Every chainable, with an example" both require `saveRecordAs`'d rows to reach the plan's output each typed to its own record contract, and today's plan returns an untyped record the caller casts | **PLANNED chunk 3 (part)** | Unchanged this round |
| Known gaps, named rather than discovered later — the recording/serverless divergence this round named | 1547 | The `runs` line's write-only rule stands even for a `recording`-only ingredient, which therefore reports `needs a server: <ingredient>` though replaying a capture needs none | NOT STARTED | Unchanged this round |
| Known gaps, named rather than discovered later — `copies:` has no target for an external tool's shape | 1548 | An ingredient copying a shape only an external tool writes — a Claude session transcript, written by the Claude CLI and only ever read in-repo — cannot honestly declare a `write` route until `copies:`'s definition is extended or an alternative is ruled | NOT STARTED | **This round sharpens the consequence**: building this repo's own ingredients confirmed it blocks the session AND sub-agent ingredients from being declared at all — every route broker behind them exists and is tested, per chunk 7 plan's own §0b finding and Q7-2; only the declaration itself is blocked, and those two ingredients back a large share of the conversion's call sites |
| Known gaps, named rather than discovered later — two lint rules are UNBUILT | 1549 | Neither the no-DOM-handle rule nor the no-clock/no-random-source rule exists, so nothing enforces either constraint today; and an eslint config entry naming a rule that does not yet exist is a FATAL config error, not a no-op, so the entry must land WITH the rule, never before it | **DELIVERED chunk 7 (part)** | Unchanged this round |
| **[NEW]** Known gaps, named rather than discovered later — the sad-path table, now mostly closed | 1552 | Most rows of the sad-path table are driven against real conditions — a genuinely refused socket, a real server answering an error with a real body, a real denied write, a real race on one file — rather than remaining spec-only. Two rows cannot be driven in this repo for structural reasons: a gate refusing a transition (the framework owns no gates by design; only the recipes package can drive it for real), and a transaction rolling back (needs a real database engine, a real constraint, and a consumer's own transaction wrapper, none of which has an honest home in a repo whose state is files) | **PLANNED chunk 6** | New row, replacing the blanket "no sad path implemented" claim the main Known-gaps row used to carry. `recipes-chunk-04-06-runner.md` §2 already assigns every row of the sad-path table an owner file and a named test, including the two exceptions (row 10/11, explicitly "Not this plan's" — chunk 8's Q8 ruling), so `PLANNED chunk 6` is the honest status; this round did not itself read the runner's code to confirm the rows actually pass, and does not claim `DELIVERED` on that basis |
| **[NEW]** Known gaps, named rather than discovered later — a composed recipe can span two stores | 1557 | A route that resolves its storage location from a global rather than the `target` it was handed breaks the isolation the design promises, and a recipe composing such a route with one that honours the target reads and writes two different stores with nothing catching the mismatch | NOT STARTED | New row, this round's combinatorial finding, and an OBSERVABLE against this repo rather than a framework rule. `guildWriteRouteBroker` registers through `StartOrchestrator`, whose brokers resolve home off `process.env.DUNGEONMASTER_HOME`, while `questWriteRouteBroker` writes straight to `target.home` — already named in `packages/siegelense-recipes/CLAUDE.md` under "A `DmTarget` alone does not isolate a `write` route from the real machine". This round records it as an observable in the spec itself and states the general rule it implies; nothing has fixed it, so `NOT STARTED` |
| **[NEW]** Known gaps, named rather than discovered later — a package the recipes depend on cannot convert its own tests | 1558 | A test inside a package the recipes package depends on cannot be converted by importing them, because the reverse dependency would be a cycle; the orchestrator-owned integration targets are descoped for exactly this reason, and duplicating ingredients into the dependent package to dodge the cycle is rejected outright | **PLANNED chunk 9 (part)** | New row, this round's ruling. `recipes-chunk-09-10-migration.md` G0-b already found the dependency (`siegelense-recipes` imports `@dungeonmaster/orchestrator`, and twenty-odd files do it) and its Q9-4 already recommended treating a failed spike as a scope-narrowing finding rather than copying ingredients across the boundary — this row turns that open question into a settled rule, and the sibling row below repeats it where the migration section states it in full. No spike has yet run to confirm the cycle fails at module-evaluation time the way Q9-4 predicts, so this stays PLANNED, not DELIVERED |
| Mechanics the framework has to implement | 1560 | The runner walks a plan (a tree of `create`/`set`/`remove`/`saveRecord`/`filter`/`extra` ops) depth-first in declaration order, never crosses the MCP wire itself, implements no undo, splits refusals into pre-flight (computed off the plan alone: an unservable route, an unresolvable `fromSaved`, an unfilled `links` including one added at top level, or a chain call needing an undeclared `query`/`update`/`remove` route) versus mid-run (a `filter` under-matching, a route throwing); every error class carries the ingredient and the recipe name | **DELIVERED chunk 1 (part)** | Unchanged this round |
| **[NEW]** Mechanics the framework has to implement — a transition's target is refused before it runs | 1609 | A `set` asking for a transition value the ingredient's `to` never declared is refused in the PRE-FLIGHT, before anything runs, because both halves of that check — the value asked for, and the values declared — are static. This is a different failure from a gate refusing a value that IS on the list, which only happens once `reach` is actually walking a live row | **DELIVERED chunk 4** | **Status corrected this round: the code exists, and Status follows the code, not the plan.** `planPreflightBroker` (`plan-preflight-broker.ts:228`) throws `HydrationTransitionUnreachableError` for exactly this check, and `plan-preflight-broker.test.ts:372` covers it directly (`'INVALID: {a created quest set to "blocked", not on transitions.to} => throws HydrationTransitionUnreachableError'`). The prior `NOT STARTED` reading was keyed to which chunk PLAN names the check in its own table, not to whether the check was built — that was the error |
| How siegelense finds recipes without importing them | 1624 | `siegelense` lists recipes by globbing `packages/hydration-recipes/dist/index.js`, dynamically importing it, and reading its exported manifest (name/description/inputs/routes per recipe) — reading compiled output only, and distinguishing an absent directory (installation problem) from an empty one (no recipes yet, a real answer) | **DELIVERED chunk 3 (part)** | Unchanged this round |
| Where each part lands in this repo's architecture | 1647 | Maps each piece to a folder type and package — `IngredientConfig`/`Plan`/`Op`/`Target`/the manifest as `hydration` contracts, the chain builder as `hydration` transformers, `createHydration`/`ingredient`/`registry`/`recipe`/the runner as `hydration` brokers, route helpers as `hydration` adapters, refusal errors as `hydration` errors, recipe discovery/listing and the `seed` step as `siegelense` brokers, the recipes-package scaffolder as `siegelense` (already built), each ingredient/recipe as its own folder in `hydration-recipes` — and requires `hydration`'s own `ban-primitives` eslint carve-out with a comment | **DELIVERED chunk 3 (part)** | Unchanged this round |
| The migration IS the validation, not cleanup afterwards | 1669 | Converting the real suites (118 e2e specs seeding domain state, 16 convertible integration domain-seeders) to the recipe framework is in scope and is how the design gets proven, run via `python3 scrolls/tools/seed-census.py` for the target list and the running call-site count, in the order integration-then-e2e-then-one-manual-siegelense-round | NOT STARTED | Unchanged this round |
| The order, and why each step proves something the last one could not | 1727 | Step 1 (17 integration domain-seeders, files only) proves write routes and the no-`baseUrl` case; step 2 (118 e2e specs) proves api routes and one plan serving both callers; step 3 (one manual siegelense round) proves `seed`, `recipes {}` and params validation, which neither suite crosses | NOT STARTED | Unchanged this round |
| The rule that makes the conversion a proof | 1745 | A converted test keeps its assertions exactly, changing only its setup; a test needing a different assertion means the ingredient is wrong, not the test; a test that cannot convert is a finding written into this document; the one integration test whose SUBJECT is a `copies:` target (`quest-hydrate-broker.integration.test.ts`) is excluded, while tests that merely use that broker as setup are ordinary conversion targets | NOT STARTED | Unchanged this round |
| **[NEW]** The migration IS the validation, not cleanup afterwards — a package the recipes depend on is out of reach for this conversion | 1783 | States the general rule and its cost: `siegelense-recipes` depends on `orchestrator`, so `orchestrator` cannot depend back without a cycle; most of chunk 9's integration targets sit inside `orchestrator` and are descoped for exactly this reason, only the one target inside `server` converts, and the chunk 10 browser specs — the larger half of this migration — are unaffected. Duplicating ingredients into `orchestrator` to dodge the cycle is rejected outright | **PLANNED chunk 9 (part)** | New row, same finding as the Known-gaps row above, written in full where a migration-chunk reader meets it first. See that row for the evidence — `recipes-chunk-09-10-migration.md` G0-b and Q9-4 |
| An ingredient touches STATE, never a screen | 1809 | An ingredient never holds a DOM handle; enforcement is layered — a runtime guard (only the minting instance's refs resolve, invalidated on navigation/reset/restart) as the layer that actually protects, a durable step-shape type structurally omitting `ref`, and a lint rule in the published `@dungeonmaster/eslint-plugin` (not `local-eslint`) flagging any DOM mention in an ingredient file | **DELIVERED chunk 7 (part)** | Unchanged this round |
| Some claims can only be asserted in a BROWSER | 1852 | Ingredient tests come in three costs — files-only, a shared server instance, or a full server-plus-browser instance for a claim about what a URL renders — and a browser-asserted ingredient test is deliberately rare because the prelude's own `VERIFIED` run already proves rendering | NOT STARTED | Unchanged this round |
| What this is, in industry terms | 1876 | Frames this as a test-data factory library (not the industry sense of "hydration"), keeping sequence/trait/lint/explicit-dependencies from the factory family while deliberately refusing random data, fluent builders as the wire format, static fixtures, and object-mother method-per-scenario naming; names the Cucumber failure mode it guards against, and the three mechanisms that guard against it (a run-proven prelude, structural parenting, plan-as-data) | NOT STARTED | Unchanged this round |
| Guidance for other repos | 1927 | For a consumer repo, only `@dungeonmaster/hydration`, the empty scaffolded `packages/hydration-recipes/`, and five stated conventions (one ingredient per entity from that repo's own contracts, an ingredient declares its routes with `copies:` on `write`, counts are `add(n)` never a name, the chain owns the index, two-of-anything must be distinguishable) travel — not this repo's own recipes | NOT STARTED | Unchanged this round |

## Part 6 — The surface, consolidated (line 1944)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Each package needs a `../../CLAUDE.md`, and these are the entries | 1950 | Splits a set of named invariants (no `.first()`/`.last()`, no `querySelector` in eval, a command returns a reading not a verdict, the key reads own text nodes, a ref resolves only in its minting instance, an ingredient touches state never a screen, one verb sets state, `all` as an argument not a property, link-by-name, route-lives-on-ingredient, `hydration-recipes` absent from root deps, `run`-returns-status/`results`-returns-payloads, kill-the-group-skip-the-dead, `kill` never removes evidence, `dev:no-watch`) across `siegelense`'s, `hydration`'s and `hydration-recipes`'s own `CLAUDE.md` files, by which package breaks if the rule breaks | **DELIVERED chunk 1 (part)** | Unchanged this round |
| The calls this document uses | 1984 | `recipes {}` lists states with no instance needed, printing exactly the fields Part 5's "The listing `recipes {}` prints" specifies; `docs { for: … }` serves the tool's own instructions across seven scopes, per the sibling tooling document | NOT STARTED | Unchanged this round |
| Steps that are new | 2028 | A `seed` step runs a named recipe's plan against the current instance and returns the ids it made under `as:`; its `params` are typed by the `recipe` value in-process and validated by a generated zod schema before seeding over the MCP wire, naming the bad input and what that recipe takes | **DELIVERED chunk 3 (part)** | Unchanged this round |
| A worked batch | 2054 | A batch step's output is addressed as three segments, `{<step>.<row>.<field>}` (never the two-segment `{step.field}`, which the tool should refuse); `stopOn: 'error'` is the default that halts on the first unexpected failure; a step declaring `expect: 'error'` that unexpectedly SUCCEEDS itself halts the batch as a finding; a plan's output is FLAT, with exactly one shape, carrying every `saveRecordAs`'d row under its own name | NOT STARTED | Unchanged this round |
| Interleaving recipes and steps | 2113 | `seed` is placeable anywhere in a batch, not only as a prologue; a later `seed` in the same batch can take `params` from an earlier step's saved output; seeding while a page is already open is how a socket-driven live-update path gets exercised, while a non-socket-driven surface needs an explicit reload after such a seed | NOT STARTED | Unchanged this round |

---

## Sections read and deliberately given no row

Per the standing instruction, these are directions to the build rather than requirements the build must
satisfy, so they carry no row above:

- `## The prompt that builds this` (line 10) and its subsections `### What this doc asks for, and what it
  does not` (88) and `### A suggested spine, for the first planner round` (108)
- `### This branch tracks master, and siegelense is already in it` (137) and its subsections `#### What
  that leaves to schedule` (156), `#### When to merge` (172), `#### The rename is its own step, and it is
  bigger than the folder` (205)

`docs { for: … }`'s scope content and the rest of the thirteen-call MCP surface (`start`, `run`, `results`,
`kill`, `capacity`, `profile`, `status`, `cleanup`, `prune`, `compare`, `snapshots`, `docs`) belong to
`siegelense-tooling.md` and are tracked in `build-ledger.md`. This ledger only adds the `recipes` call and
the `seed` step to that surface, both of which have rows above.
