# Siegelense recipes ledger

Running coverage of `siegelense-recipes.md` against what has been planned and delivered. **One row per
heading under Part 3A, Part 5 and Part 6**, keyed by heading text and its line number in the CURRENT file.
Every round updates this file.

**Line numbers shift whenever anyone edits the spec.** Re-derive them from heading text before trusting a
number in this table — never carry one forward from an older copy of this ledger.

Status vocabulary, carried over from `build-ledger.md` (the sibling ledger for `siegelense-tooling.md`),
plus one addition this ledger needed that that one did not:

| Status | Means |
|---|---|
| `NOT STARTED` | nothing plans it yet |
| `PLANNED chunk N` | a chunk plan under `plans/` covers it, wholly |
| `PLANNED chunk N (part)` | a chunk plan covers some of it; Notes says which part |
| `DELIVERED chunk N` | chunk N built it, wholly, and the code was read against this section's own words |
| `DELIVERED chunk N (part)` | chunk N built some of it; Notes says exactly which part landed and which stays open |
| `BLOCKED chunk N` | the code exists and is believed sound but could not be RUN to confirm; Notes names the blocker. Never read as tested |
| `DELIVERED (pre-existing)` | the code already exists and was read this round, but it was built before this ledger's own chunk numbering began — by the siegelense-tooling build, not a chunk plan under `scrolls/seigelense/plans/` for this document. Add `(part)` on the same terms as the chunk-numbered statuses |

**Two chunk plans exist for the framework's types and its declaration/chain surface** —
`plans/recipes-chunk-01-03-framework-types.md` (chunks 1–3) and `plans/recipes-chunk-04-06-runner.md`
(chunks 4, 5, 6 and 6b). A third — `plans/recipes-chunk-07-repo-ingredients.md` — plans this repo's own
ingredients. A fourth — `plans/recipes-chunk-12-combinatorial-rounds.md` — plans the combinatorial
session and is new this round. Each plan's own amendments section records where the plan was wrong and
what shipped instead; a row below reads against what shipped, not against the plan's original text,
wherever the two disagree.

**This round graded the working tree directly, not a commit.** Chunk 1 remains committed at `0dcd4522d`.
Since then, uncommitted: the chain builder's four transformers (`collectionChainTransformer`,
`rowHandleChainTransformer`, `matchedSetChainTransformer`, `entryChainTransformer`) and every op and
listing transformer; the declaration broker (`ingredientDeclareBroker`), the registry
(`registryCreateBroker`), the recipe declaration (`recipeDeclareBroker`) and `hydrationCreateBroker`;
chunk 4's groundwork — a dozen pure transformers, contracts and route-calling adapters that need nothing
unbuilt — but not the runner itself; both halves of the negative type suite for rows 1–11 (plus 7a/7b)
and D1–D10, plus a positive fixture exercising every chainable; a file-target test harness; the package's
root barrels and its `exports` map; and fixes to several already-committed contracts (`hydrationRoutesContract`
gained `query`/`update`/`remove`, `ReachFn` gained `record`). **`packages/hydration/src/brokers/` holds
exactly four subfolders — `hydration/`, `ingredient/`, `recipe/`, `registry/` — and no `plan/` folder at
all**, confirmed by listing the package fresh this round. That one fact settles most of the RUN-time rows
below: `plan-run-broker.ts`, `plan-preflight-broker.ts` and every `op-*-apply-layer-broker.ts` do not
exist, so nothing in the framework throws any error yet, however complete its vocabulary.

**Two areas were flagged in flight and were each read to a decision, not left flagged.** The row-ref
grammar now folds a `CallIndex` alongside the row's own index — `guild[0:0]/quest[0:2]` — implemented
consistently across `rowRefContract`, `rowRefTransformer`, `matchedRefTransformer`, `op-create-contract`
and `collectionChainTransformer`, and it is Part 5's own "a reference disambiguates between calls"
requirement, not a stray change; it is graded delivered below. The two eslint-plugin rules
(`ban-dom-handles-in-ingredients`, `ban-nondeterminism-in-ingredients`) are fully wired — broker, a real
`RuleTester` test, registration in `eslintPluginCreateResponder`'s rule map, configuration at `'error'` in
`configDungeonmasterBroker`, and categorization in `dungeonmasterRuleEnforceOnStatics` — confirmed by
reading every one of those five files; they are graded delivered below too. Neither area reads as
half-finished on the tree as it stands today.

**A new plan landed this round for the combinatorial session** —
`plans/recipes-chunk-12-combinatorial-rounds.md` — turning both of Part 5's tables into more than fifty
named cases. No case has been RUN; the plan itself says so. Rows it covers move from `NOT STARTED` to
`PLANNED chunk 12`.

**A row reading `DELIVERED (pre-existing)` is work the siegelense-tooling build already did**, unrelated
to any chunk plan, and stays as it was unless a Note says this round touched it.

**What this ledger does not cover.** Per this round's instructions: the front matter about the prompt
that builds this document, the suggested spine, and the branch-tracking instructions ("This branch
tracks master…" through "The rename is its own step…") are directions to the build, not requirements of
it, and carry no row. `docs { for: … }`'s seven scopes and the rest of the thirteen-call surface belong to
`siegelense-tooling.md` and are tracked in `build-ledger.md`, not here — this document only adds the
`recipes` call and the `seed` step to that surface.

---

## Part 3A — The decision index (line 226)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Recipes: the three packages, the three tiers, and what holds them | 228 | Serves as the condensed decision index for every rule Part 5 states in full — three packages with import/publish boundaries, three verb tiers, the handle/chain mechanics, one state-setting verb, `filter`'s zero-match throw, link-by-name typing, route/`copies:` rules, determinism rules, the plan-as-data rule, the ingredient-test/correctness-vs-fitness split, and the migration rules — with Part 5 winning wherever the two disagree | **DELIVERED chunk 3 (part)** | The scaffolder stays delivered pre-existing (`InstallRecipesScaffoldResponder`, unchanged). The not-shipped rule stays wholly delivered: root `package.json` still excludes `@dungeonmaster/siegelense-recipes` and `packages/siegelense-recipes/package.json` still carries `private: true` (both confirmed by reading the files). `ingredient()`, `registry()` and `recipe()` now all exist and are callable through `hydrationCreateBroker` (`packages/hydration/src/brokers/hydration/create/hydration-create-broker.ts`), proven against two real target shapes in `packages/hydration/test/type-fixtures/{dm-target,sql-target}.ts`. The three verb tiers, the handle/chain mechanics, `filter`'s zero-match throw, link-by-name typing, route/`copies:` rules and the determinism rules are `DELIVERED chunk 3` or `(part)` — see Part 5's rows below for the checkable detail. Still open: `run(plan, target)` (no `packages/hydration/src/brokers/plan/` directory exists at all), the ingredient-test/correctness-vs-fitness split (chunk 7 — only prep field contracts sit in `packages/siegelense-recipes`, none wired to `ingredient()`), and the migration rules (chunks 9–10, untouched). **New this round**: root `package.json` correctly lists `@dungeonmaster/hydration` in `dependencies`, but `node_modules/@dungeonmaster/` carries no `hydration` entry at all — the workspace symlink does not exist, so no other package can resolve an import of it today despite the dependency being declared correctly |

## Part 5 — The recipe book (line 277)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Four words this document uses, and two rules that outlive the split | 287 | Defines instance/run/recipe/prelude precisely, and states that a recipe returns a plan runnable in-process with no MCP boundary, and that one row must make "the right one" and "the first one" the same value (the two-of-anything rule via `defaults(index)`) | **DELIVERED chunk 3 (part)** | The two-of-anything rule and `defaults(index)` are now proven on VALUES by three suites: `collectionChainTransformer`'s own test (`packages/hydration/src/transformers/collection-chain/collection-chain-transformer.test.ts`, asserting `Quest 1`/`Quest 2`/`Quest 3` off one `add(3, …)`), `ingredientDeclareBroker`'s test (`defaults(0)` and `defaults(1)` differ), and `registryCreateBroker`'s test (the whole op tree). `fromSaved` ships via the package's `transformers.ts` barrel, confirmed by reading it. `run(plan, target)` — the thing that makes a recipe's plan runnable with no MCP boundary — still does not exist: `packages/hydration/src/brokers/` holds no `plan/` folder, confirmed by listing the package |
| The problem | 310 | Names the four separate seeding vocabularies (e2e harnesses, hand-rolled `fs` in Jest integration tests, `questHydrateBroker` + blueprints for smoketests, a throwaway walk script) that one recipe book must replace with a single shared spelling | NOT STARTED | A narrative problem statement, not a build artifact. Replacing the four vocabularies is proven only once ingredients exist (chunk 7) and the migration lands (chunks 9–10) |
| Three packages, and what may cross between them | 339 | Three packages — `@dungeonmaster/siegelense` (ships), `@dungeonmaster/hydration` (ships, repo-agnostic), `packages/hydration-recipes` (does not ship, absent from root `package.json` `dependencies` with a colocated test pinning that) — where `siegelense` discovers recipes at run time and never imports the recipes package | **DELIVERED chunk 3 (part)** | The absent-from-root-dependencies rule stays wholly delivered (confirmed by reading both `package.json` files). `hydrationCreateBroker` now exists and instantiates the framework generically, proven against a file-backed (`DmTarget`) and a database-backed (`SqlTarget`) target in the type-fixture tree. The barrels' `exports` map is now built: `packages/hydration/package.json` exposes `./contracts`, `./brokers`, `./transformers` and `./errors` (confirmed by reading the file) — only `./adapters` stays absent, and deliberately so, per `packages/hydration/CLAUDE.md`'s own section on it. **New this round**: `node_modules/@dungeonmaster/` carries no `hydration` entry — the workspace symlink is missing, so even though `packages/siegelense-recipes/package.json` already names `@dungeonmaster/hydration` as a dependency, nothing can actually resolve that import today. `siegelense` never importing `hydration-recipes` is still unverifiable either way: no `packages/siegelense` file has been touched |
| Three tiers, and only the middle one is written per entity | 366 | Structural verbs (`add`, `filter`, `set`, `remove`, `saveRecordAs`) live once in the framework; capability verbs (like a `transitions`-driven `set`) are declared once and typed per ingredient, promoted only on the second ingredient that wants one; extras are per-ingredient verbs nothing else gets | **DELIVERED chunk 3 (part)** | `reservedVerbStatics`, `RowVerbs`/`ExtraMethods` and `extraVerbNameContract`'s refusal are unchanged from chunk 1. `opExtraTransformer` (`packages/hydration/src/transformers/op-extra/op-extra-transformer.ts`) now exists and turns a declared extra into a runnable op at CALL time, wired into `collectionChainTransformer`'s `all`. Actually running that op's `apply` still needs `op-extra-apply-layer-broker.ts` (chunk 5, gated on Q3), which does not exist — no `packages/hydration/src/brokers/plan/` folder exists at all |
| Every property on an ingredient, and what goes in it | 385 | An `ingredient()` config accepts required `name`/`description`/`fields`/`record`/`routes` and optional `links`/`transitions`/`defaults`/`copies` (required with a `write` route)/`extras`, exactly as the reference table and the worked `questIngredient` example show | **DELIVERED chunk 2 (whole)** | `ingredientConfigContract` types every property (unchanged from chunk 1). `ingredientDeclareBroker` (`packages/hydration/src/brokers/ingredient/declare/ingredient-declare-broker.ts`) now exists and IS `ingredient()` — bound through `hydrationCreateBroker`'s `ingredient` property to match the doc's own un-annotated call shape exactly. `packages/hydration/test/type-fixtures/dm-target.ts` declares real ingredients using the full reference-table property set between them: `guildIngredient` (api-only), `questIngredient` (`links`, `defaults`, `transitions` with `reach`, all five routes, `copies`), `operationIngredient` (two links), `sessionIngredient` (`extras`). `ingredientDeclareBroker`'s own test proves the three runtime backstops fire with the exact messages: no routes, a `write` route with no `copies`, an extra shadowing a reserved verb |
| Every property on an ingredient, and what goes in it — a link's `from` | 446 | A `links` entry gains an optional `from`, naming the field on the PARENT record that carries its id, defaulting to `'id'` — needed wherever a parent's own id field is not called `id`, as the session record is | **DELIVERED chunk 3 (part)** | Type-level half unchanged from chunk 1 (`linkSpecContract` carries optional `from`). `linkValuesTransformer` (`packages/hydration/src/transformers/link-values/link-values-transformer.ts`) now exists — the pure function reading a parent's id off the field `from` names, proven by its own colocated test — but nothing calls it yet: no file under `packages/hydration/src/brokers/plan/` exists to invoke it during a real walk |
| Every property on an ingredient, and what goes in it — `reach` receives the `record` | 464 | `reach` receives the `record` being walked, alongside `from`, `to` and `target`, because `add(3, …)` mints three rows and nothing else tells `reach` which one to walk | **DELIVERED chunk 3 (part)** | `ReachFn<TTarget, TValue>` now REQUIRES `record` in its own signature (confirmed by reading `transition-spec-contract.ts`), and `ingredientConfigContract` intersects it onto `transitions` via `TransitionSpecWithReachFor` (confirmed by reading `ingredient-config-contract.ts`) — an ingredient declaring `transitions` with no `reach` is now a TYPE error. **The runtime backstop is not built**: `ingredient-declare-broker.ts`, read in full, throws only three checks (no routes, write-without-copies, a reserved-verb extra) — no fourth check refuses `transitions` with no `reach` at the value level, though the chunk 4–6 plan's §9 calls for one. The runner passing the real record when it calls `reach` is still `op-set-apply-layer-broker.ts` (chunk 5), which does not exist |
| Every property on an ingredient, and what goes in it — `extras` is `{ args, apply }` | 505 | An `extras` entry is one object, `{ args, apply }` — never a bare arg contract — so a verb cannot be declared with arguments and no body to run | **DELIVERED chunk 1** | Unchanged: `ingredientConfigContract`'s `extras` field and its colocated test still refuse both wrong shapes at parse time. `opExtraTransformer` (chunk 3, `packages/hydration/src/transformers/op-extra/`) now additionally exists, turning a declared extra into a runnable op at call time — not built at the last grading. Calling the declared `apply` at run time is still `op-extra-apply-layer-broker.ts` (chunk 5, gated on Q3), not built |
| What a recipe declares | 551 | `recipe({ name, description, inputs? }, builder)` takes a required name, a one-line present-tense description of the end state that `recipes {}` prints, an optional `inputs` zod schema for what the recipe needs from an earlier step, and a builder taking the recipe's typed inputs (if any) and returning ops | **DELIVERED chunk 3 (whole)** | `recipeDefContract` types name/description/inputs (unchanged from chunk 1). `recipeDeclareBroker` (`packages/hydration/src/brokers/recipe/declare/recipe-declare-broker.ts`) now exists and, bound through `hydrationCreateBroker`'s `recipe` property, matches the doc's own two-argument call shape `recipe({ name, description, inputs? }, build)` exactly, confirmed by reading `hydration-create-broker.ts`. Its test proves a builder taking typed inputs returns ops carrying the supplied value, and that calling twice returns two structurally identical plans |
| The listing `recipes {}` prints | 577 | `recipes {}` prints, per recipe, its name, description, `inputs` (off the static input contract, now a zod schema), `runs` (`serverless` only when every ingredient in the plan has a write route, else `needs a server: <ingredient>`), and `makes` (each ingredient's description counted off the plan, `varies` wherever a filter or transition decides the count) | **DELIVERED chunk 3 (part)** | `recipeManifestContract` unchanged from chunk 1. `planRunsTransformer` and `planMakesTransformer` (`packages/hydration/src/transformers/plan-runs/`, `plan-makes/`) now exist and compute `runs`/`makes` off a real plan: `planRunsTransformer` is an ALL over the plan's declared ingredients (never a union), `planMakesTransformer` reports `varies` for any ingredient a `filter` reaches — both confirmed by reading the files and their own colocated tests. The `recipes {}` tool call that prints this is still chunk 8 (`siegelense`), untouched |
| The listing `recipes {}` prints — a transition's minted INGREDIENT, not just its count | 605 | A plan whose only reference to an ingredient is a transition, with no `filter` naming it, cannot appear in `makes` at all — not even as `varies` — because `reach` is opaque and nothing in a plan ties a transition to the child ingredient it mints | NOT STARTED | `planMakesTransformer` now exists (see the row above) but, read in full, it only walks `create` and `filter` ops — it does not distinguish "an ingredient reached only through a transition" from "an ingredient not reached at all," so the limitation stands exactly as recorded |
| Linking a child to its parent | 615 | A `links` entry names its parent by NAME (never a reference, to avoid the `TS7022` inference cycle); the registry inverts names into accessor keys; the runner walks the plan depth-first, writing each ancestor's id into the named field; a child accessor appears only on a host both named in that child's `links` and satisfying every one of them; and a `fromSaved` naming a record declared later is refused by the runner's PRE-FLIGHT, before the first write, not by the chain as it builds | **DELIVERED chunk 3 (part)** | `linkSpecContract` and `ChildAccessors`'s two-condition mapped type unchanged from chunk 1. `registryCreateBroker` (`packages/hydration/src/brokers/registry/create/registry-create-broker.ts`) now exists and actually inverts links into accessor keys, throwing `RegistryDanglingLinkError`/`RegistryDuplicateNameError` with the exact messages — confirmed by reading the file and its test, which proves the worked example (`operations` on `q[0]`, never `g[0]`) both positively and negatively, asserting a real handle's runtime key set. `linkValuesTransformer` (the runner half writing each ancestor's id into the named field) exists as a pure, unit-tested function but nothing calls it — no runner exists. The pre-flight refusing an unresolvable `fromSaved` (`plan-preflight-broker.ts`) remains unbuilt |
| Linking a child to its parent — a foreign key inside an array is not expressible | 679 | The mechanism covers one field on the child pointing at one field on the parent, and nothing narrower — a foreign key shaped like a work item's `relatedDataItems: ['operations/<id>']` (a prefixed string inside an array) cannot be written by a `links` entry or reached by `fromSaved`, so that entity cannot be an ingredient at all | NOT STARTED | Documentation-only boundary, not a build item. Matches chunk 7 plan's Q7-1, which keeps `workItems` a field on `quest`'s `fields` rather than promoting it to its own ingredient |
| The chain: every call names what it affects | 686 | `add(n, build)` hands its builder a fixed-length tuple of handles plus `all` as a second argument, so every subsequent call is made on a handle rather than an implicit current level | **DELIVERED chunk 3 (whole)** | The call-site shape (`Collection.add<N>`, `Handles`, `Tuple`) is unchanged from chunk 1. The chain builder that WALKS a call and produces the real op tree now exists and is tested: `collectionChainTransformer`, `rowHandleChainTransformer`, `matchedSetChainTransformer` and `entryChainTransformer` (all `packages/hydration/src/transformers/`) turn a call written against the signature into a real `HydrationOp[]` — confirmed by reading `collection-chain-transformer.ts` and its test, which asserts a complete three-create-plus-four-set op array in declaration order off one `add(3, (q, all) => …)` call. **A row's build-time identity now additionally disambiguates by WHICH `add` call minted it**, not only its index — `rowRefContract`'s grammar is `ingredient[callIndex:rowIndex]` (e.g. `quest[0:2]`), implemented by `rowRefTransformer` and `callIndexContract` and proven by a test asserting two separate `add(2, …)` calls on one collection mint `quest[0:0]`/`quest[0:1]` and `quest[1:0]`/`quest[1:1]` — this is Part 5's own "A reference disambiguates between calls" text, delivered in full, not a stray migration |
| One verb sets state, and the INGREDIENT decides whether that means a walk | 720 | `set()` is the only verb that assigns a value; the ingredient routes a plain field to a plain write and a field named under `transitions` to a walk through `reach`; `setRaw()` writes a transition field with no walk at all | **DELIVERED chunk 3 (part)** | `Settable<I>` narrowing and the op-level split (`opSetContract`) unchanged from chunk 1. `opSetTransformer`/`opSetRawTransformer` are now wired into the chain — confirmed by `collection-chain-transformer.test.ts`'s output showing real `{op:'set', ref, written}` nodes. Ahead of schedule: `planFoldWritesTransformer` (`packages/hydration/src/transformers/plan-fold-writes/`, chunk 4's own groundwork) already folds a `set`'s written half into its row's `create` op, pure and unit-tested. Walking a transition through `reach` at run time is still `op-set-apply-layer-broker.ts` (chunk 5), which does not exist |
| Every verb but `add` takes an object | 752 | Every chainable except `add` (which keeps a bare count) takes exactly one object argument, so a growing verb adds a key rather than a new parameter | **DELIVERED chunk 1** | Unchanged: a call-signature fact, fully checkable by reading the committed types. Now additionally proven operationally: `test/type-fixtures/positive/every-chainable.ts` exercises every one-object-argument verb over a real chain and compiles clean, and `collectionChainTransformer`'s own tests exercise `set`, `saveRecordAs`, `remove` and `under` as real calls |
| `filter` selects rows that only exist at RUN time | 766 | `filter({ where, expect })` returns a set with no index access and no `add`; `where` is a typed match object, never a closure; `expect` defaults to `'some'` and a zero match THROWS by default, mirroring the tool's own no-pick rule; and a `filter` nested inside an `add` is scoped to its immediate host, never the whole instance | **DELIVERED chunk 3 (part)** | `matchedSetContract`/`FilterArgsFor` unchanged from chunk 1, though `FilterArgsFor` now lives inline on `hydration-collection-contract.ts` rather than a separate `filter-args-contract.ts` file — a structural simplification; rows 10/11 (`expect`, `where`) are still graded, via `typescript-program-diagnostics-adapter.test.ts`. `opFilterContract`'s `scope` field unchanged. `opFilterTransformer` and `matchedSetChainTransformer` now exist and build the op at call time — confirmed by `collectionChainTransformer`'s and `registryCreateBroker`'s tests, which assert a real `{op:'filter', scope, where, expect, matchedRef, ops}` node off `q[0].operations.filter({...})`, matching the doc's own worked example. `filter` actually READING LIVE STATE against that scope remains `op-filter-apply-layer-broker.ts` (chunk 6, gated on Q2), not built |
| A recipe takes typed inputs, so it can stack on what an EARLIER STEP made | 805 | A recipe needing a value from an earlier seed step declares a typed input parameter as a zod schema under `inputs`, and a batch supplies it through a `params` object that never flattens onto the step's own keys | **DELIVERED chunk 3 (part)** | `recipeDefContract`'s `inputs` and `seedStepContract`'s `params` shape unchanged. `Collection.under(ids)`'s signature unchanged. `under()`'s actual chain behavior now exists: `collectionChainTransformer`'s `under` method rebuilds the collection with the supplied ids folded into every subsequent `create`'s fields, confirmed by its own test asserting `guildId: 'guild-1'` on the created row. A batch actually supplying `params` from an earlier step's output is still chunk 8 (`siegelense`), untouched |
| Every chainable, with an example | 850 | Every chainable — `add`, `set`, `setRaw`, `filter`, `remove`, `saveRecordAs`, `fromSaved`, `under`, and an ingredient's own extra — exists exactly as shown, and every snippet shown is copied out of `proto/usage.ts`, which the type check compiles | **DELIVERED chunk 3 (part)** | This is the heading the last round found 0% built; it is now real. `packages/hydration/test/type-fixtures/positive/every-chainable.ts` exercises `add`, `set` (plain and transition), `setRaw`, `remove`, `saveRecordAs`, `fromSaved`, `under` and an ingredient's own extra over the file-backed target, and `typescript-program-diagnostics-adapter.test.ts`'s "the positive fixture tree" block asserts it produces ZERO diagnostics via a real `tsc` run. The fixture's own header comment claims that test lives in `collection-chain-transformer.test.ts`; it does not — that file has no reference to the positive fixture or the diagnostics adapter, so the comment is stale (worth a fix, not a delivery gap — the assertion is real and does run, from the file the chunk 1–3 plan's §10b amendment actually assigns it to). **Not delivered**: the six mutation-tested SHAPE assertions (`Expect<Equal<…>>`) the chunk 1–3 plan's §4 calls for — no `expect.ts` helper and no `positive/shape-assertions.ts` exist anywhere in `test/type-fixtures/`, confirmed by listing the directory (it holds only `dm-target.ts`, `sql-target.ts` and `positive/every-chainable.ts`). The mutation-tested shape facts are proven today only by ordinary runtime tests reading a runtime key set, never by a type-level assertion that would itself fail if the restriction were dropped. **Also not delivered**: rows 12–17 (a wrong-typed recipe input, a missing recipe input, and all four `seed-step` cases) have no fixtures — no `seed-step/` directory exists — and the three database-shape re-proofs of rows 3/7b/11 do not exist either, though `sql-target.ts` (the set they would run against) does. `scrolls/seigelense/proto/` is unchanged and not yet deleted |
| A plan is data, and one plan runs three ways | 968 | `recipe()` returns pure data at build time; the same plan runs via `run(plan, { home, baseUrl })` from a walk or e2e spec, and via `run(plan, { home })` (write routes only) from a Jest integration test, with a targetless caller restricted to write-route ingredients by the types | **DELIVERED chunk 3 (part)** | `hydrationPlanContract`/`Plan<TOut>` unchanged; still no I/O anywhere in chunks 1–3. New groundwork toward `run`: `hydrationRunStateContract` and `hydrationRunResultContract` (chunk 4) now exist, typing what a walk carries between ops and what it hands back. `run(plan, target)` itself is still not built — `packages/hydration/src/brokers/` has no `plan/` folder. **A test that would prove the file-backed half exists but does not count**: `packages/hydration/test/harnesses/file-target/file-target.harness.ts` and its own `file-target.harness.integration.test.ts` are both written, but `packages/hydration/jest.config.js` sets `roots: ['<rootDir>/src']`, confirmed by reading it — a test file under `test/` is never discovered by ward at all, so this suite has never actually run. The chunk 4–6 plan's own §4b already names this and assigns moving the assertion into chunk 6b's own `src/`-colocated suite |
| Determinism is structural, not a rule to remember | 992 | The chain owns each row's index and hands it to `defaults(i)`, so no ingredient ever calls `Date.now()`, `Math.random()`, or `crypto.randomUUID()` for anything reaching a screen, and the same plan run twice produces identical bytes | **DELIVERED chunk 3 (part)** | `rowRefContract`/`rowIndexContract` unchanged in spirit; the grammar now additionally folds in a `CallIndex` (see "The chain" row above), still deriving identity with no clock or random source. `rowRefTransformer` now exists and DERIVES that identity at call time, closing what the last round called missing. **The lint rule now exists and is wired**: `@dungeonmaster/ban-nondeterminism-in-ingredients` (`packages/eslint-plugin/src/brokers/rule/ban-nondeterminism-in-ingredients/`) bans `Date.now()`, `Math.random()` and `crypto.randomUUID()`, is registered in `eslintPluginCreateResponder`'s rule map, configured `'error'` in `configDungeonmasterBroker`, categorized `pre-edit` in `dungeonmasterRuleEnforceOnStatics`, and proven firing against real ESLint by its own `RuleTester` suite — confirmed by reading all five files. Its own error message documents its blind spots (a destructured `import { randomUUID }` is invisible to it). Nothing in the repo is yet named `*-ingredient.ts`, so it has nothing to catch today, but it is real and live |
| Routes: how an ingredient makes its state | 1006 | An ingredient declares one or more of `api` (the real code path), `write` (direct storage matching what the app would write, requiring `copies:`), or `recording` (replays a captured run); the TARGET, not the recipe, picks which route runs when more than one is declared; optionally `query`, `update` and `remove` for a chain call that reaches a row the create route did not just make, refused in the pre-flight when the ingredient declares none | **DELIVERED chunk 3 (part)** | `hydrationRoutesContract`/`copiesTargetContract` unchanged in base shape. The route-CALLING adapters (`fetch-post-adapter.ts`, `fs-ensure-write-adapter.ts`) now exist, complete with proxies and tests. `routeSelectTransformer` (the route-SELECTING half, chunk 4) also exists and is unit-tested standalone, but nothing calls it — no pre-flight or runner broker exists. The write-route-side-effect-disagreement finding stays open (chunk 7 not started). The `query`/`update`/`remove` addition has its own row below |
| Routes: how an ingredient makes its state — `query`, `update` and `remove` | 1029 | `routes` gains three optional entries — `query`, `update` and `remove` — so `remove()`, `filter()` and an unfoldable `set` each have a route to call; a plan calling a verb whose ingredient does not declare the matching route is refused in the pre-flight, naming the ingredient and the verb, via a dedicated error class | **DELIVERED chunk 3 (part)** | The contract edit is landed: `hydrationRoutesContract` carries `query`/`update`/`remove` as three further optional entries (confirmed by reading it), and `HydrationRouteVerbUnavailableError` is defined and message-tested. `op-remove-apply-layer-broker.ts`, `op-update-apply-layer-broker.ts`, `op-filter-apply-layer-broker.ts`'s own query, and `plan-preflight-broker.ts` via `route-select-transformer` — none exist; no `packages/hydration/src/brokers/plan/` folder exists, so nothing yet THROWS this error |
| An ingredient for a FILE, and an ingredient for a DATABASE ROW | 1046 | The framework is generic over its own target type via `createHydration<T>()` (a file-backed home, or a database-backed transaction), proven by the prototype's `ingredients.ts` (file) and `db.ts` (database) declaring identical chain mechanics over different targets | **DELIVERED chunk 3 (whole)** | `hydrationTargetContract` (`HydrationTarget`, `TTarget extends HydrationTarget`) unchanged in spirit from the chunk 1–3 plan's own amendment. `hydrationCreateBroker` now exists and actually instantiates the framework against a real target — confirmed by reading it, and by its own test asserting `createHydration<DmTarget>()` and `createHydration<SqlTarget>()` both return exactly `{ ingredient, registry, recipe }`. **The fixture tree's two ingredient sets are now delivered**: `dm-target.ts` (guild/quest/operation/session — `guildIngredient` api-only, `questIngredient` carrying all five routes plus a `transitions` block with `reach`, `operationIngredient` linking to both `quest` and `guild`, `sessionIngredient` carrying the `withNestedChain` extra) and `sql-target.ts` (the database-backed twin) both exist and both declare real ingredients through `ingredientDeclareBroker` — the prototype's own file-vs-database proof is re-established against this repo's contracts. `run` executing against either target is still chunk 4, unbuilt |
| An ingredient with only one route, and what it costs | 1129 | A plan runs serverless only when EVERY ingredient in it declares a write route (an ALL, not an ANY); the listing reports `runs serverless` or names the first ingredient lacking one; the runner refuses that mismatch before seeding anything, naming the ingredient and the missing route | **DELIVERED chunk 3 (part)** | `HydrationRouteUnavailableError` unchanged. `planRunsTransformer` now exists and computes the ALL-not-ANY answer — confirmed by reading it: it walks the plan depth-first, including into `filter`'s nested ops, and its own test proves the ALL against a majority-writable plan. The pre-flight that actually THROWS this error before seeding anything (chunk 4) is not built |
| Every ingredient carries a test, and a two-route ingredient tests its routes against each other | 1167 | Every ingredient has a colocated integration test asserting what it made; an ingredient declaring both `api` and `write` runs both and compares them rather than snapshotting either alone; every `api`-route test in a suite shares one booted instance | NOT STARTED | Chunk 7 still needs a real ingredient to test, and none exists. `packages/siegelense-recipes` carries only field/record contracts (guild-fields, quest-fields, session-fields, subagent-fields, operation-fields, a `dm-target-contract`, and three extras' arg contracts) plus a `package-json-read-broker` and a guard — none of it is passed to `ingredient()` anywhere, confirmed by searching the package for an import of `@dungeonmaster/hydration` (none, outside `package.json`'s own `dependencies` list). This content predates the last graded commit (`f53b3e078`) and is prep work for chunk 7, not new this round |
| What the type prototype proved, and what it changed | 1200 | The committed prototype at `proto/` (eight TypeScript files under `strict`, compiled by `node proto/check.mjs`, with `@ts-expect-error` on every case that must fail) proves each design rule the doc states — link-by-name to avoid `TS7022`, `all` as a second builder argument, the immediate-parent-plus-satisfied-links accessor rule, a generic `Target` type — and its ten malformed-declaration probe found nine compile-time-catchable mistakes plus one (duplicate names in a registry) needing a runtime check | **DELIVERED chunk 3 (part)** | Both fixture halves now genuinely exist, closing what the last round called 0% built. `test/type-fixtures/declaration/` holds all nine D1–D9 fixtures (D10 needs none, being runtime-only), and `test/type-fixtures/call-site/` holds all twelve rows-1–11-plus-7a/7b fixtures. Every one is graded by a real `tsc` run and asserted to the exact diagnostic (file, line, code, message): D1–D8 in `ingredient-declare-broker.test.ts`, D9 and D10 in `registry-create-broker.test.ts`, and rows 1–11/7a/7b plus the positive tree's zero-diagnostics proof in `typescript-program-diagnostics-adapter.test.ts`. **Not delivered**: rows 12–17 (recipe-input and seed-step cases), the three database-shape re-proofs, and the six mutation-tested SHAPE assertions — see "Every chainable, with an example" above for the detail. `scrolls/seigelense/proto/` itself is unchanged and was not re-run this round |
| The sad paths, which no type catches | 1281 | Every listed run-time failure (a refused `api` connection; a 4xx/5xx with the body carried verbatim; a 2xx shape `record` rejects on `api` or `write`; a `write` failure naming the path; an absent parent directory being CREATED rather than failing; a `reach` throw naming from/to/gate; a mid-plan `filter` failure named distinctly from a zero match; a serial file-repo runner; discovery reporting "not built" rather than an empty list; and params validation before seeding) must fail loudly and name what it was doing, and a mid-batch seed failure halts the batch and marks the instance unusable rather than being retried | **DELIVERED chunk 1 (part)** | Every mid-run error class remains defined and message-tested, unchanged from chunk 1. New this round: the context-mining halves these classes need now exist too — `routeFailureTransformer` and `writeFailureTransformer` (`packages/hydration/src/transformers/route-failure/`, `write-failure/`) read `{url,status,responseBody}`/`{path}` off whatever a route actually threw, and `fetchPostAdapter`/`fsEnsureWriteAdapter` (the real route helpers) exist with proxies and tests. **Nothing throws any of these yet** — decisively confirmed: `packages/hydration/src/brokers/` holds no `plan/` folder at all, so `plan-run-broker.ts` and every layer broker chunk 4/5/6/6b would add do not exist |
| The sad paths, which no type catches — a `write` route's bad record | 1296 | A `write` route answering with a shape `record` rejects halts and names the field, exactly as a bad `api` response does, because a bad record poisons every saved record and every link the same way | **DELIVERED chunk 1 (part)** | Unchanged: `HydrationRecordShapeError` defined and message-tested; `op-create-apply-layer-broker.ts` (chunk 4), which would parse a `write` route's return through `record` and throw this, does not exist |
| The sad paths, which no type catches — who throws the transaction rollback | 1301 | Nothing inside the framework throws the transaction-rollback error; the repo's own wrapper around `run()` does, because the framework names neither files nor SQL and does not own the transaction — the target IS the transaction | **DELIVERED chunk 1 (part)** | `HydrationTransactionRolledBackError` unchanged, defined and message-tested with no thrower inside the package, by design. **`packages/hydration/CLAUDE.md` still does not document who throws it**, confirmed by reading the current file in full — despite the chunk 4–6 plan's own Q6 ruling explicitly requiring the line ("the class stays exported, and `packages/hydration/CLAUDE.md` says who throws it"). This documentation gap is now overdue against an explicit ruling, not merely open |
| Two kinds of probe, and neither substitutes for the other | 1321 | States that the type suite (fast, catches a malformed declaration and a bad call site) and a sad-path run (slow, catches a refused connection and a failed write) are two separate axes of coverage, and a green type suite proves nothing about the sad-path table | **DELIVERED chunk 3 (part)** | The type-suite axis has moved from "instrument only" to "instrument plus most of its own fixtures" — see "What the type prototype proved" above. The sad-path-run axis is unchanged: error classes and context-mining transformers exist, nothing throws them. Neither axis has a real sad-path RUN to point to yet, so the heading's own caution still holds |
| The combinatorial planning session, and what it works from | 1331 | A dedicated planning session (not a build-chunk step) works from Table 1 and Table 2 to find composition bugs the doc could not anticipate, writing every finding back into this document next to the verb it concerns rather than routing around it in a recipe | **PLANNED chunk 12** | A full plan now exists — `scrolls/seigelense/plans/recipes-chunk-12-combinatorial-rounds.md` (untracked) — turning both tables into more than fifty named cases (A1–A17, B1–B8, C1–C19, D1–D21), each with a wrong reading, an expected outcome, a way to tell, and the chunk it is blocked on. No round has RUN: every case is phrased "Expect"/"Tell by," with no recorded pass or fail. Its own §2 records which questions are already answered (CONFIRM cases, e.g. the nested-`filter` scope) versus genuinely open. Round D (the sad paths) is explicitly gated on the runner, which does not exist |
| Table 1 — what an ingredient MUST have, and what it MAY have | 1341 | For each ingredient property, states whether it is required or optional, what its absence does, and what a wrong value does — the reference the combinatorial rounds check each property against | **PLANNED chunk 12** | The chunk 12 plan's §3.1 maps every Table 1 row to a case number, skipping only the cells the specification itself calls "does not compile" (already proven at parse time by chunk 1's contracts). No case has been run |
| Table 2 — the rounds, and the question each one asks | 1356 | Four rounds — A (one property at a time), B (nesting depth and scope), C (composing pairs/triples of chainables, e.g. `filter` over rows a same-plan transition just minted), D (driving the sad-path table for real) — each producing findings written into this document, never a recipe worked around a finding | **PLANNED chunk 12** | The chunk 12 plan's §3.2 maps every Round A/B/C/D bullet to a case, and confirms rather than re-opens the nested-`filter`-scope question. No round has been run |
| Known gaps, named rather than discovered later | 1405 | Names the open gaps — an api-only plan's pre-flight, an untyped `fromSaved` field, no recipe-composition `include()`, the MCP wire's remaining wire-side validation, the unexercised `recording` route, no duplicate-name registry check, no sad path implemented or tested, a top-level orphan-link that compiles clean, production code minting uuids/timestamps that reach the screen, and the `ban-primitives` eslint carve-out — that block nothing but cost more the later they are found | **DELIVERED chunk 3 (part)** | The scaffolder, `ban-primitives` carve-out and nested-`filter`-scope closures stay closed, unchanged. **The "two lint rules are UNBUILT" gap is now substantially closed** — see its own row below. The MCP-wire gap and "no sad path is implemented" gap are unchanged |
| Known gaps, named rather than discovered later — the new gap this round added | 1414 | A typed plan output is scheduled work, not an abandoned requirement: "A plan is data, and one plan runs three ways" and "Every chainable, with an example" both require `saveRecordAs`'d rows to reach the plan's output each typed to its own record contract, and today's plan returns an untyped record the caller casts | **PLANNED chunk 3 (part)** | Unchanged: still scheduled as chunk 3b in the chunk 1–3 plan's §12, its own timeboxed pass after D4 lands and before chunk 4 starts. D4 has now landed (registry, recipe, `hydrationCreateBroker` all exist), so chunk 3b's own gate condition is satisfied and nothing blocks it starting except that nobody has |
| Known gaps, named rather than discovered later — the `recording`/serverless divergence this round named | 1417 | The `runs` line's write-only rule for `serverless` stands even for a `recording`-only ingredient, which therefore reports `needs a server: <ingredient>` though replaying a capture needs none | NOT STARTED | Unchanged. No ingredient anywhere in the tree — `dm-target.ts`, `sql-target.ts`, or `packages/siegelense-recipes`'s prep contracts — declares a `recording` route at all |
| Known gaps, named rather than discovered later — `copies:` has no target for an external tool's shape | 1418 | An ingredient copying a shape only an external tool writes — a Claude session transcript, written by the Claude CLI and only ever read in-repo — cannot honestly declare a `write` route until `copies:`'s definition is extended or an alternative is ruled | NOT STARTED | Unchanged. Part 5 itself has not ruled on chunk 7's own recommendation (`copies: 'claude-mock/bin/claude'`) |
| Known gaps, named rather than discovered later — two lint rules are UNBUILT | 1419 | Neither the no-DOM-handle rule nor the no-clock/no-random-source rule exists, so nothing enforces either constraint today; and an eslint config entry naming a rule that does not yet exist is a FATAL config error, not a no-op, so the entry must land WITH the rule, never before it | **DELIVERED chunk 7 (part)** | **Both rules now exist, are registered, are configured, and are proven firing against real ESLint.** `@dungeonmaster/ban-dom-handles-in-ingredients` and `@dungeonmaster/ban-nondeterminism-in-ingredients` (`packages/eslint-plugin/src/brokers/rule/ban-dom-handles-in-ingredients/`, `.../ban-nondeterminism-in-ingredients/`) each carry a real `RuleTester` suite, are registered in `eslint-plugin-create-responder.ts`'s rule map, configured at `'error'` in `config-dungeonmaster-broker.ts`, and categorized `pre-edit` in `dungeonmaster-rule-enforce-on-statics.ts` — every step the plugin's own `CLAUDE.md` requires for a new rule, confirmed by reading all five files. Both scope by filename (`-ingredient.ts`/`.tsx`, via `isIngredientDeclarationFileGuard` and `ingredientDeclarationStatics`) and each rule's own message is honest about its blind spots (a re-exported DOM wrapper, a destructured `import { randomUUID }`). **No `eslint.config.js` entry names either rule directly, and none is needed**: the config object they are added to (`configDungeonmasterBroker()`) is already spread into `eslint.config.js`'s repo-wide TypeScript rules block, so the fatal-config-error trap this row warns about was avoided by construction, not by omission. **Not yet exercised against a real ingredient file** — none exists in this repo yet, so the rule's very first real catch is still ahead of it |
| Mechanics the framework has to implement | 1428 | The runner walks a plan (a tree of `create`/`set`/`remove`/`saveRecord`/`filter`/`extra` ops) depth-first in declaration order, never crosses the MCP wire itself, implements no undo, splits refusals into pre-flight (computed off the plan alone: an unservable route, an unresolvable `fromSaved`, an unfilled `links` including one added at top level, or now a chain call needing an undeclared `query`/`update`/`remove` route) versus mid-run (a `filter` under-matching, a route throwing); every error class carries the ingredient and the recipe name | **DELIVERED chunk 1 (part)** | The Op table remains wholly shipped, unchanged (six kinds, `z.union` not discriminated, per the 10b amendment). Every pre-flight and mid-run error class remains defined and message-tested. New this round: several of the runner's own pure building blocks exist and are unit-tested standalone — `linkValuesTransformer`, `routeSelectTransformer`, `planFoldWritesTransformer`, `planSavedNamesTransformer`, `opDescribeTransformer`, `rowRefIngredientTransformer`, `savedRefResolveTransformer`, `fieldValuesResolveTransformer` — plus `routePlanContract`, `hydrationRunStateContract` and `hydrationRunResultContract`. **The runner that actually walks the tree and fires either group of refusal is still entirely absent** — `packages/hydration/src/brokers/` holds exactly four subfolders (`hydration/`, `ingredient/`, `recipe/`, `registry/`) and no `plan/` folder, confirmed by listing the package's full file tree |
| How siegelense finds recipes without importing them | 1491 | `siegelense` lists recipes by globbing `packages/hydration-recipes/dist/index.js`, dynamically importing it, and reading its exported manifest (name/description/inputs/routes per recipe) — reading compiled output only, and distinguishing an absent directory (installation problem) from an empty one (no recipes yet, a real answer) | **DELIVERED chunk 3 (part)** | Unchanged: `recipeManifestContract` shapes the listing data; no `packages/siegelense` file touched. **New context**: even a manual import from `siegelense` could not resolve `@dungeonmaster/hydration` today (see "Three packages" above) — though this heading's own mechanism is a directory glob plus dynamic import of `hydration-recipes`'s compiled output, not a static import, so that specific mechanism is unaffected by the missing symlink |
| Where each part lands in this repo's architecture | 1514 | Maps each piece to a folder type and package — `IngredientConfig`/`Plan`/`Op`/`Target`/the manifest as `hydration` contracts, the chain builder as `hydration` transformers, `createHydration`/`ingredient`/`registry`/`recipe`/the runner as `hydration` brokers, route helpers as `hydration` adapters, refusal errors as `hydration` errors, recipe discovery/listing and the `seed` step as `siegelense` brokers, the recipes-package scaffolder as `siegelense` (already built), each ingredient/recipe as its own folder in `hydration-recipes` — and requires `hydration`'s own `ban-primitives` eslint carve-out with a comment | **DELIVERED chunk 3 (part)** | The scaffolder and `ban-primitives` carve-out stay delivered. `hydration`'s `transformers/` and `brokers/` are now substantially populated — all of chunk 3's chain/declaration/registry/recipe brokers, plus chunk 4's groundwork transformers — confirmed by the package's own file tree. Route-calling `adapters/` (`fetch-post-adapter.ts`, `fs-ensure-write-adapter.ts`) also now exist alongside the chunk-1 test-only diagnostics adapter. Still unbuilt per this map: the runner itself (`brokers/plan/`), recipe discovery/listing and the `seed` step (`siegelense`, chunk 8), and every ingredient/recipe folder in `hydration-recipes` (chunk 7 — only prep field contracts exist, none wired to `ingredient()`) |
| The migration IS the validation, not cleanup afterwards | 1536 | Converting the real suites (118 e2e specs seeding domain state, 16 convertible integration domain-seeders) to the recipe framework is in scope and is how the design gets proven, run via `python3 scrolls/tools/seed-census.py` for the target list and the running call-site count, in the order integration-then-e2e-then-one-manual-siegelense-round | NOT STARTED | Chunks 9–10, after ingredients exist (chunk 7), which itself needs the runner (chunk 4) to exist first. Neither exists. `scrolls/tools/seed-census.py` was not re-run this round |
| The order, and why each step proves something the last one could not | 1594 | Step 1 (17 integration domain-seeders, files only) proves write routes and the no-`baseUrl` case; step 2 (118 e2e specs) proves api routes and one plan serving both callers; step 3 (one manual siegelense round) proves `seed`, `recipes {}` and params validation, which neither suite crosses | NOT STARTED | Chunks 9–10, then chunk 11's manual round |
| The rule that makes the conversion a proof | 1612 | A converted test keeps its assertions exactly, changing only its setup; a test needing a different assertion means the ingredient is wrong, not the test; a test that cannot convert is a finding written into this document; the one integration test whose SUBJECT is a `copies:` target (`quest-hydrate-broker.integration.test.ts`) is excluded, while tests that merely use that broker as setup are ordinary conversion targets | NOT STARTED | Chunks 9–10 |
| An ingredient touches STATE, never a screen | 1650 | An ingredient never holds a DOM handle; enforcement is layered — a runtime guard (only the minting instance's refs resolve, invalidated on navigation/reset/restart) as the layer that actually protects, a durable step-shape type structurally omitting `ref`, and a lint rule in the published `@dungeonmaster/eslint-plugin` (not `local-eslint`) flagging any DOM mention in an ingredient file | **DELIVERED chunk 7 (part)** | **The lint layer now exists**: `@dungeonmaster/ban-dom-handles-in-ingredients` bans a DOM ref, a DOM selector call, a DOM position read, and an import of `react` or `@playwright/test`, scoped to `-ingredient.ts`/`.tsx` files — registered, configured, categorized and proven against real ESLint (see the "two lint rules" Known-gaps row above for the full evidence). The runtime guard (only the minting instance's refs resolve) and the durable step-shape type (structurally omitting `ref`) both still need the runner and a real step contract, neither of which exists. Chunk 7 still needs a real ingredient for the rule to actually police — none exists. `packages/siegelense/CLAUDE.md` still states this rule as prose using "recipe" where the spec's own vocabulary says "ingredient" — unresolved, unrelated to this round's edits |
| Some claims can only be asserted in a BROWSER | 1693 | Ingredient tests come in three costs — files-only, a shared server instance, or a full server-plus-browser instance for a claim about what a URL renders — and a browser-asserted ingredient test is deliberately rare because the prelude's own `VERIFIED` run already proves rendering | NOT STARTED | Chunk 7 — needs a real ingredient whose claim is about what a URL renders |
| What this is, in industry terms | 1717 | Frames this as a test-data factory library (not the industry sense of "hydration"), keeping sequence/trait/lint/explicit-dependencies from the factory family while deliberately refusing random data, fluent builders as the wire format, static fixtures, and object-mother method-per-scenario naming; names the Cucumber failure mode it guards against, and the three mechanisms that guard against it (a run-proven prelude, structural parenting, plan-as-data) | NOT STARTED | Rationale, not a separate artifact — `defaults(i)` as sequence and extras as traits land under other rows above |
| Guidance for other repos | 1768 | For a consumer repo, only `@dungeonmaster/hydration`, the empty scaffolded `packages/hydration-recipes/`, and five stated conventions (one ingredient per entity from that repo's own contracts, an ingredient declares its routes with `copies:` on `write`, counts are `add(n)` never a name, the chain owns the index, two-of-anything must be distinguishable) travel — not this repo's own recipes | NOT STARTED | Nothing here ships as a consumer-usable convention yet: even setting the framework's own package aside, `node_modules/@dungeonmaster/` has no `hydration` entry, so a consumer repo could not resolve the import today either |

## Part 6 — The surface, consolidated (line 1785)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Each package needs a `../../CLAUDE.md`, and these are the entries | 1791 | Splits a set of named invariants (no `.first()`/`.last()`, no `querySelector` in eval, a command returns a reading not a verdict, the key reads own text nodes, a ref resolves only in its minting instance, an ingredient touches state never a screen, one verb sets state, `all` as an argument not a property, link-by-name, route-lives-on-ingredient, `hydration-recipes` absent from root deps, `run`-returns-status/`results`-returns-payloads, kill-the-group-skip-the-dead, `kill` never removes evidence, `dev:no-watch`) across `siegelense`'s, `hydration`'s and `hydration-recipes`'s own `CLAUDE.md` files, by which package breaks if the rule breaks | **DELIVERED chunk 1 (part)** | `packages/siegelense/CLAUDE.md` predates this ledger and carries the siegelense-specific lines, unchanged. `packages/hydration/CLAUDE.md` was re-read in full this round and still carries the same three recipe-specific lines as before ("ONE verb sets state," `all` as `add`'s second builder argument, "A route lives on the INGREDIENT"). **Still missing, confirmed again this round**: link-by-name, and who throws `HydrationTransactionRolledBackError` — the latter now overdue against the chunk 4–6 plan's own Q6 ruling (see the sad-paths row above). `packages/siegelense-recipes/CLAUDE.md` still does not exist, confirmed by listing the package's file tree — so the `hydration-recipes` root-dependencies line and "an ingredient touches state, never a screen" still have no package-level home |
| The calls this document uses | 1825 | `recipes {}` lists states with no instance needed, printing exactly the fields Part 5's "The listing `recipes {}` prints" specifies; `docs { for: … }` serves the tool's own instructions across seven scopes, per the sibling tooling document | NOT STARTED | `docs`'s seven scopes are tracked by the sibling `siegelense-tooling.md` ledger, not this one. `recipes {}` itself is chunk 8 (`siegelense`), not built by either chunk plan — the data it will print (`recipeManifestContract`, `planRunsTransformer`, `planMakesTransformer`) is further along; see the Part 5 rows above |
| Steps that are new | 1869 | A `seed` step runs a named recipe's plan against the current instance and returns the ids it made under `as:`; its `params` are typed by the `recipe` value in-process and validated by a generated zod schema before seeding over the MCP wire, naming the bad input and what that recipe takes | **DELIVERED chunk 3 (part)** | `seedStepContract` types the `seed` step's shape, unchanged from chunk 1. `plan-run-broker.ts` (chunk 4), what the step would call to actually run a plan, still does not exist. Returning ids and the wire-side validation both remain chunk 8 (`siegelense`) — no `packages/siegelense` file touched, so `step-statics.ts`'s `verbs.all` still lists no `seed` verb |
| A worked batch | 1895 | A batch step's output is addressed as three segments, `{<step>.<row>.<field>}` (never the two-segment `{step.field}`, which the tool should refuse); `stopOn: 'error'` is the default that halts on the first unexpected failure; a step declaring `expect: 'error'` that unexpectedly SUCCEEDS itself halts the batch as a finding; a plan's output is FLAT, with exactly one shape, carrying every `saveRecordAs`'d row under its own name | NOT STARTED | Chunk 8 — `as:`/`{step.row.field}` addressing and `stopOn`/`expect: 'error'` are `siegelense` runtime behavior. The FLAT-output sub-claim now has a real contract, `hydrationRunResultContract` (chunk 4), typing exactly `{}`-or-one-key-per-`saveRecordAs`, but the broker that would populate it (`opSaveRecordApplyLayerBroker`, gated behind the still-nonexistent runner) does not exist |
| Interleaving recipes and steps | 1954 | `seed` is placeable anywhere in a batch, not only as a prologue; a later `seed` in the same batch can take `params` from an earlier step's saved output; seeding while a page is already open is how a socket-driven live-update path gets exercised, while a non-socket-driven surface needs an explicit reload after such a seed | NOT STARTED | Chunk 8, same reason |

---

## Sections read and deliberately given no row

Per this round's instructions, these are directions to the build rather than requirements the build
must satisfy, so they carry no row above:

- `## The prompt that builds this` (line 10) and its subsections `### What this doc asks for, and what it
  does not` (87) and `### A suggested spine, for the first planner round` (107)
- `### This branch tracks master, and siegelense is already in it` (133) and its subsections `#### What
  that leaves to schedule` (152), `#### When to merge` (168), `#### The rename is its own step, and it is
  bigger than the folder` (201)

`docs { for: … }`'s scope content and the rest of the thirteen-call MCP surface (`start`, `run`, `results`,
`kill`, `capacity`, `profile`, `status`, `cleanup`, `prune`, `compare`, `snapshots`, `docs`) belong to
`siegelense-tooling.md` and are tracked in `build-ledger.md`. This ledger only adds the `recipes` call and
the `seed` step to that surface, both of which have rows above.
