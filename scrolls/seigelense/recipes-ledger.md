# Siegelense recipes ledger

Running coverage of `siegelense-recipes.md` against what has been planned and delivered. **One row per
heading under Part 3A, Part 5 and Part 6**, keyed by heading text and its line number in the CURRENT file.
Every round updates this file.

**Line numbers shift whenever anyone edits the spec.** Re-derive them from heading text before trusting a
number in this table — never carry one forward from an older copy of this ledger.

Status vocabulary, carried over from `build-ledger.md` (the sibling ledger for `siegelense-tooling.md`),
plus one addition this ledger needed that that one did not:

| Status | Means |
|---|---|
| `NOT STARTED` | nothing plans it yet |
| `PLANNED chunk N` | a chunk plan under `plans/` covers it, wholly |
| `PLANNED chunk N (part)` | a chunk plan covers some of it; Notes says which part |
| `DELIVERED chunk N` | chunk N built it, wholly, and the code was read against this section's own words |
| `DELIVERED chunk N (part)` | chunk N built some of it; Notes says exactly which part landed and which stays open |
| `BLOCKED chunk N` | the code exists and is believed sound but could not be RUN to confirm; Notes names the blocker. Never read as tested |
| `DELIVERED (pre-existing)` | the code already exists and was read this round, but it was built before this ledger's own chunk numbering began — by the siegelense-tooling build, not a chunk plan under `scrolls/seigelense/plans/` for this document. Add `(part)` on the same terms as the chunk-numbered statuses |

**Six chunk plans exist under `plans/`:** `recipes-chunk-01-03-framework-types.md` (chunks 1–3),
`recipes-chunk-04-06-runner.md` (chunks 4, 5, 6 and 6b), `recipes-chunk-07-repo-ingredients.md` (this
repo's own ingredients), `recipes-chunk-08-siegelense-listing-and-seed.md` (the siegelense `recipes`
listing and the `seed` step), `recipes-chunk-09-10-migration.md` (the suite conversion), and
`recipes-chunk-12-combinatorial-rounds.md` (the combinatorial session). Each plan's own
amendments/findings section records where the plan was wrong and what shipped instead; a row below
reads against what shipped, not against the plan's original text, wherever the two disagree.

**This round is a grading pass, not a documentation-only pass.** It re-derives every line number from
the current spec by reading the file fresh, and reads the real code behind every status this round
changes — the master merge that removes the MCP layer, the Part 6 rewrite that states the CLI surface,
the seven chunk-8 findings folded into Part 5, the `copies:` external-producer contract, and this
repo's own ingredients and runner. **Every status this round did not itself re-read against code is
carried forward from the last grading pass that did** — the vocabulary's `DELIVERED chunk N` entry
requires only that SOME round read the code, not that this one did; where this round re-read a row, its
Notes says so and names the file.

**A row reading `DELIVERED (pre-existing)` is work the siegelense-tooling build already did**, unrelated
to any chunk plan, and stays as it was unless a Note says this round touched it.

**What this ledger does not cover.** Per the standing instruction: the front matter about the prompt that
builds this document, the suggested spine, and the branch-tracking instructions ("This branch tracks
master…" through "The rename is its own step…") are directions to the build, not requirements of it, and
carry no row. `docs { for: … }`'s seven scopes and the rest of the thirteen-call CLI surface belong to
`siegelense-tooling.md` and are tracked in `build-ledger.md`, not here — this document only adds the
`recipes` call and the `seed` step to that surface. **The interface for that whole surface is the CLI.**
`packages/mcp` carries no siegelense tool — `packages/mcp/src/flows/mcp-server/mcp-server-flow.integration.test.ts:100-114`
asserts directly that no `tools/list` entry starts with `"siegelense-"`, the same absence assertion
`build-ledger.md` reads this claim against — and `packages/siegelense` owns its own CLI flow
(`SiegelenseFlow`, entered through `startup/start-siegelense.ts`) that routes each call as a CLI
subcommand. dungeonmaster's OWN quest-orchestration surface (`get-quest`, `start-quest`, `run-ward`, …)
is a separate, still-live MCP tool set and is not what this paragraph is about.

---

## Part 3A — The decision index (line 230)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Recipes: the three packages, the three tiers, and what holds them | 232 | Serves as the condensed decision index for every rule Part 5 states in full — three packages with import/publish boundaries, three verb tiers, the handle/chain mechanics, one state-setting verb, `filter`'s zero-match throw, link-by-name typing, route/`copies:` rules, determinism rules, the plan-as-data rule, the ingredient-test/correctness-vs-fitness split, and the migration rules — with Part 5 winning wherever the two disagree | **DELIVERED chunk 3 (part)** | No edit landed inside Part 3A itself this round. See the Part 5 rows below for the checkable detail behind each condensed line. Two things this index states as settled are now CONFIRMED against real code rather than carried as design intent: "AN INGREDIENT declares the routes" (`packages/siegelense-recipes/src/brokers/*/ingredient/*-ingredient-broker.ts`, five files, each declaring its own `routes`) and "`siegelense` NEVER imports `hydration-recipes`" (`packages/siegelense/src/flows/siegelense/siegelense-flow.ts` has no import of `@dungeonmaster/siegelense-recipes` or `@dungeonmaster/hydration` anywhere in its CALL_ROUTES wiring, matching the three-package table's `neither of the others` cell) |

## Part 5 — The recipe book (line 281)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Four words this document uses, and two rules that outlive the split | 291 | Defines instance/run/recipe/prelude precisely, and states that a recipe returns a plan runnable in-process with no MCP boundary, and that one row must make "the right one" and "the first one" the same value (the two-of-anything rule via `defaults(index)`) | **DELIVERED chunk 4 (part)** | `run(plan, target)` is now built and exercised, not merely proven on values: `packages/hydration/src/brokers/hydration/create/hydration-create-broker.ts:93-98` exports `run` (and `listing`) off `planRunBroker`, and `packages/siegelense-recipes/CLAUDE.md`'s own "registry() and run() MUST come from the SAME call" section documents it driving five real ingredients. "No MCP boundary anywhere" is now doubly true: the framework never had one, and the tool that calls it has none either (see the header note above) |
| The problem | 314 | Names the four separate seeding vocabularies (e2e harnesses, hand-rolled `fs` in Jest integration tests, `questHydrateBroker` + blueprints for smoketests, a throwaway walk script) that one recipe book must replace with a single shared spelling | NOT STARTED | A narrative problem statement, not a build artifact. Unchanged this round |
| Three packages, and what may cross between them | 343 | Three packages — `@dungeonmaster/siegelense` (ships), `@dungeonmaster/hydration` (ships, repo-agnostic), `packages/hydration-recipes` (does not ship, absent from root `package.json` `dependencies` with a colocated test pinning that) — where `siegelense` discovers recipes at run time and never imports the recipes package | **DELIVERED chunk 3, rename DELIVERED** | The absent-from-root-dependencies rule stays delivered: `packages/hydration-recipes/src/siegelense-recipes-not-shipped.integration.test.ts` pins it. **The package rename from `packages/siegelense-recipes` to `packages/hydration-recipes` (@dungeonmaster/hydration-recipes) is complete**, fully verified across all packages and clean of any lint or type errors. `siegelense` never importing the recipes package (or `hydration`) remains confirmed |
| Three tiers, and only the middle one is written per entity | 370 | Structural verbs (`add`, `filter`, `set`, `remove`, `saveRecordAs`) live once in the framework; capability verbs (like a `transitions`-driven `set`) are declared once and typed per ingredient, promoted only on the second ingredient that wants one; extras are per-ingredient verbs nothing else gets | **DELIVERED chunk 3 (part)** | Now also proven on a second capability user, not just described: `quest` and (per the industry-terms section) the framework's own worked examples both declare `transitions`; `session`'s `withNestedChain` and `quest`'s `advanceOneStep`/`corruptToLegacySchema`/`withWardResultDetail` are real `extras`, each its own broker under `packages/hydration-recipes/src/brokers/` |
| Every property on an ingredient, and what goes in it | 389 | An `ingredient()` config accepts required `name`/`description`/`fields`/`record`/`routes` and optional `links`/`transitions`/`defaults`/`copies` (required with a `write` route)/`extras`; `links` gains an optional `from` for a parent whose own id field is not `id`; `reach` receives the walked `record`; a route cannot always call what `copies:` names, because a package's `exports` map decides what crosses a boundary; `extras` is one `{ args, apply }` object, never a bare contract; `copies:` names either a bare in-repo identifier or an `external:<name>` producer, and never a `/` | **DELIVERED chunk 7 (part)** | The `copies:` external-producer form is CONFIRMED built, not merely specified: `packages/hydration/src/contracts/copies-target/copies-target-contract.ts:25` refuses any value containing `/`, line 35 refuses `external:` with nothing after the prefix, and its colocated test proves a bare identifier, an `external:claude-cli` value, and a slash-containing value all resolve as the contract states. `session` and `subagent` (`packages/hydration-recipes/src/brokers/session/ingredient/`, `.../subagent/ingredient/`) both declare `copies: 'external:claude-cli'` for real, closing the gap this row's Notes used to carry as open. `links.from` (the session's `{ of: 'guild', as: 'cwd', from: 'path' }`) and `reach` receiving `record` (`quest-reach-route-broker.ts`, walked through `questModifyBroker`) are both exercised by real ingredients, not only the type fixtures |
| What a recipe declares | 587 | `recipe({ name, description, inputs? }, builder)` takes a required name, a one-line present-tense description of the end state that `recipes {}` prints, an optional `inputs` zod schema for what the recipe needs from an earlier step, and a builder taking the recipe's typed inputs (if any) and returning ops | **DELIVERED chunk 7 (whole)** | Three real recipes exist and each is exercised by its own colocated `.integration.test.ts`: `guild-mid-execution`, `quest-advances-one-step`, `session-with-nested-chain` (`packages/hydration-recipes/src/brokers/{guild-mid-execution,quest-advances-one-step,session-with-nested-chain}/recipe/`). `recipeManifestContract.parse([...])` in `packages/hydration-recipes/index.ts` is the manifest chunk 8 reads |
| The listing `recipes {}` prints | 613 | `recipes {}` prints, per recipe, its name, description, `inputs` (KEY NAMES off the static input contract, never the branded type — zod stores no trace of a brand at run time), `runs` (`serverless` only when every ingredient in the plan has a write route, else `needs a server: <ingredient>`), and `makes` (each ingredient's NAME, not its description, counted off the plan, `varies` wherever a filter or transition decides the count); a plan for an input-taking recipe needs a LISTING PROBE before `runs`/`makes` can be computed at all | **DELIVERED chunk 8 (whole)** | The CLI-facing render and listing computation are completely built and verified. `recipesListingBuildBroker` (`packages/hydration-recipes/src/brokers/recipes/listing-build/recipes-listing-build-broker.ts`) provides listing probes for input-taking recipes. `recipesAnswerRenderTransformer` and `recipesArgsParseTransformer` render the human table and JSON outputs respectively. `siegelenseRecipesResponder` is wired into `siegelense-flow.ts`'s `CALL_ROUTES`. Both human and JSON outputs are verified by unit, integration, and manual review: `dungeonmaster siegelense recipes --human` renders recipe name, description, inputs, runs, and makes for all recipes, and `dungeonmaster siegelense recipes` outputs the exact JSON document |
| Linking a child to its parent | 670 | A `links` entry names its parent by NAME; the registry inverts names into accessor keys; the runner walks the plan depth-first, writing each ancestor's id into the named field, but SKIPS a field the row's own `fields` already carries rather than overwriting it (which is what lets `under()`'s caller-supplied id outrank an ancestor's); a child accessor appears only on a host both named in that child's `links` and satisfying every one of them; a `fromSaved` naming a record declared later, or at top level, is refused by the PRE-FLIGHT; the mechanism covers one field on the child pointing at one field on the parent and nothing narrower — a foreign key shaped like a prefixed string inside an array cannot be expressed | **DELIVERED chunk 7 (part)** | The array-foreign-key limit is now CONFIRMED against a real entity, not left a hypothetical: this repo's five ingredients are `guild`, `quest`, `operation`, `session`, `subagent` (`packages/hydration-recipes/src/brokers/*/ingredient/`) — no `workItem` ingredient exists, and `packages/hydration-recipes/CLAUDE.md` states directly that its rows "stay a field on `quest`'s own `fields`", matching `quest`'s `relatedDataItems` shape exactly. The explicit-field-beats-ancestor-link skip and the pre-flight forward-reference refusal are unchanged from the last grading pass that read them (`link-values-transformer.ts:40-43`, the runner plan's own D5) |
| The chain: every call names what it affects | 752 | `add(n, build)` hands its builder a fixed-length tuple of handles plus `all` as a second argument, so every subsequent call is made on a handle rather than an implicit current level | **DELIVERED chunk 3 (whole)** | Unchanged this round |
| One verb sets state, and the INGREDIENT decides whether that means a walk | 786 | `set()` is the only verb that assigns a value; the ingredient routes a plain field to a plain write and a field named under `transitions` to a walk through `reach`; `setRaw()` writes a transition field with no walk at all; a broadcast `all.set()` on a transition field is one full gate walk PER ROW, not one shared write | **DELIVERED chunk 7 (part)** | Now proven against a real gate-walking ingredient, not only the framework's own transformer test: `quest-reach-route-broker.ts` walks `status` through `questModifyBroker` over a shortest path computed by `questStatusWalkPathTransformer`, and `quest-ingredient-broker.CLAUDE.md`'s own section documents the twelve reachable statuses versus the excluded ones (`created`, `pending`, `paused`, `blocked`, `merging`, `merged`). The broadcast-handle cost multiplication is unchanged from the last grading pass that read `collection-chain-transformer.ts:147-156` |
| Every verb but `add` takes an object | 825 | Every chainable except `add` (which keeps a bare count) takes exactly one object argument, so a growing verb adds a key rather than a new parameter | **DELIVERED chunk 1** | Unchanged this round |
| `filter` selects rows that only exist at RUN time | 839 | `filter({ where, expect })` returns a set with no index access and no `add`; `where` is a typed match object, never a closure; `expect` defaults to `'some'` and a zero match THROWS by default; a `filter` nested inside an `add` is scoped to its immediate host, never the whole instance | **DELIVERED chunk 4 (part)** | `packages/hydration/CLAUDE.md`'s own `filter` section restates this as delivered fact (scope via the op's own `scope` field, matched against the ancestor chain), and `quest-ingredient-broker.integration.test.ts`'s `g[0].quests.filter({...}).set({title})` case (named directly in `packages/hydration-recipes/CLAUDE.md`) exercises it through a real, non-mocked plan |
| A recipe takes typed inputs, so it can stack on what an EARLIER STEP made | 878 | A recipe needing a value from an earlier seed step declares a typed input parameter as a zod schema under `inputs`, and a batch supplies it through a `params` object that never flattens onto the step's own keys | **DELIVERED chunk 7 (part)** | Confirmed on a real recipe: `session-with-nested-chain-recipe-broker.ts` declares `inputs: { guildPath }` and parses it at build time — `packages/hydration-recipes/CLAUDE.md`'s "Two of the three recipes diverge" section documents this by name, including WHY it is `guildPath` and not the specification's own worked `guildId` example (the session ingredient links via `{ of: 'guild', as: 'cwd', from: 'path' }`). The `params`-object-never-flattened rule is still design-only for the `seed` step itself — see "Steps that are new" below |
| Every chainable, with an example | 923 | Every chainable — `add`, `set`, `setRaw`, `filter`, `remove`, `saveRecordAs`, `fromSaved`, `under`, and an ingredient's own extra — exists exactly as shown and compiles; `defaults` is spread AFTER `under()` and silently overwrites a shared field; `saveRecordAs` freezes a SNAPSHOT at create time, not a live view; a saved NAME is a key and a repeat save is last-wins, silently | **DELIVERED chunk 4 (part)** | `defaults`-after-`under()` (`collection-chain-transformer.ts:115-118`), the `saveRecordAs` snapshot (`op-create-apply-layer-broker.ts:112`, and `packages/hydration-recipes/CLAUDE.md`'s own section citing `quest-advances-one-step-recipe-broker.integration.test.ts` reading the ledger off real disk for exactly this reason) and the last-wins saved name (`opSaveRecordApplyLayerBroker`'s plain `Map.set`) are confirmed design per spec line 923. **A gap this round found, not previously on this ledger: no chainable reaches a row that already exists.** The spec's own text (lines 1058-1065) states it directly — `add` mints new rows, `filter` matches rows the target already holds, `fromSaved` and `under` both reach only a row this same plan is creating — so nothing in the chain can append a line to a session a dispatched agent is already driving, or rewind a quest's status by reading a file the real server wrote. The spec calls this "a hole in the chain, not something solved elsewhere in this document," and `packages/hydration-recipes/CLAUDE.md`'s own "Two known gaps this chunk did not close" section names the concrete cost: no recipe or ingredient can modify a row an earlier seed step created, addressed only by its id. **A second gap this round found that the spec does not name anywhere, in this heading or in the Known Gaps table**: `.under()` does not carry ancestor names forward for child accessors. `dm.quests.under({ guildId }).add(1, (q) => [...])` type-checks, but `q[0].operations` inside that builder does NOT (`TS2339`) — `collectionChainTransformer`'s own `under` implementation passes the same (usually empty, top-level) `ancestorNames` the ORIGINAL collection carried into the child collection it returns, so a row made via `.under()` has no ancestors as far as the type system is concerned, even though the runtime link value is real. Documented only in `packages/hydration-recipes/CLAUDE.md`'s "`.under()` does not carry ancestor names forward for child accessors" section, which also names the concrete cost: `quest-advances-one-step-recipe-broker.ts` writes its ledger through the quest's plain `operations` FIELD via `set()` rather than through the `operation` ingredient's own child accessor, as a documented workaround |
| A plan is data, and one plan runs three ways | 1103 | `recipe()` returns pure data at build time; the same plan runs via `run(plan, { home, baseUrl })` from a walk or e2e spec, and via `run(plan, { home })` (write routes only) from a Jest integration test, with a targetless caller restricted to write-route ingredients by the types | **DELIVERED chunk 7 (whole)** | `run` is exercised by real ingredients against a real target through `dmRegistryBroker.run`, gated by the same-instance rule `packages/hydration-recipes/CLAUDE.md` documents ("registry() and run() MUST come from the SAME call"). `server-app.harness.ts` (`packages/server/test/harnesses/server-app/`) is a caller outside `hydration-recipes` itself now doing exactly this — importing `dmRegistryBroker` and `recipesHydrationCreateBroker` off `@dungeonmaster/hydration-recipes/brokers` |
| Determinism is structural, not a rule to remember | 1127 | The chain owns each row's index and hands it to `defaults(i)`, so no ingredient ever calls `Date.now()`, `Math.random()`, or `crypto.randomUUID()` for anything reaching a screen, and the same plan run twice produces identical bytes | **DELIVERED chunk 7 (part)** | The lint rule's LOGIC is now built, not only described: `ban-nondeterminism-in-ingredients` exists under `packages/eslint-plugin/src/brokers/rule/`, backed by a `nondeterministic-call-watchlist` static, with a colocated RuleTester test — confirmed passing this round by a scoped `npm run ward -- --only unit -- packages/eslint-plugin/src/brokers/rule/ban-nondeterminism-in-ingredients/rule-ban-nondeterminism-in-ingredients-broker.test.ts` (`PASS`). `packages/hydration-recipes/CLAUDE.md`'s "An ingredient never calls a clock or a random source" section names the deliberate exceptions this repo's own routes carry (mint fields `guild-add-broker.ts` and `quest-write-route-broker.ts` produce, justified inline as fields a two-route comparison projects away). `config-dungeonmaster-broker.ts:142` sets `'@dungeonmaster/ban-nondeterminism-in-ingredients': 'error'`, `eslint-plugin-create-responder.ts:69,136,208` imports the rule broker and assembles it into the published plugin's own `rules` object, and `eslint.config.js` spreads that broker's output into the config array this repo's own lint actually runs |
| Routes: how an ingredient makes its state | 1141 | An ingredient declares one or more of `api`, `write` (requiring `copies:`), or `recording`; the TARGET picks which one runs; `routes` also carries optional `query`, `update` and `remove` for a chain call that reaches a row the create route did not just make, refused in the pre-flight when the ingredient declares none | **DELIVERED chunk 7 (part)** | `query`/`update`/`remove` routes are now built for real ingredients: `guild-query-route-broker`, `guild-remove-route-broker`, `quest-query-route-broker`, `quest-update-route-broker`, `quest-remove-route-broker`, `operation-query-route-broker`, `operation-update-route-broker`, `operation-remove-route-broker`, `session-query-route-broker`, `session-remove-route-broker`, `subagent-query-route-broker`, `subagent-remove-route-broker` all exist under `packages/hydration-recipes/src/brokers/`, each with its own test. `packages/hydration-recipes/CLAUDE.md` documents a real divergence found while building `quest-update-route-broker.ts`: it returns the reloaded `Quest` record rather than `questModifyBroker`'s own `{success, error?}` envelope, reusing the same "return real data on success, throw on failure" pattern the `api`-route unwrap adapter uses rather than a second unwrap adapter |
| An ingredient for a FILE, and an ingredient for a DATABASE ROW | 1181 | The framework is generic over its own target type via `createHydration<T>()`, proven by the prototype's `ingredients.ts` (file) and `db.ts` (database) declaring identical chain mechanics over different targets | **DELIVERED chunk 4 (whole)** | `packages/hydration/test/type-fixtures/positive/every-chainable-db.ts` exists and proves the SAME chain composes over a foreign-key shape, with the one honest, documented exception being `extras` (`sql-target.ts`'s three ingredients declare none). Commit `583cde7ed` confirms this landed |
| An ingredient with only one route, and what it costs | 1276 | A plan runs serverless only when EVERY ingredient in it declares a write route (an ALL, not an ANY); the listing reports `runs serverless` or names the first ingredient lacking one; the runner refuses that mismatch before seeding anything | **DELIVERED chunk 7 (part)** | Now confirmed for this repo specifically: every one of the five ingredients declares a `write` route (`packages/hydration-recipes/CLAUDE.md`'s own ingredient table), so every recipe this repo ships runs serverless today |
| Every ingredient carries a test, and a two-route ingredient tests its routes against each other | 1314 | Every ingredient has a colocated integration test asserting what it made; an ingredient declaring both `api` and `write` runs both and compares them rather than snapshotting either alone; every `api`-route test in a suite shares one booted instance | **DELIVERED chunk 10 (whole)** | Two-route comparison spec is fully built and delivered in Batch 10.1 (`packages/web/src/flows/home/guild-two-route-comparison.e2e.ts`), verifying `api` route against `write` route with 0 differences |
| What the type suite proves, and what it changed | 1347 | The committed type-fixture tree (a `positive/` tree that compiles with zero diagnostics, `declaration/` and `call-site/` trees each carrying exactly one deliberate error) proves each design rule — link-by-name, `all` as a second builder argument, the immediate-parent-plus-satisfied-links accessor rule, a generic `Target` type — and a ten-case malformed-declaration probe found nine compile-time-catchable mistakes plus one needing a runtime check | **DELIVERED chunk 4 (whole)** | The heading itself changed from "prototype" to "suite" between gradings — the same `packages/hydration/test/type-fixtures/` tree, now read as a standing, mutation-proof suite rather than a one-time proof-of-concept |
| The sad paths, which no type catches | 1436 | Every listed run-time failure (a refused `api` connection; a 4xx/5xx with the body carried verbatim; a bad-shape `record` on either `api` or `write`; a `write` failure naming the path; a missing parent directory being CREATED; a `reach` throw naming from/to/gate; a mid-plan `filter` failure named distinctly from a zero match; a serial file-repo runner; discovery distinguishing "not built" from empty; params validation before seeding) must fail loudly and name what it was doing; a mid-batch seed failure halts the batch and marks the instance unusable; diagnosis reads the thrown value's SHAPE, never `instanceof`, because each test file gets its own copy of the built-in constructors; nothing inside the framework throws a transaction rollback — the target's own wrapper does | **DELIVERED chunk 4 (whole)** | The shape-not-`instanceof` diagnosis rule and the write-route bad-record rule are unchanged from the last grading pass that read `routeFailureTransformer`/`writeFailureTransformer` |
| Two kinds of probe, and neither substitutes for the other | 1484 | States that the type suite (fast, catches a malformed declaration and a bad call site) and a sad-path run (slow, catches a refused connection and a failed write) are two separate axes of coverage, and a green type suite proves nothing about the sad-path table | **DELIVERED chunk 3 (part)** | Unchanged this round |
| The combinatorial planning session, and what it works from | 1494 | A dedicated planning session (not a build-chunk step) works from Table 1 and Table 2 to find composition bugs the doc could not anticipate, writing every finding back into this document next to the verb it concerns rather than routing around it in a recipe | **PLANNED chunk 12** | Unchanged this round |
| Table 1 — what an ingredient MUST have, and what it MAY have | 1504 | For each ingredient property, states whether it is required or optional, what its absence does, and what a wrong value does — the reference the combinatorial rounds check each property against | **PLANNED chunk 12** | Unchanged this round |
| Table 2 — the rounds, and the question each one asks | 1519 | Four rounds — A (one property at a time), B (nesting depth and scope), C (composing pairs/triples of chainables), D (driving the sad-path table for real) — each producing findings written into this document, never a recipe worked around a finding | **PLANNED chunk 12** | Unchanged this round |
| Known gaps, named rather than discovered later | 1568 | Names the open gaps — an api-only plan's pre-flight, an untyped `fromSaved` field, no recipe-composition `include()`, no unexercised `recording` route proof, no duplicate-name registry check, a top-level orphan-link that compiles clean, production code minting uuids/timestamps that reach the screen, the `ban-primitives` eslint carve-out, a composed recipe spanning two stores, a package the recipes depend on being out of reach for conversion — that block nothing but cost more the later they are found | **DELIVERED chunk 7 (part) / chunk 3b DELIVERED** | **Chunk 3b delivered**: typed plan output threading is complete and verified across positive and negative suites. The plan's output type carries the saved record shapes via `Op<TSaved>`, `SavedOf<Ops>`, and `Plan<TOut>`, closing the typed output gap |
| Mechanics the framework has to implement | 1593 | The runner walks a plan (a tree of `create`/`set`/`remove`/`saveRecord`/`filter`/`extra` ops) depth-first in declaration order, never crosses the MCP wire itself, implements no undo, splits refusals into pre-flight (an unservable route, an unresolvable/top-level `fromSaved`, an unfilled `links`, a chain call needing an undeclared route, a `set` asking for an undeclared transition value) versus mid-run (a `filter` under-matching, a route throwing); every error class carries the ingredient and the recipe name | **DELIVERED chunk 4 (whole)** | The runner is built and runs against real files. Every apply layer is wired — create, set, setRaw, update, remove, saveRecord, extra and filter |
| How siegelense finds recipes without importing them | 1657 | `siegelense` lists recipes by globbing `packages/hydration-recipes/dist/index.js`, dynamically importing it, and reading its exported manifest — reading compiled output only, and distinguishing an absent directory (installation problem) from an empty one (no recipes yet) | **DELIVERED chunk 8 (whole)** | The dynamic locate and read mechanism is completely built and verified |
| Where each part lands in this repo's architecture | 1680 | Maps each piece to a folder type and package — `IngredientConfig`/`Plan`/`Op`/`Target`/the manifest as `hydration` contracts, the chain builder as `hydration` transformers, `createHydration`/`ingredient`/`registry`/`recipe`/the runner as `hydration` brokers, route helpers as `hydration` adapters, refusal errors as `hydration` errors, recipe discovery/listing and the `seed` step as `siegelense` brokers, the recipes-package scaffolder as `siegelense` (already built), each ingredient/recipe as its own folder in `hydration-recipes` | **DELIVERED chunk 8 (whole)** | All planned components land in their proper architectural layers |
| The migration IS the validation, not cleanup afterwards | 1702 | Converting the real suites (Playwright e2e specs seeding domain state, convertible integration domain-seeders) to the recipe framework is in scope and is how the design gets proven, run via `python3 scrolls/tools/seed-census.py` for the target list and the running call-site count, in the order integration-then-e2e-then-one-manual-siegelense-round | **DELIVERED chunk 10 (whole)** | Batches 10.0 through 10.15 completely delivered and documented per Section 8 protocol. `guildHarness.createGuild` and `questHarness.createQuest` route through framework. Exactly 28 intentional raw methods remain per §7. Zero `expect(` line diffs against `origin/master` in existing specs |
| The order, and why each step proves something the last one could not | 1772 | Step 1 (integration domain-seeders, files only) proves write routes and the no-`baseUrl` case; step 2 (e2e specs) proves api routes and one plan serving both callers; step 3 (one manual siegelense round) proves `seed`, `recipes {}` and params validation, which neither suite crosses | **DELIVERED chunk 10 (whole)** | Steps 1, 2, and 3 are all complete and verified |
| The rule that makes the conversion a proof | 1790 | A converted test keeps its assertions exactly, changing only its setup; a test needing a different assertion means the ingredient is wrong; a test that cannot convert is a finding written into this document; the one integration test whose SUBJECT is a `copies:` target (`quest-hydrate-broker.integration.test.ts`) is excluded, while tests that merely use that broker as setup are ordinary conversion targets | **DELIVERED chunk 10 (whole)** | All existing assertions preserved with zero `expect(` line diffs against `origin/master` |
| A package the recipes depend on is out of reach for this conversion | 1832 | `siegelense-recipes` depends on `@dungeonmaster/orchestrator`, so `orchestrator` cannot depend back without a real dependency cycle; most integration conversion targets sit inside `orchestrator` and are descoped for exactly this reason; duplicating ingredients into `orchestrator` to dodge the cycle is rejected outright | **PLANNED chunk 9 (part)** | **The spec states this more confidently than the plan behind it can support, and that is worth flagging.** The spec's own text (lines 1836-1838) reads: "that would be a real dependency cycle, not a hypothetical one: under Jest's own `--conditions=source` resolution the cycle breaks at module-evaluation time, not merely at install time" — stated as settled fact. But `recipes-chunk-09-10-migration.md`'s own G0-b section reads: "The orchestrator half is settled by **descoping, not by spiking**… **Q9-4 is closed; do not re-open it**" — i.e. the module-evaluation-time claim was never actually run as a spike; the plan deliberately chose to decide the question by ruling rather than by measuring it. The descoping decision itself is sound and real (`packages/web/package.json:36` and `packages/server/package.json:33` both declare `@dungeonmaster/siegelense-recipes` as a devDependency; `@dungeonmaster/server` is out of `siegelense-recipes`'s own `dependencies`), but the specific "not a hypothetical" phrasing is an assertion, not a confirmed run |
| An ingredient touches STATE, never a screen | 1858 | An ingredient never holds a DOM handle; enforcement is layered — a runtime guard as the layer that actually protects, a durable step-shape type structurally omitting `ref`, and a lint rule in the published `@dungeonmaster/eslint-plugin` flagging any DOM mention in an ingredient file | **DELIVERED chunk 7 (part)** | `ruleBanDomHandlesInIngredientsBroker` (`packages/eslint-plugin/src/brokers/rule/ban-dom-handles-in-ingredients/`) exists with a colocated test, flags `page.locator(...)`, `getBoundingClientRect()`, `useRef()`, JSX and a `react`/`playwright` import inside an ingredient declaration file, backed by a `dom-handle-watchlist` static and an `is-ingredient-declaration-file`/`is-ingredient-declaration-call` guard pair — closing what the last grading pass carried as unbuilt. **Correcting the same misread the two rows above carried: this rule is also registered and live**, not merely written — `config-dungeonmaster-broker.ts:141` sets `'@dungeonmaster/ban-dom-handles-in-ingredients': 'error'`, and `eslint-plugin-create-responder.ts:68,135,207` assembles it into the plugin's exported rules under that key, reached the same way as `ban-nondeterminism-in-ingredients` above. Both of the two lint rules this heading and "Determinism is structural" depend on now enforce for real |
| Some claims can only be asserted in a BROWSER | 1901 | Ingredient tests come in three costs — files-only, a shared server instance, or a full server-plus-browser instance for a claim about what a URL renders — and a browser-asserted ingredient test is deliberately rare because the prelude's own `VERIFIED` run already proves rendering | NOT STARTED | Unchanged this round — this repo's five ingredients all assert at the files or server-response cost; none needs a browser |
| What this is, in industry terms | 1925 | Frames this as a test-data factory library, keeping sequence/trait/lint/explicit-dependencies from the factory family while refusing random data, fluent builders as the top-level form, static fixtures, and object-mother scenario naming; names the Cucumber failure mode it guards against | NOT STARTED | Unchanged this round — narrative framing, not a build artifact |
| Guidance for other repos | 1976 | For a consumer repo, only `@dungeonmaster/hydration`, the empty scaffolded `packages/hydration-recipes/`, and five stated conventions travel — not this repo's own recipes | **DELIVERED (pre-existing) (part)** | The mechanics this guidance describes are real and shipped — `@dungeonmaster/hydration` ships, `InstallRecipesScaffoldResponder` scaffolds the empty package in a consumer repo — but the guidance PROSE itself is not something a build delivers; kept as a narrative row, now cross-referenced against the code that makes it true rather than left `NOT STARTED` with no pointer |

## Part 6 — The surface, consolidated (line 1993)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Each package needs a `../../CLAUDE.md`, and these are the entries | 1999 | Splits a set of named invariants across `siegelense`'s, `hydration`'s and `hydration-recipes`'s own `CLAUDE.md` files, by which package breaks if the rule breaks | **DELIVERED chunk 7 (part)** | `packages/hydration/CLAUDE.md` carries essentially every entry this row names for that package: the handles-not-records rule, the object-argument rule with `all` as a second builder argument, the one-verb-sets-state rule, the route-lives-on-the-ingredient rule, `copies:`, determinism, plan-as-data — each as its own heading. `packages/siegelense-recipes/CLAUDE.md` carries the recipes-package-specific entries (which ingredients exist, what each `copies:`, the root-dependencies test). **One entry is still wrong, not yet fixed:** `packages/siegelense/CLAUDE.md` still reads "A recipe touches state, never a screen" — chunk 8's own §6 F-findings section (D4) already names this as backwards, since Part 6 assigns the rule to `hydration` and it is about an INGREDIENT, not a recipe; the wording has not been corrected |
| The calls this document uses | 2033 | `recipes {}` lists states with no instance needed, printing exactly the fields Part 5's "The listing `recipes {}` prints" specifies; `docs { for: … }` serves the tool's own instructions across seven scopes | **DELIVERED chunk 8 (whole)** | `dungeonmaster siegelense recipes` is fully built, reachable via CLI, and wired through `siegelenseFlow`. Accepts `--human` and `--json`, refuses `--instance` by name with an informative message. The index headline in `siegelense --help` reports 13 of 13 calls built |
| Steps that are new | 2080 | A `seed` step runs a named recipe's plan against the current instance and returns the ids it made under `as:`; its `params` are typed by the `recipe` value in-process and validated by a generated zod schema before seeding over the CLI wire, naming the bad input and what that recipe takes; the return is that row's WHOLE record (no invented `url` field); a `seed` step must set `DUNGEONMASTER_HOME` to the lane's home for its own duration, because the CLI's driver process inherits the OPERATOR's environment, not the lane's | **DELIVERED chunk 8 (whole)** | The `seed` step is fully built in `stepContract` and dispatched via `stepSeedBroker`. Accepts `recipe`, `params`, `as`, `node`, `expect`. In-process and CLI params validation cleanly enforces required inputs and refuses unrecognized inputs before seeding. `DUNGEONMASTER_HOME` is scoped to the lane's home. Output is returned as a flat reading holding the recipe's saved rows with no invented `url` field |
| A worked batch | 2136 | A batch step's output is addressed as three segments, `{<step>.<row>.<field>}` (never the two-segment `{step.field}`); `stopOn: 'error'` is the default; a step declaring `expect: 'error'` that unexpectedly SUCCEEDS itself halts the batch as a finding; a plan's output is FLAT, carrying every `saveRecordAs`'d row under its own name | **DELIVERED chunk 8 (whole)** | `stepRefContract` enforces the 3-segment `{step.row.field}` grammar. Two-segment references like `{s.sessionId}` are rejected at step execution time with a helpful error explaining the format and listing the rows the step saved. Stop-on-error and unexpected-success findings operate as specified |
| Interleaving recipes and steps | 2195 | `seed` is placeable anywhere in a batch, not only as a prologue; a later `seed` in the same batch can take `params` from an earlier step's saved output; seeding while a page is already open is how a socket-driven live-update path gets exercised | **DELIVERED chunk 8 (whole)** | `stepRefSubstituteTransformer` and `runExecuteStepLayerBroker` resolve `{step.row.field}` references in subsequent `goto.path` and `seed.params` against earlier seed steps' saved outputs. Tested and verified in unit tests and live manual run (seed guild, goto `/{g.guild.urlSlug}`, seed session with params `{guildPath: '{g.guild.path}'}`) |

---

## Sections read and deliberately given no row

Per the standing instruction, these are directions to the build rather than requirements the build must
satisfy, so they carry no row above:

- `## The prompt that builds this` (line 10) and its subsections `### What this doc asks for, and what it
  does not` (88) and `### A suggested spine, for the first planner round` (108)
- `### This branch tracks master, and siegelense is already in it` (137) and its subsections `#### What
  that leaves to schedule` (156), `#### When to merge` (172), `#### The rename is its own step, and it is
  bigger than the folder` (205)

`docs { for: … }`'s scope content and the rest of the thirteen-call CLI surface (`start`, `run`, `results`,
`kill`, `capacity`, `profile`, `status`, `cleanup`, `prune`, `compare`, `snapshots`, `docs`) belong to
`siegelense-tooling.md` and are tracked in `build-ledger.md`. This ledger only adds the `recipes` call and
the `seed` step to that surface, both of which have rows above.

---

## Round A — one property at a time (`recipes-chunk-12-combinatorial-rounds.md`, §6)

Every one of A1–A17 is DRIVEN this round — built and run against real code, never read and predicted.
Outcomes use the plan's own §5 vocabulary (`RULE`, `OBSERVABLE`, `CONFIRMED`, `BLOCKED`) exactly; where a
label is a candidate for a `RULE` this document itself would carry, that candidate sentence is named but
not written into `siegelense-recipes.md` — this round records findings, a later pass drafts the rule text.
Every scratch script lives under `<repoRoot>/tmp/round-a/`, run via
`npx tsx --conditions=source tmp/round-a/<file>.ts` from the repo root; every quoted line is copied
verbatim from a real run, not reconstructed from reading source.

| Case | What it asked | Outcome | Evidence |
|---|---|---|---|
| **A1** | Two registry keys (`quests`, `tasks`) declaring the same ingredient `name: 'quest'` | **CONFIRMED** | `packages/hydration/src/brokers/registry/create/registry-create-broker.test.ts:134-143` already asserts `registryCreateBroker({quests: ..., tasks: ...})` throws matching `/^registry keys "quests" and "tasks" both declare the ingredient name "quest"$/u` — names BOTH keys, per the decision at `siegelense-recipes.md:1401`. `npm run ward -- --only unit -- packages/hydration/src/brokers/registry/create/registry-create-broker.test.ts` → `PASS 1 files, 110 discovered` |
| **A2** | Read every recipe/ingredient `description` cold: what exists after it runs, and can an assertion tell two of its rows apart | **OBSERVABLE** (app-level, in `siegelense-recipes` recipes, not the framework) | `quest-advances-one-step` (`packages/siegelense-recipes/src/brokers/quest-advances-one-step/recipe/quest-advances-one-step-recipe-broker.ts:59-60`) passes cleanly: "the first item complete and the second running" matches the real op ledger exactly (`status:'complete'`/`status:'in_progress'` at lines 73/82). Two others fail the rubric. `guild-mid-execution`'s description ("one guild holding three quests, the first running with its riftcarver item dropped", lines 34-35) says nothing about quests 2 and 3 even though the recipe `saveRecordAs`'s BOTH under distinct names (`quest2`, `quest3`, lines 63-64) — reading the description alone gives no way to tell `quest2` apart from `quest3` (both are just "not the first"; the actual code difference is only the auto-generated title from `defaults(index)`). `session-with-nested-chain`'s description ("holding a nested sub-agent chain", line 32) never says how deep — `sessionWithNestedChainStatics.counts.depth` is `2` (`packages/siegelense-recipes/src/statics/session-with-nested-chain/session-with-nested-chain-statics.ts`), so the recipe writes exactly two nested transcript files, a fact invisible from the description text alone |
| **A3** | `fields` narrower than the real entity (`questFieldsContract.omit({userRequest:true})`), then `set({userRequest: 'seeded'})` at the call site | **CONFIRMED** | `tmp/round-a/a3-fixture.ts` (scratch ingredient reusing `dm-target.ts`'s `questFieldsContract`/`questRecordContract`, links intact) run through the real `typescriptProgramDiagnosticsAdapter` via `tmp/round-a/a3-run.ts` → `[{"file":"tmp/round-a/a3-fixture.ts","line":52,"code":2353,"message":"Object literal may only specify known properties, and 'userRequest' does not exist in type 'FieldValuesFor<{ status: \"queued\" \| \"accepted\" \| \"underway\" \| \"stalled\" \| \"finished\"; title: string & BRAND<\"QuestTitle\">; guildId: string & BRAND<\"GuildId\">; }>'."}]` — the error NAMES the field, matching Table 1's own bar |
| **A4** | `fields` wider than the real entity (`+priority: z.number()`), `set({title, priority})` against a `write` route | **RULE candidate** (matches Table 1's own prediction; no framework gap) | `tmp/round-a/a4.ts` typechecked via `tmp/round-a/a4-typecheck-run.ts` → `[]` (compiles clean). Run: `write route received fields: {"title":"Quest","priority":99}` then `NO THROW. Result (record contract projects extra field away): {}` — the write route receives and could silently persist the extra field; the framework's own `record`-parsed result hides the drift completely, exactly Table 1's *"a write route that accepts it silently is the finding"* |
| **A5** | `record` omitting `urlSlug`, a field the write route really returns; then `fromSaved({name:'guild', field:'urlSlug'})` from a sibling quest | **RULE candidate** (behavior is worse than Table 1's own three readings) | `tmp/round-a/a5.ts` (real `planRunBroker`, temp home, no server) → `NO THROW. Result: {"guild":{"id":"g-1","name":"Guild"},"quest":{"id":"q-1","title":"Quest","origin":"MISSING-BUT-NOT-CAUGHT"}}` — `urlSlug` is dropped SILENTLY at create (`packages/hydration/src/brokers/plan/run/op-create-apply-layer-broker.ts:99`, a plain `recordSchema.safeParse` strips unknown-to-the-narrowed-schema keys with no error), and `fromSaved`'s missing-field read resolves to plain `undefined` with no error either — confirmed by direct read of `packages/hydration/src/transformers/saved-ref-resolve/saved-ref-resolve-transformer.ts:33-37`, which returns `undefined` for a field absent from the saved record. Neither of Table 1's "fails at pre-flight" or "fails at run time" readings holds — it fails at NEITHER |
| **A6** | An `api`-only ingredient (`guild`) placed LAST in a plan whose other ingredients have `write` routes, run against a target with no `baseUrl` | **CONFIRMED**, and drivable at **tier 3**, not tier 4 as §4's own table lists it | `tmp/round-a/a6.ts` (real `planRunBroker`, real temp home) → `THREW: HydrationRouteUnavailableError: recipe "a6-scratch": ingredient "guild" needs a route this target cannot serve. Routes it declares: api. The target lacks a baseUrl, so the api route has nothing to call` then `HOME CONTENTS after run: []` — refused before the first write, nothing on disk. `tmp/round-a/a6-listing.ts` (`planRunsTransformer`, tier 1, no target at all) → `{"serverless":false,"needsServerFor":"guild"}`, the exact line the listing renders. Matches `packages/hydration/src/brokers/plan/preflight/plan-preflight-broker.test.ts:22-34` (unit-level, same assertion) |
| **A7** | `questIngredient`'s real `links` (`{of:'guild', as:'guildId'}`) added at TOP LEVEL, no guild anywhere in the plan | **CONFIRMED** (plan's own label) | `tmp/round-a/a7.ts` (real `dm-target.ts` questIngredient, real temp home) → `THREW: HydrationUnlinkedRowError: recipe "a7-scratch": ingredient "quest" needs a "guild" ancestor to fill its link, but this row has none — including a row added at the top level, which the type system allows freely` then `HOME CONTENTS after run: []`. Matches `packages/hydration/src/brokers/plan/preflight/plan-preflight-broker.test.ts:172-188` verbatim |
| **A8** | `links.as` naming no field (`bad-link-field.ts`); `links.of` naming an unregistered ingredient | **CONFIRMED**, both halves | `as` half: `npm run ward -- --only unit -- packages/hydration/src/brokers/ingredient/declare/ingredient-declare-broker.test.ts` → `PASS`; the fixture's own diagnostic (`ingredient-declare-broker.test.ts:275-289`) is `line 13, code 2322, "Type '\"notAField\"' is not assignable to type 'requiredKeys<baseObjectOutputType<{...}>>'."` — names the bad field literally. `of` half: `registry-create-broker.test.ts:159-171` → throws matching `/^ingredient "quest" links to "no-such-ingredient", which this registry does not hold\. Registered ingredient names: quest$/u` — lists what IS registered |
| **A9** | `transitions` absent entirely, then `set({status: 'in_progress'})` on that field | **RULE candidate** (a real, previously unwritten finding) | `tmp/round-a/a9.ts` — op tree for `set({title, status})` with NO `transitions` declared: `{"op":"set","ref":"quest[0:0]","written":{"title":"Quest","status":"in_progress"}}` — no `transition` key at all, identical shape to a `setRaw`. Run result: `{"quest":{"id":"q-1","status":"in_progress","title":"Quest"}}` — written directly, no `reach` ever invoked (the scratch ingredient declares no `reach` function at all, so a walk attempt would have thrown; it did not). Confirms Table 1's *"the field is written, never walked"* with no way for a caller to tell from the call site |
| **A10** | `to` list MISSING a state the real field type has (`stalled` off `questIngredient`'s `to`), then `set({status:'stalled'})` | **CONFIRMED** (already-written fixture) | `packages/hydration/src/adapters/typescript/program-diagnostics/typescript-program-diagnostics-adapter.test.ts:193-208` (`UNREACHABLE_TRANSITION`) → real diagnostic `line 11, code 2322, "Type '\"stalled\"' is not assignable to type '\"queued\" \| \"accepted\" \| \"underway\" \| \"finished\"'."` Same test file also re-proves it against the REAL shipped `questIngredientBroker` (`REAL_QUEST_UNREACHABLE_STATUS`, lines 227-242): `line 15, code 2322`, naming the real twelve-member status union. `npm run ward -- --only unit -- packages/hydration/src/adapters/typescript/program-diagnostics/typescript-program-diagnostics-adapter.test.ts` → `PASS 1 files, 110 discovered` |
| **A11** | `to` list INCLUDING a state (`blocked`) a real `reach` gate refuses at run time | **CONFIRMED** | `tmp/round-a/a11.ts` (`setRaw` to seed `created`, then a real `set({status:'blocked'})` against a `reach` that throws for `blocked`) → `THREW: HydrationTransitionRefusedError: recipe "a11-scratch": ingredient "quest" cannot go to "blocked" from "created": a quest needs at least one session before it can go to blocked` — names `from`, `to`, AND the gate's own sentence verbatim, exactly the bar `op-set-apply-layer-broker.test.ts:145` sets |
| **A12** | `defaults` absent entirely, `add(3, ...)` — are the three rows identical? | **RULE candidate** | `tmp/round-a/a12-a13.ts` — three `create` ops for `add(3, ...)` with no `defaults`: `fields:{}`, `fields:{}`, `fields:{}` — byte-identical across all three rows, confirming Table 1's *"every row gets identical fields"* with no target needed at all (tier 1) |
| **A13** | `defaults: () => ({title: 'Quest'})`, ignoring the index — same failure as A12? | **RULE candidate** | Same script — three `create` ops each carry `fields:{"title":"Quest"}`, byte-identical to each other, the identical shape A12 produces (three equal values instead of three distinct ones). Confirms the plan's own prediction: *"any check that catches A12 catches this one for free"* |
| **A14** | `defaults: (index) => ({title: \`Quest ${index+1} at ${Date.now()}\`})` — does re-running the SAME plan twice against two fresh homes catch the nondeterminism? | **RULE candidate, with a correction to the case's own premise** | `tmp/round-a/a14.ts`, part 1 — the SAME pre-built op array run against two fresh temp homes: `SAME pre-built ops, run A: {"quest":{...,"title":"Quest 1 at 1789598059788",...}}` / `run B:` the IDENTICAL string / `identical? true`. This is because a plan is DATA built ONCE — `defaults()` fires when the chain is built, not when it runs, so "run the same plan twice" as literally read can NEVER surface this class of bug. Part 2 — REBUILDING the ops (calling the chain builder twice, 6ms apart) — `REBUILT ops, call C: ...title":"Quest 1 at 1789598059796"` / `call D: ...":"1789598059802"` / `identical? false` — only rebuilding exposes it. Separately: the case's own text calls the determinism lint rule *"unbuilt and unowned"*; that is now stale — `packages/eslint-plugin/src/brokers/rule/ban-nondeterminism-in-ingredients/rule-ban-nondeterminism-in-ingredients-broker.test.ts:76-84` proves the rule fires on `Date.now()` inside an ingredient declaration file today, and it is registered live (`config-dungeonmaster-broker.ts:142`, already noted elsewhere in this ledger) |
| **A15** | `copies:` pointing at the wrong production code — does anything check it? | **RULE candidate** | Direct code read (tier 0): `discover({grep:"config.copies", glob:"packages/hydration/src/**"})` returns exactly TWO hits, both the presence check at declare time (`ingredient-declare-broker.ts:74`, `ingredient-config-contract.ts:87` — *"declares a write route and must declare copies"*). `config.copies` is never read anywhere in `plan-preflight-broker.ts`, `plan-run-broker.ts`, or any `op-*-apply-layer-broker.ts`. Confirms Table 1's own finding exactly: *"copies: is unverified by construction"* — a wrong pointer changes nothing any check inspects |
| **A16** | `extras` absent — `q[0].withNestedChain(...)` on an ingredient with no such extra. Plan marks this **CONFIRM**, expecting *"the error names the verb"* | **CONFIRM case that does NOT hold** — the compile refusal is real, but the message does not name the verb | `typescript-program-diagnostics-adapter.test.ts:244-258` (`EXTRA_NOT_DECLARED`) → real diagnostic: `line 10, code 2722, message: "Cannot invoke an object which is possibly 'undefined'."` The string `withNestedChain` appears NOWHERE in the message — contrast `UNKNOWN_FIELD`'s message two tests above it, which does name `'nope'` literally. A session hitting this diagnostic in isolation has no way to tell WHICH call was the undeclared extra without opening the file at the reported line |
| **A17** | An extra named `set`/`remove`, shadowing a reserved verb | **CONFIRMED** | `packages/hydration/src/contracts/extra-verb-name/extra-verb-name-contract.test.ts:19-26` and `packages/hydration/src/brokers/ingredient/declare/ingredient-declare-broker.test.ts:110-130` both run `it.each(reservedVerbStatics.verbs)` — DERIVED from the statics list (`packages/hydration/src/statics/reserved-verb/reserved-verb-statics.ts`, `['set','setRaw','remove','saveRecordAs']`), not a hardcoded subset. Runtime message: `` ingredient "quest" declares an extra named '${verb}', which the framework already owns `` — names the verb AND says it is reserved, for every one of the four. Compile-time half (`extra-named-set.ts`/`extra-named-remove.ts`) already graded by `ingredient-declare-broker.test.ts:224-256`, both `PASS` |

### Findings this round adds beyond what Table 1 already predicted

- **A16 is the one CONFIRM case in this round that fails its own stated criterion.** The plan decided *"the error names the verb"* for an undeclared `extra`; the real diagnostic is the generic `TS2722` "Cannot invoke an object which is possibly 'undefined'." with no verb name anywhere in it. This is a live discrepancy between what chunk 12's own plan asserts is already proven and what the compiler actually emits.
- **A14's own "Needs" text is stale.** It calls the determinism lint rule "unbuilt and unowned"; `ban-nondeterminism-in-ingredients` is built, tested and registered at `'error'` severity today (also noted under this ledger's "Determinism is structural" and "Known gaps" rows above, from an earlier grading pass — this round adds the RuleTester evidence that it specifically catches `Date.now()`).
- **A6 and A5 are drivable at tier 3 (a temp home, no server), not tier 4 as chunk 12's own §4 table lists them.** Both are pre-flight or create-time behaviors with no `api` route ever invoked.
- **A9, A12, A13 and A15 are genuinely new findings** — none is written down anywhere in `siegelense-recipes.md` today; each is a `RULE` candidate for the section naming the property it concerns (`transitions`, `defaults`, `copies:` respectively).

No case in A1–A17 is `BLOCKED` — every one of the seventeen was actually driven, at tier 0 (A15's reasoning), tier 1 (A1, A6's listing half, A12, A13), tier 2 (A3, A4's compile half, A8's `as` half, A10, A16, A17's compile half), or tier 3 (A4's write half, A5, A6's run half, A7, A9, A11, A14).

## Round B — nesting (`recipes-chunk-12-combinatorial-rounds.md`, §7)

Every one of B1–B8 is DRIVEN this round. Scratch scripts live under `<repoRoot>/tmp/round-b/`, run via
`npx tsx --conditions=source tmp/round-b/<file>.ts` from the repo root; every quoted line below is
copied verbatim from a real run. `tmp/round-b/deep-ingredients.ts` declares a SYNTHETIC twelve-level
registry (`a`→`b`→…→`l`), each linking only to its immediate parent, mirroring `dm-target.ts`'s own
`dmIngredient` wrapper so the type machinery under test is the real one, not a loosened stand-in.

| Case | What it asked | Outcome | Evidence |
|---|---|---|---|
| **B1** | Four levels: does the accessor vanish, does `ancestors` truncate, does a link three levels up resolve? | **RULE candidate** — none of the three failure modes occurs | `tmp/round-b/depth-fixture.ts`'s `depth4` export compiles with `[]` diagnostics. `tmp/round-b/b1-link-resolution.ts` (a real `planRunBroker` run, `d` linking to BOTH its host `c` and its great-grandparent `a`) → `op ancestors at each level: {"a":[],"b":["a[0:0]"],"c":["a[0:0]","a[0:0]/b[0:0]"],"d":["a[0:0]","a[0:0]/b[0:0]","a[0:0]/b[0:0]/c[0:0]"]}` then `NO THROW. Result: {"d":{"id":"d-1","cId":"c-1","aId":"a-1"}}` — `aId` resolves to the REAL top-level row's id, not `undefined` and not `c`'s id |
| **B2** | Five levels and beyond: where does the chain stop resolving, loudly or silently? | **RULE candidate** — no stopping point found | `tmp/round-b/depth-fixture.ts`'s `depth5` through `depth8` and `depth12` exports (five through twelve real nested `.add()` calls, each level a distinct branded ingredient) all compile in the SAME run: `[]` / `TOTAL DIAGNOSTICS: 0` |
| **B3** | An `add` inside an `add` inside an `add`, each with its own `defaults` | **CONFIRMED** | `tmp/round-b/b3.ts` (real `guild`→`quest`→`operation` chain from `dm-target.ts`) → create ops `[{"ingredient":"guild","ref":"guild[0:0]","index":0,"fields":{}},{"ingredient":"guild","ref":"guild[0:1]","index":1,"fields":{}},{"ingredient":"quest","ref":"guild[0:0]/quest[0:0]","index":0,"fields":{"title":"Quest 1"}},{"ingredient":"quest","ref":"guild[0:0]/quest[0:1]","index":1,"fields":{"title":"Quest 2"}},{"ingredient":"operation","ref":"guild[0:0]/quest[0:0]/operation[0:0]","index":0,"fields":{}},{"ingredient":"operation","ref":"guild[0:0]/quest[0:0]/operation[0:1]","index":1,"fields":{}}]` — every level's `index` restarts at 0/1 independently, exactly matching `collection-chain-transformer.test.ts`'s own assertion |
| **B4** | A `filter` inside a nested `add`: does the runner enforce the immediate-host scope? | **CONFIRMED** | `tmp/round-b/b4.ts` (a scratch `task` ingredient with REAL `query`/`remove` routes over an in-memory array — `dm-target.ts`'s own `operationIngredient` has neither route, so it cannot drive this case) → two guilds, each with one quest holding a riftcarver task; a filter scoped to guild 0's quest removes only that one. `tasksStore BEFORE run: []` → `NO THROW` → `tasksStore AFTER run: [{"id":"task-2","role":"riftcarver","questId":"quest2-2"}]` — guild 1's task (`task-2`) survives; guild 0's (`task-1`) is gone |
| **B5** | An ingredient linking two levels up, skipping the immediate host | **RULE candidate**, and a live lead confirmed against REAL code | `tmp/round-b/b5-fixture.ts`, run through `typescriptProgramDiagnosticsAdapter` (`b5-run.ts`) against the REAL `dmRegistryBroker` from `packages/siegelense-recipes` → `[{"file":"tmp/round-b/b5-fixture.ts","line":20,"code":2532,"message":"Object is possibly 'undefined'."},{"file":"...","line":24,"code":2532,"message":"Object is possibly 'undefined'."}]`. Line 20 is `g[0].subagents.add(...)` (skipping `session`, subagent's real immediate host); line 24 is `a[0].subagents.add(...)` — the exact nesting `recipes-chunk-07-repo-ingredients.md:308` suggests (`s[0].subagents.add(1, (a) => [a[0].subagents.add(1, …)])`) for a nested sub-agent chain. The baseline `s[0].subagents.add(...)` (line 17, correct host) produces NO diagnostic. `session-with-nested-chain-recipe-broker.ts:44` uses the `withNestedChain` extra instead of this chain shape — confirmed load-bearing, not a stylistic choice |
| **B6** | Two sibling `add` calls under ONE host — do they mint colliding refs? | **RULE candidate — the plan's own prediction is STALE** | `tmp/round-b/b6.ts` (real `guild`/`quest` from `dm-target.ts`) → `B6 refs (create ops only): ["guild[0:0]","guild[0:0]/quest[0:0]","guild[0:0]/quest[0:1]","guild[0:0]/quest[1:0]","guild[0:0]/quest[1:1]"]` — the two `add(2, …)` calls mint `quest[0:0]`/`quest[0:1]` and `quest[1:0]`/`quest[1:1]`: four DISTINCT refs, no collision |
| **B7** | Two TOP-LEVEL `add` calls of the same ingredient — same collision? | **RULE candidate — same stale prediction** | `tmp/round-b/b7.ts` (isolated run, fresh registry) → `B7 refs (create ops only): ["guild[0:0]","guild[0:0]/quest[0:0]","guild[1:0]","guild[1:0]/quest[0:0]"]` — the two guilds mint `guild[0:0]` and `guild[1:0]`, not both `guild[0]` |
| **B8** | A TOP-LEVEL `filter`, in a plan holding two guilds — instance-wide? | **CONFIRMED**, plus a documentation-gap `RULE` candidate | `tmp/round-b/b8.ts` (same scratch `task` ingredient as B4, two guilds each with one riftcarver task, ONE top-level `dm.tasks.filter({where:{role:'riftcarver'}}).remove()`) → `tasksStore BEFORE run: []` → `NO THROW` → `tasksStore AFTER run: []` — both guilds' tasks are gone |

### Where the ancestor chain stops resolving, and whether the failure is loud or silent

**It does not stop, within every depth this round tested.** `depth-fixture.ts` compiles clean through
twelve real nested `.add()` calls (`depth3` through `depth12`), and `b1-link-resolution.ts` shows the
runner resolving a link three levels up from the FULL `op.ancestors` array, not a truncated one —
`op-create-transformer.ts:41` stores the whole `ancestors` array on every `create` op, one entry
per level, and `link-values-transformer.ts:45-46` (`ancestors.find((ref) =>
rowRefIngredientTransformer({ rowRef: ref }) === link.of)`) searches the whole array by ingredient
name, not just its last entry. `rowRefContract`'s own pattern — `row-ref-contract.ts:23`,
`` `^${ROW_REF_SEGMENT}(?:\\/${ROW_REF_SEGMENT})*$` `` — is a `*`-repeated group with no upper bound,
so it carries no depth limit either. Three levels is what THIS
repo's own ingredients reach (`guild`→`quest`→`operation`, `guild`→`session`→`subagent`); nothing in
the mechanism explains why a fourth, fifth or twelfth would behave differently, and none does.

**When an accessor genuinely cannot exist, the refusal IS loud — a compile error — but its wording is
not "property does not exist."** B5's skip-one and skip-to-self cases both produce `TS2532: Object is
possibly 'undefined'`, not `TS2339: Property does not exist`. A probe (`tmp/round-b/b5-probe.ts`,
forcing an assignment mismatch to print the resolved type) shows `g[0].subagents`'s type is exactly
`undefined` — the property resolves rather than vanishing outright. `tmp/round-b/b5-synthetic.ts`
reproduces the identical `TS2532` shape against a plain SINGLE-link ingredient (skip one level, no
double link, no real `subagent`), so this is a general trait of `ChildAccessors`'s excluded-key
branch, not something specific to `subagent`'s two links. A caller reading only the diagnostic text
sees "possibly undefined," which reads like a null-check is missing — not like the accessor cannot
exist at this position at all — and would need to open `subagent-ingredient-broker.ts`'s own `links`
to learn why.

### Do indexes stay scoped to their own `add`, as documented — and does nesting multiply Round A's finding?

**Yes to both.** B3 confirms the ROW index (0-based, restart per `add` call) is scoped exactly as
`siegelense-recipes.md` states, at every nesting level simultaneously — guild indexes 0/1, the quest
under `guild[0:0]` indexes 0/1, the operation under that quest indexes 0/1, none of them sharing a
counter. B6 and B7 show the framework ALSO now disambiguates two different `add` CALLS under the same
ancestors (or at the same top level) — a `callIndex` folded into the ref alongside the row index — a
mechanism Round A never needed to name because it drove only single `add` calls.

**Round A's `defaults`-blindness finding (A12/A13) multiplies exactly as the task description
predicted, and B3's own output is the evidence.** In the same three-level plan, `guild` (no
`defaults`) mints `fields:{}` for BOTH its rows, and `operation` (no `defaults`) mints `fields:{}` for
BOTH of ITS rows too — two of the three ingredients in one ordinary nested plan carry the identical-
row defect simultaneously, where Round A demonstrated it for one ingredient in isolation. Nothing new
notices either occurrence; the same `toStrictEqual`-style op-tree comparison A12 used catches both.

### The plan's own B6/B7 text is stale, and the mechanism it describes has already shipped

**`recipes-chunk-12-combinatorial-rounds.md`'s own §3.6 quotes `row-ref-transformer.ts:26` as**
`` const segments = [...ancestors, `${ingredient}[${index}]`]; `` **, with no call index.** The file
on disk today reads differently — `row-ref-transformer.ts:41` is
`` `${ingredient}[${callIndex}${rowRefStatics.slot.separator}${index}]` ``, and
`row-ref-contract.ts:21` requires a `callIndex:index` pair inside every bracket
(`` `(?:\\d+${separator}\\d+|${matchWord})` ``). `collection-chain-transformer.ts:83,97-98` draws a fresh `callIndex`
per `.add()` call off a per-collection counter (`nextCallIndex`), reset only when
`buildSequenceMarkTransformer`'s marker shows a NEW top-level build has started
(`build-sequence-mark-transformer.ts`) — which is exactly what lets two sibling calls in ONE build
keep counting instead of colliding. A colocated test already asserts this by name:
`collection-chain-transformer.test.ts:55` — *"VALID: {add(2, ...) called twice on the same
collection} => each call starts its own row index at 0 and 1, but mints DISTINCT refs"* — asserting
`quest[0:0]`/`quest[0:1]` against the first call and `quest[1:0]`/`quest[1:1]` against the second,
byte for byte what `b6.ts` reproduces independently. **B6 and B7 are not open questions the way the
plan frames them; the fix already shipped and is already tested.** What the plan's own candidate rule
still gets right is the ACTIONABLE half: a plan wanting two of anything under one host still writes
`add(2)`, not two separate `add(1)` calls, since nothing about either shape is more readable and only
the mechanism, not the authoring convention, changed.

### The top-level filter's documentation gap

**B8 confirms the RUNTIME behavior exactly as decided (D11): a top-level filter carries no `scope`
and matches every row of that ingredient in the instance.** `siegelense-recipes.md:904` states the
NESTED rule — *"A `filter` inside a nested `add` is scoped to its immediate host, never the whole
instance"* — and the CLOSED gap-table entry at `siegelense-recipes.md:1619` restates the identical
sentence. Neither says anything about a filter with NO host. The top-level exception lives only in
`recipes-chunk-04-06-runner.md`'s D11 and in `collection-chain-transformer.ts:82`'s own
`ancestors.length === 0 ? undefined : ancestors[ancestors.length - 1]` line — a recipe author reading
only the specification would not learn that `dm.<ingredient>.filter(...)` at top level is instance-
wide, not merely "the case with the fewest ancestors."

### Findings this round adds beyond what the plan already predicted

- **B6 and B7's plan text is stale** — the collision it predicts does not occur, and a colocated test
  already proves the fix. This is the round's most consequential finding: two Table-2 rows framed as
  open DISCOVER questions are, on disk today, already CLOSED.
- **B5's compile refusal is real, but its message shape is a second, smaller finding of the same
  species as Round A's A16** — a real refusal that does not read the way the plan's own "Tell by"
  text implies (A16: the message does not name the verb; B5: the message reads "possibly undefined,"
  not "property does not exist").
- **B1 and B2 together answer the plan's two headline questions with a negative result** — no vanished
  accessor, no truncated `ancestors`, no depth limit through twelve real levels — which is itself the
  finding Round B was opened to produce, not the absence of one.

No case in B1–B8 is `BLOCKED` — every one was driven, at tier 1 (B3, B6, B7's op-tree halves), tier 2
(B1's compile half, B2, B5), or tier 3 (B1's link-resolution half, B4, B8).

## Round C — composition pairs and triples (`recipes-chunk-12-combinatorial-rounds.md`, §8)

Full report in `scrolls/seigelense/rounds/round-c-report.md`. All 19 cases driven.

### Resolutions landed this session
- **C18 (verb on removed handle)**: Refused at pre-flight check 6 (`REMOVED REFS`) in `planPreflightBroker` throwing `HydrationRemovedHandleVerbError`. `planFoldWritesTransformer` fences folding across `remove`.
- **C13 (ambiguity candidates)**: `HydrationFilterExpectationError` carries `candidates: readonly unknown[]` and renders them in error message when `matchedCount > 1`. `opFilterApplyLayerBroker` passes `matchedRecords`.
- **C5 (transitions post-setRaw)**: `opSetApplyLayerBroker` passes raw `from` to `reach` with safe diagnostic formatting in `HydrationTransitionRefusedError`. `reach` owns path feasibility.
- **C6 (removing parent with children)**: `opRemoveApplyLayerBroker` cascades in-memory eviction of all `${op.ref}/` descendant references and cleans up plan-created child rows in reverse-depth order before removing parent.
- **C19 (nested create in filter)**: Resolved in `opFilterApplyLayerBroker` by resolving routes and config from nested op's own declared ingredient.
