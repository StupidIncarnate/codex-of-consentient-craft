# Siegelense recipes ledger

Running coverage of `siegelense-recipes.md` against what has been planned and delivered. **One row per
heading under Part 3A, Part 5 and Part 6**, keyed by heading text and its line number in the CURRENT file.
Every round updates this file.

**Line numbers shift whenever anyone edits the spec.** Re-derive them from heading text before trusting a
number in this table — never carry one forward from an older copy of this ledger.

Status vocabulary, carried over from `build-ledger.md` (the sibling ledger for `siegelense-tooling.md`),
plus one addition this ledger needed that that one did not:

| Status | Means |
|---|---|
| `NOT STARTED` | nothing plans it yet |
| `PLANNED chunk N` | a chunk plan under `plans/` covers it, wholly |
| `PLANNED chunk N (part)` | a chunk plan covers some of it; Notes says which part |
| `DELIVERED chunk N` | chunk N built it, wholly, and the code was read against this section's own words |
| `DELIVERED chunk N (part)` | chunk N built some of it; Notes says exactly which part landed and which stays open |
| `BLOCKED chunk N` | the code exists and is believed sound but could not be RUN to confirm; Notes names the blocker. Never read as tested |
| `DELIVERED (pre-existing)` | the code already exists and was read this round, but it was built before this ledger's own chunk numbering began — by the siegelense-tooling build, not a chunk plan under `scrolls/seigelense/plans/` for this document. Add `(part)` on the same terms as the chunk-numbered statuses |

**A chunk plan now exists, for chunks 1–3** — `plans/recipes-chunk-01-03-framework-types.md`. Its own §10
coverage table maps every Part 5 heading to where it lands, and the rows below read off it:
`PLANNED chunk 1`, `PLANNED chunk 2` or `PLANNED chunk 3` where that plan covers a heading wholly,
`(part)` where it covers only some of it, with Notes naming what remains — a later chunk, a different
package, or a scheduled sub-pass such as chunk 3b.

**A second chunk plan now exists, for chunks 4, 5, 6 and 6b** — `plans/recipes-chunk-04-06-runner.md`.
Its own §2 maps every sad-path row to an owner and a test; its §12 maps every Part 5 heading to where it
lands, on the same terms as the first plan's §10. Rows below read `PLANNED chunk 4`, `5`, `6` or `6b`
where that plan covers a heading wholly, `(part)` where it covers only some.

**That second plan also resolved five gaps a planning round found in the framework's own design, and
this round writes each resolution into Part 5 itself** — next to the verb it concerns, per the
document's own rule for a finding, rather than leaving it in the plan alone. The five: three chain
verbs (`remove`, `filter`, a non-foldable `set`) that reach rows `routes` had no way to make; an
`extras` value with arguments and no body; `reach` with no row to walk; a link naming no field on its
own parent; and two sad-path rows with no home — a `write` route's bad record, and who throws the
transaction-rollback error. Each has its own row below, alongside the heading it now amends.

**No chunk has been BUILT yet**, so no row below reads `DELIVERED chunk N` for either plan. A row
reading `DELIVERED (pre-existing)` is work the siegelense-tooling build already did, unrelated to
either chunk plan, and stays as it was unless a Note says this round touched it.

**What this ledger does not cover.** Per this round's instructions: the front matter about the prompt
that builds this document, the suggested spine, and the branch-tracking instructions ("This branch
tracks master…" through "The rename is its own step…") are directions to the build, not requirements of
it, and carry no row. `docs { for: … }`'s seven scopes and the rest of the thirteen-call surface belong to
`siegelense-tooling.md` and are tracked in `build-ledger.md`, not here — this document only adds the
`recipes` call and the `seed` step to that surface.

---

## Part 3A — The decision index (line 226)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Recipes: the three packages, the three tiers, and what holds them | 228 | Serves as the condensed decision index for every rule Part 5 states in full — three packages with import/publish boundaries, three verb tiers, the handle/chain mechanics, one state-setting verb, `filter`'s zero-match throw, link-by-name typing, route/`copies:` rules, determinism rules, the plan-as-data rule, the ingredient-test/correctness-vs-fitness split, and the migration rules — with Part 5 winning wherever the two disagree | **DELIVERED (pre-existing) (part)** | Unchanged this round. This table's own row on the recipes-package scaffolder is delivered: `InstallRecipesScaffoldResponder` at `packages/siegelense/src/responders/install/recipes-scaffold/install-recipes-scaffold-responder.ts` really creates `packages/siegelense-recipes/src/` when absent and leaves an existing one untouched (confirmed by reading the file) — it needs the rename described in "The rename is its own step," which is out of this ledger's scope. This table's row "`packages/hydration-recipes` must NOT sit in the root `package.json` `dependencies`… without the test the next scaffold run puts it back and nothing says so" is presently **VIOLATED, not merely unstarted**: `package.json`'s `dependencies` block still reads `"@dungeonmaster/siegelense-recipes": "*"` (confirmed by reading the file), and no colocated test pins its absence. Everything else this index names — the framework package, the three tiers, the chain mechanics — is now `PLANNED chunk 1/2/3 (part)`; see Part 5's own rows below for the checkable detail each one compresses to |

## Part 5 — The recipe book (line 277)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Four words this document uses, and two rules that outlive the split | 287 | Defines instance/run/recipe/prelude precisely, and states that a recipe returns a plan runnable in-process with no MCP boundary, and that one row must make "the right one" and "the first one" the same value (the two-of-anything rule via `defaults(index)`) | **PLANNED chunk 3 (part)** | The two-of-anything rule and `defaults(index)` are proven by `collectionChainTransformer`'s own test (chunk 3, D3). `fromSaved` ships as a public export via the package's barrels (chunk 3, §9). `run(plan, target)` itself — the thing that makes a recipe's plan runnable with no MCP boundary — is added to `createHydration` in chunk 4, not here |
| The problem | 310 | Names the four separate seeding vocabularies (e2e harnesses, hand-rolled `fs` in Jest integration tests, `questHydrateBroker` + blueprints for smoketests, a throwaway walk script) that one recipe book must replace with a single shared spelling | NOT STARTED | A narrative problem statement, not a build artifact. Replacing the four vocabularies is proven only once ingredients exist (chunk 7) and the migration lands (chunks 9–10) |
| Three packages, and what may cross between them | 339 | Three packages — `@dungeonmaster/siegelense` (ships), `@dungeonmaster/hydration` (ships, repo-agnostic), `packages/hydration-recipes` (does not ship, absent from root `package.json` `dependencies` with a colocated test pinning that) — where `siegelense` discovers recipes at run time and never imports the recipes package | **PLANNED chunk 3 (part)** | `hydrationCreateBroker` (chunk 3) instantiates the framework generically per repo target, and the barrels' `exports` map (chunk 3, §9 — `./contracts`, `./brokers`, `./transformers`, `./errors`, no `./adapters`) keeps `hydration`'s own surface to what a consumer should reach. The cross-package rules this heading also states — `siegelense` never importing `hydration-recipes`, and the root-`dependencies` exclusion with its colocated test — sit outside `hydration` and stay **VIOLATED, not merely unstarted**, per the Part 3A row above: `package.json` still lists `"@dungeonmaster/siegelense-recipes": "*"` |
| Three tiers, and only the middle one is written per entity | 366 | Structural verbs (`add`, `filter`, `set`, `remove`, `saveRecordAs`) live once in the framework; capability verbs (like a `transitions`-driven `set`) are declared once and typed per ingredient, promoted only on the second ingredient that wants one; extras are per-ingredient verbs nothing else gets | **PLANNED chunk 1 (part)** | `reservedVerbStatics` and `ingredientHandleContract`'s `ExtraMethods` (both chunk 1) type the three tiers and refuse an extra shadowing a built-in verb. `opExtraTransformer` (chunk 3) is what turns a declared extra into a runnable op |
| Every property on an ingredient, and what goes in it | 385 | An `ingredient()` config accepts required `name`/`description`/`fields`/`record`/`routes` and optional `links`/`transitions`/`defaults`/`copies` (required with a `write` route)/`extras`, exactly as the reference table and the worked `questIngredient` example show | **PLANNED chunk 1 (part)** | `ingredientConfigContract` and every A2a contract (chunk 1) type every property in the reference table, including the `copies`-required-with-`write` conditional type. `ingredientDeclareBroker` (chunk 2) is what actually REFUSES a malformed declaration at the value level — the ten-malformed-declaration rows. Three of this heading's own sub-properties changed this round; each has its own row below rather than being folded in here |
| Every property on an ingredient, and what goes in it — a link's `from` | 445 | A `links` entry gains an optional `from`, naming the field on the PARENT record that carries its id, defaulting to `'id'` — needed wherever a parent's own id field is not called `id`, as the session record is | **PLANNED chunk 1 (part)** | This round's own finding, resolving the runner plan's Q5. `linkSpecContract` gains the optional `from` field as a chunk 1 contract edit. The runner actually reading a parent's id off the field `from` names, rather than assuming `'id'`, is `link-values-transformer` (chunk 4, Group R1), not built yet |
| Every property on an ingredient, and what goes in it — `reach` receives the `record` | 464 | `reach` receives the `record` being walked, alongside `from`, `to` and `target`, because `add(3, …)` mints three rows and nothing else tells `reach` which one to walk | **PLANNED chunk 1 (part)** | This round's own finding, resolving the runner plan's Q4 — "one key on a type nothing imports yet, so the edit is free today." The runner passing the real record when it calls `reach` is chunk 5 ("`transitions` and `reach`"), not built yet |
| Every property on an ingredient, and what goes in it — `extras` is `{ args, apply }` | 501 | An `extras` entry is one object, `{ args, apply }` — never a bare arg contract — so a verb cannot be declared with arguments and no body to run | **PLANNED chunk 2 (part)** | This round's own finding, resolving the runner plan's Q3. `ingredient-config-contract.ts` and `ingredientDeclareBroker` (chunk 2) type and refuse the split shape. `op-extra-apply-layer-broker.ts` (chunk 4, Group R2, gated on this ruling) is what actually calls the declared `apply` at run time; not built yet |
| What a recipe declares | 535 | `recipe({ name, description, inputs? }, builder)` takes a required name, a one-line present-tense description of the end state that `recipes {}` prints, an optional `inputs` zod schema for what the recipe needs from an earlier step, and a builder taking the recipe's typed inputs (if any) and returning ops | **PLANNED chunk 3 (part)** | `recipeDefContract` (chunk 1) now types `name`/`description`/`inputs` — this round adds `inputs` to this heading's own property table, resolving the finding that a recipe's inputs are a zod schema read by three things: the printed line, the in-process type, and the wire validation. `recipeDeclareBroker` (chunk 3) turns the declaration into a callable plan-builder |
| The listing `recipes {}` prints | 561 | `recipes {}` prints, per recipe, its name, description, `inputs` (off the static input contract, now a zod schema), `runs` (`serverless` only when every ingredient in the plan has a write route, else `needs a server: <ingredient>`), and `makes` (each ingredient's description counted off the plan, `varies` wherever a filter or transition decides the count) | **PLANNED chunk 3 (part)** | `recipeManifestContract` (chunk 1) shapes name/description/inputs. `planRunsTransformer`/`planMakesTransformer` (chunk 3, D5) compute `runs` and `makes` off a plan, honoring the ALL-not-ANY rule. The actual `recipes {}` tool call that prints this is chunk 8 (`siegelense`), not built here |
| Linking a child to its parent | 592 | A `links` entry names its parent by NAME (never a reference, to avoid the `TS7022` inference cycle); the registry inverts names into accessor keys; the runner walks the plan depth-first, writing each ancestor's id into the named field; a child accessor appears only on a host both named in that child's `links` and satisfying every one of them; and a `fromSaved` naming a record declared later is refused by the runner's PRE-FLIGHT, before the first write, not by the chain as it builds | **PLANNED chunk 4 (part)** | `linkSpecContract` (chunk 1) types a link by parent NAME. `ingredientHandleContract`'s `ChildAccessors` (chunk 1) enforces the immediate-parent-plus-satisfied-links rule at the type level. `registryCreateBroker` (chunk 3) inverts links into accessor keys and refuses a dangling one (`RegistryDanglingLinkError`) or a duplicate name (`RegistryDuplicateNameError`). The runner plan is what actually builds the RUNNER writing each ancestor's id into the field (`link-values-transformer`, chunk 4, Group R1) and the pre-flight refusing an unresolvable `fromSaved` (`plan-preflight-broker.ts`, chunk 4) — the error classes were only DEFINED in chunk 1's `errors/`. This round also fixed a contradiction: the spec used to call a forward reference "a build error"; it is now stated as a pre-flight refusal, matching "Mechanics the framework has to implement" and Part 3A, which already agreed with the pre-flight reading |
| The chain: every call names what it affects | 656 | `add(n, build)` hands its builder a fixed-length tuple of handles plus `all` as a second argument, so every subsequent call is made on a handle rather than an implicit current level | **PLANNED chunk 3 (whole)** | `collectionChainTransformer` and `rowHandleChainTransformer` (chunk 3, D3) build exactly this, as one recursive knot written by a single agent |
| One verb sets state, and the INGREDIENT decides whether that means a walk | 690 | `set()` is the only verb that assigns a value; the ingredient routes a plain field to a plain write and a field named under `transitions` to a walk through `reach`; `setRaw()` writes a transition field with no walk at all | **PLANNED chunk 5 (part)** | `opSetTransformer`/`opSetRawTransformer` (chunk 3, D2) split a plain field from a transition into the op; `Settable` (chunk 1, on `ingredientHandleContract`) types which field is which and now also admits a `SavedRef` (see the `fromSaved` gap below). Actually WALKING a transition through `reach` is the runner plan's `op-set-apply-layer-broker.ts`, chunk 5, not built yet |
| Every verb but `add` takes an object | 722 | Every chainable except `add` (which keeps a bare count) takes exactly one object argument, so a growing verb adds a key rather than a new parameter | **PLANNED chunk 1 (whole)** | Every verb's signature on `ingredientHandleContract` (chunk 1) takes one object; `add` keeps its bare `<N extends number>(count, build)` count, with no `const` modifier |
| `filter` selects rows that only exist at RUN time | 736 | `filter({ where, expect })` returns a set with no index access and no `add`; `where` is a typed match object, never a closure; `expect` defaults to `'some'` and a zero match THROWS by default, mirroring the tool's own no-pick rule; and a `filter` nested inside an `add` is scoped to its immediate host, never the whole instance | **PLANNED chunk 6 (part)** | `matchedSetContract`/`filterArgsContract` (chunk 1) type the no-index/no-add set and the `where`/`expect` args. `opFilterContract`'s `scope` field (chunk 1, A2b) and `opFilterTransformer`/`matchedSetChainTransformer` (chunk 3) build the op, now carrying the scope decision this round closed — a nested `filter` records the immediate host's `RowRef`. `filter` actually READING LIVE STATE against that scope is `op-filter-apply-layer-broker.ts`, chunk 6 entire, not built yet |
| A recipe takes typed inputs, so it can stack on what an EARLIER STEP made | 765 | A recipe needing a value from an earlier seed step declares a typed input parameter as a zod schema under `inputs`, and a batch supplies it through a `params` object that never flattens onto the step's own keys | **PLANNED chunk 3 (part)** | `recipeDefContract`'s `inputs` and `seedStepContract`'s `params` shape (chunk 1) type this. `under()`'s chain behavior (chunk 3, `collectionChainTransformer`) supplies a link from a recipe input rather than an ancestor. A batch actually supplying `params` from an earlier step's output is chunk 8 (`siegelense`)'s `seed` step, not here |
| Every chainable, with an example | 810 | Every chainable — `add`, `set`, `setRaw`, `filter`, `remove`, `saveRecordAs`, `fromSaved`, `under`, and an ingredient's own extra — exists exactly as shown, and every snippet shown is copied out of `proto/usage.ts`, which the type check compiles | **PLANNED chunk 3 (whole)** | The positive fixture tree (chunk 3, Group E, after D3) replaces `proto/usage.ts` and exercises every chainable named here, graded by a real `tsc` run rather than `@ts-expect-error`. This round also documents a hole beside this heading: no chainable reaches a row that already exists — including an id the live app minted, not this recipe (`scrolls/seigelense/plans/recipes-seeding-survey.md` finding 2). Nothing in chunks 1–3 addresses it; it stays a documented limitation of the chain as specified |
| A plan is data, and one plan runs three ways | 928 | `recipe()` returns pure data at build time; the same plan runs via `run(plan, { home, baseUrl })` from a walk or e2e spec, and via `run(plan, { home })` (write routes only) from a Jest integration test, with a targetless caller restricted to write-route ingredients by the types | **PLANNED chunk 4 (part)** | `hydrationPlanContract` (chunk 1) and §3's no-I/O rule make `recipe()` return pure data, with a phantom `TOut` carrier for the plan's eventual typed output (see the typed-plan-output gap below). The three ways of RUNNING it — `run(plan, target)` itself, and its three target shapes proved in the runner plan's §8 — are `plan-run-broker.ts`, chunk 4, not built yet |
| Determinism is structural, not a rule to remember | 952 | The chain owns each row's index and hands it to `defaults(i)`, so no ingredient ever calls `Date.now()`, `Math.random()`, or `crypto.randomUUID()` for anything reaching a screen, and the same plan run twice produces identical bytes | **PLANNED chunk 3 (part)** | `rowRefTransformer` (chunk 3, D1) derives a row's build-time identity from its ancestor path and index rather than a clock or a uuid, and its own test asserts the same arguments twice give the identical string. `rowIndexContract` (chunk 1) types the index. The lint rule refusing `Date.now()`/`Math.random()`/`crypto.randomUUID()` over an ingredient's own folder is chunk 7's package, not chunks 1–3 or 4–6b, none of which write an ingredient file. The runner plan's own coverage check confirms no file across chunks 4–6 calls a clock or a random source either |
| Routes: how an ingredient makes its state | 966 | An ingredient declares one or more of `api` (the real code path), `write` (direct storage matching what the app would write, requiring `copies:`), or `recording` (replays a captured run); the TARGET, not the recipe, picks which route runs when more than one is declared; optionally `query`, `update` and `remove` for a chain call that reaches a row the create route did not just make, refused in the pre-flight when the ingredient declares none | **PLANNED chunk 4 (part)** | `hydrationRoutesContract` and `copiesTargetContract` (chunk 1) type every route and the `copies:` requirement. The route-CALLING adapters (`fetch-post-adapter.ts`, `fs-ensure-write-adapter.ts`) and the route-SELECTING `route-select-transformer.ts` are chunk 4, Group R0. This round also records a finding beside this heading: two existing seeders already disagree about whether a `write` route fires the production outbox side effect — a Playwright-side quest write appends `event-outbox.jsonl`, a Jest-side one does not (`recipes-seeding-survey.md` finding 8). The quest ingredient, not yet written (chunk 7), will have to pick one. The `query`/`update`/`remove` addition has its own row below |
| Routes: how an ingredient makes its state — `query`, `update` and `remove` | 989 | `routes` gains three optional entries — `query`, `update` and `remove` — so `remove()`, `filter()` and an unfoldable `set` each have a route to call; a plan calling a verb whose ingredient does not declare the matching route is refused in the pre-flight, naming the ingredient and the verb, via a dedicated error class | **PLANNED chunk 4 (part)** | This round's own finding, resolving the runner plan's Q2 — its own largest finding. The contract edit (`hydrationRoutesContract` gaining the three keys) is chunk 4's edit pass over `hydration-routes-contract.ts`, made once before Group R1 starts. `op-remove-apply-layer-broker.ts` (chunk 4, Group R2, gated on this ruling) implements `remove`; `op-update-apply-layer-broker.ts` and `op-filter-apply-layer-broker.ts`'s own query (both chunk 6, Group R5) implement `update` and `query`. The pre-flight refusal is `plan-preflight-broker.ts` via `route-select-transformer` (chunk 4, Group R0). The new error class is not yet named |
| An ingredient for a FILE, and an ingredient for a DATABASE ROW | 1006 | The framework is generic over its own target type via `createHydration<T>()` (a file-backed home, or a database-backed transaction), proven by the prototype's `ingredients.ts` (file) and `db.ts` (database) declaring identical chain mechanics over different targets | **PLANNED chunk 4 (part)** | `hydrationTargetContract` (chunk 1, constraining `TTarget extends { baseUrl?: Url }`) and `hydrationCreateBroker` (chunk 3) make the framework generic over its target type. The fixture tree's `dm-target.ts`/`sql-target.ts` (chunk 1, B2) prove the shape compiles. The runner plan's §8 is what actually PROVES the mechanics against disk and a transaction — `file-target.harness.ts`, `api-target.harness.ts` and `sql-target.harness.ts`, all chunk 4 |
| An ingredient with only one route, and what it costs | 1089 | A plan runs serverless only when EVERY ingredient in it declares a write route (an ALL, not an ANY); the listing reports `runs serverless` or names the first ingredient lacking one; the runner refuses that mismatch before seeding anything, naming the ingredient and the missing route | **PLANNED chunk 4 (part)** | `planRunsTransformer` (chunk 3) computes the ALL-not-ANY `serverless`/`needsServerFor` answer. The runner plan re-expresses it on top of `route-select-transformer` (chunk 4, Q10 ruling) so the two cannot disagree, and chunk 4 is what actually THROWS `HydrationRouteUnavailableError` before seeding anything, proved against a majority-writable plan (§3's ALL-not-ANY test, the exact shape an ANY gets wrong) |
| Every ingredient carries a test, and a two-route ingredient tests its routes against each other | 1127 | Every ingredient has a colocated integration test asserting what it made; an ingredient declaring both `api` and `write` runs both and compares them rather than snapshotting either alone; every `api`-route test in a suite shares one booted instance | NOT STARTED | Chunk 7 — an ingredient's own test needs a real ingredient, and none exists until `hydration-recipes` is written |
| What the type prototype proved, and what it changed | 1157 | The committed prototype at `proto/` (eight TypeScript files under `strict`, compiled by `node proto/check.mjs`, with `@ts-expect-error` on every case that must fail) proves each design rule the doc states — link-by-name to avoid `TS7022`, `all` as a second builder argument, the immediate-parent-plus-satisfied-links accessor rule, a generic `Target` type — and its ten malformed-declaration probe found nine compile-time-catchable mistakes plus one (duplicate names in a registry) needing a runtime check | **PLANNED chunk 3 (part)** | §4 of the chunk plan re-proves every one of these as a graded `tsc`-over-fixtures case rather than an `@ts-expect-error` line, plus six type-shape assertions for the five mutation-tested breaks and the `Settable` narrowing. It only holds once the whole build order (chunk 1's contracts through chunk 3's Group E fixtures) lands, since the fixtures exercise the finished chain. `scrolls/seigelense/proto/` itself (eight files, `check.mjs`) is unchanged and was not re-run this round |
| The sad paths, which no type catches | 1238 | Every listed run-time failure (a refused `api` connection; a 4xx/5xx with the body carried verbatim; a 2xx shape `record` rejects on `api` or `write`; a `write` failure naming the path; an absent parent directory being CREATED rather than failing; a `reach` throw naming from/to/gate; a mid-plan `filter` failure named distinctly from a zero match; a serial file-repo runner; discovery reporting "not built" rather than an empty list; and params validation before seeding) must fail loudly and name what it was doing, and a mid-batch seed failure halts the batch and marks the instance unusable rather than being retried | **PLANNED chunk 4 (part)** | Every mid-run error class (chunk 1, §6) is DEFINED here, each carrying the ingredient and the recipe name as this heading requires. The runner plan's §2 maps every row to an owner and a test: rows 1–5 and 9 to chunk 4, row 6 to chunk 5, row 7 to chunk 6, row 8 to chunk 6b; rows 10 and 11 stay `siegelense`'s own chunk 8. Two rows get no error class by design (a missing parent directory is CREATED; the runner is serial). This round adds two more rows this table did not have — a `write` route's bad record, and who throws the transaction rollback — each with its own row below |
| The sad paths, which no type catches — a `write` route's bad record | 1253 | A `write` route answering with a shape `record` rejects halts and names the field, exactly as a bad `api` response does, because a bad record poisons every saved record and every link the same way | **PLANNED chunk 4 (part)** | This round's own finding, resolving the runner plan's Q8. `op-create-apply-layer-broker.ts` (chunk 4) parses every route's return through `record`, `write` included. The ruling accepts that the thrown message still reads "answered 2xx" even for a `write` route, as a wording finding rather than a blocker |
| The sad paths, which no type catches — who throws the transaction rollback | 1258 | Nothing inside the framework throws the transaction-rollback error; the repo's own wrapper around `run()` does, because the framework names neither files nor SQL and does not own the transaction — the target IS the transaction | **PLANNED chunk 6b (part)** | This round's own finding, resolving the runner plan's Q6, which names the result — a class with no thrower inside its own package — a smell rather than a defect. Chunk 6b proves the shape against `sqlTargetHarness` (§2 row 8). `packages/hydration/CLAUDE.md` is where the plan says this must be documented; not written yet |
| Two kinds of probe, and neither substitutes for the other | 1278 | States that the type suite (fast, catches a malformed declaration and a bad call site) and a sad-path run (slow, catches a refused connection and a failed write) are two separate axes of coverage, and a green type suite proves nothing about the sad-path table | **PLANNED chunk 6b (part)** | §4's rationale for why a real `tsc` run over fixtures, not a type-level `Equal`/`Expect` helper, is the mechanism for most cases lands in chunk 3 (Group E). The sad-path-run half this heading says a green type suite proves nothing about is the runner plan's own §11 (chunk 6b) and its §7 cost-tier rationale, spanning chunks 4–6b generally — not built yet |
| The combinatorial planning session, and what it works from | 1288 | A dedicated planning session (not a build-chunk step) works from Table 1 and Table 2 to find composition bugs the doc could not anticipate, writing every finding back into this document next to the verb it concerns rather than routing around it in a recipe | NOT STARTED | This is its own session, scheduled as chunk 12 in the doc's own spine. Nothing to combine exists until ingredients and recipes exist (chunks 1–11). Per the runner plan's §12, Round D DRIVES the sad-path table but chunk 6b OWNS the rows it drives — so this session's Round D work on those rows, once chunk 6b lands, confirms rather than discovers |
| Table 1 — what an ingredient MUST have, and what it MAY have | 1298 | For each ingredient property, states whether it is required or optional, what its absence does, and what a wrong value does — the reference the combinatorial rounds check each property against | NOT STARTED | Reference table for chunk 12's session; not itself an artifact any chunk builds |
| Table 2 — the rounds, and the question each one asks | 1313 | Four rounds — A (one property at a time), B (nesting depth and scope), C (composing pairs/triples of chainables, e.g. `filter` over rows a same-plan transition just minted), D (driving the sad-path table for real) — each producing findings written into this document, never a recipe worked around a finding | NOT STARTED | Chunk 12. This round closed one of Round B's own open questions — the nested-`filter` scope bullet used to say "not specified anywhere"; it now says the scope is decided and names what a round should instead confirm (that the runner enforces it). The runner plan's own test for `op-filter-apply-layer-broker` (chunk 6) similarly answers one of Round C's questions ahead of the session — a filter over rows a same-plan transition just minted — so that round should confirm rather than re-open it too |
| Known gaps, named rather than discovered later | 1362 | Names the open gaps — an api-only plan's pre-flight, an untyped `fromSaved` field, no recipe-composition `include()`, the MCP wire's remaining wire-side validation, the unexercised `recording` route, no duplicate-name registry check, no sad path implemented or tested, a top-level orphan-link that compiles clean, production code minting uuids/timestamps that reach the screen, and the `ban-primitives` eslint carve-out — that block nothing but cost more the later they are found | **DELIVERED (pre-existing) (part)** | Unchanged from before: (1) the recipes-scaffolding gap is struck through CLOSED, and independently confirmed delivered — `InstallRecipesScaffoldResponder` at `packages/siegelense/src/responders/install/recipes-scaffold/install-recipes-scaffold-responder.ts`. (2) The `ban-primitives` gap is not struck through in the doc, but `eslint.config.js` already carries the scoped entry with the reasoning comment this gap asks for. **This round adds two more changes to this table**, neither of them a code delivery: the nested-`filter` scope gap is now struck through CLOSED, resolved as a DOCUMENT decision (`op-filter-contract`'s `scope` field, `PLANNED chunk 1` once built — see the `filter` row above); and the MCP-wire gap is narrowed, not closed — a recipe's `inputs` is now a declared zod schema, so what remains open is only wiring its parse into the `seed` step. The "no sad path is implemented or tested" gap now has a plan against it — see "The sad paths" row above — but nothing in it is built, so the gap itself stays open. See the next row for the one new gap the prior round added |
| Known gaps, named rather than discovered later — the new gap this round added | 1371 | A typed plan output is scheduled work, not an abandoned requirement: "A plan is data, and one plan runs three ways" and "Every chainable, with an example" both require `saveRecordAs`'d rows to reach the plan's output each typed to its own record contract, and today's plan returns an untyped record the caller casts | **PLANNED chunk 3 (part)** | Scheduled as chunk 3b in the chunk plan's §12 — its own timeboxed pass after D4 lands and before chunk 4 starts, threading saved names through every op producer's return type so `saveRecordAs({ name })` types the plan's output incrementally. Not a Known gap somebody picks up whenever: Q7's ruling is explicit that the Part 6 requirement stands unchanged. The plan's own abort condition — a `TS7022` inference cycle, a degraded negative case, a tuple check that stops firing — is to be written up as a finding if hit, not hidden by loosening a type. The runner plan's own Q11 builds chunk 4 against the untyped shape meanwhile, letting 3b widen it later |
| Mechanics the framework has to implement | 1383 | The runner walks a plan (a tree of `create`/`set`/`remove`/`saveRecord`/`filter`/`extra` ops) depth-first in declaration order, never crosses the MCP wire itself, implements no undo, splits refusals into pre-flight (computed off the plan alone: an unservable route, an unresolvable `fromSaved`, an unfilled `links` including one added at top level, or now a chain call needing an undeclared `query`/`update`/`remove` route) versus mid-run (a `filter` under-matching, a route throwing); every error class carries the ingredient and the recipe name | **PLANNED chunk 4 (part)** | Chunk 1 adds a sixth op kind, `extra`, to the Op table — every extra now produces an op, `{ ref, verb, args }`. `op-extra-contract` (chunk 1, A2b) and `op-extra-transformer` (chunk 3, D2) build it. Every other op contract (chunk 1) and §3's decisions (the derived `RowRef`, the `filter` op's `scope`) name what a plan holds. The runner that actually WALKS the tree and fires the two groups of refusal — `plan-run-broker.ts` and `plan-preflight-broker.ts` — is the runner plan's own chunk 4; the error classes were only DEFINED in chunk 1's `errors/`. This round's own pre-flight addition (an undeclared `query`/`update`/`remove` route) is the runner plan's Q2 finding, also chunk 4 |
| How siegelense finds recipes without importing them | 1446 | `siegelense` lists recipes by globbing `packages/hydration-recipes/dist/index.js`, dynamically importing it, and reading its exported manifest (name/description/inputs/routes per recipe) — reading compiled output only, and distinguishing an absent directory (installation problem) from an empty one (no recipes yet, a real answer) | **PLANNED chunk 1 (part)** | `recipeManifestContract` (chunk 1) shapes what a listing needs — every recipe's name, description and inputs, as static data. The actual glob-and-dynamic-import WALK, and the absent-vs-empty distinction, is chunk 8 (`siegelense`), not built here. The runner plan's §2 rows 10 and 11 confirm both sad-path rows this heading implies stay chunk 8's own responsibility, not the runner's |
| Where each part lands in this repo's architecture | 1469 | Maps each piece to a folder type and package — `IngredientConfig`/`Plan`/`Op`/`Target`/the manifest as `hydration` contracts, the chain builder as `hydration` transformers, `createHydration`/`ingredient`/`registry`/`recipe`/the runner as `hydration` brokers, route helpers as `hydration` adapters, refusal errors as `hydration` errors, recipe discovery/listing and the `seed` step as `siegelense` brokers, the recipes-package scaffolder as `siegelense` (already built), each ingredient/recipe as its own folder in `hydration-recipes` — and requires `hydration`'s own `ban-primitives` eslint carve-out with a comment | **DELIVERED (pre-existing) (part)** | Unchanged: the scaffolder (`InstallRecipesScaffoldResponder`) and the `ban-primitives` carve-out in `eslint.config.js` both predate this ledger and are confirmed present. The chunk 1–3 plan's own §§5–7 assign every remaining row of this table to a specific file within `hydration`'s folder types. The runner plan's own §6 file-by-file breakdown instantiates the `hydration` rows this map names for chunk 4's own share — `adapters/fetch/post/`, `adapters/fs/ensure-write/`, `brokers/plan/run/`, `errors/` — not built yet |
| The migration IS the validation, not cleanup afterwards | 1491 | Converting the real suites (118 e2e specs seeding domain state, 16 convertible integration domain-seeders) to the recipe framework is in scope and is how the design gets proven, run via `python3 scrolls/tools/seed-census.py` for the target list and the running call-site count, in the order integration-then-e2e-then-one-manual-siegelense-round | NOT STARTED | Chunks 9–10, after ingredients exist (chunk 7), which itself needs the runner (chunk 4) to exist first. `scrolls/tools/seed-census.py` exists and was not re-run this round. This round adds a finding beside this heading: some conversion targets — real git worktree/branch state, mock subprocess response queues, a rate-limit harness with no guild/quest/session shape, and one MCP protocol driver that persists no row at all — seed state no ingredient in this plan covers (`recipes-seeding-survey.md` findings 4, 5, 6 and 9). None has an ingredient today |
| The order, and why each step proves something the last one could not | 1549 | Step 1 (17 integration domain-seeders, files only) proves write routes and the no-`baseUrl` case; step 2 (118 e2e specs) proves api routes and one plan serving both callers; step 3 (one manual siegelense round) proves `seed`, `recipes {}` and params validation, which neither suite crosses | NOT STARTED | Chunks 9–10, then chunk 11's manual round |
| The rule that makes the conversion a proof | 1567 | A converted test keeps its assertions exactly, changing only its setup; a test needing a different assertion means the ingredient is wrong, not the test; a test that cannot convert is a finding written into this document; the one integration test whose SUBJECT is a `copies:` target (`quest-hydrate-broker.integration.test.ts`) is excluded, while tests that merely use that broker as setup are ordinary conversion targets | NOT STARTED | Chunks 9–10 |
| An ingredient touches STATE, never a screen | 1599 | An ingredient never holds a DOM handle; enforcement is layered — a runtime guard (only the minting instance's refs resolve, invalidated on navigation/reset/restart) as the layer that actually protects, a durable step-shape type structurally omitting `ref`, and a lint rule in the published `@dungeonmaster/eslint-plugin` (not `local-eslint`) flagging any DOM mention in an ingredient file | NOT STARTED | Chunk 7 needs a real ingredient to police, and the `@dungeonmaster/eslint-plugin` rule does not exist yet. `packages/siegelense/CLAUDE.md` already states this rule as prose ("## A recipe touches state, never a screen"), predating this ledger, but names "recipe" where the spec's own vocabulary says "ingredient" — unresolved, and unrelated to this round's edits |
| Some claims can only be asserted in a BROWSER | 1634 | Ingredient tests come in three costs — files-only, a shared server instance, or a full server-plus-browser instance for a claim about what a URL renders — and a browser-asserted ingredient test is deliberately rare because the prelude's own `VERIFIED` run already proves rendering | NOT STARTED | Chunk 7 — needs a real ingredient whose claim is about what a URL renders |
| What this is, in industry terms | 1658 | Frames this as a test-data factory library (not the industry sense of "hydration"), keeping sequence/trait/lint/explicit-dependencies from the factory family while deliberately refusing random data, fluent builders as the wire format, static fixtures, and object-mother method-per-scenario naming; names the Cucumber failure mode it guards against, and the three mechanisms that guard against it (a run-proven prelude, structural parenting, plan-as-data) | NOT STARTED | Rationale, not a separate artifact — `defaults(i)` as sequence and extras as traits land under other rows above |
| Guidance for other repos | 1709 | For a consumer repo, only `@dungeonmaster/hydration`, the empty scaffolded `packages/hydration-recipes/`, and five stated conventions (one ingredient per entity from that repo's own contracts, an ingredient declares its routes with `copies:` on `write`, counts are `add(n)` never a name, the chain owns the index, two-of-anything must be distinguishable) travel — not this repo's own recipes | NOT STARTED | Nothing in chunks 1–3 or 4–6b ships yet, and the scaffolder that would carry the convention into a consumer repo is separate, pre-existing `siegelense` work |

## Part 6 — The surface, consolidated (line 1726)

| Section | Line | Requires | Status | Notes |
|---|---|---|---|---|
| Each package needs a `../../CLAUDE.md`, and these are the entries | 1732 | Splits a set of named invariants (no `.first()`/`.last()`, no `querySelector` in eval, a command returns a reading not a verdict, the key reads own text nodes, a ref resolves only in its minting instance, an ingredient touches state never a screen, one verb sets state, `all` as an argument not a property, link-by-name, route-lives-on-ingredient, `hydration-recipes` absent from root deps, `run`-returns-status/`results`-returns-payloads, kill-the-group-skip-the-dead, `kill` never removes evidence, `dev:no-watch`) across `siegelense`'s, `hydration`'s and `hydration-recipes`'s own `CLAUDE.md` files, by which package breaks if the rule breaks | **DELIVERED (pre-existing) (part)** | Unchanged this round. `packages/siegelense/CLAUDE.md` already carries several of these lines close to verbatim (confirmed by reading the file), all predating this ledger. Missing from it: the recipe-specific lines (one-verb-sets-state, `all`-as-argument, link-by-name, route-lives-on-ingredient, the `hydration-recipes` root-dependencies line) — none yet written, because no `hydration/CLAUDE.md` or `hydration-recipes/CLAUDE.md` exists, as neither package does. The runner plan's own Q6 ruling adds a line this file will also need once it exists: who throws `HydrationTransactionRolledBackError` (nobody inside the framework) |
| The calls this document uses | 1766 | `recipes {}` lists states with no instance needed, printing exactly the fields Part 5's "The listing `recipes {}` prints" specifies; `docs { for: … }` serves the tool's own instructions across seven scopes, per the sibling tooling document | NOT STARTED | `docs`'s seven scopes are tracked by the sibling `siegelense-tooling.md` ledger, not this one. `recipes {}` itself is chunk 8 (`siegelense`), not built by either chunk plan, which only supply the data it will print (`recipeManifestContract`, `planRunsTransformer`, `planMakesTransformer` — see the Part 5 rows above) |
| Steps that are new | 1810 | A `seed` step runs a named recipe's plan against the current instance and returns the ids it made under `as:`; its `params` are typed by the `recipe` value in-process and validated by a generated zod schema before seeding over the MCP wire, naming the bad input and what that recipe takes | **PLANNED chunk 1 (part)** | `seedStepContract` (chunk 1) types the `seed` step's shape and its `params`, discriminated by the `recipe` value in-process. The step actually RUNNING a plan against a live instance now has `run()` planned as `plan-run-broker.ts` (chunk 4) to call, but returning ids and the wire-side zod validation before seeding are both still chunk 8 (`siegelense`) — `packages/siegelense/src/statics/step/step-statics.ts`'s `verbs.all` still lists no `seed` verb |
| A worked batch | 1836 | A batch step's output is addressed as three segments, `{<step>.<row>.<field>}` (never the two-segment `{step.field}`, which the tool should refuse); `stopOn: 'error'` is the default that halts on the first unexpected failure; a step declaring `expect: 'error'` that unexpectedly SUCCEEDS itself halts the batch as a finding; a plan's output is FLAT, with exactly one shape, carrying every `saveRecordAs`'d row under its own name | NOT STARTED | Chunk 8 — `as:`/`{step.row.field}` addressing and `stopOn`/`expect: 'error'` are `siegelense` runtime behavior, not `hydration`'s. The FLAT-output sub-claim is `hydration-run-result-contract` and `op-save-record-apply-layer-broker` (chunk 4, Q11), not built yet |
| Interleaving recipes and steps | 1895 | `seed` is placeable anywhere in a batch, not only as a prologue; a later `seed` in the same batch can take `params` from an earlier step's saved output; seeding while a page is already open is how a socket-driven live-update path gets exercised, while a non-socket-driven surface needs an explicit reload after such a seed | NOT STARTED | Chunk 8, same reason |

---

## Sections read and deliberately given no row

Per this round's instructions, these are directions to the build rather than requirements the build
must satisfy, so they carry no row above:

- `## The prompt that builds this` (line 10) and its subsections `### What this doc asks for, and what it
  does not` (87) and `### A suggested spine, for the first planner round` (107)
- `### This branch tracks master, and siegelense is already in it` (133) and its subsections `#### What
  that leaves to schedule` (152), `#### When to merge` (168), `#### The rename is its own step, and it is
  bigger than the folder` (201)

`docs { for: … }`'s scope content and the rest of the thirteen-call MCP surface (`start`, `run`, `results`,
`kill`, `capacity`, `profile`, `status`, `cleanup`, `prune`, `compare`, `snapshots`, `docs`) belong to
`siegelense-tooling.md` and are tracked in `build-ledger.md`. This ledger only adds the `recipes` call and
the `seed` step to that surface, both of which have rows above.
