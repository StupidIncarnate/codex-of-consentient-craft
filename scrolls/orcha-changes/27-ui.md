# 27 — the UI (a set of ~7)

```
GOAL      The panel shows what is actually happening under many sessions per scope.
AFTER     22 (it reads the new record) · 26 lands in the same window
PACKAGE   @dungeonmaster/web
MODEL     sonnet, except 27a
```

**The blast radius is narrower than expected and concentrated in one place.**

---

## Verified safe — do not audit these again

The chat surface entire — panel, entries, messages, tool rows, sub-agent chains, the follow-up tab —
reads `ChatEntry[]` off the wire and already buckets by work item, because sibling sub-agents share a
session. Parallel dispatch does not touch it. The queue page knows nothing about ledgers or work items.
**The flow diagram and the whole SPEC tab never rendered a sign-off at all**, so story 26 costs the
diagram nothing. Home, sidebars, session view, rate limits, the image pipeline: untouched.

**Only three web files touch execution semantics at all** — the row-identity chain 27a owns:
`packages/web/src/widgets/execution-panel/execution-panel-widget.tsx` (builds the row list and the
`workItemIdToLabel`/`wardResultsById`/`operationsById` lookups, `execution-panel-widget.tsx:220-241`),
`execution-row-layer-widget.tsx` (renders one row — name, role badge, status, retry badge; the leaf
both the claimed and unclaimed paths share) and `execution-work-item-row-layer-widget.tsx` (derives a
claimed row's name from its linked operation at `:122-126` — the linked operation's `text` when one
resolves, else the capitalised role name). The wider surfaces 27d/27e/27g own —
`operations-ledger-widget.tsx`, `quest-summary-widget.tsx`, `execution-status-bar-layer-widget.tsx`
and the rest — read `OperationItem[]`/quest-summary state, not the live per-work-item dispatch state
these three derive.

Verified rather than assumed: `discover({ grep: "signoff", strict: true })` scoped to
`packages/web/**` returns hits only in `bindings/use-quest-summary/`,
`widgets/quest-summary/quest-summary-widget.tsx` (+ `.test`),
`widgets/quest-summary/unconfirmable-row-layer-widget.tsx` (+ `.test`), and three `.e2e.ts` flows
(`operations-driven-dispatch`, `operations-ward-recovery`, `quest-summary-under-raccoon`) — nothing
under `widgets/react-flow-diagram/**` or `widgets/quest-spec-panel/**`.

---

### 27a — row identity, and why nothing will report it broken

```
OWNS      packages/web/src/widgets/execution-panel/execution-work-item-row-layer-widget.tsx
             — the name derivation, :122-126 (`operation ? operation.text : role`)
          packages/web/src/widgets/execution-panel/execution-panel-widget.tsx
             — the row list and its lookups, :220-241
          packages/web/src/widgets/execution-panel/execution-row-layer-widget.tsx
             — the leaf that renders the name, both paths share it
DONE      rows are grouped under their SCOPE, and every row on screen is distinguishable
          from every other
ASSERT    not "the panel renders". Assert NO TWO ROWS CARRY THE SAME NAME, given a
          codeweaver cell that ran plan, three workers, review, commit, ward and repair.
          UNIT test, in `execution-panel-widget.test.tsx`: feed `ExecutionPanelWidget` a
          quest whose `workItems[]` are several sessions on ONE scope/operation (varying
          `role`/`step`), then read `ExecutionPanelWidgetProxy().getStepRows()`
          (`execution-panel-widget.proxy.tsx:185` — `screen.queryAllByTestId
          ('execution-row-layer-widget')`) and assert the row NAMES have no duplicate —
          this is the same proxy/pattern `execution-panel-widget.test.tsx:194-198` already
          uses for row-by-row assertions. No browser-only behaviour is involved (no CSS,
          no scroll), so a Playwright e2e is not required to catch this defect — the repo's
          own e2e-are-the-verdict check is exactly what THIS defect slips past (see below).
MODEL     opus
```

**A row's identity is its OPERATION, and that identity silently stops being unique.** Under strict 1:1
it is a key; under one scope holding many sessions it is a category label, so that cell renders as
eight consecutive rows with the same name, told apart only by a role badge.

**It will be missed because nothing fails.** No contract rejects it, no test breaks, nothing errors. The
panel keeps working and keeps looking plausible while the user loses the ability to tell which session
is which — the exact thing this redesign exists to give them. **And it passes the repo's own
browser-is-the-verdict check**: a panel of eight identically-named rows is neither blank nor frozen.
That is why the assertion above is specific.

### 27b — the projection

```
OWNS      a new transformer — graph plus current state, walked forward — and its view
DONE      renders the likely remainder, recomputes as sessions land, and REDRAWS on a
          back-edge rather than being wrong
ASSERT    on the rendered output, never on a call
```

The ledger cannot be the map, because the map has cycles. **Reuse the summary refresh chain** — outbox
event, refetch, re-render. Do not build a second channel. The pattern already lives at
`packages/web/src/bindings/use-quest-summary/use-quest-summary-binding.ts` — it seeds from
`GET /api/quests/:questId/summary` and repaints on that quest's `quest-modified` broadcast
(`quest-summary-widget.tsx:9-10`: "repaints on that quest's quest-modified broadcasts, so a sign-off
write lands here without a reload"). The projection binding follows the same shape: seed from a new
endpoint, subscribe the existing `quest-modified` bus event, refetch and re-render — no new websocket
message type.

### 27c — the churn view, and the units readout

```
OWNS      the per-unit churn view; the per-row UNITS ASSIGNED vs UNITS MARKED readout
ASSERT    the churn SEQUENCE is legible without reconstruction: codeweaver marks obs-3
          unmet, a second codeweaver marks it met, its reviewer marks it unmet, a third
          gets it met
```

**The units readout slot already exists and is dead** — declared and rendered, passed by no production
caller, built for exactly this. It is `ExecutionRowLayerWidgetProps.observablesSatisfied?: ObservableId[]`
at `packages/web/src/widgets/execution-panel/execution-row-layer-widget.tsx:69`, rendered at `:419-431`
as `Satisfies: {observablesSatisfied.join(', ')}` (`data-testid="execution-row-observables"`) whenever
the array is non-empty. It is exercised only by `execution-row-layer-widget.test.tsx:1156-1192` and by
the test-only `quest.harness.ts:413` fixture — `ExecutionWorkItemRowLayerWidget` (the one production
caller of `ExecutionRowLayerWidget` for a claimed row) never passes it (confirmed: its own prop-spread
at `execution-work-item-row-layer-widget.tsx:129-147` has no `observablesSatisfied` entry). Repoint
this prop at the new per-unit marks (assigned vs marked) rather than declaring a second one.

### 27d — the seven broken surfaces

```
OWNS      the unclaimed-operations tail
             packages/web/src/transformers/unclaimed-operations/unclaimed-operations-transformer.ts
             — called from execution-panel-widget.tsx:197-200
          the progress counter
             packages/web/src/widgets/execution-panel/execution-status-bar-layer-widget.tsx
             — renders "EXECUTION — N/M OPERATIONS" or "AWAITING PLAN" when totalCount is 0;
             fed totalOperations/completedOperations computed at execution-panel-widget.tsx:216-218
          the COVERAGE section
             packages/web/src/widgets/quest-summary/quest-summary-widget.tsx:105-124
             (data-testid="QUEST_SUMMARY_SECTION_COVERAGE") · flow-row-layer-widget.tsx ·
             track-row-layer-widget.tsx (renders "N confirmed / N unconfirmable / N outstanding")
          the UNCONFIRMABLE debt list
             quest-summary-widget.tsx:149-170 (data-testid="QUEST_SUMMARY_SECTION_DEBT") ·
             unconfirmable-row-layer-widget.tsx
          the ward-mode tag — TWO sites:
             packages/web/src/widgets/operations-ledger/operation-row-layer-widget.tsx:104-112
             (data-testid="OPERATIONS_LEDGER_ROW_WARD_MODE", "({operation.wardMode})") and
             packages/web/src/widgets/execution-panel/ward-result-row-layer-widget.tsx:40-41
             ("Ward exit code: {exitCode}{wardMode ? ` (${wardMode})` : ''}")
          the retry badge
             packages/web/src/widgets/execution-panel/execution-row-layer-widget.tsx:336-349
             (data-testid="execution-row-retry-badge", "retry {attempt}/{maxAttempts}")
          the DETAILS-tab ledger
             packages/web/src/widgets/operations-ledger/operations-ledger-widget.tsx +
             operation-row-layer-widget.tsx, mounted at
             packages/web/src/widgets/quest-spec-panel/quest-spec-panel-widget.tsx:229
             (the execution panel's OWN "EXECUTION" tab no longer renders it at all —
             execution-panel-widget.proxy.tsx:188-191 says so explicitly: "the panel no
             longer renders that widget")
DONE      each either works against the new record or is deliberately removed, with the
          reason written down
```

**The progress figure is NOT asserted to rise.** It cannot: the denominator grows as families are
routed to, and a rework edge raises both numbers, so the ratio genuinely falls. Assert instead that it
never exceeds 1, that it is recomputed from the PROJECTION rather than the ledger, and that
`AWAITING PLAN` never fires for a quest with a running session.

The debt list needs new vocabulary as well as a new shape: two verdicts become three, `unconfirmable`
becomes `cant-meet`, and the new `unmet` — the one meaning *work is outstanding right now* — has
nowhere to appear at all.

### 27e — rework rather than rebuild

```
OWNS      auto-expand and auto-scroll
             packages/web/src/widgets/execution-panel/execution-row-layer-widget.tsx:147-186
             (the three useEffects driving `expanded`) ·
             packages/web/src/widgets/auto-scroll-container/auto-scroll-container-widget.tsx
          dependency labels
             packages/web/src/transformers/execution-row-subtitle/execution-row-subtitle-transformer.ts:24-30
             ("depends on: …"), fed `depLabels` built at
             execution-work-item-row-layer-widget.tsx:95-97 off the `workItemIdToLabel`
             map (WorkItem['id'] -> WorkItem['role']) built once at
             execution-panel-widget.tsx:222-225 — a role name, not a session identity,
             which is exactly what stops being unique once several sessions share a role
          the role-colour map
             packages/web/src/statics/execution-step-status-config/execution-step-status-config-statics.ts:10-22
             (`roleColors`, keyed on family names — codeweaver/flowrider/siegemaster/…)
          command-row rendering
             execution-row-layer-widget.tsx:143-146: `isCommandRow =
             isCommandWorkItemRoleGuard({ role })`, gating `mergeCommandOutputEntriesTransformer`
          the PARTIAL status and the `pt N` trail
             status label: execution-step-status-config-statics.ts:29
             (`partially_complete: { label: 'PARTIAL', … }`) · the user-visible assertion
             to change: packages/web/src/flows/quest-chat/operations-partial-continuation.e2e.ts:18
             (`PT2_TEXT = 'pt 2: core: config adapter'`) and :149
             (`await expect(rows.filter({ hasText: PT2_TEXT })).toHaveCount(1);`) — the
             whole `describe` block at :22 is titled "Operations duplicate-on-partial
             (pt-N continuation)" and drives a real partial→pt2 dispatch
DONE      four concurrent transcripts do not fight over the scroll position
ASSERT    `pt N` is asserted TODAY as user-visible behaviour, and it retires with
          duplicate-on-partial. Find that assertion — named above — and change it
          deliberately (it currently asserts a `pt 2:` continuation row and a
          `partially_complete` operation status; under the unit gate a rework edge mints a
          plain successor work item with no `pt N` text and no `partially_complete` status)
```

Auto-expand and auto-scroll were written for exactly one running row. The role-colour map is keyed on
family when the useful dimension is now step role. Command-row rendering keys on role rather than step
kind, so a `commit` step's git output would render as markdown.

### 27f — newly required

```
OWNS      a back-edge badge · a planned-versus-actual view · a step-args display ·
          a live `unmet` list near the execution rows · a concurrency readout ·
          a step-name-unknown fallback
ASSERT    the fallback FIRST: story 03 opened the step-name contract so unrecognised
          names LOAD, and today the row renderer indexes a closed record unguarded.
          That is a crash, not a blank
```

"This session exists because work item 4 marked obs-3 unmet" is the commonest way work items will now
be created, and there is no way to say it.

### 27g — recipes on the SPEC tab

```
OWNS      the flow diagram's seed display
             packages/web/src/widgets/react-flow-diagram/react-flow-diagram-widget.tsx
             (the diagram itself) and, for a per-node seed callout, its
             flow-node-detail-panel-layer-widget.tsx
DONE      a flow shows the recipes a walk of it starts from
```

Recipes are flow data now — `quest.flows[].recipes[]`. **Already flagged, not rediscovered here:**
story `18-get-quest-work-tool.md:307` marks this the same way — "OPEN — `quest.flows[].recipes[]`
does not exist. `flowContract` …" — and `:596` says adding `recipes[]` to `flowContract` is "somebody's
story and it is not this one." It is not 27's either. 27g depends on whichever story lands that field;
until then this sub-brief has no data to read.

---

## The end-to-end check, and why the usual one is not enough

A real quest in this repo through the Node dispatcher. Repo policy is that the browser is the verdict: a
blank panel or a frozen spinner is a failure even if `quest.status` reads `complete`.

**But that does not catch 27a**, and it is worth saying rather than discovering. The row-identity defect
produces a panel that is neither blank nor frozen and is still broken. So assert the specific thing —
every row distinguishable, by scope and step — alongside live output on each concurrent row without the
scroll fighting itself, the churn view showing what each work item marked, and a loop-back redrawing the
projection rather than appending to it.
