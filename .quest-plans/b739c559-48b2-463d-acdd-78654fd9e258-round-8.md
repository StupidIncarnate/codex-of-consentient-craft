# Round 8 — siegemaster-verifier — running-elapsed-on-work-item-row / P8

PATH: panel-rendered → started-at-check → row-running-check → compute-elapsed → under-minute-check
      → under-hour-check → show-minutes → paused-no-figure
FORCE: "startedAt present", "in_progress, no completedAt — end point is now", "60s or more",
       "under 1h", "quest paused"
LANE: r8-verify (booted clean on first try, no restart needed)
WORK ITEM: b739c559-48b2-463d-acdd-78654fd9e258

## Guide corrections

None found. The guide's SEEDING, ENTRY, CONTROLS, OFF-SCREEN and FORCING sections for this
exact scenario (seed a running row at 4m30s, pause it, read duration+badge before/after) matched
the real system exactly — no recipe needed correction on this walk.

## Pass 1 — walk record

Setup:
- Guild fixture dir: `<repoRoot>/tmp/dm-siege-r8-verify-guild` (bare dir, no git — pause alone
  needs no fixture repo per guide section 7).
- POST /api/guilds -> guildId 340a3ed5-47a0-415c-adbf-da61575da85d, urlSlug
  elapsed-duration-guild-r8.
- POST /api/quests -> questId a15348ea-b7a2-4d06-b6f1-12506676d0ae, filePath under lane home.
- Computed startedAt = now - 270000ms = 2026-09-09T12:36:44.026Z (node -e, stdout only).
- Wrote quest.json directly (Write tool, since filePath is under the lane's OS /tmp home):
  one work item e2e00000-0000-4000-8000-000000000001, role codeweaver, status in_progress,
  startedAt as above, no completedAt, linked via relatedDataItems to operation
  00000000-0000-4000-8000-000000000001 (status in_progress, text "codeweaver: minutes-paused row").
  Quest status "in_progress" throughout seeding.

### Node walk

- **panel-rendered**: goto `/elapsed-duration-guild-r8/quest/a15348ea-b7a2-4d06-b6f1-12506676d0ae`
  -> 200. waitFor `[data-testid="execution-panel-widget"]` -> visible, count 1. Panel text included
  "0/1 OPERATIONS" and the row text with "4m" and "RUNNING" baked into the initial box reading.
  No defect.
- **started-at-check -> edge started-at-present**: satisfied by seeding (startedAt present).
  Already S✓ from a prior round; not mine to re-sign, but the row rendering a figure at all is
  consistent with this branch having been taken. No defect.
- **row-running-check -> edge is-running**: item is in_progress with no completedAt — consistent
  with a figure being shown at all (confirmed later it disappears on pause exactly as this
  branch, not has-completed-at, predicts). Already S✓. No defect.
- **compute-elapsed**: not directly probed with new readings (its observables are already signed
  by prior rounds and not on my UNITS list) — I did read the box output at this node in passing:
  the 4m30s-old row read exactly "4m" pre-pause, consistent with elapsed-parts-transformer
  flooring 270s to 4m30s -> 4 minutes (seconds dropped per the minutes band). No defect observed.
- **under-minute-check -> edge sixty-or-more**: 270s >= 60s, correctly routed past the <1m branch
  (row read "4m", never "<1m"). Already S✓. No defect.
- **under-hour-check -> edge under-hour**: 270s < 3600s, correctly routed to show-minutes (row
  read "4m", not an hours/minutes format). Already S✓. No defect.
- **show-minutes**: box on `[data-testid="execution-row-layer-widget"]:has-text("codeweaver:
  minutes-paused row") [data-testid="execution-row-duration"]` ->
  `{count:1, present:true, visible:true, text:"4m"}`. Badge box on the same row's
  `[data-testid="execution-row-status-badge"]` -> `{count:1, present:true, visible:true,
  text:"RUNNING"}`. Both exactly as check-minutes-text (already S✓) predicts. No defect.

### THE UNIT: edge `minutes-paused` (show-minutes —"quest paused"→ paused-no-figure)

STARTED FROM: row seeded in_progress, startedAt 270000ms ago, no completedAt; confirmed reading
  duration="4m", badge="RUNNING" (readings above).
DID: clicked `[data-testid="EXECUTION_PAUSE_BUTTON"]` (siege `click` command). This POSTs
  `/api/quests/<questId>/pause`.
SAW:
  1. Before pause: duration element present, text "4m"; badge text "RUNNING".
  2. `network` capture (armed from lane boot) showed:
     `POST http://dungeonmaster.localhost:43199/api/quests/a15348ea-b7a2-4d06-b6f1-12506676d0ae/pause`
     -> status 200, responseBody `{"paused":true}`.
  3. Server-side quest.json (read via the file-change notification after the click) showed
     `status: "paused"`, work item `status: "pending"`, `pausedAtStatus: "in_progress"`,
     `completedAt` still absent, `startedAt` unchanged (2026-09-09T12:36:44.026Z).
  4. After pause: box on execution-row-duration -> `{count:0, present:false}` (element gone).
     Badge box -> `{count:1, present:true, text:"PENDING"}`.
  5. Screenshot of the panel after pause: row shows "codeweaver: minutes-paused row" /
     "PENDING" with no duration figure next to it, RESUME QUEST button now visible. No visual
     glitches, no overlap, no truncation.
  6. Console capture: only vite/React-devtools boilerplate, no errors or warnings.
BROKEN WOULD SHOW: had the flow actually routed through the sibling edge "completedAt arrives"
  -> show-final-duration instead of this one, the duration element would still be PRESENT (a
  frozen figure, e.g. still "4m" or some completed span) and the badge would read "DONE" — that
  branch's own observable check-frozen-figure-unchanged-by-tick and check-final-replaces-running-
  immediately depend on the element staying rendered. What was actually measured is the opposite
  on both axes: element absent (`count:0`/`present:false`) and badge "PENDING", not "DONE". A
  system that had wrongly special-cased pause to preserve the last figure, or that left the work
  item status untouched, would have shown the duration frozen at "4m" with either "RUNNING" or a
  stale badge — neither of which appeared.

No defects found on this path walk. Reached the end of PATH (paused-no-figure) cleanly.

### Look-at-everything note (paused-no-figure terminal, not signed — already S✓)

Read the three already-signed observables' effective state in passing while at this node:
check-paused-row-badge (badge="PENDING", matches), check-paused-row-no-elapsed (duration
absent, matches), check-resumed-row-restarts (not exercised this round — resume needs the git
fixture repo per guide section 7, out of scope for this unit which only needed pause). Nothing
wrong observed at this terminal.

## Numbered defect list

(empty — zero defects found on this path walk)

## Pass 2

Not entered — no defects on the list to dispatch against.

## Sign-off

`modify-quest` on flow `running-elapsed-on-work-item-row`, edge `minutes-paused`: verdict
`confirmed`, evidence as in SAW/BROKEN WOULD SHOW above, workItemId
b739c559-48b2-463d-acdd-78654fd9e258. Call returned `{"success": true}`.

## Return to parent

NEXT:      pass
COVERAGE:  0 of 0 on my list dispatched — zero defects found; the single assigned unit
           (edge minutes-paused) is signed confirmed.
RED TESTS:
  - none
