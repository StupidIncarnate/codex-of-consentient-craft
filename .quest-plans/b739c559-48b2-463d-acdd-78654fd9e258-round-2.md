# siegemaster-verifier round 2 — running-elapsed-on-work-item-row / P2

LANE: r2-verify
WORK ITEM: b739c559-48b2-463d-acdd-78654fd9e258
PATH: panel-rendered → started-at-check → row-running-check → no-live-figure
FORCE: "startedAt present", "no completedAt and not in_progress"

## Lane

Started `npx tsx packages/web/test/siege-driver/siege-driver.ts r2-verify` in background. `lane.json`:
apiPort 45117, webPort 36505, baseUrl `http://dungeonmaster.localhost:36505`,
home `/tmp/dm-siege-r2-verify-1091591`.

## Guide corrections

None. Every recipe used — ENTRY, the bare-directory guild fixture, the `writeQuestFile` shape,
the age-offset table, the no-op-PATCH mid-walk-mutation trick, `box` vs `dom` — worked exactly as
described on the first attempt. One reinforcement of round-1's TOOLING note: `mkdir` outside the
repo is refused even as a single-line command; routing directory creation through `Write` (which
creates missing parents) is the reliable path.

## Seeding

- Guild: `POST /api/guilds` with path `/tmp/dm-siege-r2-verify-guild` (bare dir, created via `Write`
  of a placeholder file) → `id: da5bffd9-c997-41a7-9dfa-70215e758713`,
  `urlSlug: elapsed-duration-guild-r2`.
- Quest: `POST /api/quests` → `questId: 59944d22-1c62-4922-b973-3aa9f96e294c`.
- Overwrote `quest.json` wholesale with FIVE work items, one per non-`in_progress` status member of
  `workItemStatusContract` (read from `packages/shared/src/contracts/work-item-status/work-item-status-contract.ts`:
  `pending, queued, in_progress, complete, failed, skipped`), quest itself always `in_progress`:
  - `...101` "codeweaver: not-running pending" — status `pending`, `startedAt: 2026-09-09T10:34:28.000Z`
    (real wall clock, 4m30s before seeding), no `completedAt`. Reused later as the branch-forcing row.
  - `...102` "codeweaver: not-running queued" — status `queued`, `startedAt: 2026-01-01T09:00:00.000Z`, no `completedAt`.
  - `...103` "codeweaver: not-running complete" — status `complete`, same fixed `startedAt`, no `completedAt`.
  - `...104` "codeweaver: not-running failed" — status `failed`, same fixed `startedAt`, no `completedAt`.
  - `...105` "codeweaver: not-running skipped" — status `skipped`, same fixed `startedAt`, no `completedAt`.
  Deliberately gave the `pending` row a REAL recent `startedAt` (not a distant fixed one) so it could
  later be flipped to `in_progress` and produce a genuine, checkable live figure — the two branches this
  round covers (`started-at-present`, `not-running`) both need a value the sibling edge would produce
  DIFFERENTLY, not just "ended up with no figure."

## Drive record

### edge:started-at-present
STARTED FROM: fresh quest.json, work item `...101` status `pending`, `startedAt` present
(2026-09-09T10:34:28.000Z, real clock), no `completedAt`.
DID: `goto /elapsed-duration-guild-r2/quest/59944d22-1c62-4922-b973-3aa9f96e294c` → `waitFor`
`execution-panel-widget` → `box` scoped to `...101`'s row `execution-row-duration` → **rewrote only
`status: "pending"` → `"in_progress"`** on the same work item, `startedAt` untouched → fired the
no-op PATCH (`{"status":"in_progress"}` on the already-`in_progress` quest) → `waitFor` on the same
selector.
SAW: before the flip, `{"count":0,"present":false}`. After the flip (same `startedAt`), the SAME
selector returned `{"count":1,"present":true,"text":"6m"}` — matching the ~6m1s actually elapsed
since the seeded `startedAt`.
BROKEN WOULD SHOW: if the code treated "startedAt absent" and "startedAt present" the same way (or
ignored `startedAt` in the branch decision), a status flip on a row whose `startedAt` is genuinely
absent could never produce a figure (round 1's sibling walk proved exactly that — flipping status on
a no-`startedAt` row still shows nothing, by construction of the started-at-check gate). Here the SAME
kind of status flip DID produce a figure, which is only possible if the code actually reached
row-running-check off a present `startedAt` — i.e., genuinely took `started-at-present`, not
`started-at-absent`.

### edge:not-running
STARTED FROM: same row, before the status flip — `startedAt` present, status `pending`, no `completedAt`.
DID: same `box` reading as above (pre-flip).
SAW: `{"count":0,"present":false}` while `status: "pending"`. Immediately after flipping only `status`
to `in_progress` (no other field touched) and forcing the no-op PATCH reload, the SAME row showed
`{"count":1,"present":true,"text":"6m"}`.
BROKEN WOULD SHOW: had `not-running` been mis-wired to also reach `compute-elapsed` (i.e., the same
outcome as its sibling `is-running`/`has-completed-at` edges), the pending-status row would already
have shown a duration figure BEFORE the flip. It showed none before, and a real figure after — proving
`not-running` and `is-running` are actually distinct, correctly gated branches out of the same decision
node, not two labels on one behavior.

### observable:check-no-figure-without-end-point
STARTED FROM: the five seeded rows, one per non-`in_progress` member of the status union.
DID: `box` on `[data-testid="execution-row-layer-widget"]:has-text("<row text>") [data-testid="execution-row-duration"]`
for each of the five rows (`...102` queued, `...103` complete, `...104` failed, `...105` skipped, and
`...101` pending pre-flip) + a `screenshot` of the whole panel + a `console` read.
SAW: all five returned `{"count":0,"present":false}`. Screenshot showed clean badges (PENDING running
6m — post-flip state at capture time —, QUEUED, DONE in green, FAILED in red) with no stray duration
text on any non-running row, no truncation, no overlap. Console carried only vite/react-devtools debug
lines, no warnings or errors.
BROKEN WOULD SHOW: any status among `queued`/`complete`/`failed`/`skipped`/`pending` (pre-flip)
returning `present:true` with a rendered text value instead of `count:0` — none did, across the full
status union the contract declares.

## Pass 1 defect list

None. Zero defects found on this path walk. Every non-`in_progress` status in the real
`workItemStatusContract` union rendered no duration element, and the two sibling branches at
`started-at-check` and at `row-running-check` were shown to produce genuinely different results
(no figure vs. a live `6m` figure) off the same underlying `startedAt`, proving both edges are
correctly and distinctly gated rather than coincidentally landing on the same terminal. No console
errors, no visual defects, no orphaned state after the mid-walk status/PATCH mutation.

## Pass 2

Nothing dispatched — the defect list is empty, so there is nothing to turn into a failing test.

## Signoffs written

`modify-quest` on flow `running-elapsed-on-work-item-row`:
- edge `started-at-present`: confirmed
- edge `not-running`: confirmed
- observable `check-no-figure-without-end-point` (on node `no-live-figure`): confirmed

All three carry `workItemId: b739c559-48b2-463d-acdd-78654fd9e258`. Write call returned
`{"success": true}`.
