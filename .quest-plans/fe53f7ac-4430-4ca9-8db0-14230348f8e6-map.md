# codeweaver map — orchestrator × send-message-with-images

Operation item: fe53f7ac-4430-4ca9-8db0-14230348f8e6

## What this cell actually owns

| Node | Orchestrator's half | Code change? |
|---|---|---|
| `#forward-to-orchestrator` | `ChatStartResponder` / `FollowupChatStartResponder` already take `message: string` and pass it through untouched | none |
| `#build-prompt` | `chatPromptBuildTransformer` must append the sentinel + read-the-images line when the message carries image tokens | YES |
| `#spawn-cli` | argv already carries whatever the prompt builder produced; both spawn paths route through the one transformer | tests only |
| `#agent-reads-images` | terminal, needs a live model | nothing provable here |

Both chat spawn paths (`chatSpawnBroker` and `runChatLayerBroker`) call
`chatPromptBuildTransformer`, so one change point covers every surface that sends a user message.

GROUP 1  (new folder, nothing depends on it yet)
  packages/orchestrator/src/guards/has-pasted-image-tokens/has-pasted-image-tokens-guard.ts       new
    — boolean over a raw message string: does it hold at least one `![Pasted Image N](path)` token?
      Pattern comes from `pastedImageStatics.imageTokenPattern` (@dungeonmaster/shared/statics).
      Mirror packages/orchestrator/src/guards/is-api-overload-line/is-api-overload-line-guard.ts.
  packages/orchestrator/src/guards/has-pasted-image-tokens/has-pasted-image-tokens-guard.test.ts  new
    — no proxy file: guards are pure, the sibling guards have none.

GROUP 2  (needs group 1's guard on disk)
  packages/orchestrator/src/transformers/chat-prompt-build/chat-prompt-build-transformer.ts       edit
    — compute the trailer ONCE at the top, append at BOTH return sites:
        const trailer = hasPastedImageTokensGuard({ text: message })
          ? `\n\n${pastedImageStatics.promptSentinel}\n${pastedImageStatics.promptInstruction}`
          : '';
      resume branch  → promptTextContract.parse(message + trailer)
      template branch→ promptTextContract.parse(promptText + trailer)
      The non-chat-role throw must still fire before any append.
  packages/orchestrator/src/transformers/chat-prompt-build/chat-prompt-build-transformer.test.ts  edit
    — add cases; every existing case has a token-free message so none of them move.

GROUP 3  (needs group 2's behaviour)
  packages/orchestrator/src/brokers/chat/spawn/chat-spawn-broker.test.ts                          edit
    — the two spawn-argv cases: brand-new quest first message, and a resumed follow-up.

PROVES
  #check-trailer-appended-once        -> chat-prompt-build-transformer.test.ts
  #check-no-trailer-without-images    -> chat-prompt-build-transformer.test.ts
  #check-sentinel-from-shared-statics -> read-check: reviewer opens chat-prompt-build-transformer.ts
  #check-argv-carries-image-path      -> chat-spawn-broker.test.ts
  #check-new-quest-first-message      -> chat-spawn-broker.test.ts
  #check-followup-message-carries-path-> chat-spawn-broker.test.ts
  #check-orchestrator-sees-absolute-path -> NOT mine to prove. The server writes the file and
      rewrites the token (ledger item 6, not built). A test here would construct the rewritten
      message itself and prove nothing about the handoff.
  #check-agent-issues-read           -> needs a live model reading a real file. No unit test reaches it.

TRAPS
  - The sentinel and the instruction are read from `pastedImageStatics`, never written inline.
  - `imageTokenPattern` is a STRING; compile it with `new RegExp(pattern, 'u')` — the repo lints
    for the unicode flag.
  - Existing `chat-prompt-build-transformer.test.ts` builds `expected` by re-running the statics
    replacements. Keep that shape; never call the transformer to build its own expectation.
  - `toContain` / `stringContaining` / `toMatchObject` are banned. Assert the whole argv with
    `toStrictEqual` and exact string literals.
  - New-quest path resolves questId to the proxy's minted CREATED_QUEST_ID, so the prompt uses the
    `preCreated` bootstrap, not `mint`.
