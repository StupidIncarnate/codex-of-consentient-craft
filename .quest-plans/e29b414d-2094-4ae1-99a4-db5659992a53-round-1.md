# Round 1 — [codeweaver] Codeweaver: build this slice — package: server

## Context
Quest ID: a7520e60-430c-4d0e-b332-9952d6d5c042
Work Item ID: abe93a5a-364b-4fb0-9a70-e22ab748b31c
Operation Item ID: e29b414d-2094-4ae1-99a4-db5659992a53
Your operation item: [codeweaver] Codeweaver: build this slice — package: server

Operations ledger (in order):
1. [x] [chaoswhisperer] Author spec + implementation plan
2. [x] [riftcarver] Riftcarver: carve the quest branch, worktree and preflight build
3. [x] [codeweaver] Codeweaver: build this slice — package: shared
4. [>] [codeweaver] Codeweaver: build this slice — package: server  <-- YOUR OPERATION ITEM
5. [ ] [codeweaver] Codeweaver: build this slice — package: web
6. [ ] [ward changed] Ward gate (changed files)
7. [ ] [flowrider] Flowrider: author the flow-perspective test suites below the browser — package: server
8. [ ] [flowrider] Flowrider: author the flow-perspective test suites below the browser — seam: server + shared
9. [ ] [groundstomper] Groundstomper: author the browser walk for this flow — flow: health-badge
10. [ ] [siegemaster] Siegemaster: manual-QA this flow and review its test suite — flow: health-badge
11. [ ] [ward full] Ward gate (full monorepo)

Flows your operation item lands on: #health-badge
(A starting point, NOT a boundary — read every flow, and build whatever the flows need.)

Packages your operation item lands in: server
(Name these packages in every minion brief you write — the planner and the workers point their own searches here instead of guessing. NOT a boundary: a minion may touch another package if the work needs it.)

Your nodes (rendered from the spec as it stands right now, not from the ledger): #seed-fetch, #subscribe-heartbeat, #server-emits, #relay-broadcast, #channel-routes

Must satisfy — these are YOUR acceptance targets, verbatim:
  - check-seed-route-answers [api-call] on #seed-fetch: "GET /api/health/status returns 200 with a JSON body of exactly {status, uptimeSeconds, version} and no other keys"
  - check-liveness-probe-unchanged [api-call] on #seed-fetch: "GET /api/health still returns 200 with a body of exactly {status: 'ok', timestamp} and gains no new fields"
  - check-client-in-broadcast-set [queue-message] on #subscribe-heartbeat: "A client that completes the /ws upgrade receives the next health-status frame without sending any subscription message first"
  - check-emit-interval [queue-message] on #server-emits: "With the server running, three health-status frames arrive within 35 seconds, spaced 10 seconds apart"
  - check-emit-payload-advances [queue-message] on #server-emits: "Each health-status frame carries exactly {status, uptimeSeconds, version}, and uptimeSeconds is strictly greater than the previous frame's"
  - check-broadcast-reaches-unsubscribed-client [queue-message] on #relay-broadcast: "A client holding no quest subscription still receives every health-status frame"
  - check-dead-client-does-not-block [queue-message] on #relay-broadcast: "When one client's send throws, that client is dropped from the connected set and every remaining client still receives that same frame"
  - check-frame-crosses-wire [queue-message] on #channel-routes: "The serialised health-status envelope arrives at the browser as one WebSocket text message carrying type, payload and timestamp"

Contracts you own — every property description is a requirement:
  - HealthStatusEndpoint (endpoint, modified) [packages/server/src/statics/api-routes/api-routes-statics.ts]
      method: Read-only; the badge never mutates server state.
      path: A new key added to the existing health group in this statics file, alongside the check key that already holds /api/health.
      responseBody: The 200 body is exactly the shared HealthStatusPayload shape, so server and web agree by importing one contract rather than restating it.
  - HealthHeartbeatStatics (data, new) [packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts]
      emitIntervalMs: How often the server emits a health-status frame. Three of these fit inside the web side's 30 second silence threshold.

Design decisions constraining your scope:
  - DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload. Making any state stop listening would strand the badge on a reading that was true once and is not any more.
  - A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it.

Seams — each line is a node you share with another package, and where that package's half of it stands:
  - #seed-fetch with web — NOT BUILT YET: a later session owns these — build your half to the shape they need, and do NOT build theirs
      attributed to web — check-seed-request-issued: "On mount the badge issues GET /api/health/status exactly once, with no query string"
  - #subscribe-heartbeat with web — NOT BUILT YET: a later session owns these — build your half to the shape they need, and do NOT build theirs
      attributed to web — check-heartbeat-reaches-badge: "A health-status frame delivered after mount changes the badge text with no further HTTP request issued"
  - #server-emits with shared — ALREADY BUILT: verify each of these EXISTS in committed code before you plan, and repair it if it does not
      attributed to shared — check-event-type-member: "orchestrationEventTypeContract.options contains 'health-status' and has 22 members, the prior 21 unchanged"
  - #channel-routes with web — NOT BUILT YET: a later session owns these — build your half to the shape they need, and do NOT build theirs
      attributed to web — check-payload-updates-badge: "A delivered health-status frame changes the badge text within 1 second of arrival"
      attributed to web — check-reconnect-resumes: "After the socket drops and reconnects, the first health-status frame moves the badge out of OFFLINE with no click issued"
  - #channel-routes with shared — ALREADY BUILT: verify each of these EXISTS in committed code before you plan, and repair it if it does not
      attributed to shared — check-payload-parses: "A health-status payload parses against healthStatusPayloadContract, and a payload missing uptimeSeconds fails that parse"

Packages affected (whole quest): shared (edit, library), server (edit, http-backend), web (edit, frontend-react)

Original user request (the intent behind the flows):
The app top bar gives no indication of whether the server behind it is still
answering. When the server dies or wedges, the interface keeps rendering its
last state and the operator finds out only when an action fails.

Add a health badge to the top bar, visible on every route. It seeds itself at
mount from a new GET /api/health/status returning status, uptime and version,
then tracks a health-status heartbeat the server emits every 10 seconds over
the WebSocket the interface already holds open.

The badge reads ONLINE with uptime beside it, DEGRADED, or OFFLINE. It goes
OFFLINE when the seed request fails to reach the server, when the server
answers with an error, or when 30 seconds pass with no heartbeat — a server
can hold the socket open while no longer doing work, so silence is the signal
rather than disconnection. Clicking an OFFLINE badge retries the seed request.

/api/health stays as it is, a bare liveness probe.

## Plan

TOUCHES:
  ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts — NEW — the one place the 10-second emit period is written. Both existing server intervals (FLUSH_INTERVAL_MS at server-init-responder.ts:61, FALLBACK_RECONCILE_INTERVAL_MS at quest-driven-watchers-bootstrap-responder.ts:25) are bare module constants; this value is named by a contract entry, so it gets a statics file. Companion: health-heartbeat-statics.test.ts.
      HealthHeartbeatStatics.emitIntervalMs — must declare 10_000 ms nested as `emit.intervalMs` (root primitives are banned and every other server statics file nests: http-status-statics.ts, api-routes-statics.ts), so three emit periods span exactly the 30-second silence threshold and a single missed frame never trips it. Nothing on disk carries that threshold yet — web-config-statics.ts:50-53 holds only reconnectDelayMs and wardDetailTimeoutMs — so the 30 seconds is quoted from this contract requirement, not read off web.

  ./packages/server/src/statics/api-routes/api-routes-statics.ts — EXISTS — the canonical route-path registry for the whole architecture: httpEdgesLayerBroker.ts:46 hardcodes this exact path when resolving dotted route refs. Its `health` group at lines 10-12 holds exactly one key, `check: '/api/health'`. Companion: api-routes-statics.test.ts, whose single toStrictEqual at lines 5-63 pins the entire object, so a new key MUST be added there too or that test reds.
      HealthStatusEndpoint.path — must add `status: '/api/health/status'` inside the existing `health` group alongside `check`, leaving `check` byte-unchanged.

  ./packages/server/src/contracts/server-version/server-version-contract.ts — NEW — the `ServerVersion` brand the version adapter returns. Same brand tag as shared's healthStatusPayloadContract.version (`z.string().min(1).brand<'ServerVersion'>()`), so the payload broker's parse re-brands with no type assertion. Companions: server-version-contract.test.ts and server-version.stub.ts — every server contract folder carries all three (health-response/, iso-timestamp/, mtime-ms/, tool-name/ each hold contract + test + stub). A colocated contract test DOES import its own contract directly, the way health-status-payload-contract.test.ts:1 and comment-batch-body-contract.test.ts:1 do; the stub exists for any OTHER test that needs a ServerVersion value.

  ./packages/server/src/adapters/server-package/version/server-package-version-adapter.ts — NEW — reads @dungeonmaster/server's own package.json version at runtime, so the badge reports the running build rather than a constant that goes stale at the next release. Mirrors webBundleDistPathAdapter, which already resolves a sibling package's package.json by require.resolve. Companions: .proxy.ts (mocks the fs read) and .test.ts.

  ./packages/server/src/brokers/health/status/health-status-broker.ts — NEW — builds ONE HealthStatusPayload from process uptime plus the package version. The single source both the seed route and every heartbeat frame read, which is what makes "the same three keys on both surfaces" structural rather than a coincidence. `brokers/health/` does not exist yet. Companions: .proxy.ts (registerSpyOn on process.uptime + the version adapter proxy) and .test.ts.
      HealthStatusEndpoint.responseBody — must build the body by parsing through the shared healthStatusPayloadContract imported from @dungeonmaster/shared/contracts, never by restating {status, uptimeSeconds, version} locally.

  ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.ts — NEW — wraps one payload in a WsMessage envelope typed 'health-status' and fans it out over the connected-client set via the existing wsEventRelayBroadcastBroker. Companions: .proxy.ts and .test.ts.
      check-emit-payload-advances — must put the payload on the wire with exactly {status, uptimeSeconds, version} and nothing else, and must re-read uptime per call so consecutive frames carry strictly increasing uptimeSeconds.
      check-dead-client-does-not-block — must route through the real wsEventRelayBroadcastBroker so a throwing client is deleted from the passed Set and every remaining client still receives that same serialised frame.
      check-frame-crosses-wire — must hand each client exactly one JSON.stringify of {type, payload, timestamp}, one send call per client per emit.

  ./packages/server/src/responders/health/status/health-status-responder.ts — NEW — the GET /api/health/status handler. `responders/` has twelve subfolders and no `health/`. Returns ResponderResult; catches a version-read failure and answers 500, the responder error boundary RateLimitsGetResponder already models. Companions: .proxy.ts and .test.ts.
      check-seed-route-answers (part 1 of 2; chunk 8 owns the rest) — must return status 200 with `data` holding exactly {status, uptimeSeconds, version} and no other keys.

  ./packages/server/src/responders/server/init/server-init-responder.ts — EXISTS — owns `clients` (line 88, the global connected set, populated by onOpen at line 144 with no message read), the `/ws` upgrade (line 141), the file's only setInterval (line 771) and the package's only SIGTERM/SIGINT handlers (lines 847-860). `clients` exists solely inside this closure, so nothing outside this file can reach it. Companions: server-init-responder.test.ts and server-init-responder.proxy.ts — the proxy must instantiate the new broker's proxy or enforce-proxy-child-creation reds.
      check-emit-interval — must start a setInterval at healthHeartbeatStatics' period once the client set exists, and clear it in both signal handlers beside flushIntervalHandle.
      check-client-in-broadcast-set — must emit over `clients`, which onOpen fills on upgrade alone, so a client that has sent nothing is already a recipient.
      check-broadcast-reaches-unsubscribed-client — must NOT consult clientSubscriptions; 'health-status' is absent from PER_QUEST_EVENT_TYPES (lines 65-74) and must stay absent.

  ./packages/server/src/transformers/parse-health-status-payload/parse-health-status-payload-transformer.ts — NEW — safe-parses an unknown HTTP body into HealthStatusPayload | undefined. Exists for the same reason parseHealthResponseTransformer does: ban-contract-in-tests stops the flow's integration test importing a contract, and the transformer is how this folder already gets a body validated in a test. Companion: .test.ts.

  ./packages/server/src/flows/health/health-flow.ts — EXISTS — the Hono sub-app that owns the health group's routes; HealthFlow() is already in StartServer's subApps array (start-server.ts:47) and takes no arguments, so a route added here needs no startup change. Its existing route is an inline handler (lines 17-22), not a responder. Companion: health-flow.integration.test.ts (flows take .integration.test.ts, never .test.ts).
      HealthStatusEndpoint.method — must register the new path with app.get, read-only, in the `app.get(apiRoutesStatics.<group>.<key>, …)` form serverRouteCallsExtractTransformer.ts:30 requires for the route to appear in the architecture map at all.
      check-seed-route-answers (part 2 of 2; chunk 6 owns the rest) — must answer that exact path with 200 and a body that survives a contract parse unchanged.
      check-liveness-probe-unchanged — must leave the existing /api/health handler untouched AND strengthen its test to prove the body has no fourth key; today it asserts only status 200 and status === 'ok'.

DEPENDS:
  ./packages/server/src/brokers/health/status/health-status-broker.ts
      needs: ./packages/server/src/adapters/server-package/version/server-package-version-adapter.ts — the ServerVersion for the payload's `version` field
      needs: @dungeonmaster/shared/contracts (healthStatusPayloadContract, already built into packages/shared/dist) — the parse that produces the branded {status, uptimeSeconds, version}
      needs: the `process.uptime()` global — read per call, never cached, or every frame would repeat the first reading
      needed by: ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.ts — one fresh payload per emit
      needed by: ./packages/server/src/responders/health/status/health-status-responder.ts — the seed body

  ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.ts
      needs: ./packages/server/src/brokers/health/status/health-status-broker.ts — the payload
      needs: ./packages/server/src/brokers/ws-event-relay/broadcast/ws-event-relay-broadcast-broker.ts — `wsEventRelayBroadcastBroker({clients, message}): Set<WsClient>`, which serialises once and deletes a throwing client from the passed Set
      needs: @dungeonmaster/shared/contracts (wsMessageContract) — the {type, payload, timestamp} envelope
      needs: ./packages/server/src/contracts/iso-timestamp/iso-timestamp-contract.ts — the IsoTimestamp brand every envelope in server-init-responder already goes through
      needs: ./packages/server/src/contracts/ws-client/ws-client-contract.ts — the `WsClient` interface (`{send: (data: string) => void}`) for the Set it is handed
      needed by: ./packages/server/src/responders/server/init/server-init-responder.ts — called on each heartbeat tick with the live `clients` set

  ./packages/server/src/adapters/server-package/version/server-package-version-adapter.ts
      needs: ./packages/server/src/contracts/server-version/server-version-contract.ts — the branded return type (adapters may not return a raw string)
      needs: node's `fs` + `require.resolve` — verified: `require.resolve('@dungeonmaster/server/package.json')` resolves to packages/server/package.json in this worktree and that file's `version` is '0.1.0'; neither packages/server/package.json nor packages/web/package.json declares an `exports` map, so the subpath resolution is unrestricted
      needed by: ./packages/server/src/brokers/health/status/health-status-broker.ts — the only caller

  ./packages/server/src/contracts/server-version/server-version-contract.ts
      needs: zod only
      needed by: ./packages/server/src/adapters/server-package/version/server-package-version-adapter.ts — its declared return type. Nothing else in this round imports it.

  ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts
      needs: nothing
      needed by: ./packages/server/src/responders/server/init/server-init-responder.ts — the setInterval period

  ./packages/server/src/statics/api-routes/api-routes-statics.ts
      needs: nothing
      needed by: ./packages/server/src/flows/health/health-flow.ts — `apiRoutesStatics.health.status` as the literal first argument to app.get

  ./packages/server/src/responders/health/status/health-status-responder.ts
      needs: ./packages/server/src/brokers/health/status/health-status-broker.ts — the payload
      needs: ./packages/server/src/contracts/responder-result/responder-result-contract.ts — `{status: HttpStatusCode, data: unknown}`, the shape every flow unwraps
      needs: ./packages/server/src/statics/http-status/http-status-statics.ts — `success.ok` (200) and `serverError.internal` (500)
      needed by: ./packages/server/src/flows/health/health-flow.ts — the route's handler

  ./packages/server/src/responders/server/init/server-init-responder.ts
      needs: ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.ts — the per-tick emit
      needs: ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts — the period
      needed by: ./packages/server/src/flows/server/server-flow.ts:28 — its only construction site, unchanged by this round

  ./packages/server/src/transformers/parse-health-status-payload/parse-health-status-payload-transformer.ts
      needs: @dungeonmaster/shared/contracts (healthStatusPayloadContract) — the safeParse
      needed by: ./packages/server/src/flows/health/health-flow.integration.test.ts — the only consumer; it is how that test validates an unknown body without importing a contract

  ./packages/server/src/flows/health/health-flow.ts
      needs: ./packages/server/src/responders/health/status/health-status-responder.ts — the new route's handler
      needs: ./packages/server/src/statics/api-routes/api-routes-statics.ts — both route literals
      needed by: ./packages/server/src/startup/start-server.ts:47 — already in the subApps array, unchanged by this round

DECISIONS:
  - The heartbeat broadcasts DIRECTLY from ServerInitResponder's `clients` set, never through the orchestration event bus. server-init-responder.ts:650 builds every relayed envelope as `payload: { ...effectivePayload, processId }`, so a frame routed through the bus would carry a fourth key and fail check-emit-payload-advances' "exactly {status, uptimeSeconds, version}". Second reason: the server has no emit adapter — orchestrator-events-on-adapter.ts:23 calls `orchestrationEventsState.on` and `adapters/orchestrator/` holds no `.emit` counterpart.
  - Both ALREADY BUILT seams verified against committed code; no repair chunk is cut. check-event-type-member: orchestration-event-type-contract.ts:36 carries 'health-status' as the 22nd member, `git diff 6629af685..HEAD` on that file is one added line, and its test at :6 pins all 22 literals with toStrictEqual. check-payload-parses: health-status-payload-contract.ts:15-19 declares all three fields required, and its test at :61 asserts a payload missing uptimeSeconds throws /Required/. Both are re-exported from packages/shared/contracts.ts (:260-261 and :758-759) and both are present in packages/shared/dist, so the server needs no shared rebuild to typecheck or run.
  - check-dead-client-does-not-block is NOT settled on disk, even though ws-event-relay-broadcast-broker.ts:23-30 already deletes a throwing client and continues the loop, and its test at :89 already covers a healthy/dead mix. The observable is about a health-status frame, and nothing routes one through that broker yet; this round's proof is the heartbeat broker's own test running the REAL broadcast broker. Recorded because the tempting read is to settle it off that existing test.
  - The server always reports status 'ok'. healthStatusPayloadContract admits 'degraded', but no observable and no design decision in ## Context says what makes THIS server degraded, and inventing a threshold would put a reading on the badge that nothing measures. Every observable naming status names it as a key rather than a value, so nothing is retreated from; the enum member stays reachable for a later session.
  - The heartbeat writes no `[dev]` line, and dev-log-event-icons-statics.ts gains no health-status key. packages/server/CLAUDE.md's "When NOT to Use" rules out logging silent-by-default operation, and a line every 10 seconds forever would drown the orchestration log it shares. The icons map is a partial as-const read through a `typeof iconValue === 'string' ? iconValue : '· '` fallback at dev-log-event-format-transformer.ts:28, so the absence errors nowhere.
  - `version` is read at runtime from @dungeonmaster/server's own package.json rather than pinned in a statics file, because a hardcoded version goes stale silently at the next release. webBundleDistPathAdapter.ts:19 is the repo's existing precedent for `require.resolve` of a `@dungeonmaster/<pkg>/package.json` subpath; verified in this worktree that it resolves to packages/server/package.json and that file's version is 0.1.0. Neither packages/server/package.json nor packages/web/package.json declares an `exports` map, so nothing restricts the subpath.
  - A failed version read THROWS out of the adapter rather than returning a fallback. ban-silent-catch forbids swallowing, an adapter may not import processDevLogAdapter (adapters cannot import adapters), and responders are the architecture's error boundary — rate-limits-get-responder.ts:21-27 is the 500 shape to copy. The heartbeat tick has no HTTP response to fail into, so chunk 7 wraps its own call and logs via processDevLogAdapter, keeping one bad tick from killing the interval.
  - `process.uptime()` is called directly in the payload broker rather than wrapped in an adapter. It is a non-deterministic global, which get-testing-patterns puts in the registerSpyOn/registerMock class alongside Date.now — the brokers folder-detail proxy example mocks Date.now in exactly this position. The server has no adapters/timer/ or adapters/process/uptime/, and the only adapted timer in the repo (orchestrator's timerSetIntervalAdapter) is not on that package's public API.
  - The heartbeat's setInterval is raw and lives in the responder beside flushIntervalHandle. Both existing server intervals are raw (server-init-responder.ts:771, quest-driven-watchers-bootstrap-responder.ts:68) and the SIGTERM/SIGINT handlers at server-init-responder.ts:847-860 are the only teardown in the package. server-init-responder.test.ts:827-832 records the constraint the tests must respect — a real setInterval registered before jest.useFakeTimers() cannot be wrapped retroactively — so the heartbeat tests call jest.useFakeTimers() BEFORE proxy.callResponder(), the way the flush test at line 97 already does.
  - serverVersionContract and the adapter that returns it are ONE chunk. The contract's only importer is that adapter, so it is an adapter's private return brand rather than a layer later phases import; the standing split-the-contract-from-its-consumer rule names responders, and no responder touches it.
  - "No other keys" is proved by `expect(parsed).toStrictEqual(body)`, not by an Object.keys assertion. z.object strips unknown keys, so a body carrying a fourth key parses to something structurally different from the body itself. Object.keys inside expect, toMatchObject and expect.any(Number) are all banned by get-testing-patterns, and expect.stringMatching is the one composed matcher that survives (the responders folder-detail uses it inside a toStrictEqual).
  - The /api/health handler is not touched but its integration test is. health-flow.integration.test.ts:14-15 asserts only that response.status is 200 and parsed?.status is 'ok', so a fourth field could be added to that handler today and the suite stays green. The only exact-key assertion for it lives in packages/web/src/flows/app/smoke.e2e.ts:13-16, which no ward unit or integration run executes — check-liveness-probe-unchanged needs a guard at this level.
  - No chunk touches packages/web. Four observables under the Seams lines are attributed to web and marked NOT BUILT YET (check-seed-request-issued, check-heartbeat-reaches-badge, check-payload-updates-badge, check-reconnect-resumes). What this round leaves that session: GET /api/health/status answering the shared HealthStatusPayload shape, and a GLOBAL (non-per-quest) health-status frame carrying the same three keys, for which web-socket-channel-state.ts:174-188 will need a new `envelope.data.type` arm.
  - The working tree was clean on arrival apart from this round document — `git status --porcelain` listed only the untracked .quest-plans/e29b414d-2094-4ae1-99a4-db5659992a53-round-1.md — so no chunk repairs a dead session's leftovers, and there is no `## Rework` section because this is round 1.
  - The heartbeat emits on TICK ONLY; there is no frame at t=0. A frame at server start fires when no client is connected, and the badge's mount path is the seed route (#seed-fetch), not the socket. So the first frame lands at 10s and exactly three have landed by 35 — which is what makes the "no fourth by 35" half of check-emit-interval checkable rather than vacuous.
  - The heartbeat interval handle is `.unref()`ed. quest-driven-watchers-bootstrap-responder.ts:78 does exactly this, with the comment "Avoid keeping the process alive solely for this timer in test runtimes", and server-init-responder.test.ts calls callResponder() in dozens of tests. A 10-second un-unref'd timer per test is minutes of hang at file teardown, where the existing 100 ms flush timer is invisible.
  - The whole heartbeat chain is SYNCHRONOUS: process.uptime() and readFileSync, never fs/promises. jest.advanceTimersByTime does not flush microtasks, so one async link would leave the frame undelivered inside the very test that proves the interval. This is why the version adapter reads with sync fs rather than the package's async fsReadFileAdapter (fs-read-file-adapter.ts:22), and it matches webBundleDistPathAdapter.ts:24, which calls sync existsSync from 'fs' in the same adapter family.
  - Two lint rules were checked against the version adapter's shape before it was planned. ban-require-in-source only reports a CallExpression whose callee is the Identifier `require` (rule-ban-require-in-source-broker.ts:39-45), so `require.resolve(...)` — a MemberExpression callee — is clean, which is why webBundleDistPathAdapter.ts:19 lints today. no-hardcoded-package-names anchors on a `packages/<name>` path segment (banned-package-path-names-transformer.ts:30-33) and its PURPOSE explicitly exempts a module specifier, so the string naming the server's own package.json subpath is clean.
  - Every test in this round puts at most ONE expect(obj.property) per it block. no-multiple-property-assertions (rule-no-multiple-property-assertions-broker.ts:77-92) reports two or more assertions whose expect() argument is a MemberExpression on the same root, so the flow tests assert expect(response.status).toBe(200) once and then whole objects by Identifier — expect(body).toStrictEqual(parsed) for the no-extra-keys proof, expect(parsed).toStrictEqual(...) for the values. expect.stringMatching is permitted inside toStrictEqual provided the regex carries BOTH anchors (rule-ban-weak-asymmetric-matchers-broker.test.ts:14-18 exempts it; rule-ban-unanchored-to-match-broker.ts:56-77 requires the anchors).
  - devLogEventIconsStatics needs no new key and its test needs no change: dev-log-event-icons-statics.test.ts:5-19 pins eleven hardcoded keys and derives nothing from the event-type enum, so the shared round's 22nd member did not red it. server-init-responder.ts:539 is the ONLY consumer of orchestrationEventTypeContract.options anywhere in packages/server or packages/web.

ASSERTIONS:
  - GET /api/health/status answers 200 with a JSON body of exactly three keys — status, uptimeSeconds, version — and that body survives a healthStatusPayloadContract parse structurally unchanged.
  - GET /api/health still answers 200 with exactly two keys, status and timestamp, and a test inside packages/server fails if a third appears.
  - apiRoutesStatics.health holds exactly two keys — check pointing at /api/health and status pointing at /api/health/status — and api-routes-statics.test.ts's whole-object toStrictEqual says so.
  - With ServerInitResponder initialised, a WebSocket client that completed the upgrade and sent no message receives a health-status frame every 10 seconds: three frames have landed by 30 seconds of simulated time and no fourth by 35.
  - Every emitted health-status frame is exactly one send call per client, carrying JSON.stringify of an envelope whose type is health-status, with payload and timestamp beside it, and whose payload holds exactly status, uptimeSeconds and version.
  - Two consecutive health-status frames carry strictly increasing uptimeSeconds when the process uptime advanced between them.
  - A client whose send throws during a heartbeat emit is removed from the connected set, and every other client in that set still receives that same serialised frame.
  - A client holding no quest subscription receives every health-status frame, and health-status is absent from PER_QUEST_EVENT_TYPES in server-init-responder.ts.
  - healthStatusPayloadContract is imported from @dungeonmaster/shared/contracts by the server, and the three-key shape is restated nowhere under packages/server/src.
  - The heartbeat interval handle is cleared in BOTH the SIGTERM and the SIGINT handler of ServerInitResponder, beside flushIntervalHandle.
  - healthHeartbeatStatics declares 10000 ms under emit.intervalMs, and no source file under packages/server/src passes that number to setInterval as an inline literal. (The bare literal already occurs unrelated at comment-batch-body-contract.test.ts:264 in an 'x'.repeat(10_000) fixture, so the claim is about the setInterval argument, not the digits.)
  - The version reported on both surfaces is read at runtime from @dungeonmaster/server's package.json, so it is 0.1.0 in this worktree without any source file containing that literal.
  - No file under packages/web is changed by this round.

NO CHUNK: none

### chunk 1 — the 10-second heartbeat period gets a statics file
INTENT:
  - packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts exports healthHeartbeatStatics with emit.intervalMs equal to 10000.
  - Its colocated test asserts the WHOLE exported object in one toStrictEqual, so a key added later cannot slip in unasserted.
  - The object is `as const` and holds no primitive at its root.
FILES:
  - ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts
  - ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.test.ts
UNITS:
  - HealthHeartbeatStatics.emitIntervalMs → ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts — must expose 10000 ms as emit.intervalMs, the period chunk 7 hands to setInterval.
MIRROR: ./packages/server/src/statics/http-status/http-status-statics.ts
NOTES:
  1. FLOW. #health-badge: the app top bar gains a badge visible on every route. The user opens any page; the badge seeds itself from GET /api/health/status and then tracks a health-status heartbeat the server emits every 10 seconds over the WebSocket the interface already holds open. This chunk implements the PERIOD of node #server-emits and nothing else.
  2. OBSERVABLES. This chunk carries no observable id. The observable its value serves, quoted word for word, is check-emit-interval on #server-emits: "With the server running, three health-status frames arrive within 35 seconds, spaced 10 seconds apart" — chunk 7 proves it.
  3. CONTRACTS. None imported; a statics file may import only other statics. The contract requirement it satisfies, quoted: HealthHeartbeatStatics.emitIntervalMs — "How often the server emits a health-status frame. Three of these fit inside the web side's 30 second silence threshold."
  4. DESIGN DECISIONS, quoted. "DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload. Making any state stop listening would strand the badge on a reading that was true once and is not any more." And: "A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it." Both mean the period must stay short relative to the 30-second silence threshold, so an ordinary hiccup never reads as offline.
  5. ALREADY-BUILT EXPORTS. None consumed. The shape to copy is `httpStatusStatics` in the MIRROR file: a PURPOSE/USAGE docblock naming one key and its value, then `export const httpStatusStatics = ... as const` with two levels of nesting, no zod and no branding. Its colocated test (http-status-statics.test.ts:4-20) is a single `it` holding one toStrictEqual over the whole object — copy that exactly.
  Name the key `emit`, holding `intervalMs`. Root primitives are banned and every other statics file in this package nests. Do NOT copy orchestrator's processStaleThresholdStatics, which is flat.

### chunk 2 — /api/health/status joins the health route group
INTENT:
  - apiRoutesStatics.health.status is the string /api/health/status.
  - apiRoutesStatics.health.check is unchanged and still /api/health.
  - api-routes-statics.test.ts's single whole-object toStrictEqual carries both health keys and passes.
FILES:
  - ./packages/server/src/statics/api-routes/api-routes-statics.ts
  - ./packages/server/src/statics/api-routes/api-routes-statics.test.ts
UNITS:
  - HealthStatusEndpoint.path → ./packages/server/src/statics/api-routes/api-routes-statics.ts — must add a `status` key holding /api/health/status inside the existing `health` group, leaving `check` untouched.
MIRROR: ./packages/server/src/statics/api-routes/api-routes-statics.ts
NOTES:
  1. FLOW. #health-badge, node #seed-fetch. The badge issues one GET at mount, and clicking an OFFLINE badge retries that same GET. This is where the path is declared. Nothing else in this chunk.
  2. OBSERVABLES. No observable id. The one this path serves, quoted word for word, is check-seed-route-answers on #seed-fetch: "GET /api/health/status returns 200 with a JSON body of exactly {status, uptimeSeconds, version} and no other keys" — chunks 6 and 8 prove it.
  3. CONTRACTS. None imported. The contract requirement, quoted: HealthStatusEndpoint.path — "A new key added to the existing health group in this statics file, alongside the check key that already holds /api/health." And HealthStatusEndpoint.method — "Read-only; the badge never mutates server state" (chunk 8 registers it with app.get).
  4. DESIGN DECISIONS, quoted. "DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload. Making any state stop listening would strand the badge on a reading that was true once and is not any more." And: "A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it." Neither constrains a route literal; they are here so the worker knows the badge retries this same path on click, which is why it stays side-effect free.
  5. ALREADY-BUILT EXPORTS. `apiRoutesStatics` is at api-routes-statics.ts:9, `as const`; its `health` group at lines 10-12 currently holds only `check`. The `tooling` group at lines 54-57 is the two-key shape to copy. The colocated test at lines 5-63 is ONE toStrictEqual over the entire object, so the new key MUST be added there too — this is the single most likely way this chunk comes back red.
  This statics file is the architecture's canonical route registry: httpEdgesLayerBroker.ts:46 hardcodes its path when resolving dotted route references, so the key belongs here rather than inline in the flow.

### chunk 3 — the ServerVersion brand, and the adapter that reads it off package.json
INTENT:
  - serverVersionContract parses a non-empty string and throws on an empty one.
  - serverPackageVersionAdapter() returns the version field of the server package's own package.json, and its test proves the value came from the file the proxy staged rather than from a literal inside the adapter.
  - When the read fails, the adapter throws an Error whose message names the subpath it tried; it never returns a fallback and never swallows.
FILES:
  - ./packages/server/src/contracts/server-version/server-version-contract.ts
  - ./packages/server/src/contracts/server-version/server-version-contract.test.ts
  - ./packages/server/src/contracts/server-version/server-version.stub.ts
  - ./packages/server/src/adapters/server-package/version/server-package-version-adapter.ts
  - ./packages/server/src/adapters/server-package/version/server-package-version-adapter.proxy.ts
  - ./packages/server/src/adapters/server-package/version/server-package-version-adapter.test.ts
UNITS: none — the ServerVersion brand and the reader that returns it. No observable and no contract requirement names either; chunk 4 is the only consumer, and the requirement they serve (HealthStatusEndpoint.responseBody) is proved there.
MIRROR: ./packages/server/src/adapters/web-bundle/dist-path/web-bundle-dist-path-adapter.ts
NOTES:
  1. FLOW. #health-badge. The badge shows the running server's version beside its status, on both the seed body (#seed-fetch) and every heartbeat frame (#server-emits). This chunk is the source of that string; it touches no node directly.
  2. OBSERVABLES. None. The requirement downstream, quoted word for word: HealthStatusEndpoint.responseBody — "The 200 body is exactly the shared HealthStatusPayload shape, so server and web agree by importing one contract rather than restating it."
  3. CONTRACTS. Declare serverVersionContract as a zod string with a minimum length of 1, branded ServerVersion, and export the inferred type beside it. Use that exact brand tag: shared's healthStatusPayloadContract.version carries the same one (health-status-payload-contract.ts:18), so chunk 4's parse re-brands with no type assertion. The stub takes the branded-primitive form — a single `value` property parsed through the contract.
  4. DESIGN DECISIONS, quoted. "DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload. Making any state stop listening would strand the badge on a reading that was true once and is not any more." And: "A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it."
  5. ALREADY-BUILT EXPORTS. Copy the MIRROR closely: `webBundleDistPathAdapter` resolves a sibling package by require.resolve of that package's package.json subpath (line 19) and reads with a SYNC fs call imported straight from 'fs' (line 24). Do the same for the server's own package: resolve its package.json subpath, read it with readFileSync, JSON.parse it, and parse the version field through serverVersionContract. Four constraints, each already verified against the tree:
     - Use SYNC fs. Do NOT reach for fsReadFileAdapter (fs-read-file-adapter.ts:17) — it is async, and chunk 7's fake-timer test cannot flush a microtask between interval ticks.
     - require.resolve is legal here. ban-require-in-source only reports a callee that is the bare Identifier `require` (rule-ban-require-in-source-broker.ts:39-45); a member-expression callee is not reported, which is why the MIRROR lints clean today.
     - The package-name string is legal here. no-hardcoded-package-names anchors on a `packages/<name>` path segment (banned-package-path-names-transformer.ts:30-33) and its PURPOSE explicitly exempts a module specifier.
     - It works under jest: the MIRROR's own test (web-bundle-dist-path-adapter.test.ts:5-12) exercises require.resolve for real in this package, so no module mock is needed. Verified in this worktree that the resolution lands on packages/server/package.json and its version field is 0.1.0.
     The proxy mocks the fs read only — web-bundle-dist-path-adapter.proxy.ts:8 is the shape, using registerMock on the imported fs function. Adapters must not return void, so the failure path THROWS rather than returning null: ban-silent-catch forbids swallowing and an adapter may not import processDevLogAdapter. Every server contract folder carries contract + test + stub (health-response/, iso-timestamp/, mtime-ms/, tool-name/), and a colocated contract test imports its own contract directly, the way health-status-payload-contract.test.ts:1 does.

### chunk 4 — one HealthStatusPayload, built once for both surfaces
INTENT:
  - healthStatusBroker() returns a value that parses as healthStatusPayloadContract: status ok, uptimeSeconds the floor of process.uptime(), version from serverPackageVersionAdapter.
  - uptimeSeconds is an integer — with the proxy staging process.uptime() at 42.9, the returned uptimeSeconds is 42.
  - Two calls with different staged uptimes return different uptimeSeconds; nothing is cached between calls.
  - The broker is synchronous: its return type is the payload, not a Promise.
FILES:
  - ./packages/server/src/brokers/health/status/health-status-broker.ts
  - ./packages/server/src/brokers/health/status/health-status-broker.proxy.ts
  - ./packages/server/src/brokers/health/status/health-status-broker.test.ts
UNITS:
  - HealthStatusEndpoint.responseBody → ./packages/server/src/brokers/health/status/health-status-broker.ts — must produce the body by parsing through the shared healthStatusPayloadContract, so the three-key shape is imported rather than restated anywhere in this package.
MIRROR: ./packages/server/src/brokers/web-bundle/response/web-bundle-response-broker.ts
NOTES:
  1. FLOW. #health-badge. This is the one place the badge's reading is assembled. Node #seed-fetch reads it over HTTP (chunk 6) and node #server-emits puts the same value on the wire every 10 seconds (chunk 5). Building it once is what makes "the same three keys on both surfaces" structural rather than a coincidence.
  2. OBSERVABLES. No observable id of its own. The two it serves, quoted word for word: check-seed-route-answers on #seed-fetch — "GET /api/health/status returns 200 with a JSON body of exactly {status, uptimeSeconds, version} and no other keys"; check-emit-payload-advances on #server-emits — "Each health-status frame carries exactly {status, uptimeSeconds, version}, and uptimeSeconds is strictly greater than the previous frame's".
  3. CONTRACTS. Returns HealthStatusPayload. Import healthStatusPayloadContract and the HealthStatusPayload type from '@dungeonmaster/shared/contracts' — already built into packages/shared/dist, so no rebuild is needed. Its declaration (health-status-payload-contract.ts:15-19) is a zod object of three REQUIRED fields: status is an enum of ok and degraded branded HealthStatusValue; uptimeSeconds is an int, nonnegative, branded UptimeSeconds; version is a min-1 string branded ServerVersion. Do NOT restate that shape locally, and do NOT reuse the server's own healthResponseContract — that one is the bare /api/health liveness shape and brands HealthStatus onto a plain string (health-response-contract.ts:12).
  4. DESIGN DECISIONS, quoted. "DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload. Making any state stop listening would strand the badge on a reading that was true once and is not any more." And: "A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it."
  5. ALREADY-BUILT EXPORTS. `serverPackageVersionAdapter` from chunk 3, at ./packages/server/src/adapters/server-package/version/server-package-version-adapter.ts, returning ServerVersion synchronously. `HealthStatusPayloadStub` is exported from '@dungeonmaster/shared/contracts' and defaults to status ok, uptimeSeconds 11520, version 0.1.0 — build the expected values in the test from it.
  Report status as ok, always. The contract admits degraded, but nothing in this quest says what makes THIS server degraded, and a made-up threshold would put a reading on the badge that nothing measures. The enum member stays reachable for a later session.
  Read uptime by calling process.uptime() directly and flooring it. There is no uptime adapter in this package and none is wanted: it is a non-deterministic global, the class the testing patterns hand to registerSpyOn alongside Date.now. The proxy stages it with registerSpyOn on the process object's uptime method — the brokers folder-detail proxy example stages Date.now in exactly this position — and composes serverPackageVersionAdapterProxy. web-bundle-response-broker.proxy.ts is the shape: instantiate the child adapter proxy, expose semantic setup methods, and never call registerMock from the test file.
  Keep it SYNCHRONOUS. Chunk 7 drives this chain from a fake-timer tick that cannot flush a microtask, so a single await anywhere in it silently delivers nothing inside the test that proves the interval.

### chunk 5 — the health-status frame: envelope, fan-out, dead-client drop
INTENT:
  - healthHeartbeatEmitBroker given a Set holding one client calls that client's send exactly once, with the JSON string of an envelope whose type is health-status, whose payload holds exactly the three payload keys, and whose timestamp is an ISO string.
  - Given two clients where the first client's send throws, that first client is absent from the passed Set afterwards and the second client received that same string.
  - Two successive calls with staged uptimes 100 then 110 produce payloads whose uptimeSeconds are 100 then 110, in that order.
  - Its return value is the dead-client Set the broadcast broker hands back.
FILES:
  - ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.ts
  - ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.proxy.ts
  - ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.test.ts
UNITS:
  - check-emit-payload-advances → ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.ts — must put exactly the three payload keys on the wire and re-read uptime per call, so consecutive frames advance.
  - check-dead-client-does-not-block → ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.ts — must route through the REAL wsEventRelayBroadcastBroker so a throwing client is deleted from the Set while every remaining client still receives the identical serialised frame.
  - check-frame-crosses-wire → ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.ts — must produce one serialised text message per client per emit, carrying type, payload and timestamp.
MIRROR: ./packages/server/src/brokers/ws-event-relay/broadcast/ws-event-relay-broadcast-broker.ts
NOTES:
  1. FLOW. #health-badge, nodes #server-emits and #relay-broadcast. Every 10 seconds the server puts one frame on the socket the interface already holds open; the badge reads it and stays ONLINE. This chunk builds the frame and fans it out. Chunk 7 owns the timer that calls it.
  2. OBSERVABLES, quoted word for word. check-emit-payload-advances on #server-emits: "Each health-status frame carries exactly {status, uptimeSeconds, version}, and uptimeSeconds is strictly greater than the previous frame's". check-dead-client-does-not-block on #relay-broadcast: "When one client's send throws, that client is dropped from the connected set and every remaining client still receives that same frame". check-frame-crosses-wire on #channel-routes: "The serialised health-status envelope arrives at the browser as one WebSocket text message carrying type, payload and timestamp" — the browser-arrival half of that last one is walked by the groundstomper's browser pass after this round; what is provable HERE is the single serialised text message per client, and that is what this chunk proves.
  3. CONTRACTS. Takes a Set of WsClient and returns a Set of WsClient. Build the envelope with wsMessageContract from '@dungeonmaster/shared/contracts' — declared at ws-message-contract.ts:13-17 as type (the orchestration event type enum), payload (a record) and timestamp (an ISO datetime branded IsoTimestamp). The type literal is health-status, already the 22nd member of orchestrationEventTypeContract (orchestration-event-type-contract.ts:36), so the envelope widens with no contract edit anywhere. Brand the timestamp through isoTimestampContract at ./packages/server/src/contracts/iso-timestamp/iso-timestamp-contract.ts:14, the way every envelope in server-init-responder already does.
  4. DESIGN DECISIONS, quoted. "DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload. Making any state stop listening would strand the badge on a reading that was true once and is not any more." And: "A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it." Together they mean this broker holds NO per-client state and NO subscription filter: it sends the same frame to everything in the Set it is handed, every time.
  5. ALREADY-BUILT EXPORTS. `wsEventRelayBroadcastBroker` at ws-event-relay-broadcast-broker.ts:13 — it takes clients (a Set of WsClient) plus message (a WsMessage) and returns a Set of WsClient. It stringifies ONCE at line 21 and, at lines 23-30, deletes a throwing client from the passed Set and continues the loop. CALL IT; do not re-implement the loop, and do not stringify before handing it the envelope. `healthStatusBroker` from chunk 4 supplies the payload — call it per emit, never once at module load, or every frame repeats the first uptime. `WsClient` is the interface at ws-client-contract.ts:11, a single send method taking a string. `WsClientStub` at ws-client.stub.ts:6 builds one and accepts a throwing send; ws-event-relay-broadcast-broker.test.ts:75 and :92 show that setup.
  The proxy composes healthStatusBrokerProxy and wsEventRelayBroadcastBrokerProxy. The broadcast broker runs REAL — that is the whole point of the dead-client unit — so its proxy contributes only its capture client (ws-event-relay-broadcast-broker.proxy.ts:6-22 exposes captureClient and getCapturedMessages, and the capture client parses each sent string back into a WsMessage, which is how you read the wire envelope). Keep this broker SYNCHRONOUS for chunk 7's fake-timer tick.

### chunk 6 — GET /api/health/status returns the seed body
INTENT:
  - HealthStatusResponder() returns status 200 with `data` equal to the payload, asserted in ONE toStrictEqual over the whole result, so a fourth key in the body fails the test.
  - When the broker throws, it returns status 500 with an error message and does not propagate.
  - It is synchronous — no await, and no Promise in its return type.
FILES:
  - ./packages/server/src/responders/health/status/health-status-responder.ts
  - ./packages/server/src/responders/health/status/health-status-responder.proxy.ts
  - ./packages/server/src/responders/health/status/health-status-responder.test.ts
UNITS:
  - check-seed-route-answers (part 1 of 2; chunk 8 owns the rest) → ./packages/server/src/responders/health/status/health-status-responder.ts — must return 200 with data holding exactly the three payload keys and nothing else.
MIRROR: ./packages/server/src/responders/tooling/smoketest-state/tooling-smoketest-state-responder.ts
NOTES:
  1. FLOW. #health-badge, node #seed-fetch. On mount the badge issues one GET and paints from the answer; clicking an OFFLINE badge retries that same GET. This chunk is the handler behind that path.
  2. OBSERVABLES, quoted word for word. check-seed-route-answers on #seed-fetch: "GET /api/health/status returns 200 with a JSON body of exactly {status, uptimeSeconds, version} and no other keys". This chunk owns the BODY; chunk 8 owns the path answering.
  3. CONTRACTS. Returns ResponderResult from ./packages/server/src/contracts/responder-result/responder-result-contract.ts:11-16 — an object of a branded int status and an unknown data. `data` must BE the payload, not a wrapper around it: the observable says the BODY is exactly the three keys, so do NOT nest it under a key the way rate-limits-get-responder nests a snapshot. Status codes come from httpStatusStatics (http-status-statics.ts:9): success.ok is 200, serverError.internal is 500.
  4. DESIGN DECISIONS, quoted. "DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload. Making any state stop listening would strand the badge on a reading that was true once and is not any more." And: "A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it." The first means this route is hit again on every retry click, so it must stay read-only and cheap.
  5. ALREADY-BUILT EXPORTS. `healthStatusBroker` from chunk 4 — synchronous, returning HealthStatusPayload. `responderResultContract` and the ResponderResult type at responder-result-contract.ts:11-16. `httpStatusStatics` at http-status-statics.ts:9. The MIRROR is the synchronous responder shape (no await, plain ResponderResult return, three imports). For the 500 arm copy rate-limits-get-responder.ts:21-27, which catches, reads a message off an Error and returns serverError.internal with that message. Its proxy (rate-limits-get-responder.proxy.ts) is the shape for a proxy exposing callResponder plus setup methods, and its test (rate-limits-get-responder.test.ts:13-41) is the shape for asserting the whole result in one toStrictEqual.

### chunk 7 — the server emits a heartbeat to every connected socket
INTENT:
  - Under fake timers, a client connected via simulateConnection that sent NO message has received one health-status frame after 10 seconds are advanced, three after 30, and still three after 35.
  - A second client that sent a subscribe-quest message receives the same frames as the first, and both counts are equal.
  - Each frame those clients receive parses as an envelope whose type is health-status and whose payload holds exactly the three payload keys.
  - The heartbeat handle is cleared in BOTH signal handlers: server-init-responder.ts holds a clearInterval for it inside the SIGTERM handler and another inside the SIGINT handler, beside the two for flushIntervalHandle.
FILES:
  - ./packages/server/src/responders/server/init/server-init-responder.ts
  - ./packages/server/src/responders/server/init/server-init-responder.proxy.ts
  - ./packages/server/src/responders/server/init/server-init-responder.test.ts
UNITS:
  - check-emit-interval → ./packages/server/src/responders/server/init/server-init-responder.ts — must start the interval at healthHeartbeatStatics' period once the client set exists, and clear it in both signal handlers.
  - check-client-in-broadcast-set → ./packages/server/src/responders/server/init/server-init-responder.ts — must emit over `clients`, which onOpen fills on the upgrade alone, so a client that has sent nothing is already a recipient.
  - check-broadcast-reaches-unsubscribed-client → ./packages/server/src/responders/server/init/server-init-responder.ts — must not consult clientSubscriptions, and must leave health-status out of PER_QUEST_EVENT_TYPES.
MIRROR: ./packages/server/src/responders/quest-driven-watchers/bootstrap/quest-driven-watchers-bootstrap-responder.ts
NOTES:
  1. FLOW. #health-badge, nodes #subscribe-heartbeat and #relay-broadcast. Every browser holding the app open gets the frame, whatever page it is on and whether or not it has subscribed to a quest — the badge is in the top bar on every route. This chunk is the timer and the wiring; chunk 5 owns the frame itself.
  2. OBSERVABLES, quoted word for word. check-emit-interval on #server-emits: "With the server running, three health-status frames arrive within 35 seconds, spaced 10 seconds apart". check-client-in-broadcast-set on #subscribe-heartbeat: "A client that completes the /ws upgrade receives the next health-status frame without sending any subscription message first". check-broadcast-reaches-unsubscribed-client on #relay-broadcast: "A client holding no quest subscription still receives every health-status frame".
  3. CONTRACTS. None new here; the frame's contracts belong to chunk 5. What crosses into this file is healthHeartbeatEmitBroker taking the Set of WsClient this file already owns.
  4. DESIGN DECISIONS, quoted. "DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload. Making any state stop listening would strand the badge on a reading that was true once and is not any more." And: "A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it." The first is why the emit is unconditional — no client state, no gating on whether a badge already read healthy. The second is why nothing here reacts to a socket opening or closing beyond the existing onOpen and onClose.
  5. ALREADY-BUILT EXPORTS, all in the file you are editing. `clients` at server-init-responder.ts:88 is the Set of WsClient, filled at line 144 by onOpen with no message read — that line alone satisfies check-client-in-broadcast-set once something emits. `clientSubscriptions` at line 91 is the per-quest map you must NOT consult. `PER_QUEST_EVENT_TYPES` at lines 65-74 does not contain health-status and must stay that way. `flushIntervalHandle` at line 771 is the in-file interval to place the new one beside, and the SIGTERM/SIGINT handlers at lines 847-860 each already call clearInterval on it. `processDevLogAdapter` is already imported at line 33. `healthHeartbeatEmitBroker` from chunk 5 takes the clients Set; `healthHeartbeatStatics` from chunk 1 holds the period.
  Emit on TICK ONLY — no frame at responder start. A frame at t=0 fires with nobody connected, and the badge's mount path is the seed route, not the socket. This is what makes the "and still three at 35" half of the interval assertion checkable rather than vacuous.
  Call .unref() on the new handle, with the reason in a comment. The MIRROR does exactly this at line 78 ("Avoid keeping the process alive solely for this timer in test runtimes"); server-init-responder.test.ts calls callResponder() in dozens of tests, and a 10-second timer left referenced in each is minutes of hang at teardown, where the existing 100 ms one is invisible.
  Wrap the per-tick call so one throw cannot kill the interval, and log the reason via processDevLogAdapter. That is the ONLY logging the heartbeat does — do NOT log a line per successful tick; a line every 10 seconds forever drowns the orchestration log it shares, and packages/server/CLAUDE.md rules out logging silent-by-default operation.
  TEST MECHANICS, and this is where the chunk goes wrong if it goes wrong. Call jest.useFakeTimers() BEFORE proxy.callResponder(), the way the flush test at server-init-responder.test.ts:97-107 does — the comment at lines 827-832 records that a real setInterval registered first cannot be wrapped retroactively. Then advance, then jest.useRealTimers(). The whole emit chain is synchronous by design so a tick delivers inside the same advance with no microtask flush. Add the heartbeat broker's proxy as a child in server-init-responder.proxy.ts or enforce-proxy-child-creation reds — that child is also what keeps process.uptime and the fs read mocked for every OTHER test in this file. Existing tests assert that a connected client's send call list is empty; they run on real timers and finish in milliseconds, so a 10-second period never fires in them. Do not shorten the period to make a test convenient. The proxy already pins Date.prototype.toISOString to a fixed value (server-init-responder.proxy.ts:106-111), so the envelope timestamp is deterministic in assertions.

### chunk 8 — the health sub-app answers both routes, and neither body leaks a key
INTENT:
  - HealthFlow() requested at /api/health/status answers 200, and its raw body toStrictEquals what parseHealthStatusPayloadTransformer returns for it — proving it carries exactly the three payload keys and no fourth.
  - HealthFlow() requested at /api/health answers 200, its raw body toStrictEquals what parseHealthResponseTransformer returns for it, and that parsed value toStrictEquals an object of exactly status ok plus a fully anchored ISO timestamp.
  - parseHealthStatusPayloadTransformer returns undefined for a body missing uptimeSeconds and for a non-object.
  - The existing /api/health handler in health-flow.ts is unchanged.
FILES:
  - ./packages/server/src/flows/health/health-flow.ts
  - ./packages/server/src/flows/health/health-flow.integration.test.ts
  - ./packages/server/src/transformers/parse-health-status-payload/parse-health-status-payload-transformer.ts
  - ./packages/server/src/transformers/parse-health-status-payload/parse-health-status-payload-transformer.test.ts
UNITS:
  - HealthStatusEndpoint.method → ./packages/server/src/flows/health/health-flow.ts — must register the new path with app.get, read-only, mutating nothing.
  - check-seed-route-answers (part 2 of 2; chunk 6 owns the rest) → ./packages/server/src/flows/health/health-flow.ts — must answer that exact path with 200 and a body that survives a contract parse unchanged.
  - check-liveness-probe-unchanged → ./packages/server/src/flows/health/health-flow.ts — must leave the existing handler untouched AND add the assertion that proves its body gains no third key.
MIRROR: ./packages/server/src/flows/tooling/tooling-flow.ts
NOTES:
  1. FLOW. #health-badge, nodes #seed-fetch and #channel-routes. This is the last wiring step: the badge's GET now resolves. HealthFlow() is already in StartServer's subApps array (start-server.ts:47) and takes no arguments, so nothing under startup/ changes.
  2. OBSERVABLES, quoted word for word. check-seed-route-answers on #seed-fetch: "GET /api/health/status returns 200 with a JSON body of exactly {status, uptimeSeconds, version} and no other keys". check-liveness-probe-unchanged on #seed-fetch: "GET /api/health still returns 200 with a body of exactly {status: 'ok', timestamp} and gains no new fields".
  3. CONTRACTS. The new transformer safe-parses an unknown body through healthStatusPayloadContract from '@dungeonmaster/shared/contracts' and returns the payload or undefined. Copy parse-health-response-transformer.ts:12-19 exactly — same signature shape, same safeParse-then-ternary body, same PURPOSE/USAGE header form — and its test at parse-health-response-transformer.test.ts:3-27 for the three cases. Server transformers import shared contract VALUES routinely (parse-chat-output-entries-transformer.ts:13 imports chatEntryContract), so this is the ordinary shape.
  4. DESIGN DECISIONS, quoted. "DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload. Making any state stop listening would strand the badge on a reading that was true once and is not any more." And: "A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it." The first means the route is hit again on every retry click, so it stays read-only.
  5. ALREADY-BUILT EXPORTS. `HealthFlow` at health-flow.ts:14 returns a Hono app; its existing route sits at lines 17-22 as an inline handler and must NOT be touched. `apiRoutesStatics.health.status` comes from chunk 2. `HealthStatusResponder` comes from chunk 6, synchronous. `parseHealthResponseTransformer` at parse-health-response-transformer.ts:12 is already imported by the integration test. The MIRROR's SECOND route (tooling-flow.ts:26-29) is the exact form to copy for a synchronous responder-backed GET. Register it as app.get with the dotted statics reference as the LITERAL first argument — serverRouteCallsExtractTransformer.ts:30 only recognises that form, and a route it cannot read never appears in the architecture map.
  TEST MECHANICS. Drive the flow the way the existing test does (health-flow.integration.test.ts:8-12): build the app with HealthFlow(), await app.request on the path, read the body as unknown, hand it to the transformer. Prove "no other keys" with expect(body).toStrictEqual(parsed) — a zod object STRIPS unknown keys, so a body carrying a fourth parses into something structurally different from itself, and the same assertion also proves the parse succeeded. Assert at most ONE expect over a member expression per it block: no-multiple-property-assertions (rule-no-multiple-property-assertions-broker.ts:77-92) reports two or more assertions on properties of the same root, so use expect(response.status).toBe(200) once and whole-object assertions on the Identifier for everything else. For the liveness timestamp use expect.stringMatching inside the toStrictEqual with BOTH anchors present — unanchored is a lint error (rule-ban-unanchored-to-match-broker.ts:56-77) and the composed matcher is explicitly permitted inside toStrictEqual (rule-ban-weak-asymmetric-matchers-broker.test.ts:14-18). Do NOT assert the seed route's uptimeSeconds value here: it is real and non-deterministic in an integration test, expect.any(Number) is banned, and chunk 6 already pins the exact three-key body deterministically. Flows take .integration.test.ts and never a .test.ts or a .proxy.ts.

PHASES:
  1: wave 1 — the statics and the version source every later chunk reads
  2: waves 2-3 — the payload broker, then the frame emitter and the HTTP responder over it
  3: wave 4 — the wiring: the heartbeat interval on the live client set, and the route

WAVES:
  1: 1, 2, 3
  2: 4
  3: 5, 6
  4: 7, 8

## Round log
### report — chunk 1
RESULT:
  - packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts exports healthHeartbeatStatics with emit.intervalMs equal to 10000 — yes — the file exports `export const healthHeartbeatStatics = { emit: { intervalMs: 10000 } } as const;`
  - Its colocated test asserts the WHOLE exported object in one toStrictEqual, so a key added later cannot slip in unasserted — yes — health-heartbeat-statics.test.ts has one it() with a single `expect(healthHeartbeatStatics).toStrictEqual({ emit: { intervalMs: 10000 } })` over the entire object.
  - The object is `as const` and holds no primitive at its root — yes — the root holds one key, `emit`, itself an object; `as const` closes the literal.
FILES:    ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts, ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.test.ts
EVIDENCE:
  - HealthHeartbeatStatics.emitIntervalMs: staged the shell at `intervalMs: 0` and ran scoped ward; the toStrictEqual failed with `Object {"emit": Object {- "intervalMs": 10000, + "intervalMs": 0}}` (unit AND integration both FAIL on this file) — a wrong-value red. Fixed the value to 10000 and reran; unit/integration both PASS.
USAGES:   discover grep for `healthHeartbeatStatics` returns only this chunk's own two files — no other chunk consumes it yet. Chunk 7 (server-init-responder.ts, wave 4) is the only planned consumer and has not started.
GOTCHAS:  none.
MARKERS:  none
WARD:     npm run ward -- -- ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.test.ts — green — lint PASS, typecheck PASS, unit PASS (1/1), integration PASS (1/1), e2e skip (package not e2e-eligible content on this scope).

### report — chunk 2
RESULT:
  - apiRoutesStatics.health.status is the string /api/health/status. — yes — api-routes-statics.ts's health group now reads `{ check: '/api/health', status: '/api/health/status' }`.
  - apiRoutesStatics.health.check is unchanged and still /api/health. — yes — the `check: '/api/health'` line is byte-unchanged from before this chunk.
  - api-routes-statics.test.ts's single whole-object toStrictEqual carries both health keys and passes. — yes — ward run 1787937490939-eca3: unit PASS, 2 files, 176 discovered, 0 failures.
FILES:    ./packages/server/src/statics/api-routes/api-routes-statics.ts, ./packages/server/src/statics/api-routes/api-routes-statics.test.ts
EVIDENCE:
  - apiRoutesStatics toStrictEqual(full object incl. health.status) — red before the implementation edit: `expect(received).toStrictEqual(expected)` diff showed `- "status": "/api/health/status",` absent from received (received health object held only `check`). Ward run 1787937399130-2e03, unit FAIL 1/176.
  - Same assertion after adding the key to the implementation — green: ward run 1787937490939-eca3, unit PASS 2/176, integration PASS 13/14.
USAGES:   discover grep `apiRoutesStatics.health` — hits: packages/shared/src/brokers/architecture/edge-graph/http-edges-layer-broker.test.ts:164 (`apiRoutesStatics.health.check`), packages/shared/src/transformers/statics-path-resolve/statics-path-resolve-transformer.test.ts:38,41 (`apiRoutesStatics.health.check`), packages/server/src/flows/health/health-flow.ts:17 (`apiRoutesStatics.health.check`). All three read `.check` only, which this chunk left byte-unchanged; none read `.status` yet (chunk 8 wires that). Nothing broken.
GOTCHAS:
  - The FIRST scoped ward run (taken before the implementation edit, to capture the red) also failed typecheck on packages/server/src/statics/health-heartbeat/health-heartbeat-statics.test.ts ("Cannot find module './health-heartbeat-statics'"), even though both files existed on disk under packages/server/src/statics/health-heartbeat/ — that folder is chunk 1's, a sibling in this same wave, evidently mid-write at that moment. I did not touch anything in that folder. The SECOND (final) scoped ward run came back fully green including typecheck across all 12 packages / 6157 files, so chunk 1 had landed by then. Flagging this so the reviewer reads it as a wave-timing artifact, not a defect in this chunk.
MARKERS:  none
WARD:     npm run ward -- -- ./packages/server/src/statics/api-routes/api-routes-statics.ts ./packages/server/src/statics/api-routes/api-routes-statics.test.ts — green — lint PASS (2/2), typecheck PASS (12 packages/6157 files), unit PASS (2/176), integration PASS (13/14), e2e skip (server is not e2e-eligible).

### report — chunk 3
RESULT:
  - serverVersionContract parses a non-empty string and throws on an empty one — yes — `serverVersionContract.parse('')` throws (regex `/too_small/u` matched); `ServerVersionStub({value: '0.1.0'})` returns `'0.1.0'`.
  - serverPackageVersionAdapter() returns the version field of the server package's own package.json, and its test proves the value came from the file the proxy staged rather than from a literal inside the adapter — yes — with the proxy staging `{version: '9.9.9'}` as the readFileSync content, `serverPackageVersionAdapter()` returns `'9.9.9'`.
  - When the read fails, the adapter throws an Error whose message names the subpath it tried; it never returns a fallback and never swallows — yes — `readFileSync` staged to throw, and package.json staged with no `version` field, both produce a thrown Error matching `/@dungeonmaster\/server\/package\.json/u`; the catch block attaches `{cause: error}` (required by the `preserve-caught-error` lint rule) rather than swallowing.
FILES:
  - ./packages/server/src/contracts/server-version/server-version-contract.ts
  - ./packages/server/src/contracts/server-version/server-version-contract.test.ts
  - ./packages/server/src/contracts/server-version/server-version.stub.ts
  - ./packages/server/src/adapters/server-package/version/server-package-version-adapter.ts
  - ./packages/server/src/adapters/server-package/version/server-package-version-adapter.proxy.ts
  - ./packages/server/src/adapters/server-package/version/server-package-version-adapter.test.ts
EVIDENCE:
  - server-version-contract.test.ts "INVALID: {value: \"\"} => throws on empty string" — shell contract was `z.string().brand<'ServerVersion'>()` (no `.min(1)`); red: `Error: expect(received).toThrow(expected)` — the parse did not throw.
  - server-package-version-adapter.test.ts "VALID: ... => returns the staged version, not a literal" — shell adapter was `serverVersionContract.parse('0.0.0-shell')`; red: `Expected: "9.9.9" / Received: "0.0.0-shell"`.
  - server-package-version-adapter.test.ts "ERROR: {readFileSync throws} => throws an Error naming the package.json subpath" — same shell; red: `Expected pattern: /@dungeonmaster\/server\/package\.json/u / Received function did not throw`.
  - server-package-version-adapter.test.ts "ERROR: {package.json has no version field} => throws without falling back" — same shell; red: `Expected pattern: /@dungeonmaster\/server\/package\.json/u / Received function did not throw`.
  - All four reds captured via `npm run ward -- --only unit -- <file>` against the shells, then the real implementation was restored (edited back, never `git checkout --`) and the same scoped ward turned green.
USAGES:   Searched `discover({grep: "serverPackageVersionAdapter|serverVersionContract"})` across the whole repo. Only hits are this chunk's own six files (contract, stub, test, adapter, proxy, test) — nothing else imports either export yet. Matches NOTES: "UNITS: none ... chunk 4 is the only consumer," and chunk 4 has not started (wave 2).
GOTCHAS:
  - The plan's DECISIONS section says a failed version read "THROWS ... rather than returning a fallback" and names `rate-limits-get-responder.ts` as the 500-shape precedent — that precedent is for chunk 6 (the responder), not this chunk; this adapter itself has no HTTP boundary to answer into, so it just throws with context.
  - Hit ESLint's `preserve-caught-error` rule on the first scoped ward pass (not called out in the plan's NOTES): re-throwing inside a `catch (error)` block requires attaching `{cause: error}` to the new Error, or lint fails. Fixed by adding `{ cause: error }` as the second argument to `new Error(...)`.
  - Field extraction off `JSON.parse`'d package.json uses `typeof parsed === 'object' && parsed !== null && 'version' in parsed ? (parsed as Record<'version', unknown>).version : undefined`, mirroring the already-committed idiom in `packages/shared/src/brokers/architecture/package-inventory/read-package-description-layer-broker.ts` (that one returns a fallback on failure; this one instead feeds the possibly-`undefined` value into `serverVersionContract.parse`, which throws for undefined/non-string/empty, and the outer catch wraps all of it with the subpath).
  - The proxy addresses `readFileSync` with a catch-all `calledWith([])` rather than keying on the real resolved path, copying the MIRROR (`web-bundle-dist-path-adapter.proxy.ts`) exactly as chunk 3's NOTES instructed — there is only one fs call site in this adapter, so no collision risk within this file's own tests.
  - ESLint's `--fix` (ward runs lint with `--fix`) reformatted the two multi-line `expect(() => ...).toThrow(...)` calls in the test file onto single lines; left as ward wrote them.
MARKERS:  none
WARD:     `npm run ward -- -- packages/server/src/contracts/server-version/server-version-contract.ts packages/server/src/contracts/server-version/server-version-contract.test.ts packages/server/src/contracts/server-version/server-version.stub.ts packages/server/src/adapters/server-package/version/server-package-version-adapter.ts packages/server/src/adapters/server-package/version/server-package-version-adapter.proxy.ts packages/server/src/adapters/server-package/version/server-package-version-adapter.test.ts` — green — lint 6/6, typecheck 6163/6163, unit 2/2, integration 2/2, e2e skip (server is not e2e-eligible).
NEXT: continue

### report — chunk 4
RESULT:
  - healthStatusBroker() returns a value that parses as healthStatusPayloadContract: status ok, uptimeSeconds the floor of process.uptime(), version from serverPackageVersionAdapter — yes — with the proxy staging uptime 42.9 and version '0.1.0', `healthStatusBroker()` returns `{status: 'ok', uptimeSeconds: 42, version: '0.1.0'}`, asserted via `toStrictEqual(HealthStatusPayloadStub({uptimeSeconds: 42, version: '0.1.0'}))`, and `HealthStatusPayloadStub` builds its value by parsing through `healthStatusPayloadContract`.
  - uptimeSeconds is an integer — with the proxy staging process.uptime() at 42.9, the returned uptimeSeconds is 42 — yes — same test as above; `Math.floor(42.9)` produces `42`.
  - Two calls with different staged uptimes return different uptimeSeconds; nothing is cached between calls — yes — staged uptime 100 then 110 in one test; the first call returned `uptimeSeconds: 100` and the second returned `uptimeSeconds: 110`.
  - The broker is synchronous: its return type is the payload, not a Promise — yes — `result instanceof Promise` is `false`, and the function's declared return type is `HealthStatusPayload` (no `async`, no `await`), confirmed by a PASSing typecheck across all 12 packages/6166 files.
FILES:
  - ./packages/server/src/brokers/health/status/health-status-broker.ts
  - ./packages/server/src/brokers/health/status/health-status-broker.proxy.ts
  - ./packages/server/src/brokers/health/status/health-status-broker.test.ts
EVIDENCE:
  - "VALID: {uptime: 42.9, version: \"0.1.0\"} => returns {status: \"ok\", uptimeSeconds: 42, version: \"0.1.0\"}" — red against the empty shell (`uptimeSeconds: 0, version: '0.0.0-shell'` hardcoded, ignoring the proxy's staged values): `- "uptimeSeconds": 42, - "version": "0.1.0", + "uptimeSeconds": 0, + "version": "0.0.0-shell"`.
  - "VALID: {uptime 100 then 110 staged in turn} => each call returns its own uptimeSeconds, nothing cached" — red against the same shell on its first assertion: `- "uptimeSeconds": 100, - "version": "0.1.0", + "uptimeSeconds": 0, + "version": "0.0.0-shell"`.
  - "VALID: {} => returns the payload synchronously, not a Promise" — not separately reddable: the shell (no `await` anywhere in the file, same as the real implementation) already returns a plain object rather than a Promise, so this assertion passed even before the real logic was written. Noting this per the "already holds" case rather than claiming a red that wasn't there.
  - Both wrong-value reds captured via `npm run ward -- --only unit -- <these 3 files>` against the shell (run 1787938390487-2184); implementation then written and the same scoped ward turned green.
USAGES: `discover({grep: "healthStatusBroker"})` across the whole repo — every hit is inside this chunk's own three files (broker, proxy, test). Nothing else imports it yet: chunk 5 (heartbeat-emit broker) and chunk 6 (health-status responder) are the only planned consumers and both are later waves that have not started.
GOTCHAS:
  - `healthStatusBrokerProxy.stagesHealth` takes raw `uptime: number` and `version: string` (not branded) — `ban-primitives` is configured `allowPrimitiveInputs: true`, and this mirrors get-testing-patterns' own global-mock example (`uuidHandle.calledWith([]).returns('f47ac10b-...')`, a raw string): `process.uptime()`'s real signature returns a plain unbranded `number`, so staging it with a branded type would invent a type the real API doesn't have.
  - The version adapter's staging still goes through `serverPackageVersionAdapterProxy().stagesVersion({version})` (chunk 3's own proxy, composed as a child) rather than being re-implemented here — no `registerMock` was called directly from this proxy for the fs read.
MARKERS:  none
WARD:     npm run ward -- -- ./packages/server/src/brokers/health/status/health-status-broker.ts ./packages/server/src/brokers/health/status/health-status-broker.proxy.ts ./packages/server/src/brokers/health/status/health-status-broker.test.ts — green — lint PASS (3/3), typecheck PASS (12 packages/6166 files), unit PASS (1/179 discovered), integration PASS (1/14 discovered), e2e skip (server is not e2e-eligible).
NEXT: continue

### report — chunk 5
RESULT:
  - healthHeartbeatEmitBroker given a Set holding one client calls that client's send exactly once, with the JSON string of an envelope whose type is health-status, whose payload holds exactly the three payload keys, and whose timestamp is an ISO string — yes — with uptime 42.9 and version '0.1.0' staged, `proxy.getCapturedMessages()` after one call is `[{type: 'health-status', payload: {status: 'ok', uptimeSeconds: 42, version: '0.1.0'}, timestamp: '2024-01-01T00:00:00.000Z'}]`, a one-element array (proving exactly one send).
  - Given two clients where the first client's send throws, that first client is absent from the passed Set afterwards and the second client received that same string — yes — after the call, `clients.has(deadClient)` is `false` and `deadClients.has(deadClient)` is `true`; `proxy.getCapturedMessages()` (the second, healthy client's parsed sends) is `[{type: 'health-status', payload: {status: 'ok', uptimeSeconds: 100, version: '0.1.0'}, timestamp: '2024-01-01T00:00:00.000Z'}]`.
  - Two successive calls with staged uptimes 100 then 110 produce payloads whose uptimeSeconds are 100 then 110, in that order — yes — `proxy.getCapturedMessages()` after both calls is `[{..., payload: {..., uptimeSeconds: 100, ...}, ...}, {..., payload: {..., uptimeSeconds: 110, ...}, ...}]`, in that order.
  - Its return value is the dead-client Set the broadcast broker hands back — yes — the implementation's only line of logic is `return wsEventRelayBroadcastBroker({ clients, message });`, and the dead-client test above reads that exact return value (`deadClients`) to find the dropped client.
FILES:
  - ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.ts
  - ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.proxy.ts
  - ./packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.test.ts
EVIDENCE:
  - check-emit-payload-advances, "VALID: {one client} => sends one health-status frame with exactly status, uptimeSeconds, version" — red against the empty shell: `Expected [{"payload": {...42...}, "timestamp": "2024-01-01T00:00:00.000Z", "type": "health-status"}] / Received: Array []` (shell ignored `clients` and always returned `new Set()` with no send call).
  - check-emit-payload-advances, "VALID: {uptime staged 100 then 110} => uptimeSeconds advances per call, in order" — red against the same shell: `Expected [{...uptimeSeconds:100...}, {...uptimeSeconds:110...}] / Received: Array []`.
  - check-dead-client-does-not-block, "ERROR: {first client throws} => first client removed from the set and returned dead, second client still receives the frame" — red against the same shell: `expect(clients.has(deadClient)).toBe(false)` → `Expected: false / Received: true` (shell never touched the passed `clients` Set, so the dead client was still in it).
  - check-frame-crosses-wire, "VALID: {three clients} => each receives exactly one serialised health-status frame per emit" — red against the same shell: `Expected [["...serialized JSON..."]] / Received: Array []` for each of the three send mocks (shell never called `.send`).
  - All four reds captured in one run via `npm run ward -- --only unit -- <these 3 files>` (run 1787938713228-8b3a) against the empty shell (`export const healthHeartbeatEmitBroker = ({clients}) => { void clients; return new Set(); }`); the real implementation was then written and the same scoped ward turned green. The fifth test in the file ("return value is an empty dead-client set") passed even against the shell — noted rather than claimed as a red, since a shell that always returns `new Set()` already satisfies "empty set, one client, nothing dead."
USAGES:   `discover({grep: "healthHeartbeatEmitBroker"})` across the whole repo — every hit is inside this chunk's own three files (broker, proxy, test). Nothing else imports it yet: chunk 7 (server-init-responder.ts) is the only planned consumer and is a later wave (4) that has not started.
GOTCHAS:
  - The proxy composes both `healthStatusBrokerProxy` (chunk 4) and `wsEventRelayBroadcastBrokerProxy` (the MIRROR's own proxy), per NOTES 5. Instantiating `healthHeartbeatEmitBrokerProxy()` therefore also stages `process.uptime` and the fs-backed version adapter as a side effect — this is what lets chunk 7 wire it in as a child proxy purely to keep those two mocked for every other test in `server-init-responder.test.ts`, without necessarily needing this proxy's `stagesHealth`/`captureClient` for its own assertions.
  - `captureClient`/`getCapturedMessages` are forwarded straight through from the composed `wsEventRelayBroadcastBrokerProxy`, never a nested child proxy handed to the test — matches the Proxy Encapsulation Rule (semantic values/methods only, no chained `proxy.child.method()`).
  - The "three clients" test could not use `captureClient` (there is only one) and instead asserts each plain `jest.fn` client's `.mock.calls` against a hand-built `JSON.stringify(...)` of the expected envelope — the same style the MIRROR's own "three healthy clients" test already uses.
  - ESLint's `--fix` (ward runs lint with `--fix`) reformatted the broker's destructured parameter and the proxy's arrow-function body onto multiple lines / with braces; left as ward wrote them.
MARKERS:  none
WARD:     npm run ward -- -- packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.ts packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.proxy.ts packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.test.ts — green — lint PASS (3/3), typecheck PASS (12 packages/6172 files), unit PASS (1 file/181 discovered), integration PASS (1 file/14 discovered), e2e skip (server is not e2e-eligible).
NEXT: continue

### report — chunk 6
RESULT:
  - HealthStatusResponder() returns status 200 with `data` equal to the payload, asserted in ONE toStrictEqual over the whole result, so a fourth key in the body fails the test — yes — VALID test asserts `expect(result).toStrictEqual({status: 200, data: HealthStatusPayloadStub({uptimeSeconds: 42, version: '0.1.0'})})` in one call, and it passes.
  - When the broker throws, it returns status 500 with an error message and does not propagate — yes — ERROR test stages `serverPackageVersionAdapter`'s read to throw; `HealthStatusResponder()` returns (does not throw) `{status: 500, data: {error: 'serverPackageVersionAdapter: failed to read version from @dungeonmaster/server/package.json: Error: kaboom'}}`.
  - It is synchronous — no await, and no Promise in its return type — yes — the file has no `async`/`await`; declared return type is `ResponderResult`; a dedicated test asserts `result instanceof Promise` is `false`.
FILES:
  - ./packages/server/src/responders/health/status/health-status-responder.ts
  - ./packages/server/src/responders/health/status/health-status-responder.proxy.ts
  - ./packages/server/src/responders/health/status/health-status-responder.test.ts
  - ./packages/server/src/brokers/health/status/health-status-broker.proxy.ts (extended; see GOTCHAS)
EVIDENCE:
  - "VALID: {uptime: 42.9, version: \"0.1.0\"} => returns 200 with the payload as the whole body" — red against the empty shell (hardcoded `{status: 'shell', uptimeSeconds: 0, version: '0.0.0-shell'}`): `- "status": "ok", "uptimeSeconds": 42, "version": "0.1.0", + "status": "shell", "uptimeSeconds": 0, "version": "0.0.0-shell"`.
  - "ERROR: {version read throws} => returns 500 with an error message and does not propagate" — red against the same shell: `- "error": "serverPackageVersionAdapter: failed to read version from @dungeonmaster/server/package.json: Error: kaboom", "status": 500 + "status": "shell", "uptimeSeconds": 0, "version": "0.0.0-shell", "status": 200`.
  - "VALID: {} => returns the result synchronously, not a Promise" — not separately reddable: the shell has no `await` either, so `result instanceof Promise` was already `false` before the real logic existed. Noted per the "already holds" case, not claimed as a red that wasn't there.
  - Both wrong-value reds captured via `npm run ward -- --only unit -- <these 3 files>` (run 1787938665528-ea15) against the shell; implementation then written and the same scoped ward turned green.
USAGES: `discover({grep: "HealthStatusResponder"})` and `discover({grep: "healthStatusBrokerProxy"})` across the whole repo. `HealthStatusResponder` has no hit outside this chunk's own three files — chunk 8 (health-flow.ts, a later wave) is the only planned consumer and has not started. `healthStatusBrokerProxy` has one hit outside this chunk and chunk 4's own test: `packages/server/src/brokers/health/heartbeat-emit/health-heartbeat-emit-broker.proxy.ts:13` (chunk 5, same wave, live sibling) — read it and confirmed it only calls `.stagesHealth(...)`, a method I did not change, so my addition of a sibling method does not affect it. Not touched.
GOTCHAS:
  - Extended chunk 4's already-landed `health-status-broker.proxy.ts` (wave 2, committed report) with a new `stagesVersionReadFailure` method, rather than reimplementing uptime/version staging inside this chunk's own proxy. My chunk's second INTENT line needs the REAL `healthStatusBroker` to throw for real (business logic runs real in tests; only I/O is mocked), and the only way it throws is via `serverPackageVersionAdapter`'s read failing — one layer below the broker's own proxy surface. This is a same-file addition, not a duplicate: `health-heartbeat-emit-broker.proxy.ts` (chunk 5) already composes the same `healthStatusBrokerProxy`, so the version/uptime staging stays in one place.
  - The new method must ALSO stage a harmless `process.uptime()` return (`0`), not just `versionProxy.readFails`. `healthStatusBroker`'s object literal evaluates `uptimeSeconds: Math.floor(process.uptime())` before `version: serverPackageVersionAdapter()`, so an unstaged `registerSpyOn` on `process.uptime` throws its own "nothing set up for the call uptime()" error first, masking the version-read failure the test means to prove. First scoped ward run after wiring the real logic caught this directly: the ERROR test's actual value was `"registerMock: nothing set up for the call uptime(). Calls that ARE set up: "` instead of the expected version-adapter message. Fixed by staging uptime inside `stagesVersionReadFailure` itself.
  - One scoped ward run (1787938717358-dad6, taken right after wiring the real logic but before fixing the uptime-staging gotcha above) also showed `health-heartbeat-emit-broker.test.ts` (chunk 5's own file, not mine, same wave) FAILING under both unit-shaped assertions — 4 of its tests red. I did not touch that file. The next scoped ward run over the identical file list (1787938780728-702e) came back fully green, matching chunk 2's report's "wave-timing artifact" note: a sibling wave-mate mid-write at the moment of that one run, landed by the next. Flagging so the reviewer reads it as timing, not a defect this chunk caused.
  - `data` is the payload itself, never wrapped (`data: payload`, not `data: {payload}` or `data: {snapshot: payload}`) — the MIRROR's sibling `rate-limits-get-responder.ts` nests under `snapshot`, but this chunk's NOTES are explicit that the observable is about the BODY being exactly the three payload keys.
  - The 500 error string is asserted as one exact literal (not `expect.stringMatching`) because it is fully deterministic — no timestamp, no path that varies across machines — matching `no-multiple-property-assertions`'s single whole-object `toStrictEqual` per test.
MARKERS:  none
WARD:     npm run ward -- -- ./packages/server/src/responders/health/status/health-status-responder.ts ./packages/server/src/responders/health/status/health-status-responder.proxy.ts ./packages/server/src/responders/health/status/health-status-responder.test.ts ./packages/server/src/brokers/health/status/health-status-broker.proxy.ts — green — lint PASS (4/4), typecheck PASS (12 packages/6172 files), unit PASS (1/181 discovered), integration PASS (3/14 discovered), e2e skip (server is not e2e-eligible).
NEXT: continue
