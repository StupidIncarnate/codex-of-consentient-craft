# Siegemaster verify round 4 — running-elapsed-on-work-item-row, PATH P4

FLOW: running-elapsed-on-work-item-row
LANE: r4-verify
PATH: panel-rendered -> started-at-check -> row-running-check -> compute-elapsed -> under-minute-check -> show-under-minute -> show-final-duration
FORCE: startedAt present, is-running (in_progress, no completedAt), under-sixty, under-minute-completes ("completedAt arrives")
WORK ITEM: b739c559-48b2-463d-acdd-78654fd9e258

## Guide correctness note

`.quest-plans/a0559e14-35ff-42c9-8006-6260e53c102c-guide.md` was read start to end before driving anything.
No error found in it on this walk — every recipe used (SEEDING steps 1-4 and 6, the age table, the no-op PATCH
trick, the `box` selector shape) worked exactly as written. The one place the guide explicitly says "NOT FOUND"
(pause from the completedAt-present group) was not on this path and was not exercised. No correction needed to
the guide from this round.

## Lane

Started once: `npx tsx packages/web/test/siege-driver/siege-driver.ts r4-verify` (background). Booted clean,
`tmp/siege/r4-verify/lane.json` -> apiPort 35189, webPort 46869, home `/tmp/dm-siege-r4-verify-1105221`. Never
restarted, never sent `end`.

## Seed

Guild `895e8962-6b51-445d-b899-3da2678fb4ca` (elapsed-duration-guild-r4), quest
`e2ff44d4-23fe-4a94-bc0e-9e043f4a792a`. Three work items / operations, one quest.json write (SEEDING step 3),
one no-op PATCH later (SEEDING step 6):

- Row A "codeweaver: thirty second row" — startedAt = now-30000ms, in_progress, no completedAt.
- Row B "codeweaver: four minute row" — startedAt = now-270000ms, in_progress, no completedAt.
- Row C "codeweaver: anchor running row" — startedAt = now-10000ms, in_progress, no completedAt. Kept
  running the whole walk, as a live control to prove the shared tick was actually firing during the later
  60s-wait units (rather than "nothing changed because the interval died").

## Record

### started-at-check / row-running-check / compute-elapsed / under-minute-check (already-signed units, observed only, NOT re-signed)

STARTED FROM: fresh guild+quest, three in_progress work items all carrying startedAt, no completedAt.
DID: `goto /elapsed-duration-guild-r4/quest/e2ff44d4-23fe-4a94-bc0e-9e043f4a792a`, `waitFor
[data-testid="execution-panel-widget"]`.
SAW: panel visible, "0/3 OPERATIONS" checklist, then all three RUNNING rows: "thirty second row" <1m RUNNING,
"four minute row" 4m RUNNING, "anchor running row" <1m RUNNING (box text, not just the panel-wide text dump).
This confirms started-at-present -> row-running-check -> is-running -> compute-elapsed -> under-minute-check
routed correctly for all three (A and C land in show-under-minute at <60s, B lands in show-minutes at 4m30s+ —
consistent with the age table). No defect. These units belong to earlier rounds' checklists (started-at-present,
is-running, under-sixty, check-under-minute-text, check-first-value-on-mount, etc.) and are NOT re-signed here —
observed only, per SIGN ONCE.
BROKEN WOULD SHOW: a stale/absent duration element, a wrong band (e.g. Row A reading "0m" or "30s"), or Row B
reading "<1m" instead of "4m".

### edge:under-minute-completes [MINE — signed]

STARTED FROM: Row A in #show-under-minute, box-confirmed text "<1m", badge RUNNING (implicit from the panel
dump's "RUNNING" tag next to that row).
DID: rewrote Row A's `completedAt` to startedAt+12000ms (`2026-09-09T11:23:49.804Z`) and status to `complete`
directly in quest.json on disk, then `curl -X PATCH .../api/quests/<id> -d '{"status":"in_progress"}'` (no-op
PATCH, SEEDING step 6) to force the server to re-read and broadcast.
SAW: box on Row A's duration element (no reload, no navigation) returned text "<1m"; box on Row A's status
badge returned text "DONE" (not "PENDING"). A duration element remained PRESENT.
BROKEN WOULD SHOW: had this instead routed to the sibling branch (`under-minute-paused` -> paused-no-figure),
the duration box read would have come back `present:false`/`count:0` and the badge would read "PENDING" — the
two branches are told apart exactly by presence-of-figure + non-PENDING badge vs. absence-of-figure + PENDING.
Signed `confirmed` on edge id `under-minute-completes` (checklist id
`running-elapsed-on-work-item-row:branch:under-minute-completes`).

### terminal:show-final-duration [MINE — signed]

STARTED FROM: Row A completed (12s span, reads "<1m" DONE) and Row B completed (2h span, reads "2h" DONE, see
below), Row C still genuinely running.
DID: real 70s wall-clock wait (`sleep 70`) crossing at least one 60000ms tick (`elapsedDisplayConfigStatics
.refresh.tickMs`), then re-read all three rows' duration boxes.
SAW: Row A still "<1m", Row B still "2h" — byte-identical to their pre-wait readings. Row C, which had read
"1m" before the wait, read "3m" after — proving the shared interval fired at least once (likely twice, given
elapsed real time from earlier steps) during the window, and that A/B's freeze is not an artifact of the
interval itself dying.
BROKEN WOULD SHOW: if the finished rows used `now` instead of `completedAt` as their end point, they would
have advanced alongside Row C during the same 70s window (e.g. "<1m" creeping toward "2m", "2h" ticking up a
notch) instead of staying fixed. If the whole interval had instead been cleared (a different possible defect),
Row C would also have stayed frozen — it did not.
Signed `confirmed` on node id `show-final-duration` (checklist id
`running-elapsed-on-work-item-row:terminal:show-final-duration`).

### observable:check-finished-under-minute [MINE — signed]

STARTED FROM: Row A, completedAt written as startedAt+12000ms exactly (12.000s span).
DID: box on `[data-testid="execution-row-layer-widget"]:has-text("codeweaver: thirty second row")
[data-testid="execution-row-duration"]`, taken twice (immediately after the PATCH, and again after the 70s
wait).
SAW: text "<1m" both times, exactly.
BROKEN WOULD SHOW: any string other than the literal "<1m" — "12s", "0m", a negative number, "NaN".
Signed `confirmed` (checklist id
`running-elapsed-on-work-item-row:observable:check-finished-under-minute`).

### observable:check-final-replaces-running-immediately [MINE — signed]

STARTED FROM: Row B, running, startedAt = now-270000ms (~4m old, per the guide's explicit instruction to use a
SEPARATE row from the under-60s branch for this specific check).
DID: box-read Row B's duration before any mutation ("4m"). Rewrote Row B's `completedAt` to
startedAt+7200000ms (a 2h span — deliberately far from the live-running value, so a deferred switch would be
unmistakable) and status to `complete`, fired the no-op PATCH, then immediately (no sleep, no waiting for a
tick) box-read Row B's duration again.
SAW: "4m" before, "2h" immediately after — read within the same short exchange as the PATCH, well inside 60s
of it and with the true wall-clock elapsed time since Row B's real startedAt still only ~4 minutes.
BROKEN WOULD SHOW: a defect deferring the switch to the next 60s tick would have shown "4m" (or something close
to a live now-based recompute) on this immediate read, only flipping to "2h" after a further ~60s wait — "2h"
could not have come from a live-now calculation at 4-minutes-elapsed, so seeing it instantly proves the switch
happened on the completing render.
Signed `confirmed` (checklist id
`running-elapsed-on-work-item-row:observable:check-final-replaces-running-immediately`).

### observable:check-frozen-figure-unchanged-by-tick [MINE — signed]

STARTED FROM: Row A ("<1m" DONE) and Row B ("2h" DONE), Row C still running as a live control.
DID: real 70s wait (same one used for the terminal above), re-read all three duration boxes.
SAW: Row A "<1m" -> "<1m" (unchanged), Row B "2h" -> "2h" (unchanged), Row C "1m" -> "3m" (changed, proving the
tick fired).
BROKEN WOULD SHOW: if a finished row's `endedAt` were `now` rather than `completedAt`, it would have advanced
alongside Row C in the same window.
Signed `confirmed` (checklist id
`running-elapsed-on-work-item-row:observable:check-frozen-figure-unchanged-by-tick`).

## Look-at-everything pass

- Panel screenshot taken after all mutations (`round4-final-panel.png`, 1280x720): 2/3 OPERATIONS checked off,
  three rows laid out cleanly, no overlap, no truncation, badges color-coded (green DONE / orange RUNNING),
  duration column right-aligned and legible against the dark theme. No visual defect.
- Console capture (`{"name":"console"}`) showed only vite HMR connect/connected debug lines and the React
  DevTools info line — no warnings, no errors, no uncaught exceptions.
- quest.json round-tripped correctly through the server's persist-and-rewrite (verified by re-reading it after
  the no-op PATCH): completedAt values, status "complete", and the third row's continued in_progress/no-
  completedAt state all persisted as written. No orphaned row, no half-written state, no silently-dropped
  field.

## Defect list

None found. Zero defects on this walk.

## Pass 2

Not entered — the numbered defect list from pass 1 is empty, so there is nothing to dispatch a fixer sub-agent
against. Per the role's own rule, this is a valid outcome ("Zero defects is a good answer. Do not manufacture
one to look productive.").

## Coverage

5 of 5 units on this round's UNITS list signed: branch `under-minute-completes`, terminal `show-final-duration`,
observables `check-finished-under-minute`, `check-final-replaces-running-immediately`,
`check-frozen-figure-unchanged-by-tick`. All confirmed via modify-quest in one batched call. No `unconfirmable`
verdicts needed — every unit on the list was directly observable through the lane.

Nothing on the PATH stopped the walk; the full route panel-rendered -> ... -> show-final-duration was reached
and observed end to end, including the adjacent (not-on-path but guide-instructed) minutes-completes route used
for Row B per the guide's explicit note.

Lane left running (never sent `end`, per LANE DISCIPLINE) — it will self-close on the 15-minute idle timeout.
