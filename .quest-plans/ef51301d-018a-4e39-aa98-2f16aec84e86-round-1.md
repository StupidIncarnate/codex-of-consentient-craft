# Round 1 — [flowrider] Flowrider: author the flow-perspective test suites below the browser — seam: server + shared

## Context

Quest ID: a7520e60-430c-4d0e-b332-9952d6d5c042
Work Item ID: f7ce3d78-41f2-421a-9382-7f01120fbefc
Operation Item ID: ef51301d-018a-4e39-aa98-2f16aec84e86
Your operation item: [flowrider] Flowrider: author the flow-perspective test suites below the browser — seam: server + shared

Operations ledger (in order):
1. [x] [chaoswhisperer] Author spec + implementation plan
2. [x] [riftcarver] Riftcarver: carve the quest branch, worktree and preflight build
3. [x] [codeweaver] Codeweaver: build this slice — package: shared
4. [x] [codeweaver] Codeweaver: build this slice — package: server
5. [x] [codeweaver] Codeweaver: build this slice — package: web
6. [x] [ward changed] Ward gate (changed files)
7. [x] [flowrider] Flowrider: author the flow-perspective test suites below the browser — package: server
8. [>] [flowrider] Flowrider: author the flow-perspective test suites below the browser — seam: server + shared  <-- YOUR OPERATION ITEM
9. [ ] [groundstomper] Groundstomper: author the browser walk for this flow — flow: health-badge
10. [ ] [siegemaster] Siegemaster: manual-QA this flow and review its test suite — flow: health-badge
11. [ ] [ward full] Ward gate (full monorepo)

Your flows: #health-badge
(YOUR unit of accountability — every flow listed here, and no unit a sibling item owns. Not a starting point: work them, delegating where your role has minions.)

Your packages: server, shared
(YOUR coverage slice — you own every verification unit whose owning NODE tags one of these packages, and a unit spanning two of them belongs to the seam item, not to you. Read these packages first.)

Packages affected (whole quest): shared (edit, library), server (edit, http-backend), web (edit, frontend-react)

Original user request (the intent behind the flows):
The app top bar gives no indication of whether the server behind it is still
answering. When the server dies or wedges, the interface keeps rendering its
last state and the operator finds out only when an action fails.

Add a health badge to the top bar, visible on every route. It seeds itself at
mount from a new GET /api/health/status returning status, uptime and version,
then tracks a health-status heartbeat the server emits every 10 seconds over
the WebSocket the interface already holds open.

The badge reads ONLINE with uptime beside it, DEGRADED, or OFFLINE. It goes
OFFLINE when the seed request fails to reach the server, when the server
answers with an error, or when 30 seconds pass with no heartbeat — a server
can hold the socket open while no longer doing work, so silence is the signal
rather than disconnection. Clicking an OFFLINE badge retries the seed request.

/api/health stays as it is, a bare liveness probe.

## Plan

TOUCHES:
  ./packages/server/src/flows/server/server-flow.integration.test.ts — EXISTS (queue) — 67 lines, two cases. :16 asserts `expect(ServerFlow).toStrictEqual(expect.any(Function))` and nothing else. :27 (inside `describe('health-status heartbeat over a real /ws upgrade')`, with `const serverWs = serverWsHarness()` at describe scope) boots the REAL ServerFlow through the harness, opens TWO real `ws` clients (A silent, B sending `subscribe-quest` for a nonexistent quest), awaits `waitForHealthStatusFrame({timeoutMs: 15000})` on each — ONE frame apiece — and asserts `[silentFrame, subscribedFrame] toStrictEqual [expectedFrame, expectedFrame]` where expectedFrame is a `WsMessageStub` carrying `type: 'health-status'` and a `HealthStatusPayloadStub` whose uptimeSeconds/version/timestamp are ECHOED off the silent client's own frame. So it pins the literal type, the payload's three keys and the two-client equality, and pins NO uptime/version value. It never collects a second frame, never times an arrival, never compares two uptimeSeconds, and never looks at the RAW text — `parseHealthStatusFrame` (harness:61) REBUILDS a three-key object out of a `wsMessageContract.parse()` result, and that contract is a non-strict z.object, so a fourth envelope key is stripped before the toStrictEqual ever sees it.
      health-badge:observable:check-emit-interval — three health-status frames collected off ONE real client socket, arriving inside a 35-second deadline and spaced ~10s apart by their own recorded arrival times
      health-badge:observable:check-emit-payload-advances — each of those three frames carries exactly {status, uptimeSeconds, version}, and each uptimeSeconds is strictly greater than the one before it
      health-badge:observable:check-frame-crosses-wire — the RAW text of a collected frame, JSON.parsed and compared whole, carries exactly {type, payload, timestamp} and no fourth key, and each emit reached the client as exactly ONE non-binary message
  ./packages/server/test/harnesses/server-ws/server-ws.harness.ts — EXISTS — 263 lines. Boots the REAL `ServerFlow({subApps: []})` fully unmocked on a `netFreePortAdapter` port under a temp DUNGEONMASTER_HOME (composing `serverAppHarness().setupTestHome`), finds the bound `net.Server` by scanning `process._getActiveHandles()`, opens real `ws` clients at `ws://${environmentStatics.hostname}:<port>/ws`, and closes every socket and the listener in an `afterEach` PROPERTY (auto-wired by the ts-jest transformer — the suite writes no hook). Its `openClient()` returns `send`, `getMessages()` (raw text only, NO arrival times and NO binary flag) and `waitForHealthStatusFrame({timeoutMs})`, which returns the FIRST matching frame and stops. Serves the server-flow suite alone; no other file imports it.
      (no units of its own — it is the driver all three server-side units cross)
  ./packages/web/src/widgets/app/app-widget.integration.test.tsx — EXISTS (jsdom) — 248 lines, two cases. :27 stages three MSW endpoints, spies `globalThis.WebSocket`, calls `webSocketChannelState.clear()` + `.connect()`, mounts four bindings through `testingLibraryRenderHookAdapter` and asserts each binding's idle state plus `socketConstructions toStrictEqual [true]`; it renders no DOM. :152 renders the WHOLE AppWidget through `mantineRenderAdapter` inside a MemoryRouter with a trivial child route, stages FOUR MSW endpoints, spies `globalThis.fetch` with `passthrough: true`, waits for `getByTestId(healthBadgeStatics.testId).textContent` to be `'ONLINE 3h 12m'`, asserts the recorded seed-fetch URL list `toStrictEqual ['/api/health/status']`, pushes ONE frame through `webSocketChannelState.dispatchInbound({...uptimeSeconds: 11580...})` inside `testingLibraryActAsyncAdapter`, then asserts the text is `'ONLINE 3h 13m'` and `healthStatusEndpoint.getRequestCount()` is 1. It bounds nothing in TIME, and it never closes or re-opens a socket — `dispatchInbound` is called directly on the singleton, so no socket lifecycle is exercised at all.
      health-badge:observable:check-payload-updates-badge — the badge's rendered text moves on a delivered frame with ZERO timer time elapsed under a frozen clock, which is what makes "within 1 second" bite rather than describe
      health-badge:observable:check-reconnect-resumes — the rendered badge goes ONLINE -> (socket onclose) -> OFFLINE by silence -> (reconnect) -> ONLINE on the first frame after, with the recorded seed-fetch call list still exactly one entry
  ./packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.test.ts — EXISTS (module) — a UNIT test, listed because a unit of this round is already true in it. :6 asserts `orchestrationEventTypeContract.options toStrictEqual` a 22-element literal array IN ORDER, ending `'health-status'`, so all 21 prior members are pinned by position and spelling, not merely by membership. :33 `it.each(orchestrationEventTypeContract.options)` round-trips every member through `.parse`. :62 rejects the removed `'smoketest-progress'`.
      health-badge:observable:check-event-type-member — already true here, NO CHUNK
  ./packages/shared/src/contracts/health-status-payload/health-status-payload-contract.test.ts — EXISTS (module) — a UNIT test, listed because a unit of this round is already true in it. :22 parses `{status:'ok', uptimeSeconds:11520, version:'0.1.0'}` and asserts the result `toStrictEqual` exactly those three keys. :61 asserts a payload with `status` and `version` but NO `uptimeSeconds` throws `/Required/u`. Seven further INVALID cases pin the enum, the integer/nonnegative bounds and the min-1 version.
      health-badge:observable:check-payload-parses — already true here, NO CHUNK

DEPENDS:
  ./packages/server/test/harnesses/server-ws/server-ws.harness.ts
      needed by: ./packages/server/src/flows/server/server-flow.integration.test.ts — EVERYTHING the suite is forbidden to do itself, and all three server-side units cross this one link. The suite may import no `node:fs` / `node:net` / `node:child_process` and no `.proxy.ts`, so the temp DUNGEONMASTER_HOME, the free-port DUNGEONMASTER_PORT, the real ServerFlow boot, the real `/ws` client sockets, the per-client frame log and the teardown all live here. What crosses the link ANEW this round is a multi-frame read: the suite needs three successive health-status frames off ONE client with each frame's ARRIVAL INSTANT and its RAW text and its text/binary flag, and the harness today records none of those three things — `getMessages()` (harness:224) returns bare strings and `waitForHealthStatusFrame` (harness:225) returns the first match and stops.
      needed by: nothing else. `discover` over the repo returns exactly one importer, and the predecessor round's DEPENDS says the same.
      needs: ./packages/server/test/harnesses/server-app/server-app.harness.ts — `setupTestHome({baseName})`, already wired at harness:155. A harness importing another harness is inside the import boundary; this round changes nothing about that link.
  ./packages/server/src/flows/server/server-flow.integration.test.ts
      needs: ./packages/server/test/harnesses/server-ws/server-ws.harness.ts — the boot, the client, the multi-frame collection and the teardown, per the line above. ONE chunk owns BOTH files, so this link never crosses a wave boundary and the harness needs no chunk of its own.
      needed by: nothing. No other suite in the round reads it.
  ./packages/web/src/widgets/app/app-widget.integration.test.tsx
      needs: nothing new, and no harness — packages/web's 24 harnesses under `test/harnesses` are all Playwright, and this file drives jsdom. Everything it needs is already imported by it or reachable as a PUBLIC member: `StartEndpointMock` and `registerSpyOn` from @dungeonmaster/testing (both already imported), `webSocketChannelState.clear()` / `.connect()` / `.openConnection()` / `.dispatchInbound()` (all public; `openConnection` is the exact member web-socket-channel-state.proxy.ts:72 calls to simulate a reconnect), `mantineRenderAdapter`, `testingLibraryActAdapter`, `testingLibraryActAsyncAdapter`, `testingLibraryWaitForAdapter`, and `healthBadgeStatics.testId`.
      needed by: nothing.
  ./packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.test.ts
      needs: nothing — unchanged this round; listed only because one unit is already true in it.
      needed by: nothing.
  ./packages/shared/src/contracts/health-status-payload/health-status-payload-contract.test.ts
      needs: nothing — unchanged this round; listed only because one unit is already true in it.
      needed by: nothing.

  No harness is shared by two chunks: server-ws.harness.ts has exactly one importer and rides in that
  importer's chunk. No entry's fixtures are unseeded — the server suite's uptime/version come live off the
  real booted process and its home from `setupTestHome`, and the web suite stages its own four MSW
  endpoints. Every `[ ]` unit on the checklist lands on exactly one entry above, and no unit lands on two.

DECISIONS:
  - My checker returned the single line NO DEFECTS over the whole of TOUCHES and DEPENDS, so there is
    no finding I disagreed with and nothing was folded in. It was asked by name for the five member
    names the plan leans on — `webSocketChannelState.openConnection` / `.dispatchInbound` / `.clear` /
    `.connect`, `testingLibraryActAdapter`, `serverWsHarness().openClient().getMessages`,
    `serverAppHarness().setupTestHome` — and contradicted none of them.
  - check-event-type-member is ALREADY TRUE and no chunk improves it. Opened
    orchestration-event-type-contract.test.ts:6-31: `orchestrationEventTypeContract.options` is
    compared by `toStrictEqual` against a 22-element literal array written out IN ORDER and ending
    `'health-status'`. Every clause of the observable is bitten by that one assertion — dropping
    'health-status' reds it, adding a 23rd member reds it, and re-spelling or re-ordering ANY of the
    prior 21 reds it, which is the "the prior 21 unchanged" arm a membership check
    (`options.includes('health-status')`) could not reach. :62 additionally proves the enum still
    rejects the removed 'smoketest-progress', so the count is 22 by content and not by coincidence.
  - check-payload-parses is ALREADY TRUE and no chunk improves it. Opened
    health-status-payload-contract.test.ts:22-34 and :61-68. The first parses
    `{status:'ok', uptimeSeconds:11520, version:'0.1.0'}` and `toStrictEqual`s exactly those three
    keys; the second parses `{status, version}` with uptimeSeconds ABSENT and asserts
    `toThrow(/Required/u)`. Those are the observable's two halves verbatim — it names
    `healthStatusPayloadContract` and a payload missing `uptimeSeconds`, and that file is the
    contract's own suite. The CHANNEL side of the same claim is separately covered at
    web-socket-channel-state.test.ts:343 (a raw `health-status` text frame makes `healthStatus$` emit
    the parsed payload inside the same synchronous `dispatchInbound`) and :369 (a frame whose payload
    omits `uptimeSeconds` makes it emit nothing), so making `uptimeSeconds` optional would red four
    cases across two packages. An integration-tier restatement would prove less than either.
  - NO `[ ]` unit was routed away from this slice; all 7 are worked here. Two of them —
    check-payload-updates-badge and check-reconnect-resumes — sit on #channel-routes, whose files
    live in packages/web. That node's other package is a BROWSER package with no flowrider slice at
    all, so the seam rule (which needs two packages flowrider owns) cannot send them anywhere, and
    both arrive `[ ]` on this item's own checklist, which is the authority. The predecessor item
    (package: server) recorded the mirror image of this call in its own DECISIONS.
  - This round cuts NO new integration test file, inside flows/startup or outside it. Both suites it
    writes into already exist, so the "a NEW integration test outside flows/ or startup/ needs a
    DECISIONS line" rule is not reached. app-widget.integration.test.tsx sits under widgets/ rather
    than flows/ because flows/app/app-flow.integration.test.tsx is a nine-line
    `expect(AppFlow).toStrictEqual(expect.any(Function))` that renders nothing, and AppFlow is a
    `<Route>` tree that does not itself mount the badge — AppLayoutResponder -> AppWidget does. I
    opened both. That placement was settled by the predecessor round and is inherited, not re-made.
  - The three server units go in ONE `it`, not three. `serverWsHarness` declares `afterEach` as a
    property and the ts-jest transformer auto-wires it per TEST, so a second case in that describe
    closes the listener and needs a whole second boot plus a second 35-second wait. One case that
    collects three frames and carries three commented assertion groups costs ~45s; three cases cost
    ~150s and prove nothing more. The existing :27 case stays exactly as it is.
  - The interval is measured from ARRIVAL times the harness records, not from the frames' own
    `timestamp` field. `healthHeartbeatEmitBroker` stamps that field itself
    (health-heartbeat-emit-broker.ts:25), so asserting on it would measure the server's stamping and
    not its delivery — an emitter that built three frames and flushed them together would pass.
  - The uptime assertion is STRICT INCREASE, never a fixed delta. `healthStatusBroker` returns
    `Math.floor(process.uptime())` (health-status-broker.ts:18), so across a 10 000 ms interval the
    floored difference lands on 9, 10 or 11 depending on where the ticks fall inside a second. A
    `toStrictEqual([10, 10])` on the uptime gaps would be flaky; on the ARRIVAL gaps rounded to whole
    seconds it is not, because those are real milliseconds and jitter is tens of ms.
  - The wire assertion goes on the RAW text, because the harness's current parse cannot see an extra
    key. `parseHealthStatusFrame` (server-ws.harness.ts:61-65) REBUILDS `{type, payload, timestamp}`
    out of a `wsMessageContract.parse()` result, and `wsMessageContract` is a non-strict z.object
    that STRIPS unknown keys — so a fourth envelope key is gone before the existing toStrictEqual at
    server-flow.integration.test.ts:64 ever sees it, and "carrying type, payload and timestamp" is
    not currently bitten by anything. `JSON.parse(raw)` compared whole is what makes it bite.
  - @types/ws/index.d.ts MUST be widened, and I settled that by reading it. Line 15 declares
    `on(event: 'message', listener: (data: Buffer | ArrayBuffer | Buffer[]) => void)` — ONE
    parameter. The real `ws` v8 emits `'message'` with `(data, isBinary)`, and TypeScript rejects a
    listener declaring MORE parameters than the declared signature, so the text-not-binary half of
    check-frame-crosses-wire cannot be written until that line takes a second `isBinary: boolean`
    parameter. It is a root `@types` declaration, not a `.claude/settings*.json` / `.mcp.json` /
    `.env*` infrastructure file, and no other chunk this round touches it.
  - OFFLINE is reached by SILENCE in the reconnect case, not by a 500 seed. #dd-offline-rule makes
    silence the definition of offline and #dd-reconnect's whole claim is that a drop is invisible to
    the badge's logic — "frames stop, the threshold may elapse, and the first frame after reconnect
    moves the badge on its ordinary path". A seed-500 start reaches OFFLINE without the socket
    participating at all, so it would leave the sentence "after the socket drops" unproved. The
    mechanics are settled by reading: web-socket-channel-state.ts:118-126 schedules its OWN 3 000 ms
    reconnect on `onClose`, so one `jest.advanceTimersByTime(30000)` drives the real reconnect at
    t+3000 AND the badge's silence flip at t+30000 with no `openConnection()` call from the test at
    all; and health-badge-widget.test.tsx:159-186 already proves fake timers, `mantineRenderAdapter`
    and a staged MSW endpoint coexist in this package, provided the MSW response is never awaited.
  - The web suite reads the fetch SPY's call list, never MSW's `getRequestCount()`, for the "no click
    issued" arm. Under fake timers an MSW response may not have settled, so its counter is
    timer-dependent; `registerSpyOn({object: globalThis, method: 'fetch', passthrough: true})` records
    at CALL time and is not. That spy is already installed in this file at :187 and its address is the
    URL, which is also what makes the existing no-query-string assertion at :225 work.
  - I expect chunk 1 back as rework, not chunk 2. It is a ~45-second test whose first frame is one
    real interval away, it needs a `.d.ts` widened before it will typecheck, and the predecessor round
    already recorded that this same harness cost a whole turn per hazard.
  - Mess that is not this round's: start-server.integration.test.ts, app-flow.integration.test.tsx,
    app-mount-flow.integration.test.tsx, home-flow and queue-flow are all bare
    `expect(X).toStrictEqual(expect.any(Function))` stubs, and health-flow.integration.test.ts:28
    pins the seed body's key SET but no value of `status`, `uptimeSeconds` or `version` despite its
    title. None of those carries a `[ ]` unit of mine — check-seed-route-answers is `[x]`, settled by
    the predecessor round — so none gets a chunk here.

ASSERTIONS:
  - `packages/server/src/flows/server/server-flow.integration.test.ts` collects THREE health-status
    frames off ONE real client socket in a single case. Check: the file contains a call asking the
    harness for three frames, and that `it` carries a per-test timeout argument greater than 35000.
  - The arrival spacing of those three frames is asserted, and it would red at any emit period other
    than ten seconds. Check: one `toStrictEqual` whose expected value is `[10, 10]` over gaps derived
    from recorded arrival instants divided by 1000 and rounded — not over the frames' own `timestamp`
    field, and not over their `uptimeSeconds`.
  - Each of the three frames' `uptimeSeconds` is asserted strictly greater than the one before it.
    Check: a `toStrictEqual` over a two-element array of booleans derived from pairwise `>`
    comparisons, with no fixed delta anywhere in the expected value.
  - One collected frame's RAW text is `JSON.parse`d and compared WHOLE. Check: a `toStrictEqual`
    whose left side is a `JSON.parse` of the raw string (not of a harness-rebuilt object) and whose
    expected value has exactly the three keys `type`, `payload`, `timestamp`, with `type` pinned to
    the literal `'health-status'` and `payload` pinned to exactly three keys.
  - Every collected message reached the client as text, and the silent client received nothing but
    heartbeats. Check: a `toStrictEqual` over the three frames' binary flags whose expected value is
    `[false, false, false]`, and an assertion that the client's whole collected list is three
    health-status messages and nothing else.
  - `@types/ws/index.d.ts` declares a second `isBinary: boolean` parameter on the `'message'`
    listener. Check: the file contains `isBinary`, and `npm run build` exits 0.
  - `packages/server/test/harnesses/server-ws/server-ws.harness.ts` records, per inbound message, its
    raw text, its arrival instant and its binary flag, and can wait for N health-status frames rather
    than one. Check: the harness's returned client object exposes a multi-frame waiter, AND its
    existing `waitForHealthStatusFrame` still exists — the case at
    server-flow.integration.test.ts:27 calls it and must keep passing unchanged.
  - `packages/web/src/widgets/app/app-widget.integration.test.tsx` contains a case in which the
    rendered badge's textContent moves ONLINE -> OFFLINE -> ONLINE across a socket `onclose` and a
    reconnect. Check: three or more `expect(...).toBe(...)` assertions on
    `getByTestId(healthBadgeStatics.testId).textContent` in one case, with `'OFFLINE'` between two
    DIFFERENT ONLINE strings.
  - That reconnect is REAL: a second WebSocket is constructed by the channel's own logic, and the
    frame that restores the badge arrives on that second socket. Check: the case's recorded socket
    constructions number two, and the restoring frame is delivered through the SECOND recorded
    socket's `onmessage`, not through `dispatchInbound` directly.
  - No retry click and no second seed request happen anywhere in that case. Check: the fetch spy's
    calls addressed to `webConfigStatics.api.routes.healthStatus` are exactly one, asserted AFTER the
    reconnect.
  - A second case proves the badge's text moves on a frame SYNCHRONOUSLY, with a measured elapsed
    under 1000 ms. Check: an `expect` on the badge textContent placed immediately after a synchronous
    `testingLibraryActAdapter` with nothing awaited between them, plus an assertion on a wall-clock
    elapsed value compared against 1000.
  - Both suites' pre-existing cases are untouched. Check: `git diff` shows no change inside
    server-flow.integration.test.ts's two existing case bodies or app-widget.integration.test.tsx's
    two existing case bodies.
  - The round changes NO file outside the four paths named in the chunks below, except a REPAIR a
    worker names in its report. Check: `git status --porcelain` at review time lists exactly those
    four paths plus this document, or a fifth that a `REPAIR:` line accounts for.
  - Every one of the 7 `[ ]` units `get-qa-checklist({questId, operationItemId})` returns for this
    item is named exactly once, in a chunk's UNITS or in a NO CHUNK line. Check: 5 in UNITS rows, 2 in
    `settled` lines, 7 total, no id appearing twice.

NO CHUNK:
  - settled health-badge:observable:check-event-type-member at a0b6434fa -> ./packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.test.ts:6 (module) — `it('VALID: {options} => exposes exactly 22 members, ending in health-status')` asserts `expect(orchestrationEventTypeContract.options).toStrictEqual([...])` against a 22-element literal array written out IN ORDER, 'phase-change' first through 'health-status' last. All three clauses of the observable bite off that one comparison: removing 'health-status' reds it, adding a 23rd member reds it, and re-spelling or re-ordering any of the prior 21 reds it. :33's `it.each(orchestrationEventTypeContract.options)` round-trips every member through `.parse`, and :62 still rejects the removed 'smoketest-progress', so the 22 is a content count and not a coincidence.
  - settled health-badge:observable:check-payload-parses at 903f8b5cf -> ./packages/shared/src/contracts/health-status-payload/health-status-payload-contract.test.ts:22 (module) — `it('VALID: {status: ok, uptimeSeconds: 11520, version: 0.1.0} => parses returning exactly those three keys')` calls `healthStatusPayloadContract.parse({status:'ok', uptimeSeconds:11520, version:'0.1.0'})` and asserts the result `toStrictEqual` exactly those three keys, which is the observable's first half; :61 `it('INVALID: {uptimeSeconds missing} => throws validation error')` parses `{status:'ok', version:'0.1.0'}` with uptimeSeconds ABSENT and asserts `toThrow(/Required/u)`, which is its second half verbatim. The channel-side restatement of the same claim is separately live at ./packages/web/src/state/web-socket-channel/web-socket-channel-state.test.ts:343 and :369, so relaxing the field would red four cases across two packages.

### chunk 1 — three real heartbeats off one socket: their spacing, their advancing uptime, and the raw text they arrived in
INTENT:
  - THREE health-status frames are collected off ONE real `/ws` client socket in a single case. Settled by: a harness call asking for three frames, and an `it` whose third argument is a per-test timeout above 35000.
  - The gaps between those three ARRIVALS are ten seconds each. Settled by: one `toStrictEqual` whose expected value is `[10, 10]`, computed from the harness's recorded arrival instants — not from the frames' own `timestamp` field, which the server stamps itself and which a batched flush would satisfy.
  - Each frame's `uptimeSeconds` is strictly greater than the previous frame's. Settled by: a `toStrictEqual` over a two-element boolean array derived from pairwise `>` comparisons, with no fixed delta in the expected value.
  - A collected frame's RAW text, `JSON.parse`d and compared whole, has exactly the keys `type`, `payload` and `timestamp`, and its payload exactly `status`, `uptimeSeconds` and `version`. Settled by: a `toStrictEqual` whose left side is `JSON.parse(<the raw string>)`, never the harness's rebuilt object — `wsMessageContract` is non-strict and strips a fourth key before the existing assertion can see it.
  - Every message that client received was a non-binary text frame, and it received nothing but the three heartbeats. Settled by: a `toStrictEqual` over the frames' binary flags whose expected value is `[false, false, false]`, plus an assertion that the client's whole collected list is those three health-status messages and nothing else.
  - The pre-existing two-client case still passes untouched. Settled by: `git diff` shows no change inside it, and `waitForHealthStatusFrame` still exists with its current signature.
FILES:
  - ./packages/server/src/flows/server/server-flow.integration.test.ts
  - ./packages/server/test/harnesses/server-ws/server-ws.harness.ts
  - ./@types/ws/index.d.ts
UNITS:
  - health-badge:observable:check-emit-interval -> ./packages/server/src/flows/server/server-flow.integration.test.ts (queue) — assert the whole-second gaps between three RECORDED ARRIVAL instants are exactly `[10, 10]`, and let the harness's 35-second deadline throw be the "within 35 seconds" arm. The SHAPE is arrival-time arithmetic on a real socket; a gap read off the frames' own server-stamped `timestamp` proves the stamping, not the delivery.
  - health-badge:observable:check-emit-payload-advances -> ./packages/server/src/flows/server/server-flow.integration.test.ts (queue) — assert BOTH halves: each raw payload compared whole against a three-key object built from that frame's own uptime/version, AND the pairwise uptime differences all positive. The SHAPE is strict increase, never a fixed delta — `Math.floor(process.uptime())` across 10 000 ms yields 9, 10 or 11.
  - health-badge:observable:check-frame-crosses-wire -> ./packages/server/src/flows/server/server-flow.integration.test.ts (queue) — assert the JSON.parse of the RAW collected string against a whole three-key envelope, plus the non-binary flags and the "three messages, all health-status" list. The SHAPE is the unstripped wire bytes; the existing `expectedFrame` toStrictEqual at :64 is over a harness-rebuilt object and cannot see a fourth key.
MIRROR: ./packages/server/src/flows/server/server-flow.integration.test.ts (the existing case at :27, and the harness it drives)
NOTES:
  1. ENTRY POINT and FLOWS. This file drives `ServerFlow` (packages/server/src/flows/server/server-flow.ts), which builds the main Hono app and calls `ServerInitResponder({app, serveWebBundle})`. That responder is the ONLY place `/ws` is registered (server-init-responder.ts:143-149, whose `onOpen` does `clients.add(ws)`) and the ONLY place the heartbeat starts (server-init-responder.ts:811-819 — `setInterval(() => healthHeartbeatEmitBroker({clients}), healthHeartbeatStatics.emit.intervalMs)` at 10000, then `.unref()`). Flow #health-badge reaches it at #server-emits -> #relay-broadcast -> #channel-routes, on paths P13-P19.
  2. WHAT ALREADY COVERS THEM. At integration tier, only the case at server-flow.integration.test.ts:27, which I opened: it awaits ONE frame per client, never times an arrival, never compares two uptimes and never looks at raw text. At UNIT tier, server-init-responder.test.ts:1232 / :1267 / :1303 drive the interval under fake timers through `proxy.simulateConnection`, and server-init-responder.proxy.ts replaces BOTH npm transports (`@hono/node-server`'s `serve` by a module mock, `createNodeWebSocket` by its own adapter proxy), so nothing there is wire evidence. health-heartbeat-emit-broker.test.ts:8 / :36 / :86 run the emit broker over a Set of test-controlled clients. Do NOT delete or weaken any of those; this case is additive.
  3. HARNESS. This chunk OWNS ./packages/server/test/harnesses/server-ws/server-ws.harness.ts and EXTENDS it — it does not create it, and no other chunk uses it. It USES ./packages/server/test/harnesses/server-app/server-app.harness.ts's `setupTestHome`, already wired at server-ws.harness.ts:155; leave that alone. The harness's `afterEach` is a PROPERTY (server-ws.harness.ts:237) that the ts-jest transformer auto-wires per test — write NO `afterEach` in the test file and do NOT call `serverWs.afterEach()` by hand. The test file may import no `node:fs` / `node:net` / `node:os` / `node:child_process` and no `.proxy.ts` at all; every piece of infrastructure belongs in the harness.
  4. AUTHORITY. You may extend the harness, add ONE case to server-flow.integration.test.ts, and widen @types/ws/index.d.ts. You may NOT change the existing case at server-flow.integration.test.ts:27, nor the existing `waitForHealthStatusFrame` signature at server-ws.harness.ts:225 — that case calls it and must keep passing. You may NOT touch server-init-responder.ts, health-heartbeat-emit-broker.ts, health-status-broker.ts, ws-event-relay-broadcast-broker.ts or any statics: this round writes no product code. You may NOT touch server-init-responder.proxy.ts or server-init-responder.test.ts (unit tier, not yours), and you may NOT touch packages/web — chunk 2 owns it. A defect this case exposes in product code is a `REPAIR:` line in your report, not a change you make.
  5. DESIGN DECISIONS. #dd-offline-rule governs check-emit-interval: "A server can hold the socket open while no longer doing any work, so absence of heartbeats is the honest signal rather than a closed connection. The threshold is 30 seconds against a 10-second emit interval, which spans three heartbeats, so a single dropped frame never flips the badge." — the browser's 30s threshold is only safe because THREE heartbeats fit inside it, so what bites is three frames at ten-second gaps, not one frame arriving eventually. #dd-refresh governs the wire: "The interface already holds an open socket for quest, queue and rate-limit updates, so health rides the connection that exists instead of adding a polling timer. A pushed heartbeat also distinguishes a wedged server from a merely slow one, which request-response polling cannot do." — so the frames must arrive on the ORDINARY `/ws` socket, unasked-for. #dd-ws-scope governs the envelope: "health-status becomes a member of orchestrationEventTypeContract so both sides import one declaration of the frame rather than each restating its own. Every other event on the socket is already routed this way, and a second inbound shape parsed ahead of the envelope would be a parallel path maintained by hand." — so the raw envelope must be the same three-key `{type, payload, timestamp}` every other event uses, with no health-specific extra field, which is exactly what the whole-object raw comparison catches.
  6. FIXTURES. ONE client, and it must be SILENT. A client that sends `subscribe-quest` also receives its own reply (quest-modified or quest-load-failed) before any heartbeat, which would break the "this client received nothing but three health-status messages" assertion — that is why the existing case's poll filters by type. Nothing needs seeding: `uptimeSeconds` advances on its own across three real frames. Values you may NOT pin: `uptimeSeconds` and `version` are read live off the real process (health-status-broker.ts:18-19), so echo each frame's own back into its expected object. Values you MAY pin literally: `type` is `'health-status'`, and `status` is always `'ok'` (health-status-broker.ts:17 hardcodes it).
  MECHANICS.
  - The harness must record THREE things per inbound message that it records none of today: the raw text, the arrival instant, and the text/binary flag. Its `socket.on('message', ...)` handler is at server-ws.harness.ts:216 and currently pushes `contentTextContract.parse(String(data))` and nothing else.
  - `@types/ws/index.d.ts:15` declares `on(event: 'message', listener: (data: Buffer | ArrayBuffer | Buffer[]) => void): this` — ONE parameter. Real `ws` v8 emits `(data, isBinary)`, and TypeScript rejects a listener declaring MORE parameters than the declared type, so that line takes a second `isBinary: boolean` parameter before anything else typechecks. That file is a root ambient declaration the predecessor round created for this same harness; it is not a `.claude/settings*.json`, `.mcp.json` or `.env*` file and is yours to edit.
  - Add a multi-frame waiter BESIDE `waitForHealthStatusFrame` rather than changing it. It should poll recursively (never `while(true)` — mirror `pollForHealthStatusFrame` at server-ws.harness.ts:107) until N health-status frames exist or the deadline passes, and throw naming the count and types collected, the way :124 already does.
  - ONE `it`, three commented assertion groups, each naming its unit id. `serverWsHarness().afterEach` is auto-wired PER TEST, so a second case means a second full boot and a second 35-second wait for no extra evidence. DECISIONS records this.
  FOUR HAZARDS. Say in your report which you hit.
  (a) THE TIMEOUT. jest.config.base.js sets no `testTimeout` and packages/server/jest.config.cjs adds none, so the default is 5000ms. The first frame is one full interval away and the third ~20s after that, plus the boot. Pass 60000 as `it`'s third argument. The existing case passes 20000 — do not copy that number.
  (b) THE RUN GETS ~45 SECONDS SLOWER. That is the honest cost of check-emit-interval and is expected; do NOT reach for `jest.useFakeTimers` to avoid it. The interval is registered inside the unmocked boot and the sockets are real, so faking timers beside them replaces the very clock the observable is about.
  (c) THE BOOT'S OWN HAZARDS ARE ALREADY SOLVED — do not re-solve them. The harness deliberately does not intercept the transport (see its header), finds the listener via `process._getActiveHandles()`, clears leftover SIGTERM/SIGINT handlers at :175-176 so the responder's `process.exit` handlers never accumulate, and connects to `environmentStatics.hostname` rather than `127.0.0.1` because that is the address the listener actually bound.
  (d) NO FOURTH FRAME MAY LAND BEFORE YOU COUNT. Assert the "nothing but three messages" claim over the SAME snapshot the three frames came from — have the waiter hand back the list it held when the third match landed — rather than re-reading the client afterwards, where a fourth heartbeat may have arrived between the wait and the read.

### chunk 2 — the rendered badge crosses a real socket drop and comes back on a frame, not a click
INTENT:
  - The rendered badge's text moves ONLINE -> OFFLINE -> ONLINE in one case, across a real `onclose` and a real reconnect. Settled by: three or more `expect(...).toBe(...)` assertions on `screen.getByTestId(healthBadgeStatics.testId).textContent` inside that case, with `'OFFLINE'` between two DIFFERENT ONLINE strings.
  - The OFFLINE in the middle was caused by SILENCE, not by a failed request. Settled by: the badge reads an ONLINE string BEFORE the flip, and the only thing that happens between that reading and the OFFLINE reading is the socket closing and the clock advancing past the silence threshold.
  - The reconnect is real: a SECOND WebSocket is constructed, by the channel's own reconnect logic rather than by the test. Settled by: the recorded socket constructions number two, and the restoring frame is delivered through the SECOND recorded socket's `onmessage`, not through `dispatchInbound` directly.
  - No click and no second seed request happen anywhere in that case. Settled by: the fetch spy's calls addressed to `webConfigStatics.api.routes.healthStatus` are exactly one, asserted AFTER the reconnect.
  - A separate case proves a delivered frame moves the badge text SYNCHRONOUSLY, inside a measured window under 1000 ms. Settled by: an `expect` on the badge's textContent placed immediately after a synchronous `testingLibraryActAdapter` with nothing awaited between them, plus an assertion that a wall-clock elapsed value is under 1000.
  - Both pre-existing cases in the file are untouched. Settled by: `git diff` shows no change inside either.
FILES:
  - ./packages/web/src/widgets/app/app-widget.integration.test.tsx
UNITS:
  - health-badge:observable:check-payload-updates-badge -> ./packages/web/src/widgets/app/app-widget.integration.test.tsx (jsdom) — assert the badge's textContent immediately after a SYNCHRONOUS act that delivers one frame, with nothing awaited in between, and pair it with a measured wall-clock elapsed under 1000. The SHAPE is synchronicity plus a measured bound; an `await waitFor` around the read would satisfy the text change while hiding whether it took 5ms or 5s, which is the whole content of "within 1 second".
  - health-badge:observable:check-reconnect-resumes -> ./packages/web/src/widgets/app/app-widget.integration.test.tsx (jsdom) — assert the RENDERED text across the full sequence ONLINE -> (onclose) -> OFFLINE by silence -> (reconnect) -> ONLINE, with the seed-fetch call list still exactly one entry at the end. The SHAPE is the DOM moving across a real socket lifecycle; reading the binding's `badgeState` object is what use-health-status-binding.test.ts:157 already does, and reaching OFFLINE from a 500 seed instead of from silence leaves "after the socket drops" unproved.
MIRROR: ./packages/web/src/widgets/app/app-widget.integration.test.tsx (the existing case at :152), with ./packages/web/src/widgets/health-badge/health-badge-widget.test.tsx:159 for the fake-clock-plus-MSW shape
NOTES:
  1. ENTRY POINT and FLOWS. The file's implementation is packages/web/src/widgets/app/app-widget.tsx, the app shell, which renders `<HealthBadgeWidget />` at :80 in the logo row so one mount covers every route. `useHealthStatusBinding` (use-health-status-binding.ts:44-77) seeds once in a mount effect, subscribes `webSocketChannelState.healthStatus$()` unconditionally, stamps `lastHeartbeatAt: new Date().toISOString()` on every frame, and runs a `setInterval` at `healthBadgeStatics.silenceTickMs` (1000) that compares `Date.now()` against `lastHeartbeatAt` through `isHeartbeatSilentGuard` and flips to `{state:'offline', offlineCause:'silence'}` at 30000. Flow #health-badge reaches this file at #channel-routes, on paths P13-P19.
  2. WHAT ALREADY COVERS THEM. Partially, and never joined up — I opened all four. app-widget.integration.test.tsx:152 moves the text on one `dispatchInbound` frame but bounds nothing in time and never closes a socket. use-health-status-binding.test.ts:157 does close and reconnect, but reads `badgeState` rather than the DOM and reaches OFFLINE from a 500 seed, so no socket participates in the OFFLINE. web-socket-channel-state.test.ts:645 proves the health-status subject survives close+reconnect, with no badge anywhere. health-badge-widget.test.tsx:159 reaches OFFLINE by silence and stops there; :224 and :246 are the repo's ONLY OFFLINE -> ONLINE recoveries and both are driven by `proxy.clickBadge()`, which is precisely what check-reconnect-resumes forbids. Nothing anywhere asserts a timing bound. Keep both existing cases in this file intact and ADD to them.
  3. HARNESS. None, and this chunk creates and owns none. packages/web's 24 harnesses under `test/harnesses` are all Playwright; this file drives jsdom. Everything needed is already imported by the file or is a PUBLIC member of `webSocketChannelState`.
  4. AUTHORITY. You may add cases and imports to app-widget.integration.test.tsx. You may NOT modify either existing case. You may NOT import any `.proxy.ts` — an `.integration.test` file is barred from proxies outright, so `webSocketChannelStateProxy` and `useHealthStatusBindingProxy` are off limits even though they do exactly what you need; drive the singleton's public members instead. You may NOT touch any product file under packages/web/src, anything in packages/testing, or packages/server — chunk 1 owns that. A defect you expose in product code is a `REPAIR:` line in your report, not a change you make.
  5. DESIGN DECISIONS. #dd-reconnect governs check-reconnect-resumes: "Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it." — so the badge must have NO socket-close awareness; a case that reached OFFLINE by any route other than silence would not test the sentence. #dd-offline-rule: "A server can hold the socket open while no longer doing any work, so absence of heartbeats is the honest signal rather than a closed connection." #dd-degraded-recovers governs the recovery: "The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload. Making any state stop listening would strand the badge on a reading that was true once and is not any more." — that is the "with no click issued" arm, and it is why the fetch-call-count assertion is load-bearing rather than decorative. #dd-refresh governs check-payload-updates-badge: "health rides the connection that exists instead of adding a polling timer" — a badge that only picked frames up on its own 1000ms tick would still pass a lazy `await waitFor`, which is why the assertion sits immediately after a SYNCHRONOUS act.
  6. FIXTURES. Stage the health seed as a 500, not a 200: the badge can then never read an ONLINE string from the seed, so every ONLINE reading in the case is attributable to a frame and to nothing else. Values that can be told apart: frame one carries `uptimeSeconds: 11520` (renders `'ONLINE 3h 12m'`) and the post-reconnect frame carries `11580` (renders `'ONLINE 3h 13m'`) — they MUST differ, or "the frame after reconnect moved the badge" is unprovable; that exact false green was already fixed once in health-badge-widget.test.tsx. Rendering the WHOLE AppWidget mounts four HTTP-backed bindings and MSW runs with `onUnhandledRequest: 'error'`, so stage FOUR endpoints or the render throws: `webConfigStatics.api.routes.questsQueue`, `.rateLimits`, `.orchestrationDispatch` and `.healthStatus`. Render inside a MemoryRouter whose child route element is a trivial `<div>`, so `useGuildsBinding` and `/api/guilds` never mount — the existing case at :196-211 is the shape to copy.
  MECHANICS.
  - THE SOCKET SPY IS THE WHOLE TRICK. `registerSpyOn({object: globalThis as never, method: 'WebSocket'})` with `.calledWith([<the WsUrl>]).implement(...)` is already used at :55-78; extend that implementation to PUSH each constructed fake socket into an array the test holds. Each fake needs assignable `onmessage` and `onclose` fields (the adapter ASSIGNS them at websocket-connect-adapter.ts:34 and :43) plus the same `onopen` setter that fires its handler synchronously, `readyState`, `close` and `send`. The test then drives the real path with `sockets[0].onmessage({data: JSON.stringify(envelope)})` — the adapter `JSON.parse`s that string and calls the REAL `dispatchInbound`, so the frame goes through the real envelope parse and the real payload parse, which delivering a pre-parsed object to `dispatchInbound` skips.
  - THE RECONNECT DRIVES ITSELF. web-socket-channel-state.ts:118-126: `onClose` sets `socket = null` and, when `shouldReconnect`, schedules `globalThis.setTimeout(openConnection, 3000)`. So under fake timers a single `jest.advanceTimersByTime(30000)` fires the channel's OWN reconnect at t+3000 (constructing socket #2, whose `onopen` setter fires synchronously) and the badge's silence flip at t+30000. The test calls `webSocketChannelState.openConnection()` itself only if that does not happen — and if you take that fallback, say so in your report, because it means the channel's own reconnect did not run.
  - THE CLOCK. Install `jest.useFakeTimers({now: <a fixed instant>})` BEFORE the render and `jest.useRealTimers()` before the case ends, inline — no hooks. Under modern fake timers `Date` is faked too, so the binding's `new Date().toISOString()` stamp and its `Date.now()` comparison move together and the 30000 boundary lands exactly. health-badge-widget.test.tsx:159-186 is the working precedent in this package for fake timers beside `mantineRenderAdapter` and a staged MSW endpoint. Every state change goes inside `testingLibraryActAdapter`.
  - DO NOT AWAIT MSW UNDER THE FAKE CLOCK. That is why the seed is a 500 and why the case never waits for it — the reconnect case reads the badge only after frames it delivered itself. Read the request evidence off the FETCH SPY (`fetchSpy.callsMatching([webConfigStatics.api.routes.healthStatus])`, already installed at :187), never off `healthStatusEndpoint.getRequestCount()`, whose counter depends on an MSW response having settled.
  - THE TIMING CASE USES REAL TIMERS. Keep it separate from the reconnect case: stage the 500 seed, `await` the badge to read `'OFFLINE'`, capture `Date.now()`, deliver ONE frame inside a SYNCHRONOUS `testingLibraryActAdapter`, then assert the textContent immediately with nothing awaited between the act and the expect — the shape use-health-status-binding.test.ts:84-89 and web-socket-channel-state.test.ts:364-366 both use, each with a comment saying exactly that. Then assert the elapsed against the literal `1000`, written out rather than taken from `healthBadgeStatics.silenceTickMs`: the observable's one second is the spec's number, and coupling it to the tick period would silently follow that period if it ever changed.
  - No `beforeEach`/`afterEach`, no conditionals in a test body, no `toEqual`/`toMatchObject`/`toContain`. Read the badge as `screen.getByTestId(healthBadgeStatics.testId).textContent` and assert with `toBe`.

PHASES:
  1: wave 1 — the whole round. The one harness this round touches has exactly one importer and rides in that importer's own chunk, so no harness is shared and there is nothing for a mid-round gate to read between the two chunks.

WAVES:
  1: 1, 2

## Round log
