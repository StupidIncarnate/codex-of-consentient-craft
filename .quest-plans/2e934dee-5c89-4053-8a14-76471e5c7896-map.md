# shared · send-message-with-images

Operation item: 2e934dee-5c89-4053-8a14-76471e5c7896
Flow: #send-message-with-images · Package: shared

GROUP 1  (different files — go out together)
  A. packages/shared/src/statics/pasted-image/pasted-image-statics.ts        new  — every literal the paste feature shares across web/server/orchestrator
     packages/shared/src/statics/pasted-image/pasted-image-statics.test.ts   new  — one toStrictEqual over the whole object, mirror locations-statics.test.ts
     packages/shared/statics.ts                                              edit — barrel line for the new statics
  B. packages/shared/src/statics/locations/locations-statics.ts              edit — add `imagesDir: 'images'` to the `quest` group
     packages/shared/src/statics/locations/locations-statics.test.ts         edit — add imagesDir to the quest block of the existing toStrictEqual

GROUP 2  (needs group 1 — different files from each other)
  D. packages/shared/src/contracts/pasted-image-media-type/pasted-image-media-type-contract.ts       new  — z.enum(pastedImageStatics.allowedMediaTypes).brand, mirror base-branch-name-contract.ts
     packages/shared/src/contracts/pasted-image-media-type/pasted-image-media-type.stub.ts           new  — mirror base-branch-name.stub.ts
     packages/shared/src/contracts/pasted-image-media-type/pasted-image-media-type-contract.test.ts  new
     packages/shared/contracts.ts                                                                    edit — barrel lines (contract + stub)
  F. packages/shared/src/brokers/locations/quest-images-path-find/locations-quest-images-path-find-broker.ts        new  — <questFolderPath>/images, mirror ward-results-path-find
     packages/shared/src/brokers/locations/quest-images-path-find/locations-quest-images-path-find-broker.proxy.ts  new
     packages/shared/src/brokers/locations/quest-images-path-find/locations-quest-images-path-find-broker.test.ts   new
     packages/shared/brokers.ts                                                                                     edit — barrel line

GROUP 3  (needs group 2 D — touches contracts.ts, which D also edits)
  E. packages/shared/src/contracts/pasted-image-upload/pasted-image-upload-contract.ts       new  — { mediaType, dataBase64 }, mirror ward-result-contract.ts
     packages/shared/src/contracts/pasted-image-upload/pasted-image-upload.stub.ts           new  — StubArgument spread, mirror ward-result.stub.ts
     packages/shared/src/contracts/pasted-image-upload/pasted-image-upload-contract.test.ts  new
     packages/shared/contracts.ts                                                            edit — barrel lines (contract + stub)

PROVES
  #check-images-dir-name-is-shared  -> packages/shared/src/statics/locations/locations-statics.test.ts

TRAPS
  statics/ may import ONLY statics/ — pastedImageStatics is a plain `as const` object, NOT branded, NOT importing zod.
  questImagesDirName lands in locations-statics as `quest.imagesDir`, NOT as a key of pastedImageStatics.
  Regex properties are STRINGS (regex source), never RegExp objects — a shared RegExp with /g carries lastIndex across callers.
  Number literals stay literal (5242880, not 5 * 1024 * 1024) — no statics file in this repo uses arithmetic and `as const` would widen it to `number`.
  Base64ImageData is an inline brand inside pasted-image-upload-contract.ts — no separate contract folder for it.
  Barrels live at the package ROOT (packages/shared/statics.ts, contracts.ts, brokers.ts). contracts.ts exports the .stub too.
  Tests import types from STUBS, never from contracts.

SEAM
  #resolve-images-dir is shared+server. Server (a later session) owns create-if-absent + writing files.
  My half: the dirname literal and the path resolver. Do NOT mkdir, do NOT write files, do NOT touch packages/server.
