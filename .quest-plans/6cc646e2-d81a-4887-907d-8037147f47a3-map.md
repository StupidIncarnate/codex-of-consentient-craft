# codeweaver — orchestrator × send-message-with-images

Operation item: 6cc646e2-d81a-4887-907d-8037147f47a3
Cell: package `orchestrator`, flow `send-message-with-images`

Seam: the SERVER writes the image files and rewrites each `[Pasted Image N]` token into
`![Pasted Image N](<absolute path>)` BEFORE it calls this package. The orchestrator's entry points keep
their existing `message: string` signature — no `images` parameter reaches this package, and none is added.

No `## Contracts` heading rendered for this cell — no contract work here.

## GROUP 1  (different files — go out together)

  packages/orchestrator/src/transformers/image-prompt-trailer/image-prompt-trailer-transformer.ts   new
    — `imagePromptTrailerTransformer({ promptText }): PromptText`. Reads `pastedImageStatics` from
      `@dungeonmaster/shared/statics`. When `promptText` carries an image token
      (`new RegExp(pastedImageStatics.imageTokenPattern, 'u')`) AND does not already carry
      `promptSentinel`, returns `` `${promptText}\n\n${promptSentinel}\n${promptInstruction}` ``.
      Otherwise returns `promptText` unchanged. Parses through `promptTextContract`.
  packages/orchestrator/src/transformers/image-prompt-trailer/image-prompt-trailer-transformer.proxy.ts  new
    — empty proxy, mirror `transformers/chat-prompt-build/chat-prompt-build-transformer.proxy.ts`
  packages/orchestrator/src/transformers/image-prompt-trailer/image-prompt-trailer-transformer.test.ts   new
    — trailer append / no-append / exactly-once branches

  packages/orchestrator/src/adapters/child-process/spawn-stream-json/child-process-spawn-stream-json-adapter.test.ts  edit
    — one added test: prompt carrying `![Pasted Image 1](<abs>)` → full argv asserted with toStrictEqual,
      `-p` at index 0 and the prompt verbatim at index 1

## GROUP 2  (needs group 1's transformer on disk)

  packages/orchestrator/src/transformers/chat-prompt-build/chat-prompt-build-transformer.ts   edit
    — wrap BOTH return points in `imagePromptTrailerTransformer`. The `if (sessionId)` early return
      (resume / follow-up turns) is the one that matters most and is the one a naive edit misses.
  packages/orchestrator/src/transformers/chat-prompt-build/chat-prompt-build-transformer.test.ts  edit
    — trailer-appended-once, no-trailer-without-images, on both the resume path and a template path

## GROUP 3  (needs group 2 — these assert the composed prompt as it reaches argv)

  packages/orchestrator/src/brokers/chat/spawn/chat-spawn-broker.test.ts   edit
    — brand-new quest (`setupNewSession`) first message carrying an image → spawned argv's `-p` value
  packages/orchestrator/src/responders/followup-chat/start/followup-chat-start-responder.test.ts  edit
    — existing tavernkeeper item WITH sessionId (resume path) → `-p` is exactly `message + trailer`
  packages/orchestrator/src/responders/chat/start/chat-start-responder.proxy.ts   edit
    — add `getSpawnedArgs: () => unknown` delegating to `spawnProxy.getSpawnedArgs()`
  packages/orchestrator/src/responders/chat/start/chat-start-responder.test.ts   edit
    — resumed session, message carrying the absolute path → `-p` pinned whole

## PROVES

  #check-sentinel-from-shared-statics  -> read-check: image-prompt-trailer-transformer.ts imports
                                          pastedImageStatics; reviewer reports the file:line
  #check-trailer-appended-once         -> chat-prompt-build-transformer.test.ts
  #check-no-trailer-without-images     -> chat-prompt-build-transformer.test.ts
  #check-argv-carries-image-path       -> child-process-spawn-stream-json-adapter.test.ts
  #check-new-quest-first-message       -> chat-spawn-broker.test.ts
  #check-followup-message-carries-path -> followup-chat-start-responder.test.ts
  #check-orchestrator-sees-absolute-path -> chat-start-responder.test.ts
  edge "spawns in the background"      -> chat-start-responder.test.ts (responder resolves before
                                          onComplete fires)
  edge "answers the HTTP request"      -> chat-start-responder.test.ts (responder resolves with
                                          { chatProcessId })
  #check-agent-issues-read             -> needs a live Claude CLI session and its JSONL transcript.
                                          Not reachable from a unit test — unconfirmable.
  terminal #agent-reads-images         -> same. A unit test cannot observe a model's context.

## TRAPS

  Assertions: no toEqual / toMatchObject / toContain / toBeDefined / toHaveLength / toBeTruthy /
    .not.*; no `expect(str.includes(x)).toBe(true)`. Pin the whole `-p` string with `.toBe(...)` where
    the prompt is short (every resume path is), and where it is a full template use
    `expect(prompt.split(ABS_PATH).length).toBe(2)` — an exact occurrence count, which bites on both
    absence (1) and duplication (3).
  `args[0]` is `'-p'` and `args[1]` is the prompt — the adapter builds
    `['-p', prompt, '--output-format', 'stream-json', '--verbose', '--model', model, '--chrome', ...]`.
  `pastedImageStatics.imageTokenPattern` is a STRING, not a RegExp. Build it with `new RegExp(p, 'u')`.
  No beforeEach / afterEach. Fresh proxy per test, created BEFORE the call under test.
  No jest.mock / jest.mocked / jest.spyOn — registerMock / registerSpyOn, inside proxies only.
  Branded returns: exported functions return contract types, never raw `string`.
  git: `git -C <path> …` is refused whatever it asks — call git bare from the worktree. Never chain a
    git call with `&&` and never pipe its output; use git's own flags (`-n`, `--oneline`, `--stat`,
    `--name-only`).
