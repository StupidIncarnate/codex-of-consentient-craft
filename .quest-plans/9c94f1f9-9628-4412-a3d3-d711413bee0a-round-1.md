# Round 1 — [codeweaver] Codeweaver: build this slice — package: server

## Context

Quest ID: a7520e60-430c-4d0e-b332-9952d6d5c042
Work Item ID: ef842293-363c-4351-b3a7-1dfca5a7ad28
Operation Item ID: 9c94f1f9-9628-4412-a3d3-d711413bee0a
Your operation item: [codeweaver] Codeweaver: build this slice — package: server

Operations ledger (in order):
1. [x] [chaoswhisperer] Author spec + implementation plan
2. [x] [riftcarver] Riftcarver: carve the quest branch, worktree and preflight build
3. [x] [codeweaver] Codeweaver: build this slice — package: shared
4. [>] [codeweaver] Codeweaver: build this slice — package: server  <-- YOUR OPERATION ITEM
5. [ ] [codeweaver] Codeweaver: build this slice — package: web
6. [ ] [ward changed] Ward gate (changed files)
7. [ ] [flowrider] Flowrider: author the flow-perspective test suites below the browser — package: server
8. [ ] [flowrider] Flowrider: author the flow-perspective test suites below the browser — seam: server + shared
9. [ ] [groundstomper] Groundstomper: author the browser walk for this flow — flow: health-badge
10. [ ] [siegemaster] Siegemaster: manual-QA this flow and review its test suite — flow: health-badge
11. [ ] [ward full] Ward gate (full monorepo)

Flows your operation item lands on: #health-badge
(A starting point, NOT a boundary — read every flow, and build whatever the flows need.)

Packages your operation item lands in: server
(Name these packages in every minion brief you write — the planner and the workers point their own searches here instead of guessing. NOT a boundary: a minion may touch another package if the work needs it.)

Your nodes (rendered from the spec as it stands right now, not from the ledger): #seed-fetch, #subscribe-heartbeat, #server-emits, #relay-broadcast, #channel-routes

Must satisfy — these are YOUR acceptance targets, verbatim:
  - check-seed-route-answers [api-call] on #seed-fetch: "GET /api/health/status returns 200 with a JSON body of exactly {status, uptimeSeconds, version} and no other keys"
  - check-liveness-probe-unchanged [api-call] on #seed-fetch: "GET /api/health still returns 200 with a body of exactly {status: 'ok', timestamp} and gains no new fields"
  - check-client-in-broadcast-set [queue-message] on #subscribe-heartbeat: "A client that completes the /ws upgrade receives the next health-status frame without sending any subscription message first"
  - check-emit-interval [queue-message] on #server-emits: "With the server running, three health-status frames arrive within 35 seconds, spaced 10 seconds apart"
  - check-emit-payload-advances [queue-message] on #server-emits: "Each health-status frame carries exactly {status, uptimeSeconds, version}, and uptimeSeconds is strictly greater than the previous frame's"
  - check-broadcast-reaches-unsubscribed-client [queue-message] on #relay-broadcast: "A client holding no quest subscription still receives every health-status frame"
  - check-dead-client-does-not-block [queue-message] on #relay-broadcast: "When one client's send throws, that client is dropped from the connected set and every remaining client still receives that same frame"
  - check-frame-crosses-wire [queue-message] on #channel-routes: "The serialised health-status envelope arrives at the browser as one WebSocket text message carrying type, payload and timestamp"

Contracts you own — every property description is a requirement:
  - HealthStatusEndpoint (endpoint, modified) [packages/server/src/statics/api-routes/api-routes-statics.ts]
      method: Read-only; the badge never mutates server state.
      path: A new key added to the existing health group in this statics file, alongside the check key that already holds /api/health.
      responseBody: The 200 body is exactly the shared HealthStatusPayload shape, so server and web agree by importing one contract rather than restating it.
  - HealthHeartbeatStatics (data, new) [packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts]
      emitIntervalMs: How often the server emits a health-status frame. Three of these fit inside the web side's 30 second silence threshold.

Design decisions constraining your scope:
  - DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload. Making any state stop listening would strand the badge on a reading that was true once and is not any more.
  - A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it.

Seams — each line is a node you share with another package, and where that package's half of it stands:
  - #seed-fetch with web — NOT BUILT YET: a later session owns these — build your half to the shape they need, and do NOT build theirs
      attributed to web — check-seed-request-issued: "On mount the badge issues GET /api/health/status exactly once, with no query string"
  - #subscribe-heartbeat with web — NOT BUILT YET: a later session owns these — build your half to the shape they need, and do NOT build theirs
      attributed to web — check-heartbeat-reaches-badge: "A health-status frame delivered after mount changes the badge text with no further HTTP request issued"
  - #server-emits with shared — ALREADY BUILT: verify each of these EXISTS in committed code before you plan, and repair it if it does not
      attributed to shared — check-event-type-member: "orchestrationEventTypeContract.options contains 'health-status' and has 22 members, the prior 21 unchanged"
  - #channel-routes with web — NOT BUILT YET: a later session owns these — build your half to the shape they need, and do NOT build theirs
      attributed to web — check-payload-updates-badge: "A delivered health-status frame changes the badge text within 1 second of arrival"
      attributed to web — check-reconnect-resumes: "After the socket drops and reconnects, the first health-status frame moves the badge out of OFFLINE with no click issued"
  - #channel-routes with shared — ALREADY BUILT: verify each of these EXISTS in committed code before you plan, and repair it if it does not
      attributed to shared — check-payload-parses: "A health-status payload parses against healthStatusPayloadContract, and a payload missing uptimeSeconds fails that parse"

Packages affected (whole quest): shared (edit, library), server (edit, http-backend), web (edit, frontend-react)

Original user request (the intent behind the flows):
The app top bar gives no indication of whether the server behind it is still
answering. When the server dies or wedges, the interface keeps rendering its
last state and the operator finds out only when an action fails.

Add a health badge to the top bar, visible on every route. It seeds itself at
mount from a new GET /api/health/status returning status, uptime and version,
then tracks a health-status heartbeat the server emits every 10 seconds over
the WebSocket the interface already holds open.

The badge reads ONLINE with uptime beside it, DEGRADED, or OFFLINE. It goes
OFFLINE when the seed request fails to reach the server, when the server
answers with an error, or when 30 seconds pass with no heartbeat — a server
can hold the socket open while no longer doing work, so silence is the signal
rather than disconnection. Clicking an OFFLINE badge retries the seed request.

/api/health stays as it is, a bare liveness probe.

## Plan

TOUCHES:
  ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts — NEW — the one place the heartbeat's tick period is written down; exports `healthHeartbeatStatics` as a frozen `as const` object, and the bootstrap responder reads `.emitIntervalMs` from it rather than holding a module constant the way `server-init-responder.ts:61`'s `FLUSH_INTERVAL_MS` does. Companion: `health-heartbeat-statics.test.ts` (server statics carry exactly one companion — no proxy, no stub, verified across all six existing `statics/*` folders).
      HealthHeartbeatStatics.emitIntervalMs — must declare `emitIntervalMs: 10_000`, pinned by a whole-object `toStrictEqual`, so three ticks fit inside the web side's 30 000 ms silence threshold with nothing left over.

  ./packages/server/src/statics/api-routes/api-routes-statics.ts — EXISTS — the repo's route table; `apiRoutesStatics.health` currently holds the single key `check: '/api/health'` at :10-12, and every server flow imports this file to register its paths. Companion: `api-routes-statics.test.ts`, whose single `toStrictEqual` at :5-63 pins the WHOLE object, so a new key must land in both files or the test reds.
      HealthStatusEndpoint.path — must add `status: '/api/health/status'` as a second key inside the existing `health` group, beside `check`, leaving `check` byte-identical.

  ./packages/server/src/adapters/process/uptime/process-uptime-adapter.ts — NEW — the I/O boundary over `process.uptime()`, which is non-deterministic and therefore unmockable anywhere above the adapter layer; exports `processUptimeAdapter`. Nothing in `packages/server` reads `process.uptime()` today and no boot timestamp is captured anywhere in `src/state/`. Companions: `.proxy.ts` (required for adapters) and `.test.ts`.

  ./packages/server/src/adapters/server-version/read/server-version-read-adapter.ts — NEW — resolves the running server's own version string; exports `serverVersionReadAdapter`. Nothing in `packages/server/src/**` reads `packages/server/package.json:3` (`"version": "0.1.0"`) today, and there is no build-time inject, so this is the only producer the payload's `version` field can have. Companions: `.proxy.ts` and `.test.ts`.

  ./packages/server/src/adapters/timer/set-interval/timer-set-interval-adapter.ts — NEW — a mockable wrapper over `setInterval`/`clearInterval`; exports `timerSetIntervalAdapter` returning `{ stop }`. `packages/server/src/adapters/` has seven folders and no `timer/`; both existing server intervals (`server-init-responder.ts:771`, `quest-driven-watchers-bootstrap-responder.ts:68`) call the global raw, which is why `server-init-responder.test.ts:827-832` can only wait 150 ms of real wall clock. Companions: `.proxy.ts` (the whole point — it is what lets a test drive a tick) and `.test.ts`.

  ./packages/server/src/brokers/health-status/snapshot/health-status-snapshot-broker.ts — NEW — the SINGLE producer of a `HealthStatusPayload`, composing the two adapters above and parsing through the shared contract; exports `healthStatusSnapshotBroker`. Both consumers — the `GET /api/health/status` seed responder and the heartbeat responder — call this rather than each restating the shape. Companions: `.proxy.ts` and `.test.ts`.

  ./packages/server/src/responders/health/status/health-status-responder.ts — NEW — the seed endpoint's handler; exports `HealthStatusResponder` returning a `ResponderResult`. There is no `responders/health/` directory of any kind today. Companions: `.proxy.ts` and `.test.ts`.
      HealthStatusEndpoint.responseBody — must put the value `healthStatusSnapshotBroker` returned into `data` unchanged, so the 200 body IS `healthStatusPayloadContract`'s shape by construction rather than by a second hand-written object literal.

  ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.ts — NEW — owns the recurring emit: exports `healthHeartbeatStartBroker({ clients })` returning `{ stop: () => void }`, registering one interval at `healthHeartbeatStatics.emitIntervalMs` whose tick builds a `wsMessageContract` envelope and hands it to the existing broadcast broker. Nothing in the repo schedules a health-status frame today. A BROKER rather than a responder because `server-init-responder.ts` is what has `clients` in scope and `responders/` may import `brokers/` but not another domain's responder; `designStartBroker` at `brokers/design/start/design-start-broker.ts:24` is the in-package precedent for a broker returning an inline handle (`Promise<{ kill: () => void }>`). Companions: `.proxy.ts` and `.test.ts`.
      check-emit-interval — must register its tick at exactly `healthHeartbeatStatics.emitIntervalMs`, and three ticks must produce three broadcasts and no more.
      check-emit-payload-advances — must build each frame's payload freshly from `healthStatusSnapshotBroker` per tick, so two ticks over a rising uptime reading produce two payloads of exactly three keys whose `uptimeSeconds` strictly increases.
      check-dead-client-does-not-block — must route the tick through `wsEventRelayBroadcastBroker` and hand it the live `clients` set, so a throwing client is deleted from that set while the surviving client still receives that same tick's frame.
      check-frame-crosses-wire — must produce ONE `client.send` call per client per tick, carrying one JSON string that parses to exactly `{type, payload, timestamp}` with `type: 'health-status'`.

  ./packages/server/src/flows/health/health-flow.ts — EXISTS — the Hono sub-app holding every health route; `HealthFlow()` registers the liveness probe inline at :17-22 and is already mounted from `start-server.ts:47`. Companion: `health-flow.integration.test.ts` (flows take an `.integration.test.ts` INSTEAD of a unit test).
      check-seed-route-answers — must register a second GET at `apiRoutesStatics.health.status` delegating to `HealthStatusResponder`, answering 200 with a body of exactly `{status, uptimeSeconds, version}`.
      check-liveness-probe-unchanged — must leave the `/api/health` registration answering 200 with a body of exactly `{status: 'ok', timestamp}`, and gain an assertion on the RAW body that reds if a third key ever appears.
      HealthStatusEndpoint.method — must register the new route with `app.get` and no other verb, so the endpoint cannot mutate server state.

  ./packages/server/src/responders/server/init/server-init-responder.ts — EXISTS — owns `clients` (the every-connected-WS set, `:88`), the `/ws` upgrade that fills it (`:141-145`), and the only server-lifetime timer teardown path (`clearInterval` inside the SIGTERM/SIGINT handlers at `:849`/`:856`). This is the one place with `clients` in scope, so it is where the heartbeat is started and stopped. Companions: `.proxy.ts` and `.test.ts`.
      check-client-in-broadcast-set — must start the heartbeat over the same `clients` set `onOpen` adds to, so a client that only completes the upgrade receives the next tick with no inbound message parsed.
      check-broadcast-reaches-unsubscribed-client — must keep the heartbeat off the `clientSubscriptions` map entirely, so a client with no quest subscription receives every tick.

  ./packages/server/src/brokers/ws-event-relay/broadcast/ws-event-relay-broadcast-broker.ts — EXISTS — the package's only fan-out-to-everyone primitive: `wsEventRelayBroadcastBroker({clients, message})` serialises once at :21 and, at :23-30, deletes a throwing client from the caller's live set and continues the loop. The round REUSES it unchanged; its proxy's `captureClient` / `getCapturedMessages()` is how the heartbeat test reads what crossed the wire.

DEPENDS:
  ./packages/server/src/brokers/health-status/snapshot/health-status-snapshot-broker.ts
      needed by: ./packages/server/src/responders/health/status/health-status-responder.ts — the whole 200 body of the seed route; the responder puts the returned value into `data` untouched.
      needed by: ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.ts — a FRESH payload per tick; calling it once and reusing the value would freeze `uptimeSeconds` and break check-emit-payload-advances.
      needs: ./packages/server/src/adapters/process/uptime/process-uptime-adapter.ts — the live uptime reading.
      needs: ./packages/server/src/adapters/server-version/read/server-version-read-adapter.ts — the version string.
      needs: `healthStatusPayloadContract` from `@dungeonmaster/shared/contracts` (packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts:15-19, barrelled at packages/shared/contracts.ts:757) — the three-key object shape and the `UptimeSeconds` / `ServerVersion` brands. A `brokers/` file importing another package's `contracts` subpath is on the allow-list.

  ./packages/server/src/statics/api-routes/api-routes-statics.ts
      needed by: ./packages/server/src/flows/health/health-flow.ts — the literal `'/api/health/status'` the new `app.get` registers, read as `apiRoutesStatics.health.status`.
      needed by: nine other flow files (design, directory, guild, orchestration, process, quest, rate-limits, session, tooling) that each read their own group — none of them reads `health`, so adding a key inside `health` cannot reach them.
      needs: nothing. `statics/` may import only `statics/`, and this file imports nothing today.

  ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts
      needed by: ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.ts — the interval passed to the timer adapter. It is ALSO the address that broker's proxy keys its spy on, so the value is load-bearing in both the source and the test.
      needs: nothing.

  ./packages/server/src/adapters/process/uptime/process-uptime-adapter.ts
      needed by: ./packages/server/src/brokers/health-status/snapshot/health-status-snapshot-broker.ts — a whole-second, non-negative reading already branded `UptimeSeconds`.
      needs: the Node `process` global, and `healthStatusPayloadContract` from `@dungeonmaster/shared/contracts` for the brand it parses through. `adapters/` may import `node_modules` and `contracts/`.

  ./packages/server/src/adapters/server-version/read/server-version-read-adapter.ts
      needed by: ./packages/server/src/brokers/health-status/snapshot/health-status-snapshot-broker.ts — a non-empty string already branded `ServerVersion`.
      needs: `require.resolve` and `fs.readFileSync`, plus `healthStatusPayloadContract` from `@dungeonmaster/shared/contracts` for the brand.

  ./packages/server/src/adapters/timer/set-interval/timer-set-interval-adapter.ts
      needed by: ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.ts — the tick registration and the `stop` that clears it.
      needs: the `setInterval`/`clearInterval` globals only.

  ./packages/server/src/brokers/ws-event-relay/broadcast/ws-event-relay-broadcast-broker.ts
      needed by: ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.ts — the fan-out itself AND the dead-client drop; check-dead-client-does-not-block is satisfied by REUSING this, not by reimplementing the loop.
      needed by: ./packages/server/src/responders/server/init/server-init-responder.ts — its existing global-event arm at :763-766, unchanged by this round.
      needs: `WsMessage` from `@dungeonmaster/shared/contracts` and `WsClient` from ./packages/server/src/contracts/ws-client/ws-client-contract.ts. Both already in place; this file is not edited.

  ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.ts
      needed by: ./packages/server/src/responders/server/init/server-init-responder.ts — the `{ stop }` handle it holds and calls from the SIGTERM and SIGINT handlers, beside the existing `clearInterval(flushIntervalHandle)`.
      needs: ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts — the interval.
      needs: ./packages/server/src/adapters/timer/set-interval/timer-set-interval-adapter.ts — the tick.
      needs: ./packages/server/src/brokers/health-status/snapshot/health-status-snapshot-broker.ts — the per-tick payload.
      needs: ./packages/server/src/brokers/ws-event-relay/broadcast/ws-event-relay-broadcast-broker.ts — the fan-out over the caller's `clients` set.
      needs: `wsMessageContract` from `@dungeonmaster/shared/contracts` and `isoTimestampContract` from ./packages/server/src/contracts/iso-timestamp/iso-timestamp-contract.ts — the `{type, payload, timestamp}` envelope, built exactly as server-init-responder.ts:648-652 builds its own.

  ./packages/server/src/responders/health/status/health-status-responder.ts
      needed by: ./packages/server/src/flows/health/health-flow.ts — a `ResponderResult` the flow unwraps into `c.json(result.data, result.status)`.
      needs: ./packages/server/src/brokers/health-status/snapshot/health-status-snapshot-broker.ts — the body.
      needs: ./packages/server/src/contracts/responder-result/responder-result-contract.ts and ./packages/server/src/statics/http-status/http-status-statics.ts — the `{status, data}` envelope and the 200/500 codes, exactly as rate-limits-get-responder.ts:14-28 does.

  ./packages/server/src/flows/health/health-flow.ts
      needed by: ./packages/server/src/startup/start-server.ts — already, via the `subApps` array at :47. This round adds a route to a flow that is ALREADY mounted, so `start-server.ts` needs no edit and is not in this round.
      needs: ./packages/server/src/statics/api-routes/api-routes-statics.ts — both route literals.
      needs: ./packages/server/src/responders/health/status/health-status-responder.ts — the seed handler.
      needs: `hono` — `Hono` and `ContentfulStatusCode`, as rate-limits-flow.ts:10-11 imports them.

  ./packages/server/src/responders/server/init/server-init-responder.ts
      needed by: ./packages/server/src/flows/server/server-flow.ts — already, at :28. Unchanged by this round.
      needs: ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.ts — started once with the `clients` set built at :88, stopped in the two signal handlers.

DECISIONS:
  - The heartbeat does NOT ride the orchestrator event bus, even though the subscription for it already exists. `server-init-responder.ts:539` iterates `orchestrationEventTypeContract.options`, so `'health-status'` has been subscribed since the shared round landed — but every relayed envelope is built at :648-652 as `payload: { ...effectivePayload, processId }`, and `orchestrator-events-on-adapter.ts:21` types `processId: ProcessId` as REQUIRED. A bus-routed frame therefore carries a fourth payload key, which check-emit-payload-advances ("carries exactly {status, uptimeSeconds, version}") forbids. The heartbeat calls `wsEventRelayBroadcastBroker` directly instead. The relay's health-status subscription is left exactly as it is: nothing in `packages/server` can emit on the bus anyway — there is no `events-emit` sibling among the 45 adapters in `adapters/orchestrator/`, only `events-on`.
  - The heartbeat is a BROKER, not a responder. `server-init-responder.ts:88` is the only place `clients` exists, and the `responders/` import allow-list carries `brokers/` but not `responders/`, so a sibling-domain bootstrap responder could not be reached from there. `brokers/design/start/design-start-broker.ts:24` returns `Promise<{ kill: () => void }>` — an inline handle from a server broker that passes lint today — so `{ stop: () => void }` is a legal broker return despite the folder's ad-hoc-types rule.
  - Both ALREADY BUILT shared seams are genuinely built, so no repair chunk is cut. Opened `packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.ts`: the `z.enum` opens at :15 and closes at :41 with `'health-status'` last at :40 — 22 literals, the prior 21 unchanged — and its test at :12-36 pins all 22 in order with one `toStrictEqual`. Opened `packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts:15-19`: a `z.object` with exactly `status` (`z.enum(['ok','degraded'])`), `uptimeSeconds` (`.int().nonnegative().brand<'UptimeSeconds'>()`) and `version` (`.min(1).brand<'ServerVersion'>()`), none optional, with the missing-`uptimeSeconds` failure pinned at its test :52-59. Both are barrelled at `packages/shared/contracts.ts:757-758` and present in `packages/shared/dist`. Committed at c11520524 and 2cd42e558. check-event-type-member and check-payload-parses hold as written.
  - All three web seams are NOT BUILT YET and stay that way. `packages/web/src/statics/web-config/web-config-statics.ts:11-45` carries no health key at all, and `packages/web/src/state/web-socket-channel/web-socket-channel-state.ts` has no `'health-status'` arm, so an unmatched frame falls through its if-chain silently. This round builds the server half to the shape web will need and touches no web file.
  - check-liveness-probe-unchanged gets a chunk rather than a `settled` line, because only half of it is true today. The BEHAVIOUR holds — `health-flow.ts:18-21` returns exactly `{status: 'ok', timestamp}` — but nothing in `packages/server` asserts it: `health-flow.integration.test.ts:12-15` parses through `parseHealthResponseTransformer`, and zod STRIPS unknown keys, so a third field passes that test unnoticed. The only exact-body assertion anywhere is `packages/web/src/flows/app/smoke.e2e.ts:6-17`, in another package and another role's medium. The round edits `health-flow.ts`, so the guard belongs in the same chunk that edits it.
  - `version` has no producer in the server, so one is built, and a resolution failure yields `'unknown'` rather than a 500. `packages/server/package.json:3` is `"version": "0.1.0"` and nothing under `packages/server/src/**` reads it; there is no build-time inject. `adapters/web-bundle/dist-path/web-bundle-dist-path-adapter.ts:17-32` is the in-package idiom — `require.resolve('@dungeonmaster/web/package.json')` inside a `try/catch`, degrading rather than throwing. `version` is `z.string().min(1)`, so `'unknown'` parses; 500-ing instead would drive the badge OFFLINE over a detail the operator cannot act on, which is the opposite of what the badge is for.
  - `status` is always `'ok'` on both surfaces. The contract's enum has two members (`health-status-payload-contract.ts:16`), but nothing in `packages/server` measures degradation, no observable in `Must satisfy` names a degraded producer, and the quest's design decision "DEGRADED and OFFLINE both stay in the heartbeat loop" constrains how the BADGE consumes frames, not how the server classifies itself. A hand-rolled heuristic here would ship a claim no test could pin.
  - Every file on this path is synchronous. `process.uptime()` and `readFileSync` are both sync, so `healthStatusSnapshotBroker` and `HealthStatusResponder` need no `async`; `responders/tooling/smoketest-state/tooling-smoketest-state-responder.ts:14-20` is the in-package precedent for a sync responder and `flows/tooling/tooling-flow.ts:26-29` for the matching sync flow registration. It also leaves no floating promise inside the interval callback.
  - `uptimeSeconds` and `version` are branded through the SHARED contract's own shape, not through new server contracts. Both brands are declared inline at `health-status-payload-contract.ts:17-18` and exported nowhere else, so `healthStatusPayloadContract.shape.uptimeSeconds.parse(...)` re-uses them while a server-local copy would mint a second, structurally incompatible brand. `process.uptime()` returns fractional seconds, so the adapter floors before parsing or `.int()` rejects it.
  - Nothing is added to `statics/dev-log-event-icons/dev-log-event-icons-statics.ts`. The dev-log call is at `server-init-responder.ts:557-562`, inside the BUS handler the heartbeat bypasses, so no health frame reaches `devLogEventFormatTransformer` and no icon entry is needed. This repo's `dev` and `prod` scripts both set `VERBOSE=1`, so a bus-routed heartbeat would have written six `[dev]` lines a minute into the log an operator reads agent transcripts in.
  - `start-server.ts` is NOT in this round. `HealthFlow()` is already in its `subApps` array at :47 and `server-flow.ts:24-26` routes every sub-app onto the real Hono app, so a route added to an already-mounted flow reaches the server with no startup edit and no new bootstrap seat.
  - The heartbeat's TEARDOWN is settled by reading the source, not by a test, and that is a limit of the surface rather than a hole in the round. Both signal handlers end in `process.exit(0)` (`server-init-responder.ts:852` and `:859`), and `ServerInitResponderProxy.callResponder` calls `process.removeAllListeners('SIGTERM')` / `('SIGINT')` at `server-init-responder.proxy.ts:139-140` before every construction, precisely so leftover handlers cannot leak between tests — so no jest case can fire either one, and `server-init-responder.test.ts` mentions neither signal in its ~1035 lines. The existing `clearInterval(flushIntervalHandle)` at :849/:856 sits under exactly that same limit today. Chunk 9's `INTENT` therefore states the teardown as a source-level claim its reviewer opens the file to settle, and no chunk pretends to test it. (Raised by the stage-5 checker against an earlier ASSERTIONS line that claimed it was provable; the line is restated rather than dropped.)
  - No Playwright spec and no whole-flow suite is authored here. Ledger items 7-8 are the flowrider suites below the browser, 9 is the groundstomper browser walk and 10 is the siegemaster QA. This round writes only the colocated companions each folder type demands.

ASSERTIONS:
  - `GET /api/health/status` answers 200 with a JSON body of exactly three keys — `status`, `uptimeSeconds`, `version` — and `health-flow.integration.test.ts` proves it with one `toStrictEqual` over the RAW parsed body, never through a zod parse that would strip a fourth key.
  - `GET /api/health` answers 200 with a body of exactly two keys, `status: 'ok'` and `timestamp`, and `health-flow.integration.test.ts` now asserts that over the RAW body too — a third key reds it.
  - `apiRoutesStatics.health` has exactly two keys, `check: '/api/health'` and `status: '/api/health/status'`, and `api-routes-statics.test.ts`'s whole-object `toStrictEqual` carries both.
  - `healthHeartbeatStatics` is exactly `{ emitIntervalMs: 10_000 }`, pinned by a whole-object `toStrictEqual` — three of them fit inside the web side's 30 000 ms threshold.
  - `healthStatusSnapshotBroker` is the only place in `packages/server` that constructs a `{status, uptimeSeconds, version}` object; the seed responder and the heartbeat both call it, and neither writes a second object literal of that shape.
  - `healthHeartbeatStartBroker` registers its tick at exactly `healthHeartbeatStatics.emitIntervalMs`: `healthHeartbeatStartBroker.proxy.ts` keys its timer spy on that value, so its `triggerTick()` reaches a registered callback only while the broker registers at that same interval — a broker registering at any other value makes every tick case in its test red. The test calls the broker proxy's semantic method and never touches the timer proxy itself.
  - Three ticks produce exactly three broadcasts, each one `client.send` call carrying one JSON string that parses to `{type: 'health-status', payload, timestamp}` and no fourth envelope key, with `uptimeSeconds` strictly greater on each successive frame.
  - A client whose `send` throws on a tick is absent from the `clients` set afterwards and the other client in that same set received that same tick's frame — asserted over the whole set contents and the whole captured message, not over counts.
  - A client that has only completed the `/ws` upgrade (`simulateConnection`, no `simulateMessage`) receives the next tick, and a client with no entry in `clientSubscriptions` receives it alongside a subscribed one.
  - `server-init-responder.ts` calls the heartbeat handle's `stop()` inside BOTH the SIGTERM and the SIGINT handler, beside the existing `clearInterval(flushIntervalHandle)` at :849 and :856. A reader settles this by OPENING those two handlers; it is deliberately not stated as a test-provable claim — see the teardown decision below.
  - The round's diff touches `packages/server` only — `git diff --stat -- packages/shared packages/web` is empty.
  - `brokers/ws-event-relay/broadcast/ws-event-relay-broadcast-broker.ts` is byte-identical to HEAD: the round reuses its fan-out and its dead-client removal rather than reimplementing either.

NO CHUNK: none

### chunk 1 — the tick period and the new route key, in the two statics files
INTENT:
  - `healthHeartbeatStatics` exists at `packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts` and is exactly `{ emitIntervalMs: 10_000 }`.
  - `apiRoutesStatics.health` is exactly `{ check: '/api/health', status: '/api/health/status' }`, with `check` byte-identical to HEAD.
  - Both statics tests pass with a single whole-object `toStrictEqual` each; `api-routes-statics.test.ts`'s existing literal gains the one new line and nothing else.
FILES:
  - ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts
  - ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.test.ts
  - ./packages/server/src/statics/api-routes/api-routes-statics.ts
  - ./packages/server/src/statics/api-routes/api-routes-statics.test.ts
UNITS:
  - HealthHeartbeatStatics.emitIntervalMs -> ./packages/server/src/statics/health-heartbeat/health-heartbeat-statics.ts — must declare `emitIntervalMs: 10_000` and nothing else, so three ticks fit inside 30 000 ms.
  - HealthStatusEndpoint.path -> ./packages/server/src/statics/api-routes/api-routes-statics.ts — must add `status: '/api/health/status'` inside the existing `health` group beside `check`.
MIRROR: ./packages/server/src/statics/http-status/http-status-statics.ts
NOTES:
  Flow `#health-badge`: the operator loads any route; the top bar's badge seeds itself from `GET /api/health/status` at mount, then tracks a `health-status` heartbeat over the WebSocket the interface already holds open, reading ONLINE with uptime, DEGRADED, or OFFLINE. This chunk is the constants layer under nodes `#seed-fetch` and `#server-emits`; every later chunk in this round imports one of these two files.

  It satisfies no observable directly. It satisfies two CONTRACT property requirements, quoted word for word:
    - `HealthStatusEndpoint.path`: "A new key added to the existing health group in this statics file, alongside the check key that already holds /api/health."
    - `HealthHeartbeatStatics.emitIntervalMs`: "How often the server emits a health-status frame. Three of these fit inside the web side's 30 second silence threshold."
  The web side's threshold is 30 000 ms, so `emitIntervalMs` is 10_000 — three whole ticks, no remainder. Do not pick 9 000 or 15 000; the arithmetic in that sentence is the requirement.

  Contracts: none taken, none returned. `statics/` may import only `statics/`, and both files import nothing.

  Design decision that constrains this chunk, quoted: "DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats." The interval is therefore unconditional: there is no second, slower period for a degraded server, and no reason to make this value a range.

  Already-built exports you wire into:
    - `apiRoutesStatics` — `packages/server/src/statics/api-routes/api-routes-statics.ts:9`, an `as const` object closing at :67. Its `health` group at :10-12 today holds ONLY `check: '/api/health'`.
    - Its test at `api-routes-statics.test.ts:5-63` is ONE `it` with ONE `toStrictEqual` over the whole object. Add the new key at :7-8 and change nothing else; the other nine groups are read by nine other flow files and none of them reads `health`.

  GOTCHA: the new statics file takes exactly ONE companion, `health-heartbeat-statics.test.ts`. All six existing `packages/server/src/statics/*` folders carry a `.test.ts` and nothing else — no `.proxy.ts`, no `.stub.ts`. Adding either trips `enforce-implementation-colocation`.

### chunk 2 — the uptime adapter
INTENT:
  - `processUptimeAdapter()` returns the process's uptime as a whole, non-negative number of seconds, already carrying the `UptimeSeconds` brand.
  - A fractional reading (`process.uptime()` returning e.g. 12.7) comes back as 12, not as a zod `.int()` failure.
  - Its test drives the value through the proxy, never through real wall-clock time.
FILES:
  - ./packages/server/src/adapters/process/uptime/process-uptime-adapter.ts
  - ./packages/server/src/adapters/process/uptime/process-uptime-adapter.proxy.ts
  - ./packages/server/src/adapters/process/uptime/process-uptime-adapter.test.ts
UNITS: none — this is the I/O boundary the payload's `uptimeSeconds` field is read at; the observables over it land on chunks 5, 7 and 8.
MIRROR: ./packages/server/src/adapters/process/dev-log/process-dev-log-adapter.ts
NOTES:
  Flow `#health-badge`, nodes `#seed-fetch` and `#server-emits`: both the seed body and every heartbeat frame carry an uptime the badge renders beside ONLINE, and the badge's whole point is that this number keeps moving. `process.uptime()` is non-deterministic and so unmockable anywhere above the adapter layer — this file is what makes chunks 5 and 7 testable at all.

  Observable this chunk serves (satisfied downstream, quoted so you know what the number is for): check-emit-payload-advances — "Each health-status frame carries exactly {status, uptimeSeconds, version}, and uptimeSeconds is strictly greater than the previous frame's".

  Contracts: returns `HealthStatusPayload['uptimeSeconds']`. Do NOT mint a new server contract — the `UptimeSeconds` brand is declared inline at `packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts:17` as `z.number().int().nonnegative().brand<'UptimeSeconds'>()` and exported nowhere else, so a server-local copy would be a second, structurally incompatible brand. Parse through `healthStatusPayloadContract.shape.uptimeSeconds.parse(Math.floor(process.uptime()))`.

  Design decisions: none constrain this file.

  Already-built exports you wire into:
    - `healthStatusPayloadContract` and the type `HealthStatusPayload` — both from `@dungeonmaster/shared/contracts` (source `packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts:15`, barrelled at `packages/shared/contracts.ts:757`). An `adapters/` file may import another package's `contracts` subpath; `process-dev-log-adapter.ts:9` already imports `AdapterResult` from that exact subpath.

  GOTCHA: `.int()` rejects a fractional value outright, so the `Math.floor` is load-bearing — write a case that feeds a fractional reading and asserts the floored result. The proxy is `registerSpyOn({ object: process, method: 'uptime' })`, mirroring `process-dev-log-adapter.proxy.ts:9`'s `registerSpyOn({ object: process.stdout, method: 'write' })`; expose a semantic `returns({ seconds })`, never the raw handle.

### chunk 3 — the server-version adapter
INTENT:
  - `serverVersionReadAdapter()` returns the running server package's own `version` string, branded `ServerVersion`.
  - When that package.json cannot be resolved or read, it returns `'unknown'` rather than throwing, so neither the seed route nor a heartbeat tick can be taken down by a resolution failure.
  - Its test covers both the resolved case and the failure case.
FILES:
  - ./packages/server/src/adapters/server-version/read/server-version-read-adapter.ts
  - ./packages/server/src/adapters/server-version/read/server-version-read-adapter.proxy.ts
  - ./packages/server/src/adapters/server-version/read/server-version-read-adapter.test.ts
UNITS: none — this is the I/O boundary the payload's `version` field is read at; the observables over it land on chunks 5, 7 and 8.
MIRROR: ./packages/server/src/adapters/web-bundle/dist-path/web-bundle-dist-path-adapter.ts
NOTES:
  Flow `#health-badge`, nodes `#seed-fetch` and `#server-emits`: `version` is the third field of every seed response and every heartbeat frame. Nothing in `packages/server/src/**` reads `packages/server/package.json:3` (`"version": "0.1.0"`) today and there is no build-time inject, so this file is the only producer that field can have.

  Observable this chunk serves (satisfied downstream, quoted): check-seed-route-answers — "GET /api/health/status returns 200 with a JSON body of exactly {status, uptimeSeconds, version} and no other keys".

  Contracts: returns `HealthStatusPayload['version']`. The `ServerVersion` brand is declared inline at `packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts:18` as `z.string().min(1).brand<'ServerVersion'>()` and exported nowhere else — parse through `healthStatusPayloadContract.shape.version.parse(...)`, do not mint a server-local brand.

  Design decisions: none constrain this file directly. Note WHY the fallback is a string rather than a throw: the badge exists so an operator learns the server stopped answering, and 500-ing the seed over a package.json resolution detail would paint OFFLINE for a reason that is not the one the badge reports. `version` is `z.string().min(1)`, so `'unknown'` parses.

  Already-built exports you wire into:
    - `webBundleDistPathAdapter` — `packages/server/src/adapters/web-bundle/dist-path/web-bundle-dist-path-adapter.ts:17-32`. Copy its shape exactly: `require.resolve` of a `<pkg>/package.json` inside a `try`, a degraded value in the `catch`, no path adapters. Yours resolves `'@dungeonmaster/server/package.json'`.
    - `healthStatusPayloadContract` from `@dungeonmaster/shared/contracts`.

  GOTCHA: `require.resolve('@dungeonmaster/server/package.json')` was RUN in this worktree and resolves to the repo's own `packages/server/package.json` from both the repo root and from inside `packages/server/src/**` — `node_modules/@dungeonmaster/server` is a workspace symlink and `packages/server/package.json` declares no `exports` map, so the subpath is unrestricted. `require.resolve` itself cannot be staged with `registerMock` (it is not an imported binding), so the proxy mocks `readFileSync` and addresses it with a PREDICATE — the idiom already used at `packages/server/src/brokers/design/scaffold/design-scaffold-broker.proxy.ts:29` (`filePath: (value) => String(value).endsWith('/package.json')`). Drive the failure case by staging a throw on that same address.

### chunk 4 — the timer adapter
INTENT:
  - `timerSetIntervalAdapter({ callback, intervalMs })` registers a recurring tick and returns `{ stop }` that clears it.
  - `timerSetIntervalAdapterProxy({ intervalMs })` captures the callback registered at that exact interval and exposes `triggerTick()` and `getRegisteredCallback()`, without disturbing any other interval running in the same test process.
FILES:
  - ./packages/server/src/adapters/timer/set-interval/timer-set-interval-adapter.ts
  - ./packages/server/src/adapters/timer/set-interval/timer-set-interval-adapter.proxy.ts
  - ./packages/server/src/adapters/timer/set-interval/timer-set-interval-adapter.test.ts
UNITS: none — this is the mockable boundary that makes check-emit-interval provable at all; that observable lands on chunk 7.
MIRROR: ./packages/orchestrator/src/adapters/timer/set-interval/timer-set-interval-adapter.ts
NOTES:
  Flow `#health-badge`, node `#server-emits`: the server emits a frame every ten seconds. `packages/server/src/adapters/` has seven folders and no `timer/`; both existing server intervals call the global raw (`server-init-responder.ts:771`, `quest-driven-watchers-bootstrap-responder.ts:68`), which is why `server-init-responder.test.ts:827-832` can only wait 150 ms of real wall clock and says so in a comment. Without this adapter, chunk 7 would have to sleep ten real seconds per case.

  Observable this chunk serves (satisfied downstream, quoted): check-emit-interval — "With the server running, three health-status frames arrive within 35 seconds, spaced 10 seconds apart".

  Contracts: none. The adapter takes `{ callback: () => void; intervalMs: number }` and returns `{ stop: () => void }` — copy the orchestrator file's signature verbatim.

  Design decisions: none constrain this file.

  Already-built exports you wire into:
    - `timerSetIntervalAdapter` — `packages/orchestrator/src/adapters/timer/set-interval/timer-set-interval-adapter.ts:10`. You are creating a `server` COPY, not importing it: `packages/orchestrator/package.json` maps only `"."` and `"./testing"` in its `exports`, and `packages/orchestrator/src/index.ts` does not re-export the adapter, so it is unreachable from `server`.
    - `timerSetIntervalAdapterProxy` — `packages/orchestrator/src/adapters/timer/set-interval/timer-set-interval-adapter.proxy.ts:7-45`. Copy it whole, and read its :3-6 comment before changing anything.

  GOTCHA: `passthrough: true` on both `registerSpyOn` calls is load-bearing and must survive the copy. `ServerInitResponder` registers its OWN 100 ms flush interval at `server-init-responder.ts:771`; without passthrough that unaddressed call throws and every server-init test dies. The discriminating address is the pair `[<a function predicate>, intervalMs]` — the argument shape is what keeps the 100 ms interval from matching the 10 000 ms one.

### chunk 5 — the snapshot broker: the single producer of a HealthStatusPayload
INTENT:
  - `healthStatusSnapshotBroker()` returns a `HealthStatusPayload` of exactly `{status, uptimeSeconds, version}`, built from `processUptimeAdapter` and `serverVersionReadAdapter` and parsed through `healthStatusPayloadContract`.
  - `status` is `'ok'`.
  - Calling it twice over a rising uptime reading returns two payloads whose `uptimeSeconds` differ — it holds no cached value.
FILES:
  - ./packages/server/src/brokers/health-status/snapshot/health-status-snapshot-broker.ts
  - ./packages/server/src/brokers/health-status/snapshot/health-status-snapshot-broker.proxy.ts
  - ./packages/server/src/brokers/health-status/snapshot/health-status-snapshot-broker.test.ts
UNITS: none — this is the one place the payload is CONSTRUCTED; the observables asserting what that payload contains land on chunks 7 and 8, which both call this.
MIRROR: ./packages/server/src/brokers/web-bundle/response/web-bundle-response-broker.ts
NOTES:
  Flow `#health-badge`, nodes `#seed-fetch` and `#server-emits`: the badge gets the same three-field body two ways — once over HTTP at mount, then every ten seconds over the socket. This broker is what makes those two bodies identical by construction rather than by two hand-written object literals that drift.

  Observables this chunk serves (both satisfied downstream, quoted so you build to them):
    - check-seed-route-answers — "GET /api/health/status returns 200 with a JSON body of exactly {status, uptimeSeconds, version} and no other keys"
    - check-emit-payload-advances — "Each health-status frame carries exactly {status, uptimeSeconds, version}, and uptimeSeconds is strictly greater than the previous frame's"

  Contract requirement this serves, quoted: `HealthStatusEndpoint.responseBody` — "The 200 body is exactly the shared HealthStatusPayload shape, so server and web agree by importing one contract rather than restating it."

  Contracts: takes nothing, returns `HealthStatusPayload` from `@dungeonmaster/shared/contracts`. Parse the assembled object through `healthStatusPayloadContract.parse(...)` so a fourth key cannot survive.

  Design decision that constrains this chunk, quoted: "DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload." That decision governs the BADGE's consumption, not a server-side classifier: `status` is `'ok'`, full stop. The contract's enum has a second member (`z.enum(['ok', 'degraded'])` at `health-status-payload-contract.ts:16`) but nothing in `packages/server` measures degradation and no observable in this round names a degraded producer. Do NOT invent a heuristic — an untestable classifier is worse than a constant.

  Already-built exports you wire into:
    - `healthStatusPayloadContract` and type `HealthStatusPayload` — `@dungeonmaster/shared/contracts`, source at `packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts:15-19`: `status: z.enum(['ok','degraded'])`, `uptimeSeconds: z.number().int().nonnegative().brand<'UptimeSeconds'>()`, `version: z.string().min(1).brand<'ServerVersion'>()`. All three REQUIRED.
    - `HealthStatusPayloadStub` — same folder, `health-status-payload.stub.ts:6`, defaults `{status: 'ok', uptimeSeconds: 120, version: '1.0.0'}`, barrelled at `packages/shared/contracts.ts:758`. Use it for expected values in the test; tests take their types from stubs, never from contracts.
    - `processUptimeAdapter` (chunk 2) and `serverVersionReadAdapter` (chunk 3), plus their proxies, which your `.proxy.ts` composes into semantic methods such as `setupSnapshot({ uptimeSeconds, version })`. Expose semantic methods only — never hand a child proxy back to the test.

  GOTCHA: this broker is SYNCHRONOUS. Both adapters are sync (`process.uptime()`, `readFileSync`), so do not mark it `async` — chunk 7 calls it from inside an interval callback, where a promise would be left floating.

### chunk 6 — the seed responder
INTENT:
  - `HealthStatusResponder()` returns `{ status: 200, data: <the value healthStatusSnapshotBroker returned> }`, with `data` passed through untouched.
  - When the broker throws, it returns a 500 with an error message rather than propagating.
  - Its test asserts the whole result with `toStrictEqual`, including that `data` has exactly the three payload keys.
FILES:
  - ./packages/server/src/responders/health/status/health-status-responder.ts
  - ./packages/server/src/responders/health/status/health-status-responder.proxy.ts
  - ./packages/server/src/responders/health/status/health-status-responder.test.ts
UNITS:
  - HealthStatusEndpoint.responseBody -> ./packages/server/src/responders/health/status/health-status-responder.ts — must place the broker's return value into `data` unchanged, so the 200 body is `healthStatusPayloadContract`'s shape by construction and not by a second literal.
MIRROR: ./packages/server/src/responders/rate-limits/get/rate-limits-get-responder.ts
NOTES:
  Flow `#health-badge`, node `#seed-fetch`: on mount the badge issues one GET and renders ONLINE with the uptime it gets back, or OFFLINE if the request fails or the server answers with an error. This responder is the server half of that node. There is no `responders/health/` directory of any kind today.

  Contract requirement, quoted word for word: `HealthStatusEndpoint.responseBody` — "The 200 body is exactly the shared HealthStatusPayload shape, so server and web agree by importing one contract rather than restating it." Read that as a ban on re-describing the body here: call the broker, put its value in `data`, add nothing.

  Contracts: returns `ResponderResult` — `packages/server/src/contracts/responder-result/responder-result-contract.ts:11-14`, `{ status: z.number().int().brand<'HttpStatusCode'>(), data: z.unknown() }`. Takes nothing.

  Design decision that constrains this chunk, quoted: "DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats." The badge subscribes at mount regardless of what this route answers, so a 500 here is an ordinary answer the badge handles, not a state the server has to avoid producing.

  SEAM — `#seed-fetch` with `web`, NOT BUILT YET. A later session owns the web half: check-seed-request-issued — "On mount the badge issues GET /api/health/status exactly once, with no query string". Build to that shape and do NOT build theirs: no query parameters are read, no headers are required, and the path takes no params. `packages/web/src/statics/web-config/web-config-statics.ts:11-45` carries no health key yet — the web session adds it, you do not.

  Already-built exports you wire into:
    - `RateLimitsGetResponder` — `packages/server/src/responders/rate-limits/get/rate-limits-get-responder.ts:14-28`: `responderResultContract.parse({status: httpStatusStatics.success.ok, data: {...}})` inside a `try`, and a `catch` arm returning `httpStatusStatics.serverError.internal` with `{ error: message }`. Copy that shape.
    - `RateLimitsGetResponderProxy` — `rate-limits-get-responder.proxy.ts:8-24`: composes the child proxy, exposes `setupX` / `setupError`, and re-exports the responder itself as `callResponder`. Copy that shape too.
    - `httpStatusStatics` — `packages/server/src/statics/http-status/http-status-statics.ts:11` (`success.ok = 200`) and `:20` (`serverError.internal = 500`).
    - `healthStatusSnapshotBroker` and `healthStatusSnapshotBrokerProxy` from chunk 5.

  GOTCHA: this responder is SYNCHRONOUS, unlike the `RateLimitsGetResponder` mirror. `ToolingSmoketestStateResponder` at `packages/server/src/responders/tooling/smoketest-state/tooling-smoketest-state-responder.ts:14-20` is the in-package precedent for a non-`async` responder returning `ResponderResult` — take the try/catch shape from rate-limits and the sync signature from tooling.

### chunk 7 — the heartbeat: one frame every emitIntervalMs, to every connected client
INTENT:
  - `healthHeartbeatStartBroker({ clients })` registers exactly one tick at `healthHeartbeatStatics.emitIntervalMs` and returns `{ stop }` that clears it.
  - Three ticks produce exactly three broadcasts and no more.
  - Each tick's frame parses to exactly `{type: 'health-status', payload, timestamp}` — three envelope keys — with `payload` carrying exactly `{status, uptimeSeconds, version}`.
  - Over two ticks with a rising uptime reading, the second frame's `uptimeSeconds` is strictly greater than the first's.
  - When one client's `send` throws on a tick, that client is gone from the `clients` set afterwards and the other client in that same set received that same tick's frame.
  - After `stop()`, a further tick produces no broadcast.
FILES:
  - ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.ts
  - ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.proxy.ts
  - ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.test.ts
UNITS:
  - check-emit-interval -> ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.ts — must register its tick at exactly `healthHeartbeatStatics.emitIntervalMs`, and three ticks must produce three broadcasts and no more.
  - check-emit-payload-advances -> ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.ts — must call `healthStatusSnapshotBroker` freshly per tick, so successive frames carry exactly three payload keys with a strictly increasing `uptimeSeconds`.
  - check-dead-client-does-not-block -> ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.ts — must hand the live `clients` set to `wsEventRelayBroadcastBroker`, so a throwing client is removed from that set while every remaining client still receives that same frame.
  - check-frame-crosses-wire -> ./packages/server/src/brokers/health-heartbeat/start/health-heartbeat-start-broker.ts — must produce ONE `client.send` per client per tick, carrying one JSON string that parses to an envelope of exactly `type`, `payload` and `timestamp`.
MIRROR: ./packages/server/src/brokers/design/start/design-start-broker.ts
NOTES:
  Flow `#health-badge`, nodes `#server-emits`, `#relay-broadcast` and `#channel-routes`: the server emits a `health-status` frame every ten seconds; the relay fans it to every connected browser; the badge moves on each arrival and goes OFFLINE after thirty seconds of silence. This chunk is all three server-side nodes.

  The observables it must satisfy, quoted word for word:
    - check-emit-interval — "With the server running, three health-status frames arrive within 35 seconds, spaced 10 seconds apart"
    - check-emit-payload-advances — "Each health-status frame carries exactly {status, uptimeSeconds, version}, and uptimeSeconds is strictly greater than the previous frame's"
    - check-dead-client-does-not-block — "When one client's send throws, that client is dropped from the connected set and every remaining client still receives that same frame"
    - check-frame-crosses-wire — "The serialised health-status envelope arrives at the browser as one WebSocket text message carrying type, payload and timestamp"
  The BROWSER half of the last one — that the frame actually lands in a real browser — is the groundstomper walk on ledger item 9. Your half is the send boundary: one serialised text message per client per tick, with those three envelope keys and no fourth.

  Contracts: takes `{ clients: Set<WsClient> }` (`packages/server/src/contracts/ws-client/ws-client-contract.ts:11`, `interface WsClient { send: (data: string) => void }`) and returns `{ stop: () => void }`. Builds `wsMessageContract` from `@dungeonmaster/shared/contracts` — `packages/shared/src/contracts/ws-message/ws-message-contract.ts:13-17`, `{type: orchestrationEventTypeContract, payload: z.record(branded key, z.unknown()), timestamp}`. `payload` is an open record, so the three-key body needs no envelope change.

  Design decisions that constrain this chunk, both quoted:
    - "DEGRADED and OFFLINE both stay in the heartbeat loop — The badge subscribes at mount rather than after a healthy seed, so every rendered state keeps consuming heartbeats. A server that recovers moves the badge back to ONLINE on its next frame with no click and no reload. Making any state stop listening would strand the badge on a reading that was true once and is not any more." Therefore the emit is UNCONDITIONAL: never gate a tick on how many clients are connected, on what the last frame said, or on whether anything changed since. A quiet server that skips a frame is indistinguishable from a dead one, which is exactly the failure the badge exists to report.
    - "A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it." Therefore add no reconnect detection, no resend-on-connect and no per-client backlog. A client that joins mid-interval simply gets the next tick.

  Already-built exports you wire into:
    - `wsEventRelayBroadcastBroker` — `packages/server/src/brokers/ws-event-relay/broadcast/ws-event-relay-broadcast-broker.ts:13`, signature `({ clients, message }: { clients: Set<WsClient>; message: WsMessage }): Set<WsClient>`. It serialises ONCE at :21 and, at :23-30, calls `clients.delete(client)` on a throwing send and continues the loop. check-dead-client-does-not-block is satisfied by CALLING this, not by reimplementing the loop — leave that file byte-identical.
    - `wsEventRelayBroadcastBrokerProxy` — `ws-event-relay-broadcast-broker.proxy.ts:6-22`, giving `captureClient` (a `WsClientStub` whose `send` pushes `JSON.parse(data)`) and `getCapturedMessages(): WsMessage[]`. That is how you read what crossed the wire.
    - `WsClientStub` — `packages/server/src/contracts/ws-client/ws-client.stub.ts:6`. For the dead client, pass a `send` that throws.
    - `isoTimestampContract` — `packages/server/src/contracts/iso-timestamp/iso-timestamp-contract.ts:14`. Build the envelope exactly as `server-init-responder.ts:648-652` does: `wsMessageContract.parse({ type: 'health-status', payload, timestamp: isoTimestampContract.parse(new Date().toISOString()) })`.
    - `timerSetIntervalAdapter` (chunk 4), `healthStatusSnapshotBroker` (chunk 5), `healthHeartbeatStatics` (chunk 1).

  GOTCHA — do NOT route this through the orchestrator event bus, even though `server-init-responder.ts:539` already subscribes `'health-status'` by iterating `orchestrationEventTypeContract.options`. Every relayed envelope is built at :648-652 as `payload: { ...effectivePayload, processId }`, and `orchestrator-events-on-adapter.ts:21` types `processId` as required — a bus-routed frame carries a FOURTH payload key and check-emit-payload-advances forbids it. Broadcast directly. (There is no emit adapter in `server` anyway: `adapters/orchestrator/` has 45 entries and `events-on` is the only event one.)

  GOTCHA — your `.proxy.ts` keys its timer spy on `healthHeartbeatStatics.emitIntervalMs` and exposes `triggerTick()` as a semantic method. It must also stub the timestamp: copy `server-init-responder.proxy.ts:106-111`, `registerSpyOn({ object: Date.prototype, method: 'toISOString', passthrough: true })` staged to a fixed ISO string, or every envelope assertion is a moving target.

  GOTCHA — assert the CONTENT, not the count. The nearest existing broadcast case, `server-init-responder.test.ts:753-787`, ends on a bare count comparison, which the testing rules ban as the whole assertion. Assert the parsed frame with `toStrictEqual`, and assert the surviving `clients` set by its CONTENTS, not by its size.

  GOTCHA — the tick callback is synchronous end to end: `healthStatusSnapshotBroker` is sync, so nothing inside the interval returns a promise.

### chunk 8 — the health flow: the new seed route, and a real guard on the liveness probe
INTENT:
  - `GET /api/health/status` answers 200 with a body of exactly `{status, uptimeSeconds, version}`, asserted with one `toStrictEqual` over the RAW parsed body.
  - `GET /api/health` still answers 200 with a body of exactly `{status: 'ok', timestamp}`, asserted over the RAW body, so a third key reds it.
  - The new route is registered with `app.get` and no other verb.
FILES:
  - ./packages/server/src/flows/health/health-flow.ts
  - ./packages/server/src/flows/health/health-flow.integration.test.ts
UNITS:
  - check-seed-route-answers -> ./packages/server/src/flows/health/health-flow.ts — must register a GET at `apiRoutesStatics.health.status` delegating to `HealthStatusResponder`, answering 200 with exactly the three payload keys.
  - check-liveness-probe-unchanged -> ./packages/server/src/flows/health/health-flow.ts — must leave the `/api/health` handler answering exactly `{status: 'ok', timestamp}`, now pinned by an assertion on the raw body.
  - HealthStatusEndpoint.method -> ./packages/server/src/flows/health/health-flow.ts — must register the seed route with `app.get` and no other verb.
MIRROR: ./packages/server/src/flows/rate-limits/rate-limits-flow.ts
NOTES:
  Flow `#health-badge`, node `#seed-fetch`: this is where the seed route becomes reachable. `HealthFlow()` is ALREADY in the `subApps` array at `start-server.ts:47` and `server-flow.ts:24-26` routes every sub-app onto the real Hono app, so adding a route to this flow reaches the running server with no startup edit — `start-server.ts` is not in this round.

  The observables it must satisfy, quoted word for word:
    - check-seed-route-answers — "GET /api/health/status returns 200 with a JSON body of exactly {status, uptimeSeconds, version} and no other keys"
    - check-liveness-probe-unchanged — "GET /api/health still returns 200 with a body of exactly {status: 'ok', timestamp} and gains no new fields"

  Contract requirement, quoted: `HealthStatusEndpoint.method` — "Read-only; the badge never mutates server state."

  Contracts: the flow imports `apiRoutesStatics` and `HealthStatusResponder` and hands `result.data` to `c.json`. `flows/` may import `contracts`, `transformers`, `guards`, `statics`, `errors`, `flows`, `responders` and `hono` — brokers and adapters are NOT on that list, so the seed body must come through the responder.

  Design decisions: neither of the quest's two decisions constrains this file — both govern how the badge consumes what this route and the heartbeat produce.

  SEAM — `#seed-fetch` with `web`, NOT BUILT YET: check-seed-request-issued — "On mount the badge issues GET /api/health/status exactly once, with no query string". Register the path with no params and no query handling; the web session adds the matching `webConfigStatics.api.routes` key, which does not exist yet.

  Already-built exports you wire into:
    - `HealthFlow` — `packages/server/src/flows/health/health-flow.ts:14-25`. Its liveness registration at :17-22 is `app.get(apiRoutesStatics.health.check, (c) => c.json({ status: 'ok', timestamp: new Date().toISOString() }))`. Leave that expression alone and add a second `app.get` beside it.
    - `RateLimitsFlow` — `packages/server/src/flows/rate-limits/rate-limits-flow.ts:19-22`, the registration idiom: call the responder, then `return c.json(result.data as object, result.status as ContentfulStatusCode);` with `ContentfulStatusCode` imported from `hono/utils/http-status` at :11. `ToolingFlow` at `flows/tooling/tooling-flow.ts:26-29` is the SYNCHRONOUS variant — use that one, since `HealthStatusResponder` is sync.
    - `apiRoutesStatics.health.status` (chunk 1) and `HealthStatusResponder` (chunk 6).
    - The existing test at `health-flow.integration.test.ts:5-18` calls `HealthFlow()` directly and `app.request('/api/health')` — keep that shape, no harness needed. It will need the chunk 2 and chunk 3 adapter proxies so the seed body is deterministic.

  GOTCHA: the existing liveness test asserts `parsed?.status` AFTER running the body through `parseHealthResponseTransformer`, and zod STRIPS unknown keys — so a third field on that body passes it today, unnoticed. That is why check-liveness-probe-unchanged is a unit here rather than a settled line: assert the RAW `await response.json()` value with `toStrictEqual`, matching `timestamp` against an ISO pattern the way `packages/web/src/flows/app/smoke.e2e.ts:6-17` does. Do the same on the new route — never assert the seed body through a zod parse, or "no other keys" is unprovable.

### chunk 9 — start the heartbeat where the connected clients live, and stop it on shutdown
INTENT:
  - `ServerInitResponder` starts the heartbeat once, over the same `clients` set its `/ws` `onOpen` adds to.
  - A client that has only completed the upgrade — `simulateConnection`, no `simulateMessage` — receives the next tick's health-status frame.
  - A client with no entry in `clientSubscriptions` receives that tick alongside a client that IS subscribed to a quest.
  - Both received frames parse to the same `{type: 'health-status', payload, timestamp}` envelope, asserted by content.
  - `server-init-responder.ts` calls the handle's `stop()` inside BOTH the SIGTERM and the SIGINT handler, beside the existing `clearInterval(flushIntervalHandle)` at :849 and :856. This one is settled by READING the file — see the last GOTCHA.
FILES:
  - ./packages/server/src/responders/server/init/server-init-responder.ts
  - ./packages/server/src/responders/server/init/server-init-responder.proxy.ts
  - ./packages/server/src/responders/server/init/server-init-responder.test.ts
UNITS:
  - check-client-in-broadcast-set -> ./packages/server/src/responders/server/init/server-init-responder.ts — must start the heartbeat over the `clients` set `onOpen` fills at :145, so a client that sends nothing after the upgrade still receives the next tick.
  - check-broadcast-reaches-unsubscribed-client -> ./packages/server/src/responders/server/init/server-init-responder.ts — must keep the heartbeat off `clientSubscriptions` entirely, so a client holding no quest subscription receives every tick.
MIRROR: ./packages/server/src/responders/server/init/server-init-responder.ts
NOTES:
  Flow `#health-badge`, nodes `#subscribe-heartbeat` and `#relay-broadcast`: the badge holds no subscription of its own — it rides the socket the interface already keeps open, and health frames reach every connected browser. This chunk is the wiring that makes that true.

  The observables it must satisfy, quoted word for word:
    - check-client-in-broadcast-set — "A client that completes the /ws upgrade receives the next health-status frame without sending any subscription message first"
    - check-broadcast-reaches-unsubscribed-client — "A client holding no quest subscription still receives every health-status frame"

  Contracts: none are defined here. You pass `Set<WsClient>` into the broker and hold the returned `{ stop: () => void }`.

  Design decision that constrains this chunk, quoted: "A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it." Therefore touch nothing in `onClose` (`:467-481`) and add no reconnect bookkeeping. A client removed from `clients` on close and re-added on the next `onOpen` is the whole of it.

  SEAMS with `web`, both NOT BUILT YET — build to their shape, do not build theirs:
    - `#subscribe-heartbeat`: check-heartbeat-reaches-badge — "A health-status frame delivered after mount changes the badge text with no further HTTP request issued". So the frame must be self-sufficient: it carries the full payload, and nothing about it requires the browser to ask a follow-up question.
    - `#channel-routes`: check-payload-updates-badge — "A delivered health-status frame changes the badge text within 1 second of arrival", and check-reconnect-resumes — "After the socket drops and reconnects, the first health-status frame moves the badge out of OFFLINE with no click issued". `packages/web/src/state/web-socket-channel/web-socket-channel-state.ts` has no `'health-status'` arm yet and drops the frame silently; that arm is the web session's, not yours.

  Already-built exports you wire into:
    - `clients` — `server-init-responder.ts:88`, `const clients = new Set<WsClient>()`, filled at `:145` inside `onOpen` and drained at `:469` inside `onClose`. Start the heartbeat AFTER that declaration and hold its handle in the same scope as `flushIntervalHandle`.
    - `clientSubscriptions` — `:91`, gained only in the `subscribe-quest` branch at `:217-230`. The heartbeat must not read or write it.
    - `flushIntervalHandle` — `:771`, cleared at `:849` and `:856`. Put `stop()` beside each `clearInterval`.
    - `ServerInitResponderProxy` — `server-init-responder.proxy.ts:80-105` is its return type, `:159` `simulateConnection`, `:162` `simulateMessage`, `:106-111` the `Date.prototype.toISOString` spy pinned to a fixed ISO string, `:128` a bare `wsEventRelayBroadcastBrokerProxy()` instantiated only to satisfy `enforce-proxy-child-creation`. Add `healthHeartbeatStartBrokerProxy()` as a child and expose ONE new semantic method, `triggerHealthHeartbeat()`, delegating to its `triggerTick()`. Do not return the child proxy.
    - The nearest existing case is `server-init-responder.test.ts:752-787`, `describe('global event broadcast')` — two clients, one subscribed, one not, then assert both received. Copy its setup; do NOT copy its assertion, which is a bare count comparison. Assert the parsed frame's content.
    - `healthHeartbeatStartBroker` and `healthHeartbeatStartBrokerProxy` from chunk 7.

  GOTCHA — `passthrough: true` on the timer spy (chunk 4) is what keeps this file's own 100 ms flush interval at `:771` running for real. If server-init tests start throwing on an unmatched `setInterval`, that is the cause.

  GOTCHA — the teardown is a READ-check, not a test, and that is deliberate. Both signal handlers end in `process.exit(0)` (`:852`, `:859`), and `ServerInitResponderProxy.callResponder` calls `process.removeAllListeners('SIGTERM')` / `('SIGINT')` at `server-init-responder.proxy.ts:139-140` before every construction, so no jest case can fire either one — `server-init-responder.test.ts` mentions neither signal in its ~1035 lines, and the existing `clearInterval(flushIntervalHandle)` sits under exactly the same limit. Write the two `stop()` calls, and do NOT invent a test that pretends to exercise them; the round's reviewer opens those two handlers to settle it.

PHASES:
  1: wave 1 — the two statics and the three adapters every later chunk imports, each with its proxy, so the mockable boundaries exist before anything is built on them
  2: waves 2-4 — the snapshot broker, then the two consumers over it, then the route registration and the relay wiring that make both reachable from a running server

WAVES:
  1: 1, 2, 3, 4
  2: 5
  3: 6, 7
  4: 8, 9

## Round log

