# Round 10 — siegemaster-verifier — running-elapsed-on-work-item-row / P12

Lane: r12-verify (booted clean, single boot, never restarted, never ended by this session)
Guild: d425b88c-74ee-4151-8b4d-1e8353d59336 (fixture dir tmp/dm-siege-r12-verify-guild)
Quest: 01c3685d-7cd0-477d-a76a-b3d1be5bda6b (urlSlug elapsed-duration-guild-r12)

Scope: exactly ONE unit per brief — `running-elapsed-on-work-item-row:branch:has-completed-at`.
Every other unit on this flow was already signed in rounds 1-11; this round signs nothing else,
per explicit instruction, but still walked the whole PATH and looked at every node per
[LOOK AT EVERYTHING].

## GUIDE

Read `.quest-plans/a0559e14-35ff-42c9-8006-6260e53c102c-guide.md` in full. No correction needed —
every recipe used (SEEDING step 3/4 completed-span table, the box/eval selector shapes, the
LOGO_LINK client-side-unmount trick) worked exactly as written on the first try.

## PATH WALK

panel-rendered → started-at-check → row-running-check → compute-elapsed → under-minute-check →
show-under-minute → tick → still-running-check → timer-cleared

### Seeding

One work item, `e2e00000-0000-4000-8000-0000000000c1`, operation text "codeweaver: has completed at
row", `startedAt: 2026-01-01T09:00:00.000Z`, `completedAt: 2026-01-01T09:00:12.000Z` (12s span,
under the 60s band), `status: complete`. This is the ONLY work item in the seeded quest — nothing
else in the browser's view is `in_progress`, so `hasRunningWorkItem` is false and no tick interval
is ever registered for this walk. Quest status stayed `in_progress` throughout (per TRAPS).

### running-elapsed-on-work-item-row:branch:has-completed-at

STARTED FROM: fresh `goto` to `/elapsed-duration-guild-r12/quest/01c3685d-7cd0-477d-a76a-b3d1be5bda6b`,
waited for `[data-testid="execution-panel-widget"]` visible.

DID:
1. `box` on the panel — row visible, panel text includes `codeweaver: has completed at row` `<1m`
   `DONE`.
2. `eval` (atomic, reading #1): `{text: <duration textContent>, badge: <badge textContent>, now: Date.now()}`
   → `{"text":"<1m","badge":"DONE","now":1788958257114}`.
3. `sleep 65` (real wall clock, bash).
4. `eval` (atomic, reading #2, identical script) → `{"text":"<1m","badge":"DONE","now":1788958365431}`.
5. `click` `[data-testid="LOGO_LINK"]` → `urlBefore` the quest route, `urlAfter`
   `http://dungeonmaster.localhost:39185/` — same page object, no `goto`, confirming a client-side
   unmount (walks on to `timer-cleared`, already signed, not re-signed here).
6. `console` (armed from lane boot) — only `[vite] connecting/connected` and the React DevTools
   info line; no error, no warning, no uncaught exception anywhere across the whole walk.

SAW: Reading #1 → `<1m` / `DONE` at `now=1788958257114`. Reading #2 → `<1m` / `DONE` at
`now=1788958365431` — **108.3 real seconds later**, comfortably past the 60s tick boundary the
FORCE requires, and the two readings are byte-identical.

BROKEN WOULD SHOW:
- If this branch had actually landed on **`not-running`** (`→ no-live-figure`), the row would
  render NO `execution-row-duration` element at all — `box`/`eval` would return `present:false` /
  `null` instead of the text it actually returned. It returned `<1m` both times, ruling this out.
- If this branch had actually landed on **`is-running`** (`elapsedEndPoint = now`), the figure
  recomputes against the browser's live clock every render. `startedAt` here is pinned to
  `2026-01-01T09:00:00.000Z`; real wall-clock `now` at the time of this walk is
  `2026-09-09T~12:50Z` — a span of **months**, not seconds. A mis-wired `now` end point would have
  shown something like `61344h…` or at the very least a value that visibly MOVED between the two
  readings (e.g. `<1m` → `1m`+ once past 60s elapsed real time, exactly as the sibling `is-running`
  unit's own tick observable proves elsewhere on this flow). Instead the text was frozen at exactly
  `<1m` across a 108s real gap, which only happens when `elapsedEndPoint` is the fixed
  `completedAt` value and not `now`.

Both siblings are ruled out with values they produce differently, not by destination alone.

## DEFECTS FOUND

None. Panel rendered cleanly, row text/badge matched the seeded values, no console errors, the
frozen-figure discriminator held across a real 60s+ gap, and the LOGO_LINK unmount produced a clean
client-side route change with no leftover panel content. Pass 2 has nothing to dispatch — the
numbered defect list is empty.

## SIGN-OFF

`running-elapsed-on-work-item-row` edge `has-completed-at`: `confirmed`, evidence as in SAW/BROKEN
WOULD SHOW above, `workItemId: b739c559-48b2-463d-acdd-78654fd9e258`.

Lane `r12-verify` was left running (never ended, never killed) — it will self-close on its own
15-minute idle timeout per lane discipline.
