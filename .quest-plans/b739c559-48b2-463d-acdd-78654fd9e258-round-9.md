# Round 9 — running-elapsed-on-work-item-row — path P9

LANE: r9-verify (single boot, never restarted, never ended by me)
Guild: ca029605-6a78-4850-94ee-d980e448beda (elapsed-duration-guild-r9)
Quest (seeded): 933d699b-4866-4b50-b7d5-51e6471ed6ad

PATH walked: panel-rendered → started-at-check → row-running-check → compute-elapsed →
under-minute-check → under-hour-check → show-hours-minutes → tick → still-running-check → timer-cleared

## GUIDE CORRECTNESS

The guide (`.quest-plans/a0559e14-35ff-42c9-8006-6260e53c102c-guide.md`) was accurate and complete for
this walk. Two notes, neither a correction:
- The `eval` command's JS source goes in the `value` field, not `target` — the guide's own eval example
  (line 46) already shows this correctly (`value` carries the payload in the vocabulary line), I simply
  misread it on first try and got back `{"source": ""}` with no `result` key. Left here in case another
  round hits the same mis-read.
- SEEDING step 4's offset table (60m/1h13m/2h) matched exactly what this round's brief demanded, byte
  for byte.

## SEEDING

Three work items in ONE quest.json, each with its own operation `text` so `:has-text()` never collides:
- "codeweaver: sixty minute row r9" — startedAt = now − 3,600,000ms
- "codeweaver: one hour thirteen row r9" — startedAt = now − 4,380,000ms
- "codeweaver: two hour row r9" — startedAt = now − 7,200,000ms

All three `status: "in_progress"`, no `completedAt`, quest itself `"in_progress"`.

## THE ATOMIC READING (the whole point of this round)

Single `eval` call reading all three rows' `execution-row-duration` text plus `Date.now()` together:

```
Date.now() = 1788957770270  →  2026-09-09T12:42:50.270Z   (~63.3s after the seeded startedAt values)
"sixty minute row r9"        → "1h"
"one hour thirteen row r9"   → "1h13m"
"two hour row r9"            → "2h"
```

The figure is computed once at mount and frozen until the next 60s tick, so this single read is the
first-render value for all three rows — computed moments after the offsets were written, not at the
63.3s-later wall-clock moment of the read itself. A LATER screenshot (post one tick) showed all three
had advanced together to `1h1m` / `1h14m` / `2h1m` — confirming the atomic read landed cleanly on the
boundary side before the next tick rolled every row forward by one band, and confirming (incidentally,
not mine to sign) that the shared tick advances all three rows together.

No wrong forms appeared on any row: no `60m`, no `1h0m`, no `73m` (which would have meant the sibling
under-hour branch/format fired), no `2h0m`.

## UNITS

running-elapsed-on-work-item-row:branch:hour-or-more
  STARTED FROM: three fresh in_progress rows seeded at 60m/1h13m/2h offsets, quest in_progress, no prior state.
  DID: `eval` — read all three `execution-row-duration` texts + `Date.now()` in one page.evaluate call.
  SAW: "1h", "1h13m", "2h" — every reading NhNm/Nh shaped, none bare Nm.
  BROKEN WOULD SHOW: the middle row (1h13m target) reading "73m" if the sibling under-hour branch
    (show-minutes) had fired instead of show-hours-minutes.
  SIGNED: confirmed.

running-elapsed-on-work-item-row:observable:check-hour-boundary
  STARTED FROM: row "sixty minute row r9", startedAt = now-3,600,000ms, in_progress, no completedAt.
  DID: same atomic eval read.
  SAW: "1h" exactly.
  BROKEN WOULD SHOW: "60m" (band not switched) or "1h0m" (zero minutes not dropped).
  SIGNED: confirmed.

running-elapsed-on-work-item-row:observable:check-hours-minutes-text
  STARTED FROM: row "one hour thirteen row r9", startedAt = now-4,380,000ms, in_progress, no completedAt.
  DID: same atomic eval read.
  SAW: "1h13m" exactly.
  BROKEN WOULD SHOW: "73m" (hour-or-more branch not reached).
  SIGNED: confirmed.

running-elapsed-on-work-item-row:observable:check-whole-hour-drops-minutes
  STARTED FROM: row "two hour row r9", startedAt = now-7,200,000ms, in_progress, no completedAt.
  DID: same atomic eval read; corroborated by a later screenshot showing the same row at "2h1m" one
    tick afterward, proving the atomic read was on the correct (pre-rollover) side of the boundary.
  SAW: "2h" exactly.
  BROKEN WOULD SHOW: "2h0m" (zero minutes not dropped on a whole-hour span).
  SIGNED: confirmed.

## REST OF PATH (walked, not signed — already S✓ from prior rounds)

- panel-rendered, started-at-check ("startedAt present"), row-running-check ("in_progress, no
  completedAt — end point is now"), compute-elapsed, under-minute-check ("60s or more") — all reached
  cleanly; the three rows rendered with badges reading RUNNING and no console errors during the whole
  walk (`console` capture showed only vite/React-devtools debug/info lines, nothing else).
- tick: a real tick fired between the atomic eval (031) and the follow-up screenshot (040), advancing
  all three rows from 1h/1h13m/2h to 1h1m/1h14m/2h1m simultaneously — visible, incidental confirmation
  of the shared-tick design, not something this round signs.
- still-running-check → timer-cleared, forced via "unmounted or no longer in_progress": clicked
  `[data-testid="LOGO_LINK"]`. Confirmed a CLIENT-SIDE route change (`urlBefore` the quest route,
  `urlAfter` "/") and that `[data-testid="execution-panel-widget"]` box-reads `count:0`/`present:false`
  immediately after. Nothing else on screen showed damage.

## DEFECT LIST

None. Zero defects found on this path walk — every reading matched its expected value exactly, no
console errors, no visual misalignment, clean unmount.

## PASS 2

Nothing to dispatch — the defect list is empty.

## LANE STATE AT HAND-OFF

Lane `r9-verify` was left running (never stopped, per LANE DISCIPLINE) with the browser navigated to
`/` (post LOGO_LINK click) and the seeded quest `933d699b-4866-4b50-b7d5-51e6471ed6ad` still on disk
under guild `ca029605-6a78-4850-94ee-d980e448beda`. It will self-close on its own 15-minute idle timeout.
