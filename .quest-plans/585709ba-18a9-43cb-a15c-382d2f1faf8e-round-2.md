# Round 2 — Groundstomper: author the browser walk for this flow — flow: health-badge

## Context
Quest ID: a7520e60-430c-4d0e-b332-9952d6d5c042
Work Item ID: 78b79071-77f9-4418-9d6e-a7d1c2b00bbb
Operation Item ID: 585709ba-18a9-43cb-a15c-382d2f1faf8e
Your operation item: [groundstomper] Groundstomper: author the browser walk for this flow — flow: health-badge

Operations ledger (in order):
1. [x] [chaoswhisperer] Author spec + implementation plan
2. [x] [riftcarver] Riftcarver: carve the quest branch, worktree and preflight build
3. [x] [codeweaver] Codeweaver: build this slice — package: shared
4. [x] [codeweaver] Codeweaver: build this slice — package: server
5. [x] [codeweaver] Codeweaver: build this slice — package: web
6. [x] [ward changed] Ward gate (changed files)
7. [x] [flowrider] Flowrider: author the flow-perspective test suites below the browser — package: server
8. [x] [flowrider] Flowrider: author the flow-perspective test suites below the browser — seam: server + shared
9. [>] [groundstomper] Groundstomper: author the browser walk for this flow — flow: health-badge  <-- YOUR OPERATION ITEM
10. [ ] [siegemaster] Siegemaster: manual-QA this flow and review its test suite — flow: health-badge
11. [ ] [ward full] Ward gate (full monorepo)

Your flows: #health-badge
(YOUR unit of accountability — every flow listed here, and no unit a sibling item owns. Not a starting point: work them, delegating where your role has minions.)

Your packages: web
(YOUR coverage slice — you own every verification unit whose owning NODE tags ANY of these packages, a unit spanning two of them included: your track has no seam item, so a glue unit is yours and nobody else claims it. Read these packages first.)

Packages affected (whole quest): shared (edit, library), server (edit, http-backend), web (edit, frontend-react)

Original user request (the intent behind the flows):
The app top bar gives no indication of whether the server behind it is still
answering. When the server dies or wedges, the interface keeps rendering its
last state and the operator finds out only when an action fails.

Add a health badge to the top bar, visible on every route. It seeds itself at
mount from a new GET /api/health/status returning status, uptime and version,
then tracks a health-status heartbeat the server emits every 10 seconds over
the WebSocket the interface already holds open.

The badge reads ONLINE with uptime beside it, DEGRADED, or OFFLINE. It goes
OFFLINE when the seed request fails to reach the server, when the server
answers with an error, or when 30 seconds pass with no heartbeat — a server
can hold the socket open while no longer doing work, so silence is the signal
rather than disconnection. Clicking an OFFLINE badge retries the seed request.

/api/health stays as it is, a bare liveness probe.

## Rework
chunk 2's health-badge:observable:check-still-online-before-threshold is unproven: health-badge-heartbeat.e2e.ts:63-70 samples the ONLINE hold at roughly 0.1s after a frame, not at the near-30s elapsed the unit names, so a badge with a 1s or 5s threshold passes it; prove it inside chunk 3's silence walk, which already waits that long, by asserting the ONLINE text at a measured elapsed above 20000ms and below 30000ms before the OFFLINE assertion.

## Plan

TOUCHES:
  ./packages/web/src/flows/app/health-badge-silence.e2e.ts — EXISTS — `page.goto` `/`; arms the harness, waits for one real health-status frame, cuts the wire, force-closes every tracked `/ws` socket, asserts a non-zero closed-socket count, then waits out 30 real seconds of genuine silence for exactly `OFFLINE`, the `No heartbeat for 30 seconds` title, and an enabled control that accepts a click
      health-badge:observable:check-still-online-before-threshold — a MEASURED-elapsed sample taken inside that same silence, after the sockets are closed and before the OFFLINE assertion, reading the badge's ONLINE label at an elapsed strictly above 20000ms and strictly below 30000ms since the reference frame's arrival

  ./packages/web/test/harnesses/health-badge/health-badge.harness.ts — EXISTS — the one place the badge's browser mechanics live: `arm()` installs the WebSocket-constructor tracker and the pageerror/console/request/response/websocket listeners, `badge()`/`labelText()`/`titleText()` read the DOM, `waitForFrames`/`frameAt`/`lastFrame`/`frameWithLaterUptimeMinute` read captured real frames with their arrival instants, `expectedOnlineLabel` re-derives the `ONLINE Xh Ym` string independently of `formatUptimeTransformer`, `elapsedSinceLastFrameAndLabel` samples `Date.now()` and the rendered text in ONE `page.evaluate`, and `cutWire`/`restoreWire`/`closeBackendSockets` force the real faults. Gains ONE new reader this round: an elapsed-GATED sampler that waits until a caller-named elapsed has passed since the last captured frame and only then takes that same single-evaluate sample
      no unit lands on this file; it is the surface the unit above is measured through

  ./packages/web/src/flows/app/health-badge-heartbeat.e2e.ts — EXISTS — `page.goto` `/`; two consecutive real health-status frames, the label re-asserted against each frame's own uptimeSeconds, a third frame whose uptime crosses a 60-second boundary so the rendered text is proved to MOVE, and exactly one seed request across the whole window. Carries at :63-70 the sample this round's unit was assigned to in round 1 — one `page.evaluate` reading `Date.now()` and the badge text together, taken ~0.1s after a frame. NO unit lands on it this round and this round does not edit it; it is chunk 1's MIRROR, because :64-70 is the exact assertion SHAPE the new sample follows

  ./packages/web/src/flows/app/health-badge-mount.e2e.ts — EXISTS — `page.goto` `/`; badge visible beside LOGO_LINK and a seeded RATE_LIMITS_STACK, the real 200 seed pinned at :52-56, the ONLINE label off that response's own uptimeSeconds at :60, and a navigation-entry count of 1 at :79. No unit lands on it; listed because its entry node is this flow's entry
  ./packages/web/src/flows/app/health-badge-retry.e2e.ts — EXISTS — `page.goto` `/`; cut wire, the badge's own click, OFFLINE with the `No response from server` title, then the wire restored and a second click back to ONLINE. No unit lands on it; listed because its entry node is this flow's entry
  ./packages/web/src/flows/app/health-badge-unmount.e2e.ts — EXISTS — `page.goto` `/`; navigating off the shell from the live reading and from the OFFLINE reading, the `/ws` socket closed and no pageerror. No unit lands on it; listed because its entry node is this flow's entry
  ./packages/web/src/flows/app/smoke.e2e.ts — EXISTS — `page.goto` `/`; first-load, empty-state and guild-list rendering. Carries NO health-badge assertion. No unit lands on it; listed because its entry node is this flow's entry
  ./packages/web/src/flows/app/visual.e2e.ts — EXISTS — `page.goto` `/`; asserts rendered CSS colours with `toHaveCSS` and text visibility after a guild is selected — it takes no screenshots. Carries NO health-badge assertion. No unit lands on it; listed because its entry node is this flow's entry
  ./packages/web/test/harnesses/e2e-fixtures.ts — EXISTS — exports the `test`/`expect` every spec above imports instead of `@playwright/test`, and the `wireHarnessLifecycle` that bridges a harness's setup/teardown into Playwright's lifecycle. READ ONLY this round. No unit lands on it

DEPENDS:
  ./packages/web/test/harnesses/health-badge/health-badge.harness.ts
      needs: ./packages/web/test/harnesses/e2e-fixtures.ts — nothing crosses this link at author time; the harness is bridged into each spec's lifecycle by `wireHarnessLifecycle`, which that file exports
      needed by: ./packages/web/src/flows/app/health-badge-silence.e2e.ts — the wire cut, the socket force-close, the captured frames with their arrival instants, `expectedOnlineLabel`, and THIS ROUND the new elapsed-gated sampler the ONLINE-hold assertion reads
      needed by: ./packages/web/src/flows/app/health-badge-heartbeat.e2e.ts — `frameAt`, `frameWithLaterUptimeMinute`, `expectedOnlineLabel`, `elapsedSinceLastFrameAndLabel`, `getSeedRequestCount`. NOT edited this round, but it shares this file, which is why the new reader is ADDED beside `elapsedSinceLastFrameAndLabel` rather than that method's signature being changed
      needed by: ./packages/web/src/flows/app/health-badge-mount.e2e.ts — `lastSeedResponse`, `expectedOnlineLabel`. NOT edited this round
      needed by: ./packages/web/src/flows/app/health-badge-retry.e2e.ts — `cutWire`/`restoreWire`, `seedResponseAt`, `titleText`. NOT edited this round
      needed by: ./packages/web/src/flows/app/health-badge-unmount.e2e.ts — `cutWire`, `getPageErrors`, `getConsoleErrors`. NOT edited this round

  ./packages/web/src/flows/app/health-badge-silence.e2e.ts
      needs: ./packages/web/test/harnesses/health-badge/health-badge.harness.ts — the elapsed-gated sampler that waits out ~22 of the silence's 30 seconds and then reads `Date.now()` and the badge's rendered text in ONE `page.evaluate`, returning the reference frame's own `uptimeSeconds` alongside so the expected label and the measured elapsed come from the SAME frame
      needs: ./packages/web/test/harnesses/e2e-fixtures.ts — its `test`/`expect`
      needed by: nothing this round touches

  ./packages/web/src/flows/app/health-badge-heartbeat.e2e.ts
      needs: ./packages/web/test/harnesses/health-badge/health-badge.harness.ts — READ ONLY this round; its :63-70 sample keeps calling `elapsedSinceLastFrameAndLabel`, whose signature this round does not change
      needed by: nothing this round touches — it is chunk 1's MIRROR, read for its assertion shape and not edited

  ./packages/web/src/flows/app/health-badge-mount.e2e.ts
      needs: nothing this round writes
      needed by: nothing this round touches — READ ONLY, listed for its entry node
  ./packages/web/src/flows/app/health-badge-retry.e2e.ts
      needs: nothing this round writes
      needed by: nothing this round touches — READ ONLY, listed for its entry node
  ./packages/web/src/flows/app/health-badge-unmount.e2e.ts
      needs: nothing this round writes
      needed by: nothing this round touches — READ ONLY, listed for its entry node
  ./packages/web/src/flows/app/smoke.e2e.ts
      needs: nothing this round writes
      needed by: nothing this round touches — READ ONLY, listed for its entry node
  ./packages/web/src/flows/app/visual.e2e.ts
      needs: nothing this round writes
      needed by: nothing this round touches — READ ONLY, listed for its entry node

  ./packages/web/test/harnesses/e2e-fixtures.ts
      needs: nothing this round writes
      needed by: every `.e2e.ts` entry above, for `test`/`expect` and `wireHarnessLifecycle`. READ ONLY this round

DECISIONS:
  - This round's whole denominator is ONE unit. `get-qa-checklist({ operationItemId: '585709ba-18a9-43cb-a15c-382d2f1faf8e' })` reports `REMAINING (awaiting your flowriderSignoff): 1 of 52`, and that one id is `health-badge:observable:check-still-online-before-threshold` — the same unit `## Rework` names. The other 51 are already settled on this track by round 1's reviewer commit `54ebe4ebc` (30 flowriderSignoffs over 31 units, 19 `confirmed` and 11 `unconfirmable`), so they are not re-litigated here and take no `settled` line of their own. That one unit is WORK, so `NO CHUNK` reads `none`.
  - The proof MOVES to the silence walk and `health-badge-heartbeat.e2e.ts` is NOT edited. Its :63-70 sample still asserts something true and unclaimed — the rendered label equals the newest frame's expected label at a measured elapsed under the threshold, which is `timer-fresh`'s own territory — and the file names no unit id anywhere in it, so leaving it is not a false green. Putting it in the chunk would add a 120_000-budget spec to that chunk's ward run for no assertion gained.
  - The round costs ZERO added wall clock. The new sample is taken 25 seconds into a silence the spec already waits 30 seconds through, so nothing new is waited for. Round 1's own chunk-3 report measures that whole test at 38.1s, with its single real frame at `+7456ms` and the channel's keepalive `{"type":"ping"}` at `+30061ms` confirming no reconnect and no second frame. The existing `test.describe.configure({ timeout: 90_000 })` and the OFFLINE assertion's own `{ timeout: 40_000 }` therefore both stay exactly as they are — the OFFLINE wait simply starts 25s later and has ~5s left to observe.
  - The sample instant is 25000ms: the MIDPOINT of the window `## Rework` requires (above 20000ms, below 30000ms), 5 seconds clear of each edge. `playwright.config.ts:43` sets `retries: 0` locally, so a boundary sample landing outside that window reds the round rather than being absorbed by a retry. The gate can only ever OVERSHOOT — it sleeps the remaining time and then MEASURES what actually elapsed — so the `> 20000` half cannot fail; the `< 30000` half is protected by 5 seconds against an evaluate round trip of tens of milliseconds.
  - The ONLINE hold at 25s is a genuine test of the threshold VALUE, not of socket state. `use-health-status-binding.ts:44-77` reaches `offline` through exactly one path — the `setInterval` at :60-71 calling `isHeartbeatSilentGuard`, which returns true only at `elapsedMs >= healthBadgeStatics.silenceThresholdMs` (`is-heartbeat-silent-guard.ts:27-28`). That binding registers no close, error or connection-state handler at all, so a force-closed socket cannot short-circuit the badge into OFFLINE early and the 25 seconds it holds are being held on the timer alone.
  - The elapsed is measured from the harness's `arrivedAt` (stamped in the Playwright process, harness :371) while the badge's own timer runs off `lastHeartbeatAt` (stamped in the browser, `use-health-status-binding.ts:55`). The skew is one IPC hop and always makes the HARNESS clock the lagging one, so the badge can only flip later than the harness believes — the safe direction for a `< 30000` assertion. `elapsedSinceLastFrameAndLabel` at harness :508-517 already mixes those two clocks the same way and was green throughout round 1.
  - The new reader goes in the HARNESS, not in the spec. `forbid-non-exported-functions` rejects a helper declared inside a `.e2e.ts` and the pre-edit hook refuses the write outright, so a spec cannot compute its own gated wait. `health-badge.harness.ts` is this operation item's own file — created by round 1 chunk 1 and owned by chunk 3 — not a sibling piece of work's, so editing it is in scope.
  - No manufactured value shortens the wait. `page.route` is lint-banned in `.e2e.ts` by `@dungeonmaster/ban-page-route-in-e2e`, `@dungeonmaster/ban-wait-for-timeout` forbids `page.waitForTimeout`, and `page.clock` appears in zero files repo-wide. The gate is a plain `setTimeout` inside the harness, the shape that file already uses five times (:153-155, :182-184, :213-215, :237-239, :268-270), and the 25 seconds are real.
  - Checker findings I disagreed with: it reported that the silence spec has no intermediate sample, that the harness has no elapsed-gated sampler, that `elapsedSinceLastFrameAndLabel` returns no `uptimeSeconds`, and that the silence spec never calls `expectedOnlineLabel`. All four are TRUE of the tree today and all four are precisely what chunk 1 adds — `TOUCHES` and `DEPENDS` describe the round's end state, not its starting state. Its fifth finding was real and is corrected above: `visual.e2e.ts` takes no screenshots, it asserts `toHaveCSS` and text visibility.

ASSERTIONS:
  - `./packages/web/src/flows/app/health-badge-silence.e2e.ts` carries an assertion whose MEASURED elapsed is asserted both strictly greater than 20000 and strictly less than 30000, in the same `expect` as the badge's ONLINE label, and that assertion's line number is BELOW the `closeBackendSockets()` call and ABOVE the `toHaveText('OFFLINE'` line. A reader checks by opening the file and comparing the three line numbers.
  - Mutating `packages/web/src/statics/health-badge/health-badge-statics.ts:21` `silenceThresholdMs` from `30000` to `5000` — that one line, nothing else — reds `health-badge-silence.e2e.ts` at the new assertion and nowhere earlier, reporting the label as `OFFLINE` where `ONLINE …` was expected. A reader checks by making that one-line change, running the spec, and reading which assertion failed first.
  - `./packages/web/test/harnesses/health-badge/health-badge.harness.ts` gains exactly one new method, declared BOTH in the harness's return-type annotation and in its returned object, and `elapsedSinceLastFrameAndLabel`'s signature is unchanged — so `git diff` on `./packages/web/src/flows/app/health-badge-heartbeat.e2e.ts` at the end of this round is empty and that spec still typechecks untouched.
  - `git diff 54ebe4ebc --name-only` at the end of this round names exactly two source paths: `packages/web/src/flows/app/health-badge-silence.e2e.ts` and `packages/web/test/harnesses/health-badge/health-badge.harness.ts` (plus this round document).
  - Neither file this round writes contains `page.route`, `page.waitForTimeout` or `page.clock`. A reader checks with one `discover({ grep: 'page\.(route|waitForTimeout|clock)', strict: true })` over the two written paths and gets no hits.
  - `./packages/web/playwright.config.ts` is byte-identical to its state at `54ebe4ebc` when the round ends — `git diff 54ebe4ebc -- packages/web/playwright.config.ts` is empty — and `health-badge-silence.e2e.ts`'s own `test.describe.configure` budget is still `90_000`.
  - The single unit id `get-qa-checklist` reports as REMAINING for this operation item appears in exactly one chunk's `UNITS` row and on no `NO CHUNK` line.

NO CHUNK: none

### chunk 1 — the ONLINE hold, sampled 25 seconds into the silence walk it already waits out
INTENT:
  - `./packages/web/src/flows/app/health-badge-silence.e2e.ts` asserts, in ONE `expect(...).toStrictEqual(...)`, that the badge's rendered text equals the `ONLINE Xh Ym` label computed from the reference frame's own `uptimeSeconds` AND that the elapsed that same read measured is strictly greater than 20000 and strictly less than 30000
  - that assertion's line number is BELOW the `closeBackendSockets()` count check and ABOVE the `toHaveText('OFFLINE'` line, in the same `test()` body
  - `./packages/web/test/harnesses/health-badge/health-badge.harness.ts` exposes ONE new elapsed-gated sampler, declared both in the harness's return-TYPE annotation and in its returned object, and `elapsedSinceLastFrameAndLabel`'s signature is unchanged
  - changing `packages/web/src/statics/health-badge/health-badge-statics.ts:21` `silenceThresholdMs` from `30000` to `5000` — that one line and nothing else — reds this spec AT the new assertion and at no assertion before it, reporting `OFFLINE` where an `ONLINE …` label was expected; and `git diff` on that statics file is empty again before this chunk reports
  - the spec's `test.describe.configure` budget is still `90_000`, the OFFLINE assertion's own timeout is still `40_000`, and the test's measured duration is still in the same range as round 1's 38.1s
  - `git diff` on `./packages/web/src/flows/app/health-badge-heartbeat.e2e.ts` and on `./packages/web/playwright.config.ts` is empty
FILES:
  - ./packages/web/src/flows/app/health-badge-silence.e2e.ts
  - ./packages/web/test/harnesses/health-badge/health-badge.harness.ts
UNITS:
  - health-badge:observable:check-still-online-before-threshold → ./packages/web/src/flows/app/health-badge-silence.e2e.ts — at a MEASURED elapsed above 20000ms and below 30000ms since the reference frame's arrival, the badge still reads the `ONLINE Xh Ym` label computed from that same frame's `uptimeSeconds`
MIRROR: ./packages/web/src/flows/app/health-badge-heartbeat.e2e.ts
NOTES:
  WHY this exists: round 1 proved this unit at :63-70 of the MIRROR, ~0.1s after a frame. A badge
  whose threshold were 1s or 5s passes that. The unit names 29 seconds, so the hold has to be read
  near the boundary — and the silence walk is the one walk already sitting in that silence.

  THE HARNESS METHOD. A `.e2e.ts` may declare no function (`forbid-non-exported-functions`, with a
  pre-edit hook that refuses the write), so the gated wait cannot live in the spec. Add ONE method
  beside `elapsedSinceLastFrameAndLabel` (harness :508-517) rather than changing that method's
  signature — the MIRROR calls it and must keep compiling untouched:

    labelAtElapsedSinceLastFrame: async ({ minElapsedMs }: { minElapsedMs: number }): Promise<{
      elapsedMs: NowMs;
      label: BadgeText;
      uptimeSeconds: HealthStatusPayload['uptimeSeconds'];
    }> => {
      const last = capturedFrames.at(-1);
      if (last === undefined) { throw new Error('health-badge harness: labelAtElapsedSinceLastFrame called before any health-status frame arrived'); }
      const arrivedAtMs = last.arrivedAt.getTime();
      await new Promise((resolve) => { setTimeout(resolve, Math.max(0, minElapsedMs - (Date.now() - arrivedAtMs))); });
      const sample = await page.evaluate(sampleNowAndLabelBrowserFn, HEALTH_BADGE_TEST_ID);
      return { elapsedMs: sample.now - arrivedAtMs, label: sample.label, uptimeSeconds: last.uptimeSeconds };
    },

  Four things that file already settles, so do not re-derive them:
    - `NowMs`, `BadgeText` and the `HealthStatusPayload` type import exist at :98-104 and :26.
      `@dungeonmaster/ban-primitives` forbids a raw `number`/`string` RETURN type even here, so
      reuse those aliases; PARAMS typed `number` follow `frameAt`/`seedResponseAt`.
    - The harness annotates its FULL return type at :278-304. A method added to the returned object
      but not to that annotation fails typecheck. Add it in BOTH places.
    - `await new Promise((resolve) => { setTimeout(resolve, N); })` is the file's own established
      sleep — :153-155, :182-184, :213-215, :237-239, :268-270. Never `while(true)`; never
      `page.waitForTimeout`, which `@dungeonmaster/ban-wait-for-timeout` rejects at error level.
    - `sampleNowAndLabelBrowserFn` (:89-92) is the existing single-`page.evaluate` reader. Reuse it,
      so the elapsed and the label are read at ONE instant rather than racing two evaluates.
  Returning `uptimeSeconds` is the point of the new method over the old one: the expected label and
  the measured elapsed then come from the SAME frame, with no second read that could pick a
  different one.

  THE SPEC EDIT. It goes between the `expect(closedSockets.length).toBeGreaterThan(0)` line and the
  `toHaveText('OFFLINE', …)` line, and NOWHERE else — the OFFLINE assertion is what consumes the
  remaining silence, and the `titleText()` read and the `click()` after it must stay last, because
  the click fires the widget's `retry()` and flips the title off the silence string.
  Three module-level constants beside the file's existing ones (no magic numbers; match the file's
  `15_000`/`40_000`/`90_000` separator style): the 20000 floor, the 25000 sample instant and the
  30000 threshold, each commented with `health-badge-statics.ts silenceThresholdMs` as its source.
  Shape, following the MIRROR's :64-70 exactly:

    const hold = await hb.labelAtElapsedSinceLastFrame({ minElapsedMs: STILL_ONLINE_SAMPLE_AT_MS });
    expect({
      elapsedAboveFloor: hold.elapsedMs > STILL_ONLINE_FLOOR_MS,
      elapsedBelowThreshold: hold.elapsedMs < SILENCE_THRESHOLD_MS,
      label: hold.label,
    }).toStrictEqual({
      elapsedAboveFloor: true,
      elapsedBelowThreshold: true,
      label: hb.expectedOnlineLabel({ uptimeSeconds: hold.uptimeSeconds }),
    });

  Extend the test's TITLE to name what it now proves — it currently claims only the OFFLINE flip.
  Keep the `VALID:` prefix and the `{input} => result` shape (`enforce-test-name-prefix`).

  WHY 25000. It is the midpoint of the window `## Rework` requires. The gate can only OVERSHOOT —
  it sleeps the remaining time and then measures what actually elapsed — so `> 20000` cannot fail;
  `< 30000` has 5 seconds against an evaluate round trip of tens of ms. `playwright.config.ts:43`
  sets `retries: 0` locally, so there is no retry to absorb a sample that lands outside the window.

  WHY IT COSTS NOTHING. The sample sits INSIDE the 30 seconds this walk already waits through, so
  the walk gets no longer: the OFFLINE assertion simply starts 25s in with ~5s left, well inside its
  unchanged 40_000. Do not raise the 90_000 describe budget and do not touch `playwright.config.ts`
  — one config is shared by every session on this quest and an edit here is last-write-wins.

  THE RED. Change `packages/web/src/statics/health-badge/health-badge-statics.ts:21`
  `silenceThresholdMs` from `30000` to `5000` — ONE line, and unlike round 1's `timer-fresh` proof
  you do NOT also need `silenceTickMs`, because the sample is 25s downstream and the 1000ms tick
  cadence cannot race it. Nothing earlier in this spec breaks under that mutation: the badge holds
  its seeded ONLINE until the first frame (`isHeartbeatSilentGuard` returns false while
  `lastHeartbeatAt` is undefined, `is-heartbeat-silent-guard.ts:24-26`), and the only assertions
  before the new one are `toBeVisible()` and the socket count. So the new assertion is the FIRST to
  fail, reading the label as `OFFLINE` against an expected `ONLINE …`. Restore the line and confirm
  `git diff` on that statics file is empty before reporting. This is the isolated red round 1's
  reviewer could not get for this unit — get it.

  ORDER, unchanged from round 1 and still load-bearing: `arm()` BEFORE `page.goto('/')`;
  `waitForFrames` for one frame BEFORE `cutWire()` (cut before any frame lands and the badge stays
  correctly ONLINE forever); `cutWire()` BEFORE `closeBackendSockets()` (`setOffline(true)` does not
  close an established socket — it blocks the channel's own 3s reconnect from landing).

  NOTHING ELSE MOVES. Do not edit `health-badge-heartbeat.e2e.ts`: its :63-70 sample asserts
  `timer-fresh`'s own territory, names no unit id, and dragging it in would add a 120_000-budget
  spec to this chunk's ward run for nothing.

  WHAT THIS CHANGES THAT OTHERS USE: the harness's exported surface gains exactly one method and
  loses none — `elapsedSinceLastFrameAndLabel`, `expectedOnlineLabel`, `frameAt`,
  `frameWithLaterUptimeMinute`, `waitForFrames`, `cutWire`, `restoreWire`, `closeBackendSockets`,
  `lastSeedResponse`, `seedResponseAt`, `titleText`, `getSeedRequestCount`, `getPageErrors` and
  `getConsoleErrors` all keep their current signatures. The four sibling specs
  (`health-badge-mount`, `-heartbeat`, `-retry`, `-unmount`) import this harness and none of them is
  edited; confirm that with a `healthBadgeHarness` discover sweep and an empty `git diff` on the
  four, the way round 1's own chunk reports did.

PHASES:
  1: wave 1 — the whole round: the harness's elapsed-gated sampler and the silence walk's ONLINE-hold assertion, which are one file pair and one worker

WAVES:
  1: 1

## Round log
### report — chunk 1
RESULT:
  - `health-badge-silence.e2e.ts` asserts, in ONE `expect(...).toStrictEqual(...)`, the badge's rendered text equals the `ONLINE Xh Ym` label computed from the reference frame's own `uptimeSeconds` AND that the same read's measured elapsed is strictly greater than 20000 and strictly less than 30000 — yes — lines 62-71: `hold.elapsedMs > STILL_ONLINE_FLOOR_MS` and `hold.elapsedMs < SILENCE_THRESHOLD_MS` sit in the same object as `label: hold.label` vs expected `label: hb.expectedOnlineLabel({ uptimeSeconds: hold.uptimeSeconds })`
  - that assertion's line number is BELOW the `closeBackendSockets()` count check and ABOVE the `toHaveText('OFFLINE'` line, in the same `test()` body — yes — `expect(closedSockets.length).toBeGreaterThan(0)` is line 55, the new `toStrictEqual` is lines 63-71, `toHaveText('OFFLINE'` is line 78
  - `health-badge.harness.ts` exposes ONE new elapsed-gated sampler, declared both in the return-TYPE annotation and in the returned object, and `elapsedSinceLastFrameAndLabel`'s signature is unchanged — yes — `labelAtElapsedSinceLastFrame` added to the type annotation (:299-303) and the returned object (:530-555); `elapsedSinceLastFrameAndLabel` (:513-522) is byte-identical to before this chunk
  - mutating `health-badge-statics.ts:21` `silenceThresholdMs` 30000→5000 (one line, nothing else) reds this spec AT the new assertion and at no assertion before it, reporting `OFFLINE` where `ONLINE …` was expected; `git diff` on that statics file is empty again before this report — yes — witnessed red on ward run `1787972909707-d2cd`: `toStrictEqual` fails at spec line 67 with `- "label": "ONLINE 0h 0m", + "label": "OFFLINE"`; every assertion before it (visibility, socket count) had already passed; `git diff --stat -- packages/web/src/statics/health-badge/health-badge-statics.ts` is empty after restore
  - the spec's `test.describe.configure` budget is still `90_000`, the OFFLINE assertion's own timeout is still `40_000`, and the test's measured duration is still in the same range as round 1's 38.1s — yes — both constants untouched at lines 12-13; the real passing run (`1787973004190-58fe`) measured 38534ms
  - `git diff` on `health-badge-heartbeat.e2e.ts` and on `playwright.config.ts` is empty — yes — confirmed via `git diff --stat`, no output on either path (also checked `health-badge-mount.e2e.ts`, `health-badge-retry.e2e.ts`, `health-badge-unmount.e2e.ts`: all empty)
FILES:
  - ./packages/web/src/flows/app/health-badge-silence.e2e.ts
  - ./packages/web/test/harnesses/health-badge/health-badge.harness.ts
EVIDENCE:
  - health-badge:observable:check-still-online-before-threshold — `packages/web/src/flows/app/health-badge-silence.e2e.ts:63-71` (the `toStrictEqual` call at :67) — assertion: `expect({ elapsedAboveFloor: hold.elapsedMs > STILL_ONLINE_FLOOR_MS, elapsedBelowThreshold: hold.elapsedMs < SILENCE_THRESHOLD_MS, label: hold.label }).toStrictEqual({ elapsedAboveFloor: true, elapsedBelowThreshold: true, label: hb.expectedOnlineLabel({ uptimeSeconds: hold.uptimeSeconds }) })` — fails when `packages/web/src/statics/health-badge/health-badge-statics.ts:21` `silenceThresholdMs` is 5000 instead of 30000, because the real badge then flips OFFLINE well before the 25s sample, so `hold.label` reads `'OFFLINE'` against an expected `'ONLINE 0h 0m'` — witnessed red on ward run `1787972909707-d2cd` (`npm run ward -- -- ./packages/web/src/flows/app/health-badge-silence.e2e.ts`, e2e FAIL, detail: `- "label": "ONLINE 0h 0m", + "label": "OFFLINE"` at the line-67 `toStrictEqual`), then the statics line was restored and reverified green on ward run `1787973004190-58fe` (38534ms)
USAGES: `discover({ grep: 'healthBadgeHarness' })` — hits in `health-badge.harness.ts` itself and the five sibling specs (`health-badge-heartbeat.e2e.ts`, `health-badge-mount.e2e.ts`, `health-badge-retry.e2e.ts`, `health-badge-silence.e2e.ts`, `health-badge-unmount.e2e.ts`). None of the four untouched siblings calls the new `labelAtElapsedSinceLastFrame`, and `elapsedSinceLastFrameAndLabel`'s signature did not change, so nothing else uses this change. Confirmed with empty `git diff --stat` on all four sibling specs plus `playwright.config.ts`.
GOTCHAS:
  - The gate can only OVERSHOOT the target elapsed (it sleeps the remaining time, then measures what actually elapsed), so the `> 20000` half of the window can never fail; the `< 30000` half is protected by 5 seconds of margin against an evaluate round trip of tens of ms — held on both the red run and the green run.
  - The green run's total duration (38534ms) lands in the same range as round 1's reported 38.1s baseline, confirming the new 25s sample adds no wall-clock cost — it is read inside the 30s silence window the walk already waits through, not an additional wait.
MARKERS:  none
WARD:     `npm run ward -- -- ./packages/web/src/flows/app/health-badge-silence.e2e.ts ./packages/web/test/harnesses/health-badge/health-badge.harness.ts` — green — lint PASS (2 files), typecheck PASS (1136 files / 7311 project-wide), e2e PASS (1 file, 88 discovered, 43.1s). `DISCOVERY MISMATCH` reported against `unit`/`integration` is a skip, not a regression: neither written file is a `.test.ts`/`.integration.test.ts` — one is a `.e2e.ts` spec, the other a `.harness.ts`, both e2e-only surfaces with no unit/integration counterpart.
