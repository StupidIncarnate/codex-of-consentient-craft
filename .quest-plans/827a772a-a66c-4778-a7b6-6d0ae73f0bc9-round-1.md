# Round 1 — [codeweaver] Codeweaver: build this slice — package: shared

## Context

Quest ID: a7520e60-430c-4d0e-b332-9952d6d5c042
Work Item ID: a1be0735-7bc2-48fe-bd7c-76eb0ffe42c3
Operation Item ID: 827a772a-a66c-4778-a7b6-6d0ae73f0bc9
Your operation item: [codeweaver] Codeweaver: build this slice — package: shared

Operations ledger (in order):
1. [x] [chaoswhisperer] Author spec + implementation plan
2. [x] [riftcarver] Riftcarver: carve the quest branch, worktree and preflight build
3. [>] [codeweaver] Codeweaver: build this slice — package: shared  <-- YOUR OPERATION ITEM
4. [ ] [codeweaver] Codeweaver: build this slice — package: server
5. [ ] [codeweaver] Codeweaver: build this slice — package: web
6. [ ] [ward changed] Ward gate (changed files)
7. [ ] [flowrider] Flowrider: author the flow-perspective test suites below the browser — package: server
8. [ ] [flowrider] Flowrider: author the flow-perspective test suites below the browser — seam: server + shared
9. [ ] [groundstomper] Groundstomper: author the browser walk for this flow — flow: health-badge
10. [ ] [siegemaster] Siegemaster: manual-QA this flow and review its test suite — flow: health-badge
11. [ ] [ward full] Ward gate (full monorepo)

Flows your operation item lands on: #health-badge
(A starting point, NOT a boundary — read every flow, and build whatever the flows need.)

Packages your operation item lands in: shared
(Name these packages in every minion brief you write — the planner and the workers point their own searches here instead of guessing. NOT a boundary: a minion may touch another package if the work needs it.)

Your nodes (rendered from the spec as it stands right now, not from the ledger): #server-emits, #channel-routes

Must satisfy — these are YOUR acceptance targets, verbatim:
  - check-event-type-member [custom] on #server-emits: "orchestrationEventTypeContract.options contains 'health-status' and has 22 members, the prior 21 unchanged"
  - check-payload-parses [custom] on #channel-routes: "A health-status payload parses against healthStatusPayloadContract, and a payload missing uptimeSeconds fails that parse"

Contracts you own — every property description is a requirement:
  - HealthStatusPayload (data, new) [packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts]
      status: Either the literal 'ok' or the literal 'degraded'. Deliberately NOT named HealthStatus: the server's existing health-response contract already brands that name onto z.string().min(1), and two schemas sharing a brand tag with different base types are not interchangeable. shared also cannot import from server, so the name must differ rather than be reused.
      uptimeSeconds: Whole seconds the server process has been running. Rendered as Xh Ym beside ONLINE.
      version: The server package version string, carried for display and diagnosis.
  - OrchestrationEventType (data, modified) [packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.ts]
      healthStatus: The new twenty-second member of the enum. All 21 existing members keep their spelling and order; this one is added so both the server relay and the web channel accept the frame type.

Design decisions constraining your scope:
  - A socket reconnect needs no special handling — Offline is decided by heartbeat silence rather than by transport state, so a drop and reconnect is invisible to the badge's own logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing for it.

Seams — each line is a node you share with another package, and where that package's half of it stands:
  - #server-emits with server — NOT BUILT YET: a later session owns these — build your half to the shape they need, and do NOT build theirs
      attributed to server — check-emit-interval: "With the server running, three health-status frames arrive within 35 seconds, spaced 10 seconds apart"
      attributed to server — check-emit-payload-advances: "Each health-status frame carries exactly {status, uptimeSeconds, version}, and uptimeSeconds is strictly greater than the previous frame's"
  - #channel-routes with server — NOT BUILT YET: a later session owns these — build your half to the shape they need, and do NOT build theirs
      attributed to server — check-frame-crosses-wire: "The serialised health-status envelope arrives at the browser as one WebSocket text message carrying type, payload and timestamp"
  - #channel-routes with web — NOT BUILT YET: a later session owns these — build your half to the shape they need, and do NOT build theirs
      attributed to web — check-payload-updates-badge: "A delivered health-status frame changes the badge text within 1 second of arrival"
      attributed to web — check-reconnect-resumes: "After the socket drops and reconnects, the first health-status frame moves the badge out of OFFLINE with no click issued"

Packages affected (whole quest): shared (edit, library), server (edit, http-backend), web (edit, frontend-react)

Original user request (the intent behind the flows):
The app top bar gives no indication of whether the server behind it is still
answering. When the server dies or wedges, the interface keeps rendering its
last state and the operator finds out only when an action fails.

Add a health badge to the top bar, visible on every route. It seeds itself at
mount from a new GET /api/health/status returning status, uptime and version,
then tracks a health-status heartbeat the server emits every 10 seconds over
the WebSocket the interface already holds open.

The badge reads ONLINE with uptime beside it, DEGRADED, or OFFLINE. It goes
OFFLINE when the seed request fails to reach the server, when the server
answers with an error, or when 30 seconds pass with no heartbeat — a server
can hold the socket open while no longer doing work, so silence is the signal
rather than disconnection. Clicking an OFFLINE badge retries the seed request.

/api/health stays as it is, a bare liveness probe.

## Plan

TOUCHES:
  ./packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.ts — EXISTS — the single `z.enum([...])` (lines 11-36) whose 21 string members are the whole vocabulary of the WebSocket wire; `export type OrchestrationEventType = z.infer<typeof orchestrationEventTypeContract>` on line 38 widens with no second edit, and `packages/server/src/responders/server/init/server-init-responder.ts:539` iterates `orchestrationEventTypeContract.options` to subscribe the relay, so a member added here is relayed with no server edit. Companion `orchestration-event-type-contract.test.ts` rides with it; companion `orchestration-event-type.stub.ts` needs no edit (it parses a raw `{ value }: { value: string }`).
      check-event-type-member — must declare `'health-status'` as a 22nd member with the prior 21 keeping their exact spelling and order, and its colocated test must assert `orchestrationEventTypeContract.options` equals the full 22-literal array in declaration order.
      OrchestrationEventType.healthStatus — must add the literal `'health-status'` to the enum array, appended after `'dispatch-state-changed'` so no existing member's index moves.

  ./packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts — NEW — the one declaration of the health heartbeat's payload body, imported by BOTH the server (which builds it for the 10-second frame and for the `GET /api/health/status` 200 body) and the web channel (which parses a delivered frame). It lives in shared rather than in either consumer because shared is the only package both depend on, and because `packages/server/src/contracts/health-response/health-response-contract.ts:12` already owns the brand tag `'HealthStatus'` on a `z.string().min(1)` base, which this contract must not collide with. Companions `health-status-payload-contract.test.ts` and `health-status-payload.stub.ts` ride with it; the stub is required because the barrel exports contract and stub as a pair and because `StubArgument` is how every other shared object contract is built in tests.
      check-payload-parses — must parse a whole `{status, uptimeSeconds, version}` body and must reject a body with `uptimeSeconds` omitted.
      HealthStatusPayload.status — must declare a two-member bounded set accepting exactly the literals `'ok'` and `'degraded'`, under a type name that is not `HealthStatus`.
      HealthStatusPayload.uptimeSeconds — must declare a whole-number seconds field: integer, not negative, rejecting a float and rejecting a string.
      HealthStatusPayload.version — must declare a non-empty server package version string.

  ./packages/shared/contracts.ts — EXISTS — the package's ONLY contracts barrel (root-level, 754 lines, outside `src/`, matched by tsconfig `include: ["*.ts"]`), grouped by `// <Group> Contracts` headers with a paired `-contract` + `.stub` export line under each. It is the sole wiring point: `packages/shared/package.json:6-11` already declares the `"./contracts"` subpath (`source: ./contracts.ts` for node10 typecheck/lint, `import`/`require`/`types` for the built `dist/`), and `packages/shared/src/contracts/` holds no index file of any kind. Without a line here `healthStatusPayloadContract` is unreachable from `@dungeonmaster/shared/contracts` and both the server and web sessions are blocked.

DEPENDS:
  ./packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.ts
      needs: nothing in this repo — its only import is `zod` (line 9). It is a leaf, which is why it can be built in the same wave as anything else.
      needed by: ./packages/shared/src/contracts/ws-message/ws-message-contract.ts — line 11 imports it and line 14 uses it as `type:` in the wire envelope. Until `'health-status'` is a member, `wsMessageContract.safeParse` rejects EVERY health frame, on both sides of the wire.
      needed by: ./packages/shared/contracts.ts — line 260 already re-exports this file and line 261 its stub. This link is already satisfied; no barrel edit is owed by this entry.
      needed by (OTHER SESSION, server) ./packages/server/src/responders/server/init/server-init-responder.ts:539 — `const eventTypes = orchestrationEventTypeContract.options;` then `for (const type of eventTypes)`, skipping only `'quest-modified'` and `'quest-created'`. The relay therefore subscribes to a new member with no edit. Line 654 is the branch point, `if (PER_QUEST_EVENT_TYPES.has(type)) {`, and that block ends in a `return` at line 759; `'health-status'` is not in that set (lines 65-74), so it skips the block entirely and reaches `wsEventRelayBroadcastBroker({ clients, message: envelope })` at lines 762-766 — the broadcast-to-every-client behaviour #relay-broadcast needs, under the comment "Global events broadcast to every connected client." at line 762.
      needed by (OTHER SESSION, web) ./packages/web/src/state/web-socket-channel/web-socket-channel-state.ts:134 — `wsMessageContract.safeParse(message)`, returning early on failure. A frame typed `'health-status'` is silently dropped there until this member lands.

  ./packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts
      needs: nothing in this repo — every field brands inline, so its only import is `zod`. It is a second leaf, independent of the enum file.
      needed by: ./packages/shared/src/contracts/health-status-payload/health-status-payload.stub.ts — the stub imports the contract value and its inferred type from the same folder and parses the defaults through it.
      needed by: ./packages/shared/contracts.ts — needs a NEW paired export line for this contract AND for its stub, matching the pattern at lines 260-261 and 264-265.
      needed by (OTHER SESSION, server) the `GET /api/health/status` 200 body and the 10-second heartbeat payload — both are declared by the spec to be exactly this shape, so the server imports `healthStatusPayloadContract` from `@dungeonmaster/shared/contracts` rather than restating the three fields.
      needed by (OTHER SESSION, web) the web channel's `'health-status'` arm and `packages/web/src/contracts/health-badge-state/health-badge-state-contract.ts` — the latter declares `uptimeSeconds` as the same branded field, reachable as `healthStatusPayloadContract.shape.uptimeSeconds` (the repo idiom used at `packages/web/src/contracts/quest-load-failed-payload/quest-load-failed-payload-contract.ts:20`).

  ./packages/shared/contracts.ts
      needs: ./packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts and ./packages/shared/src/contracts/health-status-payload/health-status-payload.stub.ts — an `export * from` line cannot resolve a module that is not on disk yet, so this file is edited in the same chunk that creates them.
      needs: ./packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.ts — already satisfied at line 260; adding a member changes nothing here.
      needed by: every consumer outside this package, by BOTH resolution routes, which is why a line here is not optional. `packages/shared/package.json:6-11` names `source: "./contracts.ts"` — the key node10 resolution (`moduleResolution: "node"`, repo-wide) uses, so typecheck and lint see a new contract straight off source with no build — and `import`/`require`/`types` pointing at `./dist/contracts.js` / `./dist/contracts.d.ts`, which is what Node honours at RUNTIME. The root tsconfig declares no `paths` and jest.config.base.js no `moduleNameMapper`, so nothing else redirects it. `packages/shared/src/contracts/` contains no index file, and `packages/shared/index.ts:11` is a bare `export * from './contracts'` — so this file is the only place a per-contract line is added, and the server and web sessions must run `npm run build --workspace=@dungeonmaster/shared` before they can RUN against it.

DECISIONS:
  - `'health-status'` is APPENDED last, after `'dispatch-state-changed'` on line 35 of orchestration-event-type-contract.ts. I opened lines 11-36 and counted the members: 21, in a fixed order that `server-init-responder.ts:539` iterates. Appending is the only insertion that leaves all 21 spellings AND indices untouched, which is what check-event-type-member measures.
  - ws-message-contract.ts needs NO edit and gets NO chunk. I opened it: line 14 is `type: orchestrationEventTypeContract` (so the envelope widens with the enum) and line 15 is `payload: z.record(z.string().brand<'PayloadKey'>(), z.unknown())` — the record's VALUE type is `z.unknown()`, so a body of `{status: string, uptimeSeconds: number, version: string}` already parses today. The spec's own entry for this contract says "The contract itself needs no change", and the file agrees.
  - orchestration-event-type.stub.ts needs NO edit. I opened it: `OrchestrationEventTypeStub = ({ value }: { value: string } = { value: 'phase-change' })` parses a RAW string through the contract, so `'health-status'` becomes stubbable the moment the member exists.
  - packages/shared/package.json needs NO edit. I opened lines 5-11: `"./contracts"` is already mapped to `source: ./contracts.ts` with dist import/require/types. A new contract FOLDER needs no manifest change; only the barrel line.
  - The three payload fields brand INLINE rather than importing sibling contracts, and no `uptime-seconds` / `server-version` contract folders are created. Evidence: `ward-result-contract.ts:12-15` brands a string, a datetime and an int inline inside one `z.object`, and `rate-limits-snapshot-contract.ts:15` does the same for its timestamp; shared composes a SIBLING contract only for a nested OBJECT (`rate-limits-snapshot-contract.ts:13` pulls in `rateLimitWindowContract`). Downstream reuse of a single field schema goes through `.shape.<field>`, which is this repo's own idiom — `packages/web/src/contracts/quest-load-failed-payload/quest-load-failed-payload-contract.ts:20` (`errorBodyContract.shape.error`), `packages/shared/src/contracts/package-graph-entry/package-graph-entry-contract.ts:50`, `packages/ward/src/contracts/workspace-input/workspace-input-contract.ts:15-17`.
  - `status` IS branded, as `HealthStatusValue`. `folder-type-contract.ts:25` (`z.enum([firstType, ...restTypes]).brand<'FolderType'>()`) and `package-type-contract.ts:11-23` prove `.brand<>()` chains legally onto `z.enum` here. `health-response-contract.ts:12` holds `'HealthStatus'` on a `z.string().min(1)` base, so a distinct tag is required rather than optional; `HealthStatusValue` gives the server and web sessions a named type instead of a bare literal union. A branded enum inside an object is stubbable — `QuestPackageEntryStub` (quest-package-entry.stub.ts:11) passes the raw literal `'library'` for the branded `packageTypeContract` field.
  - The existing `it.each` at orchestration-event-type-contract.test.ts:11-30 lists only 19 of the 21 members — `'quest-paused'` (line 26 of the contract) and `'quest-resumed'` (line 27) are absent from it. That hand-maintained list is REPLACED by one derived from `orchestrationEventTypeContract.options`, per get-testing-patterns ("NEVER hardcode the list of cases … a hardcoded `it.each` array silently goes stale the moment someone adds a new member"). The 22 literals are then written out exactly once, in the new `.options` assertion, where hardcoding is correct because the exact ordered list IS the thing check-event-type-member measures.
  - A contract test may import its OWN colocated contract; `ban-contract-in-tests` does not bite there. I opened three that do: orchestration-event-type-contract.test.ts:1, ward-result-contract.test.ts:1, rate-limits-snapshot-contract.test.ts:2. So check-event-type-member can assert directly on `orchestrationEventTypeContract.options` rather than through the stub, which cannot expose `.options`.
  - No statics file is created. `.int()`, `.nonnegative()` and `.min(1)` are zod modifiers, not magic numbers; the round's only numeric constants (10000ms emit interval, 30000ms silence threshold) belong to the server and web statics the spec assigns to those packages.
  - All three `Seams` lines read NOT BUILT YET, so none of the five observables attributed to server or web is mine and no `REPAIR:` chunk is cut. I verified my half is sufficient for theirs by opening both landing sites: `server-init-responder.ts:539-542` subscribes the relay by iterating `.options` (so the enum member alone wires the relay, and `'health-status'` being absent from `PER_QUEST_EVENT_TYPES` at lines 65-74 means the `if` at line 654 does not take it, so it reaches `wsEventRelayBroadcastBroker` at lines 762-766 — the global fan-out #relay-broadcast needs), and `web-socket-channel-state.ts:134` gates every frame on `wsMessageContract.safeParse`. What is left for them: the server must emit on a 10s timer and serve `GET /api/health/status`; the web must add a `'health-status'` arm to the if-chain at web-socket-channel-state.ts:137-186, which today has no `else` and therefore no-ops on an unrouted member.
  - `packages/shared/index.ts` needs NO edit and gets NO chunk. I opened it: line 11 is the whole body, `export * from './contracts';`, so the main-barrel surface widens the moment the `contracts.ts` line lands.
  - Adding a 22nd member breaks nothing that enumerates the enum. `packages/server/src/statics/dev-log-event-icons/dev-log-event-icons-statics.ts:9-23` is a PARTIAL `as const` object over 11 members — not `satisfies Record<OrchestrationEventType, …>` — and `packages/server/src/transformers/dev-log-event-format/dev-log-event-format-transformer.ts:26-28` reads it through a `typeof iconValue === 'string' ? iconValue : '· '` fallback. There is no `z.discriminatedUnion` and no exhaustive `switch` over `OrchestrationEventType` anywhere, so nothing fails to compile and no dev-log line goes blank.
  - The literal `'health-status'` occurs NOWHERE in the repo today, so the new member collides with no existing string.
  - Two checker findings landed and both were about PRECISION rather than substance; both are settled by editing the blocks above rather than by changing the round's work. (1) The global fan-out is NOT at server-init-responder.ts:654 — 654 is the `if (PER_QUEST_EVENT_TYPES.has(type))` branch point, which returns at 759; the broadcast is `wsEventRelayBroadcastBroker` at 762-766. I opened both. The substance held: `'health-status'` is not in that set, so it reaches the broadcast. (2) `package.json`'s `"./contracts"` entry maps `import`/`require`/`types` to `dist/`, and only its `source` key names `./contracts.ts`. Both routes still originate here, so `contracts.ts` remains the one wiring point — but the consequence is real and now recorded: the server and web sessions must rebuild shared before they can RUN against the new contract.
  - The working tree at dispatch held nothing but the untracked `.quest-plans/` directory (this document); `git log` shows no commit on this branch beyond master's HEAD 6629af685. There is no half-finished work from a dead session and no `## Rework` section, which is correct for round 1.

ASSERTIONS:
  - `orchestrationEventTypeContract.options` is an array of exactly 22 strings whose last element is `'health-status'`; the preceding 21 read identically, in the same order, to lines 12-35 of that file at commit 6629af685. Check by reading the file and diffing `git diff 6629af685 -- packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.ts`, which must show one added line and no other change inside the enum array.
  - `orchestrationEventTypeContract.parse('health-status')` returns `'health-status'`. Before this round the same call threw `Invalid enum value`.
  - `healthStatusPayloadContract.parse({ status: 'ok', uptimeSeconds: 11520, version: '0.1.0' })` returns an object with exactly the keys `status`, `uptimeSeconds`, `version` and no others.
  - `healthStatusPayloadContract.parse({ status: 'ok', version: '0.1.0' })` throws, and the message matches `/Required/u`.
  - `healthStatusPayloadContract.parse({ status: 'unhealthy', uptimeSeconds: 1, version: '0.1.0' })` throws, and the message matches `/Invalid enum value/u`.
  - `healthStatusPayloadContract.parse({ status: 'ok', uptimeSeconds: 1.5, version: '0.1.0' })` throws, and the message matches `/Expected integer/u`.
  - `packages/shared/contracts.ts` contains an `export * from './src/contracts/health-status-payload/health-status-payload-contract';` line AND an `export * from './src/contracts/health-status-payload/health-status-payload.stub';` line, so `import { healthStatusPayloadContract, HealthStatusPayloadStub } from '@dungeonmaster/shared/contracts'` resolves for the server and web sessions.
  - `packages/shared/src/contracts/ws-message/ws-message-contract.ts` is byte-identical to its state at commit 6629af685 — this round widens the wire vocabulary without touching the envelope.
  - Every file this round changes or adds is under `packages/shared/`. `git status --porcelain` names no path outside it apart from `.quest-plans/`.

NO CHUNK: none

### chunk 1 — add 'health-status' as the 22nd member of the orchestration event enum, and pin the whole list in its test
INTENT:
  - `orchestrationEventTypeContract.options` is an array of exactly 22 strings and its last element is `'health-status'`.
  - `orchestrationEventTypeContract.parse('health-status')` returns `'health-status'` instead of throwing `Invalid enum value`.
  - `git diff 6629af685 -- packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.ts` shows exactly ONE added line inside the enum array and no other change to it.
  - The colocated test holds one assertion spelling all 22 literals in declaration order, and its `it.each` case list is DERIVED from `orchestrationEventTypeContract.options` rather than hand-written — so `'quest-paused'` and `'quest-resumed'`, which the current hand-written list omits, are exercised too.
FILES:
  - ./packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.ts
  - ./packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.test.ts
UNITS:
  - check-event-type-member → ./packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.ts — must declare a 22-member `z.enum` whose last literal is `'health-status'` and whose first 21 literals, spellings and order are exactly what lines 12-35 hold today.
  - OrchestrationEventType.healthStatus → ./packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.ts — must add the literal `'health-status'` on its own line, immediately after `'dispatch-state-changed'`.
MIRROR: ./packages/shared/src/contracts/signoff-verdict/signoff-verdict-contract.test.ts
NOTES:
  1. THE FLOW. `#health-badge` "Server Health Badge": the badge that sits in the app top bar on every
     route — it seeds itself over HTTP at mount, then tracks a 10-second health-status heartbeat
     pushed over the existing WebSocket, and reports ONLINE, DEGRADED or OFFLINE. What the user does:
     nothing — the badge is ambient on every route. What they get: one word telling them whether the
     server behind the interface is still answering, instead of finding out only when an action fails.
     This chunk implements the SHARED half of node `#server-emits` ("Server emits health-status every
     10 seconds") and supplies the frame type that node `#channel-routes` ("Web channel parses the
     frame and pushes the payload") routes on. It builds no emitter, no timer and no route — only the
     vocabulary both ends import.

  2. THE OBSERVABLE, word for word:
     check-event-type-member — "orchestrationEventTypeContract.options contains 'health-status' and
     has 22 members, the prior 21 unchanged"

  3. THE CONTRACT. `OrchestrationEventType` (data, MODIFIED), at
     packages/shared/src/contracts/orchestration-event-type/orchestration-event-type-contract.ts.
     Its requirement, word for word:
     healthStatus — "The new twenty-second member of the enum. All 21 existing members keep their
     spelling and order; this one is added so both the server relay and the web channel accept the
     frame type."
     The schema is `z.enum([...])` and it is UNBRANDED — keep it that way. `.options` is what the
     observable measures and what `server-init-responder.ts:539` iterates, and `.brand<>()` would put
     `.options` behind `.unwrap()`.

     THE EXACT 22 LITERALS, in order, for the assertion:
     'phase-change', 'slot-update', 'progress-update', 'process-complete', 'process-failed',
     'chat-output', 'chat-complete', 'quest-created', 'quest-modified', 'quest-load-failed',
     'quest-persisted', 'quest-paused', 'quest-resumed', 'clarification-request',
     'chat-history-complete', 'quest-session-linked', 'chat-session-started',
     'execution-queue-updated', 'execution-queue-error', 'rate-limits-updated',
     'dispatch-state-changed', 'health-status'

     Leave the 3-line comment at lines 21-23 (above `'quest-load-failed'`) exactly where it is.

  4. THE DESIGN DECISIONS THAT CONSTRAIN THIS CHUNK, quoted:
     #dd-ws-scope "Frame type: health-status joins the shared event enum" — "health-status becomes a
     member of orchestrationEventTypeContract so both sides import one declaration of the frame rather
     than each restating its own. Every other event on the socket is already routed this way, and a
     second inbound shape parsed ahead of the envelope would be a parallel path maintained by hand."
     So: one declaration, in this file. Do not add a second inbound shape parsed ahead of the envelope.
     #dd-reconnect "A socket reconnect needs no special handling" — "Offline is decided by heartbeat
     silence rather than by transport state, so a drop and reconnect is invisible to the badge's own
     logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge
     on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing
     for it." So: add no reconnect handling anywhere in this chunk.

  5. THE ALREADY-BUILT EXPORTS THIS CHUNK WIRES INTO, read off disk:
     - `orchestrationEventTypeContract` (line 11) and `export type OrchestrationEventType =
       z.infer<typeof orchestrationEventTypeContract>` (line 38), in the file you are editing. The
       type widens with the enum; it needs no second edit.
     - `OrchestrationEventTypeStub = ({ value }: { value: string } = { value: 'phase-change' }):
       OrchestrationEventType` — orchestration-event-type.stub.ts:4-6. It parses a RAW string through
       the contract, so it needs NO edit and stubs the new member the moment it exists.
     - `packages/shared/contracts.ts:260-261` already exports this contract and its stub. This chunk
       makes NO barrel edit.
     - `wsMessageContract` — ws-message-contract.ts:13-17: `{ type: orchestrationEventTypeContract,
       payload: z.record(z.string().brand<'PayloadKey'>(), z.unknown()), timestamp:
       z.string().datetime().brand<'IsoTimestamp'>() }`. Do NOT edit it; it widens with the enum.

     TEST SHAPE — copy MIRROR, signoff-verdict-contract.test.ts:4-22. A `describe('enum membership')`
     holding (a) `it('VALID: {options} => …')` whose body is
     `expect(orchestrationEventTypeContract.options).toStrictEqual([ …the 22 literals… ]);` and
     (b) `it.each(orchestrationEventTypeContract.options)('VALID: {value: %s} => parses successfully',
     (type) => { expect(orchestrationEventTypeContract.parse(type)).toBe(type); })` — which REPLACES
     the hand-written 19-item array at lines 11-30. Keep the three existing INVALID tests at lines
     35-51 exactly as they are, the `'smoketest-progress'` removed-value case included. A contract
     test may import its OWN colocated contract — line 1 already does and `ban-contract-in-tests` does
     not fire there. Hardcoding the 22 literals in the `.toStrictEqual` is CORRECT (that exact ordered
     list is the thing measured); hardcoding them in the `it.each` list is not.

     WHAT THIS CHUNK CHANGES THAT OTHER FILES USE: the enum gains one member; no signature changes.
     `server-init-responder.ts:539` picks it up by iterating `.options`.
     `dev-log-event-icons-statics.ts:9-23` is a PARTIAL `as const` map read through a
     `typeof iconValue === 'string' ? iconValue : '· '` fallback at
     `dev-log-event-format-transformer.ts:28`, so no icon entry is owed and nothing fails to compile.
     LEFT FOR THE SERVER SESSION: the 10-second emitter. LEFT FOR THE WEB SESSION: a `'health-status'`
     arm in the if-chain at `web-socket-channel-state.ts:137-188`, which has no `else` and therefore
     silently no-ops on an unrouted member. Build NEITHER of those here.

### chunk 2 — declare healthStatusPayloadContract with its stub and test, and export the pair from the contracts barrel
INTENT:
  - `healthStatusPayloadContract.parse` of a whole body — status `'ok'`, uptimeSeconds `11520`, version `'0.1.0'` — returns those three keys and no others.
  - `healthStatusPayloadContract.parse` of a body with `uptimeSeconds` OMITTED throws, matching `/Required/u`.
  - `healthStatusPayloadContract.parse` of a body whose status is `'unhealthy'` throws, matching `/Invalid enum value/u`.
  - `healthStatusPayloadContract.parse` of a body whose uptimeSeconds is `1.5` throws, matching `/Expected integer/u`.
  - `HealthStatusPayloadStub()` returns a whole valid payload, and `HealthStatusPayloadStub` called with status `'degraded'` returns one whose `status` is `'degraded'`.
  - `packages/shared/contracts.ts` carries an export line for the contract AND one for the stub, so `import { healthStatusPayloadContract, HealthStatusPayloadStub } from '@dungeonmaster/shared/contracts'` resolves.
FILES:
  - ./packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts
  - ./packages/shared/src/contracts/health-status-payload/health-status-payload.stub.ts
  - ./packages/shared/src/contracts/health-status-payload/health-status-payload-contract.test.ts
  - ./packages/shared/contracts.ts
UNITS:
  - check-payload-parses → ./packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts — must accept a whole status/uptimeSeconds/version body and must REJECT one with `uptimeSeconds` omitted.
  - HealthStatusPayload.status → ./packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts — must declare a bounded two-member set accepting exactly `'ok'` and `'degraded'`, under a brand tag that is NOT `HealthStatus`.
  - HealthStatusPayload.uptimeSeconds → ./packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts — must declare a whole-number seconds field: integer, not negative, rejecting a float and rejecting a string.
  - HealthStatusPayload.version → ./packages/shared/src/contracts/health-status-payload/health-status-payload-contract.ts — must declare a non-empty string.
MIRROR: ./packages/shared/src/contracts/ward-result/ward-result-contract.ts
NOTES:
  1. THE FLOW. `#health-badge` "Server Health Badge" — the same flow as chunk 1: an ambient top-bar
     badge that seeds over HTTP at mount, then tracks a 10-second health-status heartbeat over the
     existing WebSocket and reports ONLINE, DEGRADED or OFFLINE, so an operator learns the server has
     wedged without having to fail an action first. This chunk implements the SHARED half of node
     `#channel-routes` ("Web channel parses the frame and pushes the payload"), and the body it
     declares is also what node `#server-emits` puts on the wire and what node `#seed-fetch` returns
     from `GET /api/health/status`. It builds no route, no emitter and no widget.

  2. THE OBSERVABLE, word for word:
     check-payload-parses — "A health-status payload parses against healthStatusPayloadContract, and a
     payload missing uptimeSeconds fails that parse"

  3. THE CONTRACT. `HealthStatusPayload` (data, NEW). Its three requirements, word for word:
     status — "Either the literal 'ok' or the literal 'degraded'. Deliberately NOT named
     HealthStatus: the server's existing health-response contract already brands that name onto
     z.string().min(1), and two schemas sharing a brand tag with different base types are not
     interchangeable. shared also cannot import from server, so the name must differ rather than be
     reused."
     uptimeSeconds — "Whole seconds the server process has been running. Rendered as Xh Ym beside
     ONLINE."
     version — "The server package version string, carried for display and diagnosis."

     BUILD IT EXACTLY AS:
       export const healthStatusPayloadContract = z.object({
         status: z.enum(['ok', 'degraded']).brand<'HealthStatusValue'>(),
         uptimeSeconds: z.number().int().nonnegative().brand<'UptimeSeconds'>(),
         version: z.string().min(1).brand<'ServerVersion'>(),
       });
       export type HealthStatusPayload = z.infer<typeof healthStatusPayloadContract>;
     Brand INLINE. Create no `uptime-seconds` or `server-version` sibling contract folder — this
     package brands primitives inline inside a `z.object` (ward-result-contract.ts:12-15,
     smoketest-case-result-contract.ts:14-20, rate-limits-snapshot-contract.ts:15) and composes a
     sibling contract only for a nested OBJECT.

  4. THE DESIGN DECISIONS THAT CONSTRAIN THIS CHUNK, quoted:
     #dd-ws-scope "Frame type: health-status joins the shared event enum" — "health-status becomes a
     member of orchestrationEventTypeContract so both sides import one declaration of the frame rather
     than each restating its own. Every other event on the socket is already routed this way, and a
     second inbound shape parsed ahead of the envelope would be a parallel path maintained by hand."
     One declaration serves both ends — which is why this contract lives in shared and not in web
     beside its `*-payload` siblings.
     #dd-uptime-format "Uptime renders as Xh Ym, hours never rolling into days" — "One format keeps
     the badge to a single short line at any server age: 90061 seconds reads 25h 1m rather than 1d 1h
     1m. The string never changes shape as the server ages, so the top bar layout never shifts under
     it." The FORMATTING is the web session's. This contract carries whole SECONDS so that formatting
     is possible; do NOT store a preformatted string and do NOT add an hours/minutes pair.
     #dd-reconnect "A socket reconnect needs no special handling" — "Offline is decided by heartbeat
     silence rather than by transport state, so a drop and reconnect is invisible to the badge's own
     logic: frames stop, the threshold may elapse, and the first frame after reconnect moves the badge
     on its ordinary path. The existing channel already owns reconnection, so this flow adds nothing
     for it." So: no reconnect field and no transport state on this payload.

  5. THE ALREADY-BUILT EXPORTS THIS CHUNK WIRES INTO, read off disk:
     - `StubArgument<T, Extra = unknown>` — declared at
       packages/shared/src/@types/stub-argument.type.ts:114, re-exported from
       packages/shared/@types.ts:9, imported as
       `import type { StubArgument } from '@dungeonmaster/shared/@types';`. It UNBRANDS primitives, so
       the stub takes a raw string for `status` even though the field is branded — proved in-repo by
       QuestPackageEntryStub (quest-package-entry.stub.ts:11), which passes the raw literal
       `'library'` for the branded `packageTypeContract` field. `enforce-stub-patterns` requires the
       annotation stay a bare `StubArgument<HealthStatusPayload>` reference.
     - `healthResponseContract` — packages/server/src/contracts/health-response/health-response-contract.ts:11-14,
       whose `status` is `z.string().min(1).brand<'HealthStatus'>()` and whose `timestamp` is
       `z.string().min(1).brand<'HealthTimestamp'>()`. That is the tag to avoid, and it lives in
       SERVER: shared must not import it, and does not need to.
     - `wsMessageContract.payload` is `z.record(z.string().brand<'PayloadKey'>(), z.unknown())`
       (ws-message-contract.ts:15), so these three fields ride the existing envelope with no envelope
       edit.
     - THE BARREL. packages/shared/contracts.ts currently ends at line 754, whose last group is
       `// Operation Plan Contracts`; new groups are APPENDED at the tail. Append a blank line then:
         // Health Status Contracts (the health-status heartbeat body, shared by the server that emits
         // it and the web channel that renders it)
         export * from './src/contracts/health-status-payload/health-status-payload-contract';
         export * from './src/contracts/health-status-payload/health-status-payload.stub';
       `packages/shared/index.ts:11` is `export * from './contracts';` — no edit.
       `packages/shared/package.json` lines 6-11 already declare `"./contracts"` — no edit.

     FILE SHAPES — copy the MIRROR's folder, the closest existing three-file set (a `z.object` mixing
     a branded string, a branded int and an enum): `ward-result-contract.ts` for the contract
     (PURPOSE/USAGE block, blank line, `import { z } from 'zod';`, blank line, the `z.object`, blank
     line, `export type … = z.infer<…>`), `ward-result.stub.ts` for the stub (TWO separate import
     lines — a value import and an `import type` — not the combined inline-type form), and
     `ward-result-contract.test.ts` for the test (`describe('valid …')` and `describe('invalid …')`
     blocks, `toStrictEqual` on whole objects).

     PURPOSE must say WHY this file exists and when to reach for it rather than its nearest sibling,
     and must NOT restate what the schema validates. Write it LAST, after the schema.
     `riftcarver-result-contract.ts:1-16` is the in-repo precedent for a PURPOSE carrying a
     naming/dependency-direction rationale, which is exactly what this one owes the reader: shared
     cannot import from server, and server already holds `'HealthStatus'` on a different base type.

     THE REAL ZOD v3 ERROR STRINGS, each read off a passing test in this package — assert on these,
     do not invent one:
       missing field                    → /Required/u                                 (ward-result-contract.test.ts:70)
       bad enum member (branded or not) → /Invalid enum value/u                        (ward-result-contract.test.ts:110; package-type-contract.test.ts:85)
       float where int                  → /Expected integer, received float/u          (array-index-contract.test.ts:33)
       string where number              → /Expected number, received string/u          (array-index-contract.test.ts:37)
       negative where nonnegative       → /Number must be greater than or equal to 0/u (array-index-contract.test.ts:28)
       empty string where min(1)        → /String must contain at least 1/u            (smoketest-case-result-contract.test.ts:21)

     WHAT THIS CHUNK CHANGES THAT OTHER FILES USE: three new exports on
     `@dungeonmaster/shared/contracts` — `healthStatusPayloadContract`, the type `HealthStatusPayload`,
     and `HealthStatusPayloadStub`. Downstream reuse of ONE field's schema is
     `healthStatusPayloadContract.shape.uptimeSeconds` (repo idiom:
     quest-load-failed-payload-contract.ts:20), and the status members are reachable as
     `healthStatusPayloadContract.shape.status.unwrap().options` (idiom:
     quest-package-entry-contract.test.ts:5).
     LEFT FOR THE SERVER SESSION: the `GET /api/health/status` route returning exactly this body, and
     the 10-second emitter. LEFT FOR THE WEB SESSION: the `'health-status'` channel arm that
     `safeParse`s `envelope.data.payload` through this contract — the shape every other arm already
     uses at web-socket-channel-state.ts:137-188 — and `healthBadgeStateContract`. Build NONE of them.
     After this chunk lands, both sessions need `npm run build --workspace=@dungeonmaster/shared`
     before they can RUN against it.

PHASES:
  1: wave 1 — the two shared contract declarations every later session on this quest imports

WAVES:
  1: 1, 2

## Round log
